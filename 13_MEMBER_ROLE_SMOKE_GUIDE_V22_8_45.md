# V22.8.45 참여자 권한 수동 확인

실제 소유자 계정과 삭제해도 되는 테스트 참여자를 사용합니다. 소유자 본인의 권한은 변경하지 않습니다.

## 정상 저장

1. `참여자·초대 관리`에서 현재 `승인대기`인 테스트 참여자를 찾습니다.
2. 역할을 `관리자`로 선택하고 `권한 저장`을 한 번만 누릅니다.
3. Cloudflare Error 1101 페이지가 아닌 동일한 참여자 화면으로 돌아오는지 확인합니다.
4. 성공 안내와 함께 역할이 `관리자`로 표시되는지 확인합니다.
5. 화면을 새로고침한 뒤에도 `관리자`가 유지되는지 확인합니다.

## 권한 확인

- 새 관리자가 해당 가계부의 참여자 관리 화면을 열 수 있는지 확인합니다.
- 다른 가계부의 주소에 `household_id`를 바꿔 넣었을 때 접근되지 않는지 확인합니다.
- 새 관리자가 소유자를 내보내거나 유일한 소유자의 역할을 낮출 수 없는지 확인합니다.

## 오류가 표시되는 경우

- `현재 데이터베이스 역할 규칙에서 선택한 권한을 저장할 수 없습니다`가 보이면 같은 버튼을 반복해서 누르지 않습니다.
- 이 경우 기존 `승인대기` 권한이 유지되는지 확인하고 화면 스크린샷을 전달합니다.
- V22.8.45에는 신규 SQL이 없습니다. 오류 문구만으로 기존 SQL을 재실행하지 않습니다.

## 롤백 기준

- Error 1101이 다시 발생하거나 다른 참여자의 권한이 바뀌면 V22.8.44 Worker 백업으로 전체 롤백합니다.
- 실패가 화면 내 오류로 표시되고 기존 권한이 유지된다면 데이터 손상은 방지된 상태이므로 스크린샷을 기준으로 다음 진단을 진행합니다.
