# V22.8.64 검증 보고서

검증일: 2026-07-31

## 결과

- 자동 검사: 1,705개 통과
- Worker 문법 및 ESM `default.fetch`: 통과
- `/manifest.json` MIME·ETag·HEAD와 필수 항목(name, short_name, start_url, scope,
  display, theme_color, background_color, lang): 통과
- 안드로이드 설치 요건(192·512 아이콘, maskable 항목) 선언: 통과
- `/icon-192.png`, `/icon-512.png`, `/apple-touch-icon.png` PNG 시그니처·크기·팔레트: 통과
- 사용자 화면 5종이 매니페스트를 정확히 한 번만 참조: 통과
- 아이콘 링크 태그는 추가되지 않음: 통과
- 홈 HTML 35KB 예산 유지: 통과
- 신규 SQL·환경변수·카카오 설정 변경: 없음

## 예산 실측

| 구분 | 이전 | 이번 | 예산 | 여유 |
|---|---:|---:|---:|---:|
| 기준 픽스처 홈 | 35,444 | 35,488 | 35,840 | 352 B |
| 실사용(200행) 홈 | 44,702 | 44,746 | 45,056 | 310 B |

매니페스트 링크 1줄로 44바이트만 늘었습니다. 아이콘 링크는 브라우저가 경로를
스스로 요청하므로 넣지 않았습니다.

## 아이콘 압축 효과

런타임 `CompressionStream`을 쓰도록 바꿔 아이콘 크기가 크게 줄었습니다.

| 파일 | V22.8.63 | V22.8.64 |
|---|---:|---:|
| `/apple-touch-icon.png` (180px) | 32,669 B | 303 B |
| `/icon-192.png` | — | 326 B |
| `/icon-512.png` | — | 1,739 B |

압축기를 쓸 수 없는 런타임에서는 기존 저장(비압축) 블록으로 자동 대체합니다.

## 실제 브라우저 확인

- Chromium 390px에서 홈·전체 메뉴·통계 세 화면 모두
  `link[rel=manifest]` → `/manifest.json` 200, `application/manifest+json`
- 매니페스트가 선언한 아이콘 3개 전부 로드 성공(192, 512, 512 maskable)
- 세 화면 모두 4xx 0건, 콘솔 오류 0건

## 설계 판단

- `display: standalone`은 홈 화면에서 앱처럼 열리게 합니다. 주소창과 뒤로가기를
  남기려면 `browser`로 바꾸면 되고, 이 값은 `AB_WEB_MANIFEST` 한 곳에 있습니다.
- maskable 아이콘은 안드로이드가 원형·둥근사각 등으로 자를 때를 대비합니다.
  마크가 중심에서 반지름 0.36 안에 들어와 안전 영역(0.4) 기준을 만족합니다.

## 자동화 제한

- 안드로이드 크롬의 실제 `설치` 배너와 iOS `홈 화면에 추가`는 실기기 확인이 필요합니다.
- 설치 후 `standalone` 모드에서 카카오 로그인 리다이렉트가 앱 안에서 끝나는지도
  실기기에서 한 번 확인해야 합니다.

## 운영에서 남은 수동 확인

- 안드로이드 크롬 설치 배너·설치 후 아이콘·이름·시작 화면
- iPhone 사파리 `공유 → 홈 화면에 추가` 아이콘
- 설치 상태에서 카카오 로그인 왕복
- 카카오톡 인앱 브라우저는 기존과 동일해야 함
