# V22.8.6 최종 적용 체크리스트

## 1. 적용 전

- [ ] 현재 Cloudflare Worker 코드와 Supabase 데이터를 백업
- [ ] 운영 환경변수와 Secret을 별도 보관

V22.8.6에는 새 SQL이 없습니다. 기존 V22.8.0 스키마를 그대로 사용합니다.

## 2. Worker 적용

- [ ] `src/index.js` 전체 교체 후 배포
- [ ] `/health`에서 `V22.8.6-RECEIPT-SCREEN-OPTIMIZATION` 확인
- [ ] 캐시를 지운 뒤 PC와 모바일에서 `/receipts` 확인

## 3. 영수증 화면 확인

- [ ] `앨범에서 선택`이 갤러리를, `카메라로 촬영`이 카메라를 여는지 확인
- [ ] 사진 선택 전 Tesseract 다운로드가 없는지(개발자도구 네트워크 탭) 확인
- [ ] 사진 읽기 중 `분석 취소`를 누르면 즉시 버튼과 상태가 복구되는지 확인
- [ ] PC에서 드래그&드롭과 붙여넣기(Ctrl+V)로 사진이 선택되는지 확인
- [ ] 문자 붙여넣기 → 분석 → 상호·날짜·총액(쉼표 표기) 자동 채움 확인
- [ ] 총액을 직접 입력할 때 쉼표가 자동으로 붙는지 확인
- [ ] 저장, 같은 영수증 중복 차단, 조회 전용 권한 차단 확인
- [ ] 가계부·기록 월 변경 시 화면이 바로 갱신되는지 확인

## 4. 회귀 확인

```bash
bash validate_v2286.sh
```

- [ ] 모든 검증이 오류 없이 종료
- [ ] 로그인, 기록 저장·수정·삭제, 정산, 예산, 백업을 테스트 계정으로 확인
- [ ] 장애 시 직전 Worker 버전으로 롤백하고 DB는 변경하지 않음
