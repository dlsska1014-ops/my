# V22.8.44 테마·반응형 수동 확인

테스트용 가계부에서 조회·화면 전환만 확인합니다. 거래·정산·목표 저장은 이 단계에서 실행하지 않습니다.

## 화면 크기

1. 390px와 768px에서 상단 바, `전체 메뉴`, 하단 5탭을 확인합니다.
2. 전체 메뉴 버튼과 각 주요 메뉴가 손가락으로 누르기 충분한지 확인합니다.
3. 1024px와 1440px에서 238px 사이드바와 본문이 겹치지 않는지 확인합니다.
4. 모든 폭에서 가로 스크롤이 생기지 않는지 확인합니다.

## 화면 모드와 컬러톤

`전체 메뉴 → 화면 설정`에서 아래 조합을 순서대로 확인합니다.

- 라이트: 블루, 그린, 바이올렛, 앰버
- 다크: 블루, 그린, 바이올렛, 앰버
- 시스템 설정 따르기

각 조합에서 가계부 이름, 수입·남은 예산, 최근 내역 링크, 입력 placeholder, 선택 메뉴, 안내·경고 문구가 배경과 섞이지 않는지 확인합니다.

## 접근성

- 키보드 Tab으로 상단 바, 전체 메뉴, 하단 탭, 기간 컨트롤을 이동합니다.
- 전체 메뉴를 연 뒤 Escape로 닫히고 열었던 버튼으로 초점이 돌아오는지 확인합니다.
- 운영체제의 동작 줄이기와 고대비/강제 색상을 켠 환경에서 메뉴·입력·현재 선택 상태가 사라지지 않는지 확인합니다.

## 회귀 확인

- 목표와 정산 화면이 열리고 기존 목록·이력이 그대로 보이는지 확인합니다.
- 화면을 이동해도 `month`와 `household_id`가 유지되는지 확인합니다.
- 개인 화면에 AdSense 메타·광고 스크립트가 나타나지 않는지 확인합니다.

문제가 있으면 화면 크기, 모드, 컬러톤, 경로와 스크린샷을 기록하고 V22.8.43 Worker 소스로 전체 롤백합니다.
