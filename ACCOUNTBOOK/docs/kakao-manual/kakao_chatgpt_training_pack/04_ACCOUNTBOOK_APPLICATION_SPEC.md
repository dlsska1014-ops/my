# 카카오 공동 가계부 적용 명세

## 1. 역할 분담

- 카카오톡: 지출·수입 기록, 빠른 조회, 공동 알림, 초대, 밈 카드 공유.
- 모바일 웹·앱: 전체 거래 편집, 검색, 카테고리, 예산, 고정지출, 멤버 권한, 분석, 엑셀 가져오기·내보내기.
- Cloudflare: 개인 PC가 꺼져도 스킬, Webhook, Cron, Queue, 파일 저장과 알림을 상시 처리.

## 2. 식별자 매핑

- botGroupKey -> household_ledger_id 또는 chat_room_binding.
- botUserKey -> kakao_bot_identity.
- appUserId -> 로그인된 서비스 계정 연결용 외부 식별자.
- 사용자·채팅방 식별자를 재무 데이터의 공개 URL에 그대로 노출하지 않는다.

## 3. 권장 데이터 영역

- households, household_members, chat_room_bindings
- transactions, transaction_revisions, payment_accounts
- categories, category_defaults, category_mappings
- budgets, budget_thresholds, budget_alert_logs
- recurring_transactions, recurring_runs
- invitations, member_roles, audit_logs
- meme_cards, meme_collections, analysis_snapshots
- import_jobs, export_jobs, import_errors, file_expirations

## 4. 카카오 발화 블록

- 기록: 지출, 수입, 이체, 환불, 영수증 이미지, 마지막 기록 수정·삭제·취소.
- 조회: 오늘·주간·월간, 최근 기록, 카테고리별, 멤버별, 검색.
- 예산: 전체·카테고리 예산 설정, 현황, 임계치, 알림 켜기·끄기.
- 고정지출: 추가, 목록, 수정, 일시정지, 이번 회차 건너뛰기, 종료.
- 공동 가계부: 생성, 초대코드, 가입, 멤버 목록, 역할, 탈퇴, 내보내기 권한.
- 분류: 기본 추천 목록, 사용자 추가, 이름 변경, 삭제, 병합, 거래 재분류.
- 분석: 소비 변화, 예산 대비, 상위 지출, 이상 지출, 프리미엄 리포트.
- 밈 카드: 오늘의 소비몬, 월간 소비몬, 도감, 희귀도, 공유.
- 데이터: 엑셀 내보내기, 다른 가계부 가져오기, 열 매핑, 미리보기, 오류 보고, 롤백.
- 운영: 도움말, 알림 설정, 개인정보, 보관 기간, 문의, 탈퇴.

## 5. 발화 복구 원칙

- 이미 파악한 금액·날짜·메모는 유지하고 부족한 정보 한 가지만 다시 묻는다.
- 삭제, 전체 가져오기, 공동 가계부 해제는 실행 전 재확인한다.
- 같은 카카오 요청이 재전송되어도 거래를 중복 저장하지 않도록 멱등키를 사용한다.
- 분류 신뢰도가 낮으면 임의 저장하지 말고 후보를 제시한다.

## 6. 예산과 고정지출 알림

- 80% 도달, 100% 최초 초과, 초과액 증가를 구분한다.
- 같은 예산에 연속 결제가 생겨도 매번 알리지 않고 단계별 1회 또는 하루 묶음으로 보낸다.
- 공동방 알림에는 민감한 개인 메모를 노출하지 않는다.
- 고정지출은 Cron으로 생성하되 중복 실행 방지 키와 실행 이력을 남긴다.
- 사용자가 설정한 정기 메시지는 알림 전용 블록으로 구현한다.

## 7. 밈 카드 원칙

- 실제 지출 수치에서 생성되었다는 근거는 유지하되 개인을 비난하거나 수치심을 유발하지 않는다.
- 카드 공유본에는 거래 메모, 상호명, 멤버 실명 등 민감정보를 기본적으로 숨긴다.
- 일간·월간 카드 생성은 Queue로 분리하고 결과만 R2에 저장한다.
- 챗봇에는 한 장의 요약 카드와 짧은 문구를 보내고 전체 도감은 모바일 화면으로 연결한다.

## 8. 개인정보 안내 문구

- 허용: `가계부적은 봇에게 직접 보낸 명령어만 처리합니다.`
- 허용: `기록을 제공하기 위해 명령어와 가계부 데이터를 저장하며 보관·삭제 기준은 도움말에서 확인할 수 있습니다.`
- 금지: `단톡방 대화를 읽어서 자동 분석합니다.`
- 금지: `채팅 내용을 학습해 소비 패턴을 알아냅니다.`

## 9. 출시 우선순위

1. 사용자·방·공동 가계부 권한 모델.
2. 기록·수정·취소·중복 방지.
3. 모바일 관리 화면 완결.
4. 예산·고정지출·알림.
5. 엑셀 양방향 호환과 복구.
6. 근거 있는 분석과 소비몬 카드.
