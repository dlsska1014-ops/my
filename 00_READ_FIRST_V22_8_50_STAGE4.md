# V22.8.50 UI/UX 4단계 적용 안내

버전: `V22.8.50-UIUX-STAGE4-QUICK-DOCK-SAVE-FEEDBACK`

## 반영 내용

1. PC 본문 하단에 빠른 실행 독 제공
2. 독에서 거래 검색·빠른 입력·알림·화면 설정 연결
3. 빠른 입력을 주요 동작으로 강조
4. 거래 저장 성공·예산 경고·입력 오류를 고정 피드백 카드로 표시
5. 성공 시 선택 날짜의 일별 상세 팝업 자동 복귀
6. 오류 시 선택 날짜를 유지한 빠른 입력 팝업 자동 복귀
7. 기존 입력폼·POST 저장·권한·중복 방지 로직 재사용

## 운영 전제

- V22.8.46 참여자 역할 SQL은 운영 Supabase에 적용 완료
- 이번 버전에서 추가 SQL 또는 스키마 변경 없음
- 기존 Worker 소스는 배포 전 백업

## 배포

1. 이 패키지의 `src/index.js` 전체를 Cloudflare Worker에 교체합니다.
2. 저장 및 배포합니다.
3. PowerShell에서 `14_VERIFY_AFTER_DEPLOY_V22_8_50_STAGE4.ps1`을 실행합니다.
4. PC 하단 빠른 실행 독의 네 동작을 확인합니다.
5. 날짜를 선택해 거래를 저장하고 성공 카드와 일별 상세 자동 복귀를 확인합니다.
6. 금액을 비운 오류 저장으로 오류 카드와 입력 팝업 자동 복귀를 확인합니다.
7. 모바일에서 오류 카드가 하단 메뉴 위에 표시되고 선택 날짜가 유지되는지 확인합니다.

## 변경하지 않은 항목

- Supabase 테이블·컬럼·RLS·RPC
- 거래 저장·수정·삭제·권한·중복 방지 핵심 계약
- 환경변수·Secret
- Kakao Developers·OpenBuilder
- 카카오 Skill 저장 흐름

## 자동검사

```bash
bash validate_v22850.sh
```

또는

```bash
node .codex/scripts/verify-repository.mjs
```

총 1,120개 자동검사, ESM 진입점, 패키지 체크섬을 검증합니다.
