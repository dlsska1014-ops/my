# V22.8.74 예산 재검토·다크 대비 보정 안내

버전: `V22.8.74-BUDGET-DARK-CONTRAST`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음 · 환경변수 변경 없음
- 셸 스타일시트 주소가 `/assets/accountbook-shell-v22874.css` 로 바뀝니다

## 이번에 고친 것

### 1. 다크 모드에서 읽히지 않던 글자 2곳

| 곳 | 보정 전 | 보정 후 |
|---|---:|---|
| 홈의 빈 상태 상자 | **1.82:1** (흰 상자에 연회색 글자) | 어두운 표면으로 교체 |
| 본문 바로가기 배지 | **2.87:1** (파란 배경에 연한 파란 글자) | 흰 글자 복구 |

빈 상태 상자는 밝은 배경이 `!important` 로 박혀 있어 다크에서도 그대로 남았고,
글자만 다크용 흐린 색으로 바뀌어 거의 안 보였습니다.
배지는 다크의 전역 링크색 규칙이 흰 글자를 덮어쓴 것으로, V22.8.72 에서
넣은 요소를 다크에서 확인하지 않아 생긴 문제입니다.

라이트·다크 × 포인트 컬러 4종 **8조합을 다시 재어 전부 0건**입니다.

### 2. 홈 HTML 예산이 이미 초과였습니다

예산 검사가 `2026-07` 을 고정으로 보고 있었는데, 그 달이 지나면 "오늘" 표시가
빠져 페이지가 작아집니다. 즉 **사용자가 흔히 보는 화면이 아니라 더 작은 화면**을
재고 있었습니다. 그 달 안에서 재면 45,084 B 로 예산(45,056 B)을 넘습니다.

- 선택되지 않은 `option` 의 잉여 공백을 없애 **38 B** 회수
- 검사가 언제 돌든 **이번 달**(최악 사례)을 재도록 변경
- 예산을 44 KiB → **46 KiB** 로 재설정 (실측 44,962 B, 여유 2,142 B)

여유는 피드 카드 한 장(약 1.7 KB)보다 크게 두어 카드가 커지는 회귀는 계속 잡힙니다.

## 적용 순서

1. 현재 운영 Worker 전체 소스를 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 교체하고 배포합니다.
3. 저장소 루트에서 아래를 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_74.ps1
```

4. 다크 모드로 바꿔 홈의 빈 상태 상자와 `본문 바로가기` 배지가 읽히는지 봅니다.
5. 오래 열어 둔 웹 탭은 새로고침해야 `v22874` 셸이 적용됩니다.

## 판단이 필요한 것

iPhone 홈 화면 추가 시 앱처럼 전체 화면으로 열리려면
`<meta name="apple-mobile-web-app-capable" content="yes">` 가 필요합니다.
지금은 없어 주소창이 있는 Safari 화면으로 열립니다.
넣으면 Safari 뒤로 가기 버튼이 사라지므로 제품 판단이 필요해 이번에는 넣지 않았습니다.

기존 SQL 은 다시 실행하지 않습니다.
