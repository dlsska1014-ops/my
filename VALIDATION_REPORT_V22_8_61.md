# V22.8.61 검증 보고서

검증일: 2026-07-31

## 결과

- 자동 검사: 1,501개 통과
- Worker 문법 및 ESM `default.fetch`: 통과
- 정의되지 않은 CSS 토큰 0개(회귀 검사로 고정): 통과
- 모바일 상단바 공통 작업 버튼 도킹과 겹침 제거: 통과
- 전체 메뉴 서랍의 메뉴 목록 높이·48px 터치 영역: 통과
- 빠른 입력 시트 스크롤 잠금·복원과 중첩 스크롤 제거: 통과
- 빠른 입력 칩·결제수단 칩 아이콘 매핑과 CSS 우선순위 일치: 통과
- 챌린지 날짜 입력의 그리드 이탈 방지: 통과
- 홈 HTML 기준 픽스처 35KB·실사용 44KB 예산과 `mobile-home-v22810.css` 바이트 고정: 통과
- 신규 SQL·환경변수·카카오 설정 변경: 없음

## 실제 브라우저 확인

- 저장소 QA 서버(`validation/qa-browser-server-v22818.mjs`)와 Chromium 390px 뷰포트로
  `/app`을 렌더해 아래를 재현하고 수정 후 재확인했습니다.
  - 수정 전: `작업` 버튼(x 227~298)이 `전체 메뉴` 버튼(x 294~374)과 4px 겹침
  - 수정 전: 하단 `입력` 탭 아이콘이 `var(--action)` 미정의로 배경 없는 흰 아이콘
  - 수정 전: 전체 메뉴 서랍 높이의 대부분을 달력·예산 위젯이 차지
  - 수정 전: 빠른 입력 시트가 `overscroll-behavior:auto`로 배경까지 스크롤

## 자동화 제한

- iOS 사파리·카카오 인앱 브라우저의 실제 스크롤 잠금 동작은 Chromium에서 완전히
  재현되지 않으므로 실기기 확인이 필요합니다.
- `input[type=date]`의 고유 최소 너비는 브라우저마다 달라 이탈 재현은 iOS 기준이며,
  수정은 브라우저와 무관하게 `minmax(0,1fr)`·`min-width:0`으로 방지합니다.

## 운영에서 남은 수동 확인

- 390px·768px·1440px 레이아웃과 라이트·다크·포인트 컬러
- 카카오 인앱 브라우저에서 빠른 입력 시트 스크롤과 배경 고정
- 전체 메뉴에서 각 그룹을 펼쳐 항목 터치가 어긋나지 않는지
- `/health`, `/ready`, 새 immutable 자산 확인
