# V22.8.69 카카오 입력 메모 손상·중복 방지 오작동 수정 안내

버전: `V22.8.69-KAKAO-MEMO-REPEAT-FIX`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음
- 환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음
- 모바일 홈 런타임 주소가 `/assets/mobile-home-v22869.js`,
  `/assets/mobile-home-shell-v22869.js` 로 바뀝니다 (immutable 자원)

## 배경 (중요)

`자동차점검 6만원` 입력이 실패한다는 제보를 재현하다가 **결함 3건**을 찾았습니다.
모두 카카오톡 한 줄 입력의 기본 경로에 있었습니다.

### 1. 저장되지 않은 요청까지 중복 방지에 걸렸습니다

중복 방지 잠금을 **응답을 만들기 전에** 걸고 있었습니다.
그래서 저장이 일어나지 않은 요청(가계부 선택 안내, 인식 실패 등)도 잠겼고,
사용자가 같은 문구를 다시 보내면 `방금 같은 내용을 처리했어요` 라는 안내만 나온 채
**끝내 아무것도 저장되지 않았습니다.**

| 순서 | 보정 전 | 보정 후 |
|---|---|---|
| `자동차점검 6만원` | 가계부 선택 안내 (저장 안 됨) | 동일 |
| `2` (가계부 선택) | 선택 완료 | 동일 |
| `자동차점검 6만원` 재입력 | **`방금 같은 내용을…` → 저장 실패** | **정상 저장** |

실제로 저장·수정·삭제가 끝난 뒤에만 잠그도록 바꿨습니다.
연속으로 같은 지출을 두 번 보내는 진짜 중복은 그대로 막습니다.

### 2. 메모에서 글자가 사라졌습니다

조사(`이`, `가`, `을`, `를`, `로`, `에` …)를 **위치를 가리지 않고** 지우고 있었습니다.
낱말 한가운데 글자까지 지워져 내용이 파괴됐습니다.

| 입력 | 보정 전 저장값 |
|---|---|
| 타이어교체 | `타 어교체` |
| 이발 | `발` |
| 가방 | `방` |
| 이마트 | `마트` |
| 에어컨 | `어컨` |
| 은행수수료 | `행수수료` |

표본 29개 중 **21개가 손상**됐습니다. 조사는 낱말 끝에서만 떼어내도록 바꿨습니다.
`마트에서 12000` → `마트` 같은 기존 정리는 그대로 동작합니다.

같은 결함이 **웹 빠른 입력의 `채우기`** 에도 있어 함께 고쳤습니다.

### 3. `또`가 들어간 낱말이 문장으로 쪼개졌습니다

여러 건 입력을 나누는 접속사 목록에 `또`가 위치 조건 없이 들어 있어,
`로또 10000` 이 `로` + `10000` 으로 쪼개졌습니다.
`로` 는 금액이 없어 버려지고 `10000` 은 내용이 없어, **메모가 통째로 사라졌습니다.**

접속사는 낱말 경계에서만 나누도록 바꿨습니다.
조사형(`랑`, `하고`)은 앞에 두 글자 이상 붙어 있을 때만 나눕니다
(`커피랑 3000` 은 나누고, `사랑 10000` 은 나누지 않습니다).

## 영향 범위

- 카카오톡 한 줄 입력의 메모 저장값
- 카카오톡 중복 방지 안내
- 웹 홈 `빠른 입력` 의 `채우기` 버튼
- 여러 건 한 번에 입력(`커피 3000 그리고 점심 8000`)은 그대로 동작합니다

거래 금액·분류·결제수단 인식, 수정·삭제 명령, 웹 화면 레이아웃은 바뀌지 않았습니다.

## 이미 저장된 기록에 대해

**이 배포는 과거에 잘못 저장된 메모를 되돌리지 않습니다.**
`타 어교체`, `발`, `방` 처럼 글자가 빠진 기록이 남아 있을 수 있습니다.
배포 후 최근 내역을 훑어보고 필요한 항목만 수정해 주세요.
카카오톡에서는 `수정 03번` 으로, 웹에서는 최근 내역에서 바로 고칠 수 있습니다.

## 적용 순서

1. 현재 운영 Worker 전체 소스를 별도 파일로 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 운영 소스와 교체합니다.
3. 저장 후 배포합니다.
4. 저장소 루트에서 아래 명령을 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_69.ps1
```

5. 카카오톡에서 `자동차점검 6만원` 과 `타이어교체 15만원` 을 보내
   **둘 다 저장되고 메모 글자가 그대로인지** 확인합니다.
6. 오래 열어 둔 웹 탭은 새로고침해야 `v22869` 런타임이 적용됩니다.

기존 V22.6.8·V22.7.0·V22.7.1·V22.8.46 SQL은 `/ready`가 정상이라면 다시 실행하지 않습니다.
