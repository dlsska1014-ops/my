# V22.8.70 금액이 틀리게 저장되던 결함 수정 안내

버전: `V22.8.70-AMOUNT-PARSE-FIX`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음
- 환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음
- 모바일 홈 런타임 주소가 `/assets/mobile-home-v22870.js`,
  `/assets/mobile-home-shell-v22870.js` 로 바뀝니다 (immutable 자원)

## 배경 (중요)

V22.8.69 에서 세운 방식(입력 문구를 넣고 **저장된 값을 되읽는다**)을 금액·날짜·
결제수단·분류로 넓혔습니다. **금액이 틀리게 저장되는 결함 2건**을 찾았습니다.

### 1. 자릿수 쉼표가 문장을 잘랐습니다

여러 건 입력을 나누는 구분자에 쉼표가 조건 없이 들어 있었습니다.
`12,345원` 의 쉼표에서 문장이 잘려 **345원만 저장**됐습니다.

| 입력 | 보정 전 | 보정 후 |
|---|---|---|
| `커피 12,345원` | **345원** | 12,345원 |
| `커피 1,200원` | **200원** | 1,200원 |
| `커피 60,000` | 저장 실패 | 60,000원 |
| `커피 100,000원` | 저장 실패 | 100,000원 |

숫자 사이의 쉼표는 자릿수 구분으로 보고 나누지 않습니다.
`빵 2600, 우산 4700` 처럼 항목을 나누는 쉼표는 그대로 동작합니다.

### 2. 단위가 이어진 금액이 두 건으로 쪼개졌습니다

금액 토큰을 찾는 규칙이 `만` 과 `천` 을 따로 잡아 한 번의 입력이
**두 건의 거래로 저장**됐습니다.

| 입력 | 보정 전 | 보정 후 |
|---|---|---|
| `커피 3만5천원` | 30,000원 + 5,000원 **2건** | 35,000원 1건 |
| `커피 1만5천원` | 10,000원 + 5,000원 **2건** | 15,000원 1건 |
| `커피 10만5000원` | **100,000원** (5000 유실) | 105,000원 |

### 3. 한 글자 조사 제거가 멀쩡한 낱말을 잘랐습니다

V22.8.69 에서 조사 제거를 "낱말 끝에서만" 으로 좁혔지만, `이`·`가`·`로` 같은
한 글자 조사는 **명사의 마지막 글자와 구분할 수 없습니다.**
실제 낱말 32개로 재보니 9개가 잘렸습니다.

| 입력 | 보정 전 |
|---|---|
| 고양이 | `고양` |
| 목걸이 | `목걸` |
| 어린이 | `어린` |
| 을지로 | `을지` |
| 한강로 | `한강` |

두 글자 조사(`에서`·`으로`·`에게`·`한테`)만 떼어냅니다. 주된 사례인
`마트에서 12000` → `마트` 는 그대로 처리됩니다.

대신 `회사로 5000` 은 `회사로`, `학교에 3000` 은 `학교에` 로 남습니다.
메모가 조금 덜 다듬어지는 대신 **내용이 사라지지 않는 쪽**을 골랐습니다.

## 함께 확인했지만 문제가 없던 것

금액 21종, 결제수단 8종, 날짜 4종, 수입·지출 구분 5종, 분류 자동추천 8종을
저장값 기준으로 확인했고 위 3건 외에는 모두 정확했습니다.

`주유 5만원`·`택시 12000` 처럼 분류 이름과 금액만 보내면
`지출로 기록할까요, 예산으로 설정할까요?` 하고 되묻습니다.
이는 의도된 흐름이라 그대로 두었습니다.

## 이미 저장된 기록에 대해

**이 배포는 과거에 틀리게 저장된 금액을 되돌리지 않습니다.**
쉼표를 넣어 입력한 기록이 실제보다 작은 금액으로 남아 있을 수 있고,
`3만5천원` 형태가 두 건으로 나뉘어 있을 수 있습니다.
배포 후 최근 내역에서 금액이 이상한 항목을 확인해 주세요.

## 적용 순서

1. 현재 운영 Worker 전체 소스를 별도 파일로 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 운영 소스와 교체합니다.
3. 저장 후 배포합니다.
4. 저장소 루트에서 아래 명령을 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_70.ps1
```

5. 카카오톡에서 `커피 12,345원` 과 `커피 3만5천원` 을 보내
   **각각 한 건씩, 쓴 금액 그대로** 저장되는지 확인합니다.
6. 오래 열어 둔 웹 탭은 새로고침해야 `v22870` 런타임이 적용됩니다.

기존 V22.6.8·V22.7.0·V22.7.1·V22.8.46 SQL은 `/ready`가 정상이라면 다시 실행하지 않습니다.
