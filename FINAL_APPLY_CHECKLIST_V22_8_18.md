# V22.8.18 최종 적용 체크리스트

- [ ] 현재 Worker 코드와 환경변수 이름 목록 백업
- [ ] `node .codex/scripts/verify-repository.mjs` 통과 (체크섬 32개·자동 검사 429개)
- [ ] `src/index.js` 전체 교체
- [ ] SQL·환경변수·Kakao Developers·OpenBuilder를 변경하지 않음
- [ ] `/health`에서 `V22.8.18-WEBAPP-INLINE-FEEDBACK` 확인
- [ ] 웹앱 기록 저장 → 초록 결과가 저장 버튼 자리 근처에 인라인 표시
- [ ] 금액 비운 채 저장 → 빨강 사유가 그 자리에 표시, 화면 밖이면 스크롤되어 보임
- [ ] 긴 폼 아래로 스크롤 후 실패 → 결과가 제출 자리로 끌려와 보임
- [ ] JavaScript를 끈 상태에서도 결과가 화면 상단에 그대로 표시(기능 손실 없음)
- [ ] `수정 01번` → `지출자 변경` → 번호 선택, `삭제 01번` → `복구 01번` 회귀 확인
- [ ] 기록 저장·요약·예산·정산·영수증 OCR·카카오 1:1·그룹 회귀 확인
- [ ] 홈 다크모드에서 인라인 결과 삽입 후에도 셸 스타일 우선순위(마지막 캐스케이드) 유지 확인

실패 시 직전 V22.8.17 `src/index.js`로 전체 코드만 롤백합니다(인라인 주입이라 코드 롤백만으로 완전 복구).
