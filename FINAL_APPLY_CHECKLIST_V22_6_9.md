# V22.6.9 최종 적용 체크리스트

## 배포 전

- [ ] 현재 Worker 코드와 Supabase 운영 데이터를 백업했다.
- [ ] V22.6.8 데이터 무결성 마이그레이션 적용 상태를 확인했다.
- [ ] `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `ADMIN_PASSWORD`, `USER_SESSION_SECRET`을 확인했다.
- [ ] 밈·카드 실적 관련 공개 플래그와 `INCOMPLETE_FEATURE_QA_ENABLED`가 운영에 없다.
- [ ] V22.6.9에는 추가 SQL이 없음을 확인했다.

## Worker 배포

- [ ] `src/index.js` 전체를 교체하고 배포했다.
- [ ] `/health`가 `V22.6.9-SECURITY-SPENDER-PRIVACY-HOTFIX`를 반환한다.
- [ ] `/health`의 `integrity`가 `atomic-dedup-and-cron-lease`다.
- [ ] `/meme-lab`, `/meme-archive`, `/card-benefits`가 404다.

## 보안 설정

- [ ] 로그아웃 또는 관리자 세션 만료 상태에서 `/settings`를 열었다.
- [ ] 비밀번호 입력 전 홈으로 이동하지 않고 인증 폼이 보인다.
- [ ] 틀린 비밀번호 뒤에도 보안 설정 복귀 경로가 유지된다.
- [ ] 올바른 비밀번호 뒤 `/settings`가 열린다.
- [ ] 관리자 비밀번호 변경 뒤 같은 보안 설정 화면에 완료 문구가 보인다.
- [ ] 가계부 옵션의 개인 비밀번호 설정 뒤 원래 가계부 옵션으로 돌아온다.

## 기록 지출자

- [ ] 참여자 2명 이상인 테스트 가계부를 선택했다.
- [ ] 기록 목록에 수입자·지출자 이름이 보인다.
- [ ] 지출 기록의 수정/삭제를 열면 지출자 선택이 보인다.
- [ ] 소유자·관리자가 다른 참여자로 변경해 저장할 수 있다.
- [ ] 일반 구성원은 다른 참여자로 임의 변경할 수 없다.
- [ ] 변경한 이름이 기록·분석·정산에서 일관되게 보인다.

## 문구·개인정보

- [ ] 참여자 이름 변경 뒤 `참여자 이름을 수정했습니다.`가 보인다.
- [ ] `member_alias_updated` 같은 내부 코드가 화면에 보이지 않는다.
- [ ] 시작가이드 하단의 요청된 문구가 없다.
- [ ] 참여자 관리와 정산 표에 사용자 UUID 열이 없다.
- [ ] 관리자 참여자 표에 사용자키 열이 없다.
- [ ] 개인정보 안내에 실제 처리 데이터와 삭제 기준은 계속 고지된다.

## 실기기

- [ ] iPhone Safari·카카오 인앱브라우저에서 보안 복귀와 기록 수정을 확인했다.
- [ ] Android Chrome·카카오 인앱브라우저에서 같은 흐름을 확인했다.
- [ ] 키보드가 열린 상태에서 지출자 선택·저장 버튼·하단 메뉴가 가려지지 않는다.
- [ ] 긴 가계부 이름과 긴 참여자 이름으로 가로 넘침을 확인했다.

