# V22.8.75 검증 보고서 — 통계·종합 리포트 역할 분리

버전: `V22.8.75-ANALYSIS-ROLE-SPLIT`
측정 환경: Node.js 22.22.2 · Chromium 1194 (iPhone 13 뷰포트) · QA 픽스처

---

## 1. 무엇을 재고 무엇을 고쳤나

### 1.1 실측한 중복

120건을 넣은 픽스처로 세 화면의 `<h2>` 제목을 모두 뽑아 비교했습니다.

| 화면 | 크기 | 카드 |
|---|---:|---:|
| 소비 분석(통계) | 86,927 B | 10 |
| 종합 리포트 | 123,728 B | 16 |
| 월 마감 | 72,063 B | 3 |

**제목이 완전히 같은 카드 2개** — `2026-07 한눈에 보기`, `무지출 데이 4일`.
이름만 다르고 내용이 겹치는 쌍 4개 — 지출 흐름↔일별 소비 그래프, 분류별
구성↔이번 달 지출 구성, 요일 패턴↔요일별 소비 추이, 이번 달 예산↔예산 사용 게이지.

이름만 다른 4쌍은 **없애지 않았습니다.** 통계 쪽은 필터에 반응하고(제목이
`지출 흐름`/`수입 흐름`으로 바뀜) 리포트 쪽은 달 전체를 고정해서 보여 주므로
입력이 다릅니다. 지우면 필터 경험이 깨집니다.

### 1.2 제거 방향을 정한 근거

`한눈에 보기` 코크핏은 **종합 리포트 안에서** 이미 네 번 반복되고 있었습니다.

| 코크핏 블록 | 같은 페이지의 더 깊은 카드 |
|---|---|
| 예산 사용 도넛 | 예산 사용 게이지 |
| 분류별 예산 속도(상위 4) | 분류별 예산 사용률(전체 표) |
| KPI 4종 | 총 지출·총 수입·하루 평균·최다 분류·무지출일·월말 예상 지출 |
| 인사이트 한 줄 | 핵심 인사이트 |

반면 소비 분석에서는 코크핏이 **유일한** 달 요약입니다. 나머지 카드는 전부
필터에 반응합니다. 그래서 코크핏은 소비 분석에 남기고 종합 리포트에서 뺐습니다.

`무지출 데이` 챌린지는 **양쪽에 남겼습니다.** 데스크톱 사이드바의 축약
챌린지(`.abNavChallenge`)는 좁은 화면에서 `display:none!important` 로 숨겨지므로,
소비 분석에서 본문 챌린지를 빼면 모바일에서 챌린지가 완전히 사라집니다.
설정 폼은 종합 리포트(`#reportChallenge`)에만 있어 관리 지점은 이미 하나입니다.

### 1.3 화면 안 중복도 함께 제거

소비 분석의 `이번 달 예산` 카드는 바로 위 코크핏과 같은 값을 다시 그렸습니다.
코크핏에 없는 부분만 남기고 `예산 소비 페이스`로 축소했습니다.

- 남김: 전체 예산 게이지, 남은 예산 칩, **날짜별 누적 소비 곡선**
- 제거: 분류별 상위 6개 사용률
- 총액 예산이 0이면 그릴 곡선이 없으므로 카드를 숨김 (분류만 있는 예산은 코크핏이 담당)

---

## 2. 결과 실측

| 화면 | 이전 | 이후 | 차이 |
|---|---:|---:|---:|
| 종합 리포트 HTML | 123,728 B | 122,267 B | **-1,461 B** |
| 소비 분석 HTML | 86,927 B | 87,064 B | +137 B |
| `/my/analysis/app.js` | 52,064 B | 51,385 B | **-679 B** |

소비 분석이 137 B 늘어난 것은 역할을 밝히는 안내 문구 때문입니다.
스크립트 감소분과 합치면 첫 방문 기준 **-542 B** 입니다.

제목 교집합은 **챌린지 1개만** 남았습니다(이전 2개). 월 마감과의 교집합은 0개입니다.

홈 HTML 실사용 예산은 변화 없음 — **44,962 B / 47,104 B (여유 2,142 B)**.

---

## 3. 함께 잡은 결함 — 보조 버튼이 주요 버튼으로 칠해짐

역할을 나누고 종합 리포트를 실제로 열어 보니 히어로 버튼 세 개가 모두 같은
파란색이었습니다. `secondary` 클래스가 붙어 있는데도 적용되지 않았습니다.

브라우저에서 실제로 이긴 규칙을 조회해 원인을 확인했습니다.

```
.abV2281 .btn:not(.light):not(.soft):not(.danger)          클래스 5  ← 이김
body.abV22812Shell.abV2281 :is(a.btn.secondary, ...)       클래스 4 + 요소 2
```

`:not()` 사슬이 명시도를 부풀려 주요 버튼 규칙이 보조 버튼 규칙을 이겼습니다.
바로 옆 `button[type=submit]` 규칙에는 이미 `:not(.secondary)` 예외가 있었는데
`.btn` 쪽에만 빠져 있었습니다. 같은 예외를 붙였습니다.

`.btn secondary` 를 쓰는 화면 6곳을 라이트·다크로 다시 쟀습니다.

| 화면 | 라이트 보조 | 다크 보조 |
|---|---:|---:|
| 종합 리포트 | 16.56:1 | 7.85:1 |
| 백업 | 16.56:1 | 7.85:1 |
| 모임 | 16.56:1 | 7.85:1 |
| 설정 | 16.56:1 | 7.85:1 |

주요 버튼은 두 테마 모두 6.16:1 입니다. 기준(4.5:1) 미달 **0건**입니다.
가로 넘침도 두 화면 모두 0px, JS 오류 0건입니다.

### 측정 도구를 먼저 검증한 기록

CSS 규칙을 훑는 첫 스크립트가 "일치 0건"을 냈습니다. 최신 Chromium 은 중첩
문법 때문에 **일반 규칙도 빈 `cssRules` 를 가지므로**, 그것을 그룹 규칙 신호로
쓰면 모든 규칙을 재귀로 흘려보내게 됩니다. `selectorText` 기준으로 바꾼 뒤
1,497개 규칙을 읽어 원인 규칙을 찾았습니다. "0건"을 결과로 받아들이기 전에
도구부터 확인했습니다.

---

## 4. 회귀 검사

`validation/validate-analysis-role-split-v22875.mjs` (**44개 확인**)

- 코크핏 렌더 호출이 저장소 전체에 **정확히 하나**만 존재
- 소비 분석에는 코크핏이 있고 종합 리포트에는 없음
- 종합 리포트가 링크하는 앵커(`#reportCockpitTitle`)가 소비 분석에 실제로 존재
- 코크핏이 대신하던 4개 분석이 종합 리포트에 여전히 존재
- 두 화면의 제목 교집합이 챌린지 1개 이하
- 각 화면 안에서 같은 제목이 두 번 나오지 않음
- 월 마감과의 교집합 0개
- 클라이언트 번들이 분류별 예산 사용률을 다시 그리지 않고 페이스 차트는 유지
- 보조 버튼을 삼키던 주요 버튼 규칙이 되살아나지 않음

### 되돌려서 잡히는지 확인

| 되돌린 결함 | 검사 결과 |
|---|---|
| 코크핏을 종합 리포트에 복귀 | FAIL: deep report does not repeat the cockpit |
| 분류별 예산 사용률을 소비 분석에 복귀 | FAIL: the stats budget card no longer redraws per-category usage |
| `:not(.secondary)` 제거 | FAIL: the primary blue fill no longer swallows secondary buttons |

세 경우 모두 검사가 잡았고, 되돌린 것을 원복하면 다시 통과합니다.

---

## 5. 계약 변경에 맞춘 기존 검사 수정

| 검사 | 변경 |
|---|---|
| `validate-report-dashboard-v22856.mjs` | `reportKpis` 줄바꿈 확인을 코크핏이 있는 소비 분석 쪽으로 옮기고, 종합 리포트에는 코크핏 부재 확인 추가 |
| `validate-ux-principles.mjs` | `insightClientMain` / `renderMyAnalysisHtml` 함수 블록 해시를 V22.8.75 기준으로 갱신 |
| `validate-performance-v22811.mjs` | 홈 기본 스타일시트 주소 `v22873 → v22875`, 바이트 고정값 갱신 |

---

## 6. 전체 결과

- 저장소 자동검사 **2,736개 전부 통과**
- `node --check src/index.js` 통과, ESM 진입점 통과
- 신규 SQL 없음 · 환경변수 변경 없음

## 7. 배포 후 사람이 확인할 것

1. `소비 분석`에 `한눈에 보기`가 있고 `종합 리포트`에는 없는지
2. `종합 리포트` 맨 위 버튼 세 개 중 파란 것이 하나인지
3. `← 한눈에 보기(소비 분석)`을 눌렀을 때 코크핏 위치로 이동하는지
4. 모바일에서 `소비 분석`의 챌린지가 그대로 보이는지
5. 총액 예산을 설정한 가계부에서 `예산 소비 페이스` 카드가 보이는지

운영 도메인(`https://ttokttok-accountbook.com`)은 이 환경에서 접근할 수 없어
직접 확인하지 못했습니다. 위 항목은 배포 후 실기기에서 확인해야 합니다.
