# V22.8.68 모바일 조작 영역·접근성 적용 안내

버전: `V22.8.68-TAP-TARGET-ACCESSIBILITY`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음
- 환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음
- 셸 스타일시트 주소가 `/assets/accountbook-shell-v22868.css`로 바뀝니다 (immutable 자원)

## 배경

순차 점검 5단계인 **접근성·조작 영역** 점검입니다.
사용자 화면 24개를 390px와 1440px 실제 Chromium으로 렌더하고,
보이는 조작 요소마다 실제 탭 영역 크기와 이름·구조를 검사했습니다.

### 손가락으로 누르기 어려운 조작 요소 58건

접기 헤더와 카드 헤더의 링크가 **14~39px**밖에 되지 않았습니다.
권장 최소치는 44px입니다.

| 대상 | 보정 전 높이 |
|---|---|
| 상단바 가계부 이름 링크 (18개 화면) | 28px |
| 종합 리포트 접기 헤더 6종 | 29px |
| 자산·계좌 `상세 정보 수정`·`은행·발급사 입력` | 20px |
| 통계 `표로 보기` | 19px |
| 홈 `월간 리포트 →` | 15px |
| 홈 `최근 기록 확인 →` | 19px |
| 통계·리포트 `거래 캘린더 보기`·`전체 예산 보기` | 16~19px |
| 가계부 카드 `열기`·`옵션`·`멤버` | 39px |
| 자산 등록 `＋ 빠른 등록`·`＋ 카카오페이 등록` | 22~35px |
| 가져오기·부부 정산의 체크박스 라벨 | 15~39px |

**보정 후 58건 → 1건.** 남은 1건은 문장 안에 들어간 인라인 링크
(`주간 리포트·반복지출 탐지는 종합 리포트에 있습니다`)로,
WCAG 2.5.8이 명시적으로 예외로 두는 경우입니다.

### 보정 방식

레이아웃을 늘리지 않도록 **셸 스타일시트에만** 규칙을 넣었습니다.
사용자 화면 HTML은 **한 바이트도 늘지 않았습니다.**

- 접기 헤더는 `min-height`만 키우고 `display`는 건드리지 않습니다.
  목록 표시(`::marker`)가 사라지면 접힘 여부를 알 수 없기 때문입니다.
- 표 안의 링크는 행 높이를 늘리지 않도록 여백만 바깥으로 넓혔습니다
  (`padding-block:12px; margin-block:-12px`).
- 이미 44px 이상인 요소는 `min-height`가 줄이지 않으므로 그대로입니다.

## 함께 확인했지만 문제가 없었던 것

24개 화면 × 2개 폭에서 아래 항목은 **전부 0건**이었습니다.

- 이름 없는 폼 컨트롤, 이름 없는 버튼·링크
- `alt` 없는 이미지
- `h1` 누락, 헤딩 단계 건너뜀
- 중복 `id`, 존재하지 않는 `for`·`aria-*` 참조
- 양수 `tabindex`
- `lang` 누락, `main` 랜드마크 누락

이 항목들은 이번 릴리스에서 회귀 검사로 고정했습니다.

## 적용 순서

1. 현재 운영 Worker 전체 소스를 별도 파일로 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 운영 소스와 교체합니다.
3. 저장 후 배포합니다.
4. 저장소 루트에서 아래 명령을 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_68.ps1
```

5. 실기기에서 접기 헤더(종합 리포트의 `이번 달 지출 구성`, 자산·계좌의 `상세 정보 수정`)와
   카드 헤더 링크(`최근 기록 확인 →`, `월간 리포트 →`)를 눌러 **한 번에** 반응하는지 확인합니다.
6. 오래 열어 둔 탭은 새로고침해야 `v22868` 셸이 적용됩니다.

기존 V22.6.8·V22.7.0·V22.7.1·V22.8.46 SQL은 `/ready`가 정상이라면 다시 실행하지 않습니다.
