# V22.8.69 검증 보고서

검증일: 2026-08-03

## 점검 범위

`자동차점검 6만원` 입력이 실패한다는 제보를 카카오 스킬 경로에서 재현했습니다.
재현 과정에서 같은 경로에 있는 결함 **3건**을 찾았습니다.

## 1. 중복 방지가 저장되지 않은 요청까지 잠갔습니다

중복 방지 잠금을 응답을 만들기 **전에** 걸고 있었습니다.
저장이 일어나지 않은 요청도 함께 잠겨, 같은 문구를 다시 보내면 막히고
결국 아무것도 저장되지 않았습니다.

제보 순서를 그대로 재현한 결과:

| 순서 | 보정 전 | 보정 후 |
|---|---|---|
| `자동차점검 6만원` | 가계부 선택 안내 (저장 0건) | 동일 |
| `2` | 가계부 선택 완료 | 동일 |
| `자동차점검 6만원` 재입력 | **`방금 같은 내용을…` · 저장 0건** | **`지출 저장했어요` · 저장 1건** |

실제로 저장·수정·삭제가 끝난 응답에서만 잠그도록 바꿨습니다.

진짜 중복은 계속 막힙니다.

| 확인 | 결과 |
|---|---|
| `커피 3000` 연속 2회 | 2회차 차단, 저장 **1건** |
| 인식 실패 문구 연속 2회 | 두 번 다 정상 안내(차단 없음), 저장 0건 |

## 2. 메모에서 글자가 사라졌습니다

조사를 위치와 무관하게 지우고 있어 낱말 한가운데 글자까지 지워졌습니다.
표본 29개 중 **21개 손상**.

| 입력 | 보정 전 | 보정 후 |
|---|---|---|
| 타이어교체 | `타 어교체` | `타이어교체` |
| 이발 | `발` | `이발` |
| 가방 | `방` | `가방` |
| 이마트 | `마트` | `이마트` |
| 에어컨 | `어컨` | `에어컨` |
| 은행수수료 | `행수수료` | `은행수수료` |
| 이유식 | `유식` | `이유식` |
| 가족외식 | `족외식` | `가족외식` |
| 을지로식당 | `지 식당` | `을지로식당` |
| 이어폰 | `어폰` | `이어폰` |

조사는 **낱말 끝에서만** 떼어냅니다. 기존 정리 동작은 그대로입니다.

| 입력 | 저장되는 메모 |
|---|---|
| `마트에서 12000` | `마트` |
| `회사로 5000` | `회사` |
| `병원 3000 결제` | `병원` |

같은 결함이 **웹 홈 빠른 입력의 `채우기`** 에도 있었고 함께 고쳤습니다.

## 3. `또`가 든 낱말이 문장으로 쪼개졌습니다

여러 건 입력 분리기가 `또`를 위치 조건 없이 접속사로 취급했습니다.
`로또 10000` → `로` + `10000` 으로 쪼개져, 앞쪽은 금액이 없어 버려지고
뒤쪽은 내용이 없어 메모가 통째로 사라졌습니다(`기타지출` 로 대체).

접속사는 낱말 경계에서만 나누도록 바꿨습니다.

| 입력 | 보정 전 | 보정 후 |
|---|---|---|
| `로또 10000` | 메모 사라짐 | `로또` |
| `또또 10000` | 메모 사라짐 | `또또` |
| `사랑 10000` | `사` | `사랑` |
| `가랑비 10000` | 손상 | `가랑비` |

여러 건 입력은 그대로 동작합니다.

| 입력 | 결과 |
|---|---|
| `커피 3100 그리고 점심 8100` | 2건 저장 |
| `우유 3200랑 두부 8200` | 2건 저장 |
| `택시 5300 또 버스 1300` | 2건 저장 |

## 4. 성능·자원

| 항목 | 예산 | 실측 | 여유 |
|---|---|---|---|
| 기준 픽스처 홈 HTML | 35,840 B | 34,958 B | 882 B |
| 200행 실사용 홈 HTML | 45,056 B | 44,216 B | 840 B |

**HTML 바이트 변화 0.**

모바일 홈 런타임 2종은 내용이 바뀌어 immutable 주소를 올렸습니다.

| 자원 | 보정 전 | 보정 후 |
|---|---:|---:|
| `mobile-home-*.js` | 30,639 B (`v22855`) | 30,767 B (`v22869`) |
| `mobile-home-shell-*.js` | 33,096 B (`v22855`) | 33,224 B (`v22869`) |
| `accountbook-shell-v22868.css` | 149,831 B | 동일 |
| `accountbook-nav-v22862.js` | 19,742 B | 동일 |
| `accountbook-v5-v22861.js` | 62,354 B | 동일 |

`validate-performance` 의 모바일 홈 런타임 바이트 고정값도 새 해시로 갱신했습니다.
(고정값은 "모르는 사이에 바뀌지 않게" 하는 장치이므로, 의도한 변경에서는 함께 갱신합니다.)

## 5. 자동 검사

- 신규 회귀 **57개** (`validation/validate-kakao-memo-repeat-v22869.mjs`)
- 저장소 전체 **2,308개** 자동검사 통과
- ESM `default.fetch` 확인, 체크섬 매니페스트 일치

신규 회귀가 고정하는 것:

- 조사 제거가 낱말 끝에서만 일어난다 (위치 무관 제거 정규식이 다시 들어오면 실패)
- 접속사 분리가 낱말 경계에서만 일어난다
- 메모 표본 27개가 입력 그대로 저장된다
- `마트에서`·`회사로`·`결제` 같은 조사·서술어 정리는 계속 동작한다
- 여러 건 입력이 계속 2건으로 나뉜다
- 중복 방지는 저장이 끝난 뒤에만 걸린다
- 인식 실패·가계부 선택 안내 뒤 같은 문구를 다시 보내면 저장된다
- 연속 중복 입력은 여전히 한 건만 저장된다

## 6. 운영 변경

- 신규 SQL 없음
- 환경변수·Secret 변경 없음
- Kakao Developers·OpenBuilder 변경 없음

## 배포 후 확인 부탁드립니다

1. 카카오톡에서 `자동차점검 6만원` → 저장되는지
2. `타이어교체 15만원` → 메모가 `타이어교체` 그대로인지
3. 같은 지출을 연속 2번 보내면 두 번째가 막히는지
4. 웹 홈 `빠른 입력` 에 `타이어교체 15만원` 을 넣고 `채우기` → 내용 칸이 그대로인지
5. 오래 열어 둔 웹 탭 새로고침 후 `v22869` 런타임 적용

> ⚠️ **이 배포는 과거에 잘못 저장된 메모를 되돌리지 않습니다.** 글자가 빠진 기록이
> 남아 있을 수 있으니 최근 내역을 한 번 훑어보시길 권합니다.
