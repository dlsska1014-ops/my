# V22.8.52 Validation Report

버전: `V22.8.52-SKILL-IP-GUARD-CSV-SAFE-NAME-FIX`

## 수정 범위

`SECURITY_AND_FEATURE_AUDIT_V22_8_51.md`에서 확인된 3건.

| 등급 | 항목 | 수정 |
| --- | --- | --- |
| High | S1 `/skill` 호출자 인증 없음 | **부분 조치** — IP 단위 상한(`SKILL_IP_GUARD_LIMIT`, 기본 3,000/분) 추가 |
| Medium | S2 CSV 수식 인젝션 | `csvSafeText()`로 모든 CSV 셀 이스케이프 |
| Medium | F1 웹 가계부 이름 거부 | `sanitizeWebHouseholdNameInput()` 분리 |

## S1은 완화책입니다

IP 상한은 `botUserKey` 회전을 통한 대량 남용(무제한 사용자 행 생성, 초대코드 대입)을
막습니다. **`botUserKey`가 여전히 만료·회수 없는 자격증명이라는 사실은 바뀌지
않습니다.** 근본 해결인 공유 시크릿 헤더는 Kakao OpenBuilder 콘솔에서 커스텀 헤더
설정 가능 여부를 확인한 뒤 별도로 진행해야 합니다.

검증된 동작:

| 시나리오 | 결과 |
| --- | --- |
| `SKILL_IP_GUARD_LIMIT=60`, 동일 IP에서 키 회전 80회 | 20회 차단 (이전 0회) |
| 다른 IP의 첫 요청 | 차단 없음 |
| 기본 상한(3,000)에서 키 회전 120회 | 0회 차단 (정상 트래픽 영향 없음) |
| 동일 키 70회 | 10회 차단 (사용자별 제한 유지) |
| `GET /skill` | 가드 대상 아님, 기존 동작 유지 |

## S2 검증

| 항목 | 결과 |
| --- | --- |
| `=cmd\|'/c calc'!A1`, `@SUM(1+1)`, `+1+1`, `-2+3`, 탭 시작 | 전부 `'` 접두어로 무력화 |
| `/my/backup.csv`, `/admin/csv`, `/admin/import/template.csv`, 가져오기 이력, `nlu-failures.csv` | 공통 `csvSafeText()` 경유 |
| 금액 등 순수 숫자 셀 | 접두어 없이 그대로 유지 |
| 원문 가독성 | `'@SUM(1+1)`로 원래 텍스트 확인 가능 |

## F1 검증

| 입력 | 이전 | 이후 |
| --- | --- | --- |
| `가족 생활비`, `생활비`, `가족`, `모임`, `여행`, `우리집`, `부부`, `회비`, `휴가`, `기타` | 전부 거부 | 전부 생성 가능 (10/10) |
| `도움말`, `1`, `12`, `a` | 거부 | 거부 유지 |
| 카카오 `새 가계부 만들기` → `가족` | 종류 선택 | 종류 선택 유지 |

`isUnsafeKakaoNameInputV2254()`가 내부에서 다시 `parseKakaoCreateKind()`를 호출하고
있어, 안전성 검사만 `isUnsafeHouseholdNameContent()`로 분리했습니다. 링크·이메일·
전화번호·초대코드 형태·금액만 있는 이름 차단은 웹에서도 그대로 유지됩니다.

## 자동검사

| 검증 | 개수 |
| --- | --- |
| 영수증 | 56 |
| 카카오 그룹 | 22 |
| 카카오 수정·삭제·복구 | 130 |
| 가계부·운영 보안 | 87 |
| 참여자 역할 스키마 | 20 |
| UX 원칙 | 56 |
| 사용자 화면·홈 버튼·테마·성능 | 153 |
| AdSense 심사·V2 화면 | 261 |
| V5 안정화 | 41 |
| 핵심 쓰기 스모크 | 97 |
| UI/UX 1~4단계 | 218 |
| **합계** | **1141** |

체크섬 57개, ESM `default.fetch`, 작업 트리·스테이징 공백 검사 통과.

## 추가된 회귀 검사 (12개)

`validation/validate-household-security.mjs` (75 → 87)

- 스킬 IP 가드가 키 회전을 차단하고 다른 IP는 영향받지 않음
- 기본 상한에서 정상 트래픽이 차단되지 않음
- CSV 내보내기에 수식으로 시작하는 셀이 남지 않고 숫자는 보존됨
- 웹 가계부 이름이 채팅 키워드 4종을 허용하고 봇 명령어·번호는 계속 거부함

## 수동 확인 필요

- 배포 후 카카오 챗봇 실제 응답 정상 여부
- 실제 Excel·Google Sheets에서 내보낸 CSV 열람
- Kakao OpenBuilder 콘솔의 커스텀 헤더 지원 여부 (S1 근본 해결 전제)

## 변경 없음

신규 SQL, Supabase 스키마, Secret, Kakao Developers, OpenBuilder 설정.
`SKILL_IP_GUARD_LIMIT`은 선택 환경변수로 미설정 시 기본값 3,000을 사용합니다.
정적 자산 내용 변경이 없어 자산 주소를 유지합니다.
