# 카카오 그룹 챗봇 정본 규칙과 자료 충돌 정리

## 1. 자료 우선순위

1. 기술 규격과 API 필드는 `그룹 챗봇 스킬 서버 개발 Quick Start 가이드 v1.11.1 (2026-04-20)`을 최우선으로 사용한다.
2. 가입, 권한, 프로필, 심사, 광고, 선톡, UX 정책은 `[kakao] 그룹 챗봇 beta 가이드 48장`을 사용한다.
3. 두 자료가 충돌하면 최신 버전의 기술 가이드를 우선하고, 출시 전에 `chatbot@kakaocorp.com` 또는 최신 공식 도움말로 재확인한다.
4. 이 자료는 beta 정책이므로 확정된 영구 규격으로 단정하지 않는다.

## 2. 서비스 범위와 권한

- 그룹 챗봇은 그룹채팅방에서 작동하며 beta 가이드 기준 팀채팅과 일반채팅을 지원한다.
- 나와의 채팅과 오픈채팅은 지원하지 않는다.
- 일반 채널 1:1 챗봇은 누구나 만들 수 있지만 그룹 챗봇은 제휴 및 권한 부여가 필요하다.
- 그룹 챗봇 권한이 보이지 않으면 카카오에 관리자 카카오계정을 전달해 권한을 받아야 한다.
- 사업자가 아니어도 서비스는 가능하지만 카카오 디벨로퍼스 앱 연결, 카카오 로그인·싱크, 일부 API와 Event API에 제약이 생길 수 있다.
- 운영 채널과 개발 채널은 반드시 서로 다른 채널을 연결한다.
- 개발자용 채널은 테스트 목적이며 자료 기준 팀채팅방 1개에서만 사용할 수 있다.
- 채널 연결 해제 또는 마스터 변경 시 개발 챗봇이 채팅방에서 강제 퇴장될 수 있다.

## 3. 프로필과 초기 화면

- 채널 이름과 채널 프로필 이미지가 챗봇의 이름과 프로필로 노출된다.
- 채널명은 기능을 직관적으로 이해할 수 있고, 타이핑하기 쉽고 간결해야 한다. 복잡한 대소문자와 띄어쓰기를 피한다.
- `게임봇`, `건강봇`, `이미지봇`처럼 일반 명사와 봇을 단순 결합한 이름은 피하고 브랜드 정체성을 반영한다.
- 프로필 이미지 안의 글자는 작은 화면에서 읽기 어려우므로 사용하지 않는 편이 좋다.
- 의료인 오인 등 캡처·확산 시 리스크가 생길 이름과 이미지는 피한다.
- 채널 홈에는 이메일, 전화번호, 홈페이지 또는 1:1 채팅 등 고객센터 경로를 제공한다.
- 봇 한 줄 설명은 말줄임 방지를 위해 한글 약 14자 이내를 권장한다.
- 웰컴 메시지는 채팅창 절반 이하를 차지하도록 짧게 작성하고 `챗봇 멘션하기` 버튼을 제공한다.
- 대표 명령어 설명은 명령 자체가 모호하거나 추가 파라미터 입력이 필요할 때만 사용한다.

## 4. SkillRequest와 식별자

- 스킬 URL은 SkillRequest를 JSON body에 담은 HTTP POST 요청을 받는다.
- 주요 영역은 bot, intent(block), action, userRequest, context다.
- `botUserKey`: 봇 기준 사용자 비식별 키. 봇이 달라지면 같은 사람도 다른 값이 될 수 있다.
- `appUserId`: 카카오 로그인 사용자인 경우 앱 사용자와 연결할 수 있는 식별자다.
- `botGroupKey`: 채팅방 비식별 키다.
- 개발 채널에서는 botId 끝에 `!`가 붙을 수 있다.
- SkillRequest는 하위 호환성을 유지하며 필드가 추가될 수 있으므로 알 수 없는 필드가 있어도 실패하지 않게 파싱한다.

## 5. SkillResponse와 화면 제약

- 지원: SimpleText, SimpleImage, TextCard, BasicCard, ListCard, ItemCard.
- 미지원: QuickReplies, CommerceCard, Carousel.
- SimpleText는 `extra.markdown=true`로 Markdown을 사용할 수 있다.
- ListCard는 `listLayout=ranking`을 사용할 수 있다.
- 카드 버튼은 `buttonLayout=horizontal`일 때 최대 2개, `vertical`일 때 최대 5개다.
- 챗봇 응답이 채팅창 전체를 덮지 않게 하고, 그룹 대화 흐름을 방해하지 않도록 짧게 구성한다.
- 웹·앱으로 보내기 전에 챗봇 응답 자체로도 충분한 핵심 정보를 제공한다.

## 6. 플러그인과 멘션

- 버튼 액션: guide, share, invite, inviteMember, mention, settings.
- 파라미터 입력 플러그인: 이미지 전송, 시간 선택.
- 발화에서 멘션된 사용자를 받으려면 `@sys.user.mention` 시스템 엔티티를 활성화하고 패턴 발화에 태깅한다.
- 응답 멘션은 SimpleText에서만 동작한다.
- 보수적인 구현 기준은 SkillResponse 한 번당 최대 15명이다. 48장 가이드는 말풍선 1개당 15명, 3개 말풍선 45명을 설명하지만 v1.11.1 본문은 `한 응답에 최대 15명`이라고 명시하므로 출시 전에 카카오 확인 없이 15명을 넘기지 않는다.

## 7. 초대, Webhook과 채팅방 API

- 프로모션 초대 링크 형식: `https://pf.kakao.com/{encoded_profile_id}/chatbot/invite?referer={referer}`.
- 입장 이벤트에서 inviter의 botUserKey와 referer를 받을 수 있다.
- 상태 Webhook 이벤트: entrance, leave, inviteMember.
- Webhook 신청 정보: botId, HTTPS 권장 URL, 필요한 Header.
- 채팅방 목록: `GET /v3/bots/{botId}/group-chat-rooms`, pageSize 10·20·50·100, lastBotGroupKey 커서 사용.
- 응답의 `isSubscribed`는 Event API 선톡 수신 설정 여부다.
- 멤버 목록: `GET /v2/bots/{botId}/group-chat-rooms/{botGroupKey}/members`.

## 8. 알림, 선톡과 콜백

- 사용자가 직접 켜고 시간을 관리하는 정기 메시지는 `알림 기능`으로 구현한다.
- 알림 전용 블록은 일반 대화 블록과 분리하고 일반·필수 파라미터를 설정하지 않는다.
- 조건이 맞지 않을 때는 `template.outputs=[]`로 빈 응답을 반환해 발송하지 않는다.
- 알림 등록에는 봇 ID, 대상 채널, 제목, 설명, 시간 고정 여부, 기본 시간, 주기, 이벤트 이름이 필요하다.
- 일반 Event API 선톡은 beta 가이드 기준 사전 심사가 필요하고 최대 월 2회로 제한된다. 이 제한은 사용자가 설정한 정기 알림과 구분한다.
- 선톡 심사 시 메시지 내용, 랜딩, 예상 발송 시간, 예상 채팅방 규모를 전달한다.
- 선톡은 1~2줄로 간결하게 작성하고 이미지 사용을 지양한다.
- 광고, 무관한 홍보, 유해 소재, 반응이 낮아 대량 퇴장을 유발할 메시지, 대화 흐름을 방해하는 장문은 발송할 수 없다.
- Event API 한 번에 최대 100개 채팅방을 지정할 수 있고 taskId로 결과를 조회한다.
- 생성형 AI 콜백 URL은 그룹 챗봇에서 5분간 유효하며 1회 호출할 수 있다.

## 9. URL 랜딩과 인증

- URL 버튼 클릭 시 botUserKey, appUserId, botGroupKey가 쿼리 파라미터로 자동 전달될 수 있다.
- 모든 카드의 일반 버튼, ListCard 헤더와 아이템이 대상이다.
- 전달된 쿼리 값만으로 로그인 또는 권한을 확정하지 말고 서버에서 서명, 만료, 사용자 인증과 권한을 검증한다.
- 앱 설치 시 챗봇 응답과 연관된 앱 내부 화면으로 이동하고, 미설치 시 앱스토어로 분기한다.
- 랜딩 화면을 팝업, 모달 또는 광고로 가리지 않는다.
- 사업자 앱 키 인증은 `Authorization: KakaoAK {REST_API_KEY}`를 사용한다.
- 개인 등 앱 키 발급 제한 시 신청받은 챗봇 키로 `Authorization: KakaoBK {KEY}`를 사용할 수 있다.
- 운영 호스트는 `https://bot-api.kakao.com`이다.

## 10. 개인정보와 AI

- 챗봇은 자신에게 직접 전달된 명령어만 받을 수 있다.
- `단톡방 대화를 읽는다`, `대화 내용을 학습한다`, `대화 이력을 분석한다`처럼 채팅방 전체를 볼 수 있다고 오인하게 하는 표현을 사용하지 않는다.
- 유저 발화를 AI 모델 성능 개선이나 학습 데이터로 사용하지 않는다.
- 학습에 사용하려면 명시적 동의가 필요하지만 beta 환경은 그 동의 장치를 제공하지 않으므로 원칙적으로 학습에 사용할 수 없다.
- 발화를 일정 기간 저장하면 도움말에 저장 사실과 보관 기간을 명시한다.

## 11. 광고와 오픈 심사

- 챗봇 응답 안에 광고를 포함할 수 없다.
- 서비스 랜딩에서 광고를 노출하려면 카카오에 미리 공유해 허용 여부를 검토받는다.
- 선정적 광고는 금지하며 랜딩 첫 화면과 정보 탐색을 광고가 가리면 안 된다.
- 심사 항목: 광고, 기본 사용성, 편리한 UX, 연관 랜딩, 앱 설치 여부 분기, 그룹 대화 맥락, 두 명 이상 상호작용, 지속적 업데이트, 타겟 연령 적합성.
- `결제` 같은 직접적인 구매 유도 문구와 광고로 오인될 문구를 피한다.
- 사용 횟수 제한 안내는 가능하다.

## 12. 개발, 배포와 문제 해결

- 개발 채널의 변경은 즉시 확인할 수 있고 운영 채널은 배포 후 반영된다.
- 테스트 완료 후 관리자만 있는 팀채팅 초대 링크를 카카오에 전달한다.
- 개발 챗봇은 자료 기준 팀채팅방 1개에서 테스트하며 챗봇 마스터가 방장이어야 한다.
- 운영 채널의 채널 홈에 챗봇 초대 카드를 설정할 수 있다.
- 문제 상황에서 `@{봇이름} .showmethebug`로 chp로 시작하는 Footprint ID를 확인한다.

## 13. 자료 간 충돌과 정정

### plusfriendUserKey

- 48장 가이드 슬라이드 28에는 plusfriendUserKey가 표시되어 있다.
- 개발 가이드 v1.11.1 변경 이력은 plusfriendUserKey 제거를 명시한다.
- 정본 처리: 신규 구현에서 plusfriendUserKey를 사용하지 않는다.

### 멘션 상한

- 슬라이드 33: 말풍선 1개당 15명, 3개 말풍선이면 45명으로 설명한다.
- 개발 가이드: 한 응답에 최대 15명으로 설명한다.
- 정본 처리: 카카오 확인 전까지 SkillResponse 전체 15명 상한으로 구현한다.

### 공유 플러그인 분류

- 슬라이드 34는 시간입력·공유·이미지 플러그인을 함께 소개한다.
- 개발 가이드는 share를 버튼 액션으로, 이미지·시간을 파라미터 플러그인으로 구분한다.
- 정본 처리: 구현 JSON에서는 share 버튼 액션과 파라미터 플러그인을 구분한다.

### Event 결과 조회 변수명

- 예제와 설명은 `GET /v1/tasks/{taskId}`를 사용한다.
- API 표의 일부 문구는 path variable을 botId로 잘못 표기한다.
- 정본 처리: taskId를 사용한다.
