# CHANGELOG

## V22.8.56 · 홈 HTML 예산 개정 · 2026-07-30

- `AGENTS.md` 성능 예산을 측정 조건별로 분리해 개정
  - 기준 픽스처(당월 6행) 35KB 이하 — 기존 값 유지
  - 실사용 부하(당월 200행, 피드 카드 10장) 44KB 이하 — 신규
- 근거는 V22.8.55 마크업 축소 후 실측 42.5KB(43,542바이트), 여유 약 1.4KB
- 50건·200건·400건·3,200건 모두 42.5~42.6KB로, 피드 10장 고정 이후 크기가 평탄함을 확인
- 35KB는 카드가 6장뿐인 픽스처에서 정한 값이라 실사용 부하에 적용할 수 없음을 문서화
- 검증의 `REALISTIC_HOME_CEILING`(임시 래칫)을 `REALISTIC_HOME_BUDGET`(문서화된 예산)으로 승격
- `BASELINE.md`·`RELEASE-CHECKLIST.md` 예산 문구를 측정 조건과 함께 정리
- `src/index.js`·정적 자산·검사 개수 변경 없음, 총 1,149개 자동검사와 체크섬 64개 통과
- 신규 SQL·Supabase 스키마·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.55 · 피드 카드 마크업 축소 · 2026-07-29

- 거래 카드의 삭제 폼을 수정 폼에 통합 (`formaction="/admin/delete"` + `formnovalidate`)
- 카드마다 중복됐던 숨은 입력 4개와 `<form>` 한 쌍 제거, 삭제 확인창은 유지
- 화면 텍스트를 그대로 복제하던 `data-search` 속성 제거
- 클라이언트 빠른 검색을 `data-search` 대신 카드 본문 `textContent` 기준으로 변경
- 실사용 200행 기준 홈 HTML 45.3KiB → 42.5KiB (2.8KiB 축소)
- **35KB 목표 예산은 아직 미달** — 남은 차이는 카드마다 즉시 렌더되는 수정 폼
- 검증에 실사용 200행 측정을 추가하고 44KiB 상한 래칫으로 재증가 회귀 차단
- 6행 픽스처만 측정해 실사용 초과를 놓치던 사각지대 해소
- 변경된 immutable 자산 주소 상향: `mobile-home-v22855.js`, `mobile-home-shell-v22855.js`
- 회귀 검사 2개 추가, 총 1,149개 자동검사와 체크섬 64개 통과
- 신규 SQL·Supabase 스키마·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.54 · 조회 페이지 크기 조정 · 2026-07-29

- `fetchPostgrestRows` 기본 페이지 크기를 500 → 1000으로 올려 대량 월의 DB 왕복 축소
- 8,000건 월의 `/app` DB 호출 25회 → 18회, 거래 질의 16회 → 10회
- 페이지 크기를 PostgREST `max-rows`보다 크게 잡으면 짧은 페이지를 데이터 끝으로
  오인해 조용히 잘리므로, 기존 클램프 상한 1000을 넘기지 않는 범위로 제한
- 1000 초과는 운영 PostgREST `max-rows` 확인이 선행되어야 함을 주석·문서에 명시
- 회귀 검사 4개 추가 — 페이지 크기, 질의 횟수, 합계 대조(데이터 손실 없음)
- 4라운드 점검 결과 `PERF_A11Y_AUDIT_REPORT_V22_8_53.md` 반영
- 홈 HTML 45KiB 예산 초과와 9회 예산 초과는 방향 판단이 남아 별도 과제로 분리
- 신규 SQL·Supabase 스키마·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.53 · 예산 사용률 표기 수정 · 2026-07-29

- 홈의 `예산 사용률` 표기가 100%에서 잘려 초과 지출을 숨기던 문제 수정
- 막대 너비(`budgetBarPercent`)와 표기 숫자(`displayBudgetPercent`)를 분리
- 예산 200,000원에 지출 414,680원일 때 홈이 100%가 아닌 207%를 표시
- `/budgets` 화면과 값이 일치하도록 정렬
- 회귀 검사 2개 추가, 총 1,143개 자동검사와 체크섬 61개 통과
- 3라운드 심층 점검 결과 `DEEP_AUDIT_REPORT_V22_8_52.md` 추가
- 신규 SQL·Supabase 스키마·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.52 · 스킬 IP 가드·CSV 안전·이름 검증 · 2026-07-29

- 카카오 `/skill`에 IP 단위 상한 추가 (`SKILL_IP_GUARD_LIMIT`, 기본 3000/분)
- 호출자가 정하는 `botUserKey`를 바꿔가며 보내 사용자별 제한을 우회하던 경로 차단
- 정상 그룹 챗봇 트래픽보다 훨씬 높은 상한이라 운영 영향 없음. `GET /skill`은 대상 제외
- S1은 완화책이며 공유 시크릿 헤더 검증은 OpenBuilder 콘솔 확인 후 별도 진행
- CSV 내보내기에서 `=`·`+`·`-`·`@`·탭·CR로 시작하는 셀을 `csvSafeText()`로 무력화
- `/my/backup.csv`, `/admin/csv`, 가져오기 템플릿·이력, `nlu-failures.csv` 공통 적용
- 순수 숫자 셀은 접두어 없이 유지해 금액 표시와 재가져오기 호환 보존
- 웹 가계부 이름 검증을 `sanitizeWebHouseholdNameInput()`으로 분리
- `가족 생활비`·`생활비`·`모임`·`여행` 등 카카오 종류 선택지와 겹치던 이름 허용
- 봇 명령어·번호·링크·전화번호·초대코드 형태·금액만 있는 이름은 계속 거부
- `isUnsafeHouseholdNameContent()`로 안전성 검사만 분리해 웹·카카오 공용화
- 회귀 검사 12개 추가, 총 1,141개 자동검사와 체크섬 57개 통과
- 신규 SQL·Supabase 스키마·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.51 · 입력 금액·런타임 정규식 수정 · 2026-07-29

- 웹 거래 입력·수정의 금액 칸이 카카오 발화용 자연어 추출기 대신 폼 전용 숫자 파서 사용
- 4자리 미만 숫자를 금액 후보에서 제외하던 규칙 때문에 거부되던 1,000원 미만 저장 복구
- 거래 금액 상한 20억 원과 안전 정수 검사 적용, 초과 입력은 저장 전 차단
- `/app` 스마트 한 줄 입력 런타임의 정규식 이스케이프 소실 복구 (`\d`·`\s`·`\B`·`\[`)
- 금액·날짜·유형·메모 추출과 천단위 콤마 표시가 배포 자산에서 다시 동작
- 배포 자산에서 파서를 추출해 실제 실행하는 회귀 검사 9개 추가
- 변경된 immutable 자산 주소 상향: `mobile-home-v22851.js`, `mobile-home-shell-v22851.js`
- `validMonth`가 월을 `01~12`로 제한해 `?month=2026-13` 등의 500 응답 차단
- 예산 저장이 빈 값·해석 불가 입력을 거부해 기존 예산이 0원으로 덮이는 문제 해소
- 고정지출 폼에 지출자 선택 필드를 추가해 `user_id` 필수 계약과 일치
- 총 1,129개 자동검사와 체크섬 55개 통과
- SQL·Supabase 스키마·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.50 · UI/UX 4단계 · 2026-07-29

- PC 본문 하단에 거래 검색·빠른 입력·알림·화면 설정 빠른 실행 독 배치
- 사이드바 펼침·접힘 상태에서 남은 본문 영역의 중앙을 기준으로 독 위치 보정
- 저장 성공·예산 경고·입력 오류를 단일 고정 피드백 카드로 표시
- 성공 저장 후 선택 날짜의 일별 상세, 오류 후 같은 날짜의 빠른 입력으로 자동 복귀
- URL의 일회성 결과 상태를 표시 후 정리해 새로고침 중복 안내 방지
- 기존 입력폼과 `/admin/transactions` POST·권한·분류·중복 방지 계약 유지
- PC 1440px·모바일 390px 렌더링과 총 1,120개 자동검사 통과
- V22.8.46 참여자 역할 SQL 운영 적용 완료 확인, 이번 단계 추가 SQL 없음
- 환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.49 · UI/UX 3단계 · 2026-07-29

- PC 빠른 입력을 620px 중앙 모달, 모바일 빠른 입력을 하단 바텀시트로 전환
- 모바일 주요 메뉴를 `홈 · 기록 · 입력 · 예산 · 전체`로 정리하고 중앙 입력 버튼 강조
- 날짜 상세 팝업에 `이 날 기록 추가`를 연결해 선택 날짜를 기존 입력폼에 자동 반영
- 전역 빠른 입력과 기존 `/app#add`·`#quick` 진입을 하나의 입력 화면으로 통합
- 기존 `section#add` DOM과 기존 거래 저장 처리기를 그대로 재사용하고 별도 쓰기 API는 추가하지 않음
- Esc·바깥 클릭 닫기, Tab 포커스 순환, 포커스 복귀, 배경 스크롤 잠금 적용
- PC 1440px·모바일 390px 렌더링과 총 1,031개 자동검사 통과
- SQL·Supabase 스키마·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.48 · 2026-07-29

- 좌측 미니 캘린더와 홈 캘린더 날짜 클릭 시 실제 거래 항목 팝업 제공
- PC 중앙 모달, 모바일 하단 바텀시트 반응형 적용
- 참여 가계부 범위를 확인하는 읽기 전용 `/u/api/day-transactions` 추가
- 지출·수입 합계, 메모, 분류, 결제수단, 구성원, 금액 표시
- 초기 홈 HTML·초기 DB 호출과 거래 저장·권한·중복 방지 로직은 유지


## V22.8.47 · UI/UX 1단계 · 좌측 캘린더·예산 사용률

- PC 좌측 사이드바에 선택 월 실제 거래 기반 미니 캘린더 추가
- 날짜 클릭 시 기존 날짜별 거래 필터로 연결하고 2단계 팝업 확장 기반 마련
- 선택 가계부·월의 실제 예산 사용률과 70%/90% 상태색 반영
- `/app`에서 이미 조회한 거래·예산 데이터를 재사용해 추가 DB 호출 없음
- 사이드바 252px 및 신규 immutable CSS `/assets/accountbook-shell-v22847.css` 적용
- SQL·권한·거래 저장·카카오 로직 변경 없음

# 변경 이력

## V22.8.46 · 2026-07-29

- V22.8.45 운영 화면에서 Worker의 `admin` 역할 계약과 DB `household_members.role` 저장 규칙의 불일치 확인
- 역할 컬럼이 텍스트 CHECK인지 PostgreSQL enum인지 자동 판별해 `admin`을 멱등적으로 허용하는 SQL 추가
- 알 수 없는 기존 역할·예상하지 못한 역할 제약·지원하지 않는 타입을 발견하면 변경 전 transaction 중단
- 기존 참여자 행·RLS·GRANT·RPC·인덱스를 변경하지 않고 5초 잠금 제한과 사후 readiness 결과 제공
- 참여자 역할 스키마 검사 20개를 추가해 총 902개 자동 검사와 ESM `default.fetch` 통과 기준
- 환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.45 · 2026-07-28

- 참여자 권한 저장용 Supabase PATCH 실패가 처리되지 않아 Cloudflare Worker Error 1101로 전파되던 경로 수정
- 승인대기 참여자를 관리자로 승격하는 정상 흐름을 회귀 검사로 추가
- 권한 변경용 참여자 조회를 fail-closed로 전환하고 대상 없음·0행 반영을 성공으로 오판하지 않도록 보강
- 허용되지 않은 역할 값을 조용히 일반 참여자로 바꾸지 않고 명시적으로 거부
- DB 제약·일시 장애 시 기존 권한을 유지하고 동일 화면의 인라인 오류로 복귀
- 가계부·운영 보안 75개, 총 882개 자동 검사와 ESM `default.fetch` 통과 기준
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.44 · 2026-07-28

- 운영 로그인 홈의 라이트 카드에서 공통 보조 텍스트가 4.26:1로 측정된 결함을 `#5f6b7a` 토큰으로 보정해 페이지·카드·raised 표면에서 최소 4.92:1 확보
- 다크모드 공통 `빠른 입력` 주요 링크의 흰색 전경이 전역 링크 포인트 컬러에 덮이지 않도록 우선순위 보정
- 시스템·라이트·다크와 블루·그린·바이올렛·앰버 강조색 조합을 자동 대비 계산으로 보호
- 모바일 전체 메뉴 버튼 44×44px, `prefers-contrast: more`, 강제 색상, `prefers-reduced-motion` 대응 추가
- 변경된 셸 CSS를 `/assets/accountbook-shell-v22844.css`와 새 immutable ETag로 분리
- 390px·768px·1024px 운영 화면의 가로 넘침 없음, 목표·정산 화면과 콘솔 오류 없음 확인하고 로컬 V22.8.44를 390px·768px·1024px·1440px 및 다크 4개 컬러톤에서 재검증
- 사용자 화면·홈 버튼·테마·성능 144개, 총 872개 자동 검사와 ESM `default.fetch` 통과 기준
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음
- 2026-07-28 운영 배포 후 자동 확인 PASS: V22.8.44 버전, 준비 상태와 필수 RPC 17개, 정적 자원·`ads.txt`·비공개 운영 경로 보호 확인

## V22.8.43 · 2026-07-28

- 정산 완료 이력과 저축 목표 JSON의 읽기→쓰기 경로에서 설정 읽기 실패를 빈 목록으로 오인하지 않도록 strict read 적용
- 기존 operation lease를 유지해 정산·목표 동시 저장을 가계부 단위로 직렬화하고 경합 요청에 재시도 안내 제공
- 정산 이력 읽기·쓰기 실패 시 기존 이력을 보존하고 전용 실패 안내 뒤 안전하게 재시도 가능
- 목표 조회·저장 실패를 503과 구분 가능한 `reason`·사용자 메시지로 반환하고 브라우저가 저장 오류를 숨기지 않도록 보강
- 목표 ID 생성에서 `Math.random()`을 제거하고 Worker Web Crypto `crypto.randomUUID()` 사용
- 정산·목표 읽기 실패, 쓰기 실패, 손상 JSON, 동시 요청, 실패 후 재시도, 목표 런타임 캐시 버전을 포함해 핵심 쓰기 스모크 97개로 확장
- 총 869개 자동 검사와 ESM `default.fetch` 통과 기준
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.42 · 2026-07-28

- 자산·결제수단 생성·수정·삭제를 기존 operation lease로 가계부 단위 직렬화해 v2271 자산 RPC의 동일 이름 동시 생성 경쟁 차단
- 자산 본체 변경 성공 뒤 순자산 스냅샷만 실패한 partial-success를 전용 안내로 분리
- 예산 table 저장 성공 뒤 같은 분류의 settings fallback을 정리하고 정리 실패를 저장 실패로 오인하지 않도록 보강
- 전체 예산 교체가 이전 settings 분류를 제거해 삭제한 예산이 다시 나타나지 않는지 회귀검사 추가
- v2271 자산 RPC·예산 전체 교체 RPC를 QA fixture에 재현하고 동시 생성·스냅샷 실패·저장소 정리 검사를 포함한 핵심 쓰기 스모크 61개로 확장
- 총 833개 자동 검사와 ESM `default.fetch` 통과 기준
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.41 · 2026-07-28

- 정기지출·자산·카드혜택 화면의 명시적 미접근 `household_id`가 첫 가계부로 대체되던 범위 오류 수정
- 예산 테이블 DELETE 오류를 삼키던 false-success를 제거하고 fallback 정리 실패 시 기본 테이블 행을 보존하도록 삭제 순서 안전화
- 거래 수정 RPC 성공 후 보조 표시 이력 저장만 실패했을 때 실제 수정까지 실패한 것처럼 안내하던 partial-success 피드백 수정
- 일반 웹 거래 생성에 기존 `accountbook_claim_operation`·`accountbook_release_operation` 잠금을 적용하고 잠금 보유자의 저장 실패를 후행 요청의 중복 성공으로 오인하지 않도록 보강
- 정상 거래 생성·중복·수정·삭제와 선행 저장 실패 경합, table-only·settings-only·이중 예산 삭제 실패, viewer 쓰기 차단을 실제 fixture DB 상태로 확인하는 43개 핵심 쓰기 스모크 추가
- 총 815개 자동 검사와 ESM `default.fetch` 통과 기준으로 확장
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.40 · 2026-07-28

- PostgREST 파라미터 RPC를 빈 JSON으로 호출해 존재하는 함수를 PGRST202 누락으로 오판하던 `/ready` 점검 수정
- 각 RPC의 실제 파라미터 이름과 의도적으로 무효한 UUID·입력값을 사용해 함수 본문 쓰기 전에 안전하게 시그니처 존재 확인
- V22.6.8·V22.7.0·V22.7.1 SQL, 테이블, 환경변수, Secret, Kakao Developers, OpenBuilder 변경 없음
- 실제 파라미터 probe에서 입력 검증 오류를 RPC 존재로 판정하는 회귀검사 추가

## V22.8.39 · 2026-07-28

- 요청에 명시된 `household_id`가 현재 사용자의 가계부가 아니면 첫 가계부로 자동 대체하지 않고 요청을 거부하도록 사용자·관리자·정산 경계를 통일
- 다른 가계부 ID를 넣은 거래 생성, 관리 링크, 정산 조회가 기본 가계부에 영향을 주지 않는 회귀검사 추가
- 상태를 바꾸는 수동 cron HTTP 경로를 `POST` 전용으로 바꾸고 URL query secret을 폐기해 `x-cron-secret` 또는 Bearer 인증만 허용
- `CRON_SECRET`으로 운영 대시보드·스냅샷·NLU·Skill 운영 화면을 열 수 없도록 관리자 인증과 예약 실행 인증을 분리
- 카카오 테스트 payload·raw text·고정 QA 식별자는 `INCOMPLETE_FEATURE_QA_ENABLED=1`과 관리자 인증이 동시에 있을 때만 사용 가능
- 운영 대시보드의 카카오 사용자 키를 마스킹
- `/health`를 DB 비의존 liveness로 명확히 하고 `/ready`가 3개 필수 테이블과 인증·쓰기·잠금 관련 15개 RPC를 확인하도록 확대
- 예외 안전 화면이 HTTP 200이 아닌 500을 반환하도록 바꿔 모니터링의 오탐 방지
- Git 이력의 원본과 같은 V22.6.8·V22.7.0·V22.7.1 SQL을 장애 복구 자료로 복원하고 존재하지 않는 V22.8.0 파일을 가리키던 앱 내부 배포 안내 교정
- 카카오 그룹 22개, 가계부·운영 보안 63개를 포함한 총 770개 자동 검사 통과
- 신규 스키마 설계·환경변수·Kakao Developers·OpenBuilder 변경 없음. 다만 운영 `/ready`에서 RPC 누락이 확인된 환경은 DB 백업 후 기존 V22.6.8 → V22.7.0 → 필요 시 V22.7.1 SQL 복구가 선행되어야 함

## V22.8.38 · 2026-07-24

- Claude Code V5 누적본의 연간 리포트, 저축·목표, 전 기간 거래 검색, 알림 센터와 즐겨찾기를 기존 V22.8.25 공통 셸에 통합
- 검색·알림·즐겨찾기·목표 API가 요청한 `household_id`를 엄격히 확인하도록 변경해 잘못된 값이 첫 가계부로 대체되는 교차 가계부 위험 차단
- 조회 전용 참여자는 목표를 읽을 수만 있고 생성·납입·수정·삭제할 수 없도록 서버와 화면을 함께 보강
- 목표 설정 저장에 기존 작업 잠금 RPC를 재사용해 동시 요청의 읽기-수정-쓰기 덮어쓰기 방지
- 목표 금액·납입액의 0원 저장 차단, 페이지 이탈 시 `pagehide`·`keepalive`를 사용한 되돌리기 저장 안정화
- 검색 결과 링크의 가계부 유지, 즐겨찾기 낙관적 UI 실패 복원, 350ms 검색 지연과 키보드 초점 가두기·복원 적용
- 검색·알림·즐겨찾기 오버레이를 단일 외부 번들로 제공하고 개인 홈 HTML 35KiB 성능 예산 유지
- `/assets/accountbook-shell-v22838.css`, `/assets/accountbook-nav-v22836.js`, `/assets/accountbook-v5-v22838.js` 및 기능별 immutable 자원 적용
- 모바일 작업 대화상자에서 나중에 생성되는 검색·알림 버튼도 동작하도록 이벤트 위임·알림 지연 초기화·닫기 초점 복원 적용
- 기존 705개와 신규 안정화 41개를 합친 자동 검사 746개 통과
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.26~33 · 2026-07-23 · upstream V5 통합 이력

- V22.8.26 통합검색 진입 버튼과 결과 포커스, V22.8.27 알림센터와 홈 예산위험 배너
- V22.8.28 즐겨찾기 API·검색 통합, V22.8.29 연간 리포트·연말정산 화면, V22.8.30 전역 내비게이션 진입점
- V22.8.31 저축·목표와 5초 실행취소, V22.8.32 반복 지출 자동감지, V22.8.33 거래 행 즐겨찾기
- 각 기능은 V22.8.38에서 현재 가계부 권한, 동시 저장, 오류 복원과 공통 외부 자원 기준으로 다시 통합·안정화
- 상세 설계·진행 이력은 `docs/v5/CLAUDE_DIRECTION.md`에 보존

## V22.8.25 · 2026-07-22

- 모든 로그인 화면에 실제 서버 경로를 사용하는 거래 검색, 빠른 입력, 예산 알림, 화면 설정 전역 작업 연결
- 데스크톱은 검색·빠른 입력을 주요 작업으로, 알림·화면 설정을 보조 작업으로 구분하고 모바일은 단일 작업 버튼과 대화상자로 정리
- 검색은 기존 `/app`의 `q`, `tab=transactions`, `#feed` 계약을 사용하고 빠른 입력은 기존 `#add`, 알림은 실제 `/budget-alerts` 경로만 사용
- 화면 설정은 기존 시스템·라이트·다크와 블루·그린·바이올렛·앰버 저장 계약 재사용
- 대화상자 Escape 닫기, 초점 이동·복원, 배경 스크롤 잠금과 `/` 검색 단축키 적용
- 운영에서 확인된 `/app` 홈의 서버 `app` 키와 클라이언트 `home` 키 불일치를 교정해 사이드바 현재 메뉴 유지
- 서버 HTML 마크업을 늘리지 않고 외부 자산 런타임에서 작업 UI를 생성해 홈 35KB 성능 기준 유지
- `/assets/accountbook-shell-v22825.css`, `/assets/accountbook-nav-v22825.js` immutable 자원과 새 ETag 적용
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.24 · 2026-07-22

- 최신 UI V5 시안의 데스크톱 `기록·자산·리포트·함께·관리`와 모바일 `홈·거래·정산·통계·예산` 정보구조를 실제 제공 경로에 맞춰 반영
- 페이지 전체 HTML 문자열 때문에 `/my/households`가 `참여자·초대`로 잘못 선택되던 활성 메뉴 판정을 문서 제목·경로 기준으로 교정
- `/app`의 거래·캘린더, `/my/analysis`의 통계·분석, 백업·가져오기, 가계부 전환·참여자·단톡방을 경로와 쿼리·해시로 구분
- 모바일 전체 메뉴가 실제 공통 사이드바 DOM을 드로어로 표시하도록 연결하고 열기 초점 이동, Tab 순환, Escape 닫기, 원래 버튼 초점 복원 추가
- 최신 V5 기준에 맞춰 데스크톱 전환점을 900px로 정리하고 238px 사이드바·모바일 5탭·35KB 홈 HTML 성능 기준 유지
- `/assets/accountbook-shell-v22824.css`, `/assets/accountbook-nav-v22824.js` immutable 자원과 새 ETag 적용
- 공개 AdSense 경계, 저장·CSV·정산·카카오·영수증·권한 계약과 기존 V22.8.10/11 자산 바이트 보존
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.23 · 2026-07-22

- UI V5 3단계로 분석 종합 리포트, 수입·예산 설정, 참여자·초대, 백업·가져오기, 단톡방 연결, 시작가이드의 구형 내부 메뉴 제거
- 구형 화면도 중앙 `renderUnifiedNav` 설정을 사용하도록 렌더링 후 호환 변환을 추가하고 가계부·월 쿼리 유지
- 무료 리포트·스마트 도구·자산·결제수단·분류·키워드·가계부 전환·계정 보안에 공통 페이지 헤더와 카드·컨트롤·KPI 규격 적용
- 자산 화면에 누락됐던 명시적 페이지 제목을 추가해 공통 헤더와 접근성 이름 보강
- 390px·768px·1024px·1440px에서 모바일 5탭과 데스크톱 238px 전환, 가로 넘침 없음 확인
- `/assets/accountbook-shell-v22823.css` immutable 자원과 새 ETag 적용; 기존 V22.8.21 SVG 내비게이션 런타임 재사용
- 저장·CSV·분석·카카오·영수증·가계부 권한과 AdSense 제외 경계 유지
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.22 · 2026-07-22

- UI V5 2단계로 홈·기본 소비 분석·정산에 공통 페이지 헤더, 컨트롤 바, KPI 그리드, 필터 바와 섹션 카드 적용
- 기본 소비 분석의 본문 내부 구형 전체 기능 메뉴를 제거하고 공통 238px 사이드바로 통합
- 10억 원 이상 긴 금액도 말줄임 없이 줄바꿈해 전체 의미가 보이도록 KPI 규격 보강
- 홈의 중복 하단 메뉴를 제거하면서 공통 모바일 5탭과 기존 빠른 입력·달력·저장 동작 유지
- 정산 제목을 단순한 `정산`으로 정리하고 계산·완료 저장 폼의 기존 경로와 필드 보존
- 390px·1024px·1440px 로컬 브라우저에서 메뉴 전환, 단일 열 KPI와 가로 넘침 없음 확인
- `/assets/accountbook-shell-v22822.css` immutable 자원과 새 ETag 적용; V22.8.21 내비게이션 런타임은 그대로 재사용
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.21 · 2026-07-22

- UI V5 1단계로 홈 전용 데스크톱 내비게이션을 제거하고 모든 로그인 사용자 화면을 동일한 `renderUnifiedNav` 셸로 통합
- 데스크톱 사이드바를 238px·접힘 72px로 통일하고 접기 상태와 `aria-expanded`·동작 이름을 동기화
- 문자 기호 메뉴 아이콘을 하나의 선형 SVG 아이콘 세트로 교체하고 모바일 5탭에도 같은 아이콘 적용
- V5 라이트·다크 표면, 18px 카드 반경, 8px 간격 토큰을 기존 시스템 모드와 블루·그린·바이올렛·앰버 설정에 연결
- 모바일 전체 메뉴가 별도 복제 메뉴 대신 같은 사이드바 DOM을 사용하도록 통합
- 홈 내 중복 분석·메뉴 버튼을 제거하고 공통 셸의 버전형 정적 CSS·JS를 재사용해 개인 홈 HTML 35KB 성능 기준 유지
- `/assets/accountbook-shell-v22821.css`, `/assets/accountbook-nav-v22821.js` immutable 자원과 새 ETag 적용
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.20 · 2026-07-22

- V22.8.19 기준선에 Claude 분기의 카카오 수정 V4, 삭제 복구 수정, 웹 인라인 피드백만 선별 통합
- 날짜별 기록 번호를 이용한 조회·수정·삭제와 24시간 삭제 복구 흐름 추가
- 카카오 사용자와 대화방 조합으로 수정·복구 상태를 분리하고 반복 메뉴·무한루프 방지
- 삭제 복구 버퍼에 `household_id`를 보존하고 재삽입 호환 재시도 적용
- 웹 POST 결과를 제출한 폼 근처에서 안내하고 JSON 오류에 `message`·`reason` 보강
- V22.8.19 색상·대비, UI·UX 4단계, AdSense 심사 경계와 기존 권한·분석·영수증·성능 보호
- 복구 버퍼 저장 실패 시 삭제를 중단하고, 복구 INSERT 실패 중에는 버퍼를 유지해 재시도 가능
- 자동 검사 705개와 ESM 진입점 통과
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.19 · 2026-07-22

- UI·UX 4단계 적용 뒤 발견된 사용자·공개 화면의 색상·대비 회귀 보정
- `/assets/accountbook-shell-v22819.css` immutable 자원과 새 ETag 적용
- 라이트·다크·네 컬러톤 및 AdSense 심사 안전 경계 유지

## V22.8.18 · 2026-07-21

- 첨부된 UI·UX v1~v4와 `서버이식_dashboard.css`, `서버이식_nav.css`를 비교 검토
- 데모의 가상 거래·localStorage 기능은 제외하고 현재 실데이터·권한·분석 계약을 보존
- 사용자 사이드바를 기록·리포트·함께·관리 중심으로 재편하고 데스크톱 폭을 248px로 통일
- 모바일 주요 메뉴를 홈·거래·정산·분석·예산 5개 목적지로 정리하고 전체 메뉴는 상단에서 제공
- 모바일 전체 메뉴 버튼에 `aria-controls`·`aria-expanded` 동기화 추가
- 카드·필터·표·입력·탭·거래 항목을 공통 토큰으로 통합하고 기존 시스템/라이트/다크·4컬러톤에 연결
- 홈 전용 데스크톱 내비게이션도 공통 사이드바 폭·상태색으로 통일
- `/assets/accountbook-shell-v22818.css` immutable 자원과 새 ETag 제공
- V22.8.17 AdSense 소유권·`/ads.txt`·광고 비활성·개인/관리 화면 제외 정책 유지
- 자동 검사 546개 통과 기준으로 확장

최신 ZIP 하나만으로도 이전 작업의 흐름과 보호해야 할 기준을 확인할 수 있도록 유지합니다.

## V22.8.17 · 2026-07-21

### AdSense 심사 안전 적용

- 홈과 정책·주요 공개 콘텐츠 14개 정식 경로 및 12개 별칭에 `ca-pub-8422696710972974` 소유권 메타를 정확히 한 번 적용
- 루트 `/ads.txt`에 `google.com, pub-8422696710972974, DIRECT, f08c47fec0942fa0` 판매자 정보 적용
- 승인 전 광고 스크립트, 슬롯, 광고 쿠키 코드와 광고 네트워크 허용을 비활성 상태로 유지
- 개인 가계부·거래·분석·영수증·설정·백업과 관리자·운영 화면을 광고 범위에서 제외

### V2 화면 원칙 선별 반영

- 첨부 V2 HTML의 큰 월 지출 위계, 평면 카드, 투명한 빈 달력 셀, компакт한 분석 필터 원칙만 실데이터 화면에 반영
- 홈 달력의 오늘·주말·기록일·선택일과 분석 필터·KPI·의미색을 라이트·다크 및 네 컬러톤에 맞게 보정
- 모바일 44px 터치 목표, 360px 달력 폭, 961~1023px 태블릿 메뉴 경계와 `color-mix()` 미지원 fallback 보강
- `/assets/accountbook-shell-v22817.css`를 새 immutable 자원으로 제공

### 검증·배포 판정

- 운영의 기존 게시자 환경값이 심사 소유자를 덮지 못하도록 심사 게시자 ID를 코드에 고정
- AdSense 심사·V2 화면 검증 228개를 추가해 전체 자동 검증을 533개로 확대
- 공개 별칭, 실제 관리자 인증 화면, 광고 호스트·쿠키 코드 부재, 선택 달력·분석 상태를 회귀 고정
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.16 · 2026-07-21

### 사용자 화면 전체 대비 핫픽스

- 다크 입력이 레거시 고우선순위 규칙에 밀려 흰색으로 남던 원인을 수정하고 입력·선택·텍스트 영역을 실제 다크 표면으로 통일
- 메뉴, 자산, 적립계획, 영수증, 가계부 관리, 참여자, 키워드, 백업, 가계부 그룹, 시작 안내, 보고서의 카드·보조문구·배지·의미색 표면 보정
- 전역 `*` 글자색 강제를 제거하고 화면별 의미색과 버튼 글자색을 보호
- 라이트 적립계획 경고 카드의 보조문구를 4.5:1 이상 대비로 교정
- `/assets/accountbook-shell-v22816.css`를 새 immutable 자원으로 제공

### 검증·배포 판정

- 사용자 화면·테마·성능 검증을 123개에서 134개로 확대하고 전체 자동 검증을 305개로 확장
- 18개 사용자 경로의 라이트·다크 브라우저 대비 점검에서 미달 0건
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.15 · 2026-07-21

### 다크 인증 화면 대비 핫픽스

- 다크모드 전역 글자색 보정이 로그인과 내 계정·보안의 밝은 컴포넌트를 덮던 범위 회귀 수정
- 로그인과 계정보안에 독립 화면 marker와 카카오·도움말·안내·경고·계정 식별·보조 버튼 규칙 적용
- 라이트 로그인 `다른 방법으로 접속` 문구를 4.5:1 이상 대비 색상으로 교정
- `/assets/accountbook-shell-v22815.css`를 새 immutable 자원으로 제공
- 데스크톱·375px 모바일과 주요 다크 사용자 화면에서 대비 미달·가로 넘침·오류 로그 0건

### 검증·배포 판정

- 인증 화면·테마·성능 검증을 120개에서 123개로 확대하고 전체 자동 검증을 294개로 확장
- V22.8.14의 홈 버튼, 인증·권한·데이터·영수증·카카오 로직 유지
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.14 · 2026-07-21

### 홈 전체 조회 버튼 대비 핫픽스

- 운영 홈에서 기록이 10건을 넘을 때만 나타나는 `전체 N건 조회` 링크의 다크모드 대비 미달 수정
- 전용 `homeFeedAllBtn` 표식과 `#feed` 범위로 기존 고우선순위 버튼 규칙보다 강하게 흰 글자 적용
- `/assets/accountbook-shell-v22814.css`를 새 immutable 자원으로 제공
- QA 거래를 일시적으로 11건으로 확장해 실제 버튼 HTML과 `feed=all#feed` 문맥을 검증
- 다크 네 컬러톤과 라이트 브라우저 계측에서 대비 미달 0건, 오류 로그 0건

### 검증·배포 판정

- 테마·성능 검증을 118개에서 120개로 확대하고 전체 자동 검증을 291개로 확장
- V22.8.13의 예산·정산·설정 보정, 인증·권한·데이터·영수증·카카오 로직 유지
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.13 · 2026-07-20

### 다크모드 전체 보정

- 운영 점검에서 확인된 예산·고급 정산·내 설정의 밝은 배경 누락을 화면 전용 범위로 보정
- 내 설정의 키워드 카드·칩·개수 배지·추가·삭제 컨트롤을 다크 표면 토큰으로 연결
- 파란 링크 버튼 안의 글자를 흰색으로 고정하고 보조 버튼은 다크 표면과 강조색으로 분리
- 라이트 분석 화면의 비활성 필터 글자 대비를 4.5:1 이상 토큰으로 교정
- immutable 캐시 충돌을 피하도록 `/assets/accountbook-shell-v22813.css`로 새 CSS 제공

### 검증·배포 판정

- 홈·메뉴·분석·예산·정산·내 설정의 라이트·다크 브라우저 대비 계측에서 미달 0건
- 내 설정 다크모드의 블루·그린·바이올렛·앰버 컬러톤 모두 미달 0건
- 테마·성능 검증을 112개에서 118개로 확대하고 전체 자동 검증을 289개로 확장
- 인증·권한·데이터·영수증 OCR·카카오 응답·DB 로직과 V22.8.12 테마 런타임은 변경하지 않음
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## V22.8.12 · 2026-07-20

### 접근성 대비 수정

- 홈 `SMART NOTICE`의 어두운 배경과 내부 녹색 글자 충돌을 밝은 전경 토큰으로 수정
- 분석 보조 텍스트, 수입·예산 요약, 정산 안내, 입력 placeholder의 대비 토큰 보강
- 모바일 하단 메뉴의 활성·비활성 글자색을 작은 텍스트 기준에 맞게 조정
- 분석 렌더러와 클라이언트 구조·바이트 해시는 그대로 유지하고 마지막 공통 셸에서 색상만 교정

### 화면 모드와 컬러톤

- 전체 메뉴에 시스템·라이트·다크 화면 모드와 블루·그린·바이올렛·앰버 컬러톤 설정 추가
- 선택값은 개인정보 없는 브라우저 로컬 설정으로 저장하고 시스템 모드에서는 운영체제 변경을 즉시 반영
- 카드, 입력, 내비게이션, 표, 분석 보조 텍스트와 모바일 메뉴를 다크 모드 토큰으로 함께 전환
- `/assets/accountbook-shell-v22812.css`, `/assets/accountbook-theme-v22812.js`를 새 immutable 자원으로 제공
- V22.8.11 셸과 모바일 런타임, V22.8.10 기본 자원의 바이트·ETag를 보존

### 검증·배포 판정

- 접근성 테마·홈 셸·성능 검증을 94개에서 112개로 확대하고 전체 자동 검증을 283개로 확장
- 인증·권한·데이터·영수증 OCR·카카오 응답·DB 로직은 변경하지 않음
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음
- 운영 적용은 검증된 `src/index.js` 전체 교체와 PC·iPhone·Android 수동 확인이 필요

## V22.8.11 · 2026-07-20

### 홈 UX 셸 정식 승격

- 회색 배경, 흰 카드, 짙은 본문, 파란 강조색, 20px 모서리와 얕은 그림자를 홈 디자인 토큰으로 고정
- PC 홈에 238px 사이드바를 적용하고 모바일의 홈·기록·입력·정산·메뉴 동선을 유지
- 클릭·해시·스크롤·화면 폭 변경 뒤에도 현재 메뉴가 하나만 표시되도록 전용 런타임 추가
- 중간 개발 경로를 제거하고 `/assets/accountbook-shell-v22811.css`, `/assets/mobile-home-shell-v22811.js`로 승격
- V22.8.10 기본 CSS·JavaScript의 바이트와 ETag를 그대로 보존

### 주요 사용자 화면 공통 확장

- 홈, 통합 내비게이션, 개인 appMenu 화면, 로그인, 내 계정·보안, 가계부 시작 화면에 공통 셸을 마지막 cascade로 한 번만 연결
- 공통 CSS를 `body.abV22811Shell` 아래로 제한해 공개 페이지와 운영 화면에 영향을 주지 않도록 분리
- 레거시 관리자, 가계부 관리자, 계정 감사, 진단·운영 화면을 공통 사용자 셸에서 명시적으로 제외
- 모바일 입력 16px, 터치 목표 44px, `focus-visible`, `prefers-reduced-motion` 보호 기준 추가
- 흰 글자 일반 form 동작은 4.5:1 이상 대비의 `#2563eb` action token 사용
- 기존 고우선순위 내비게이션 규칙보다 강하게 PC 238px 폭과 본문 offset을 고정하고 접힌 상태는 기존 폭 유지
- `home→app`, `keyword-guide→categories`, `members→households` 활성 메뉴 별칭만 정리하고 월·가계부 문맥은 유지

### 보호·성능·배포 판정

- 인증·가계부 권한·데이터·DB 로직, 영수증 OCR, 백업 적용, 분석 클라이언트·렌더러는 변경하지 않음
- `/my` 4회 이하, `/app` 9회 이하, 홈 HTML 35KB 미만, 개인 HTML `no-store`, 정적 자원 DB 0회를 유지
- 자동 검증은 영수증 55개, 카카오 18개, 가계부 보안 43개, UX 55개, 홈 UX 셸 94개로 총 265개
- 신규 SQL·환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음
- 배포 시 검증된 `src/index.js` 전체 교체만 필요하며 운영 배포와 실기기 검수는 별도 수동 단계

## V22.8.10 · 2026-07-20

### 홈 데이터 조회 병목 제거

- 사용자 가계부 목록의 가계부별 순차 조회를 `id=in.(...)` 묶음 조회로 변경
- 가계부 참여자 프로필의 사용자별 조회를 묶음 조회로 변경
- 선택한 역할을 다시 읽던 중복 membership 조회 제거
- 홈의 예산 fallback, 표시 이름, 정기지출 준비, 결제수단 설정을 `accountbook_settings` 한 번으로 통합
- 예산 설정 fallback과 예산 테이블을 병렬 조회하도록 변경
- 실제 반환 행이 페이지 크기보다 적을 때 빈 다음 페이지를 다시 조회하지 않도록 종료 조건 보강
- `/my`가 선택 가계부로 바로 이동하는 일반 경로에서 사용하지 않는 사용자 프로필 재조회 제거

### 홈 응답과 브라우저 캐시 최적화

- 개인 홈에 반복 삽입되던 공통 CSS·JavaScript를 `/assets/mobile-home-v22810.css`, `/assets/mobile-home-v22810.js`로 분리
- 정적 자원에 `public, max-age=31536000, immutable` 적용
- 개인 홈 HTML을 106,841 bytes에서 24,490 bytes로 축소
- 홈 데이터 요청을 16회에서 9회, `/my` 진입 요청을 7회에서 4회로 축소
- 공통 자원에는 DB를 전혀 조회하지 않으며 HTML 본문은 기존처럼 `no-store` 유지

### 경량 누적 ZIP과 검증

- 374개 항목이던 과거 누적 ZIP에서 오래된 개별 로그·중복 체크섬·원본 학습 ZIP·이미지·빌드 폴더 제외
- 버전 이력은 `CHANGELOG.md`, `HISTORY_INDEX.md`, SQL 판정은 `SQL_HISTORY.md`로 통합
- 카카오 원본 학습 파일 대신 검증된 프로젝트 메모와 적용 체크리스트만 포함
- 홈 요청 수·묶음 조회·응답 크기·정적 캐시를 확인하는 32개 성능 검증 추가
- 기존 171개 회귀 검증과 합쳐 총 203개 자동 확인
- 분석 클라이언트·렌더러 해시와 영수증 구현은 변경하지 않음
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경 없음, `src/index.js` 전체 교체만 필요

## V22.8.9 · 2026-07-20

### 계정 보안과 가계부 관리 분리

- 가계부 생성 form에서 로그인 이름·비밀번호·비밀번호 확인 입력을 모두 제거
- 가계부 생성 시 기존 계정 비밀번호 확인 또는 새 로컬 로그인 identity 생성을 하지 않도록 서버 처리 분리
- 가계부에는 비밀번호가 없고, 초대코드는 참여·단톡방 연결에만 쓰인다는 설명을 생성·초대·참여자 화면에 명시
- 가계부별 옵션의 `개인 비밀번호` 항목을 제거하고 전역 `내 계정·보안` 진입점으로 이동
- 비소유자의 가계부 나가기는 현재 로그인 세션과 명시적 이력 보존 확인만 사용하도록 단순화

### 영구 삭제 본인 확인

- 로컬 로그인 사용자는 모든 가계부에 공통인 `내 계정 로그인 비밀번호`로 확인
- 카카오 로그인 사용자는 기존 카카오 OAuth 콜백에서 `prompt=login` 재인증 후 삭제 가능
- 카카오 재인증 결과가 현재 사용자에게 연결된 카카오 ID와 일치하는지 다시 확인
- 재인증 토큰을 사용자·가계부·삭제 목적에 묶고 HttpOnly 쿠키로 5분만 유지하며 삭제 성공 시 즉시 제거
- 가계부 이름 정확히 입력, 전체 기록·참여자 삭제 확인, 소유자 권한, 기존 원자적 purge RPC를 그대로 유지

### 삭제·연결해제 단톡방 안전 안내

- 연결된 가계부가 없거나 삭제된 그룹 입력은 다른 개인 가계부로 fallback하지 않고 저장 0건 유지
- 사용자가 다른 가계부를 가진 경우와 하나도 없는 경우 모두 `방금 입력한 기록은 어디에도 저장하지 않았어요`를 명시
- 이미 삭제된 가계부를 가리키는 오래된 그룹 연결은 다음 요청에서 자동 정리
- 그룹 응답은 V22.8.7 카카오 매뉴얼 기준의 지원 output·번호형 선택지 정책을 계속 유지

### 배포·문서·검증

- 영수증 55개, 카카오 18개, 가계부·계정 보안 43개, UX·보호 화면 55개로 총 171개 자동 확인
- 분석 클라이언트·렌더러 해시와 영수증 구현을 변경하지 않음
- 이번 변경은 `src/index.js` 전체 교체만 필요하며 신규 SQL·환경변수·OpenBuilder 변경 없음
- 기존 카카오 로그인 설정과 동일한 Redirect URI를 재사용하므로 Kakao Developers 콘솔 추가 작업 없음

## V22.8.8 · 2026-07-20

### 예측 가능한 행동 피드백

- 모든 POST 동작을 일괄 `저장 중…`으로 표시하던 공통 런타임을 행동별 진행 문구로 변경
- 로그인·새 계정·가계부 생성·참여·삭제·탈퇴·가져오기·일반 저장을 서로 다른 문구로 안내
- 제출 중 form과 button에 `aria-busy`를 적용하고 화면 읽기 도구용 `aria-live` 상태 추가
- 뒤로 가기 캐시에서 원래 버튼 문구와 제출 상태를 복원하고 비밀번호 유효성도 다시 동기화
- 삭제·탈퇴·계정 통합의 공통 확인창을 실제 결과가 드러나는 문구로 세분화

### 비밀번호 확인과 화면 보호

- 새 계정 만들기와 개인 복구 비밀번호 설정에 8자리·불일치·일치 상태의 즉시 피드백 추가
- 서버의 기존 검증을 유지하고 브라우저 사전 검증을 보강
- 분석 클라이언트와 분석 렌더러를 V22.8.7 소스 해시로 고정해 변경하지 않았음을 자동 검증
- 영수증 UI는 변경하지 않고 기존 55개 회귀 검증으로 보호

### 배포·문서·검증

- Brunch UX 원칙을 프로젝트 판단 기준으로 요약한 `docs/ux/UX_PRINCIPLES_BRUNCH_110_PROJECT_MEMORY.md` 추가
- UX 원칙·진행 문구·비밀번호 피드백·분석 고정 55개 자동 검증 추가
- 전체 자동 확인은 영수증 55개, 카카오 18개, UX 55개로 총 128개
- 이번 변경은 `src/index.js` 전체 교체만 필요하며 신규 SQL·환경변수·OpenBuilder 변경 없음

## V22.8.7 · 2026-07-20

### 카카오 그룹 채팅 응답 매뉴얼 정합성

- `botGroupKey`가 있는 그룹 요청을 중앙 응답 단계에서 판별하도록 추가
- 그룹 챗봇 가이드 v1.11.1에서 미지원인 `quickReplies`를 그룹 응답에서 제거
- 제거된 선택지는 최대 5개의 번호형 입력 문장으로 `SimpleText`에 보존해 단계형 흐름 유지
- 1:1 채팅의 기존 단계형 `quickReplies`는 그대로 유지
- 그룹 응답 output을 SimpleText, SimpleImage, TextCard, BasicCard, ListCard, ItemCard로 제한하고 최대 3개로 정규화
- 알 수 없는 SkillRequest 필드가 포함된 그룹 요청을 허용하는 회귀 테스트 추가
- `botUserKey` → `appUserId` → `plusfriendUserKey` 순서를 자동 검증해 레거시 키가 신규 기본 식별자가 되지 않도록 고정

### 배포·문서·검증

- 카카오 원본 매뉴얼, 학습 요약, 정본 충돌 정리를 누적 ZIP에 복원
- `DEPLOYMENT_MATRIX.md`, `NEXT_UPDATE_PLAN.md`, V22.8.7 상세 보고서와 검증 보고서 추가
- 빌드 단계에 카카오 그룹 응답 18개 자동 검증을 추가
- 이번 변경은 Worker 코드만 교체하며 신규 SQL·환경변수·OpenBuilder 수정 없음
- 영수증 등록, 모바일 홈, 전체 메뉴, 분석 화면, 로그인 기준은 V22.8.6 그대로 보호

## V22.8.6 · 2026-07-17

### 영수증 등록 UX

- 앨범 선택과 카메라 촬영 분리 유지
- PC 사진 드래그 앤 드롭과 클립보드 이미지 붙여넣기 추가
- 사진 미리보기·선택 취소·25MB 제한·이미지 형식 검사 유지
- OCR 실행 중 `분석 취소`와 전체 과정 90초 제한 추가
- 인식 금액과 직접 입력 금액에 천 단위 쉼표 표시
- 가계부와 기록 월을 변경하면 즉시 적용하고 기존 `선택 적용` 버튼은 무스크립트 대비용으로 유지
- 상태 안내에 스크린리더용 실시간 알림 적용

### 안정화 보강

- 취소 후 늦게 완료된 OCR 작업자가 새 실행에 섞이거나 메모리에 남지 않도록 실행 세대 가드 추가
- OCR 시간 제한을 이미지 축소 단계부터 적용해 화면이 잠긴 채 남는 경우 차단
- 드래그 진입 깊이를 추적해 자식 요소 위에서 강조 표시가 깜빡이는 현상 방지
- 조회 전용 가계부에서 드래그·붙여넣기로 등록 흐름이 우회되지 않도록 차단
- 숨김 요소가 다른 CSS에 의해 노출되지 않도록 `[hidden]` 우선 규칙 적용
- 영수증 렌더러의 특정 버전명을 제거해 다음 버전에서도 같은 구현을 재사용하도록 정리

### 성능 유지

- Tesseract 5.1.1 지연 로딩
- 기기 성능에 따른 1400px 또는 1800px 사전 축소
- 여러 영수증을 연속 처리할 때 OCR 작업자 재사용, 유휴 시 메모리 해제
- 영수증 화면 전용 최소 데이터 조회와 요청 단위 인증 캐시 유지

### 자동 검증

- 영수증 화면·파서·저장·중복 차단·조회 예산 검증을 빌드 단계에 포함
- 모바일 홈·전체 메뉴·분석·로그인 화면 회귀와 모바일 확대 허용 여부 검증

## V22.8.5 · 2026-07-17

- 모바일에서 로컬 계정으로 안전하게 접속할 수 있는 로그인 경로 정리
- 기존 계정 로그인과 새 계정 만들기의 위계 분리
- 전체 메뉴를 자주 쓰는 기능과 관리 기능으로 차등 배치
- 모바일 접근성·메뉴 위계 개선 유지
- 영수증 앨범/카메라 분리, 미리보기, OCR 지연 로딩과 데이터 조회 최적화 1차 적용
- 분석 화면은 사용자 요청에 따라 고정 대상으로 지정

## V22.8.4 이하 요약

- V22.8.4: 전체 UI·성능 재검증
- V22.8.3: 외부 안정화본 검토 및 선별 병합
- V22.8.2: 인증 UI와 세션 안정화
- V22.8.1: 전체 UI·UX 위계 개편
- V22.8.0: 자산·결제수단 대시보드 완성

세부 보호 기준은 `BASELINE.md`, 미해결 외부 제약은 `KNOWN-ISSUES.md`, 배포 검증 결과는 `RELEASE-CHECKLIST.md`를 확인합니다.
# V22.8.19

- UI·UX 4단계 적용 뒤 남은 공개·로그인 화면의 색상 및 대비 회귀를 보정했습니다.
- 새 immutable 셸 `/assets/accountbook-shell-v22819.css`를 추가했습니다.
- 라이트·다크·네 컬러톤과 AdSense 심사 경계를 포함해 573개 자동 검사를 통과하도록 회귀 검사를 확장했습니다.
