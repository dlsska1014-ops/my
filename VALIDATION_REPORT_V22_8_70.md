# V22.8.70 검증 보고서

검증일: 2026-08-03

## 점검 범위

순차 점검 **7단계 — 입력 문구 → 저장값 되읽기 확대**입니다.

V22.8.69 의 결함 3건이 모두 "저장된 값을 되읽어야만" 보였기 때문에, 같은 방식을
금액·결제수단·날짜·수입지출 구분·분류로 넓혔습니다.
표본 **47종**을 카카오 스킬로 보내고 **DB 에 저장된 행을 직접 읽어** 대조했습니다.

## 1. 검사 설계를 먼저 두 번 고쳤습니다

첫 실행에서 불일치 9건이 나왔지만, 그중 상당수는 검사 자체의 결함이었습니다.

| 검사 결함 | 증상 | 교정 |
|---|---|---|
| 메모에 숫자를 넣음(`항목18`) | `항목18 십만원` 의 `18` 이 금액 파싱에 섞여 180원이 됨 | 한글만 쓰는 메모로 교체 |
| 저장 실패를 이전 행으로 읽음 | 저장 안 된 입력에서 직전 거래를 읽어 엉뚱한 불일치 보고 | 저장 건수 증가를 먼저 확인 |
| 분당 60건 상한에 걸림 | 후반 입력이 리미터에 막혀 "분류 오류"처럼 보임 | 이 검사에서만 상한 상향 |

검사가 거짓말을 하면 없는 결함을 좇게 됩니다. 교정 후 남은 것만 실제 결함입니다.

## 2. 자릿수 쉼표가 문장을 잘랐습니다 (금액 오저장)

여러 건 입력 구분자에 쉼표가 조건 없이 들어 있었습니다.

| 입력 | 보정 전 | 보정 후 |
|---|---|---|
| `커피 12,345원` | **345원** | 12,345원 |
| `커피 1,200원` | **200원** | 1,200원 |
| `커피 60,000` | 저장 실패 | 60,000원 |
| `커피 100,000원` | 저장 실패 | 100,000원 |
| `가습기 1,500,000원` | 저장 실패 | 1,500,000원 |

숫자 사이 쉼표만 예외로 두었고, 항목을 나누는 쉼표(`빵 2600, 우산 4700`)는
그대로 두 건으로 나뉩니다.

## 3. 단위가 이어진 금액이 두 건으로 쪼개졌습니다 (금액 오저장)

금액 토큰 탐색기가 `만` 과 `천` 을 각각 잡아 한 입력이 두 거래가 됐습니다.
금액 추출기(`collectCombinedNumericUnitCandidates`)는 원래 조합을 알고 있었지만,
문장이 먼저 쪼개져 조합 전체를 볼 기회가 없었습니다.

| 입력 | 보정 전 | 보정 후 |
|---|---|---|
| `커피 3만5천원` | 30,000원 + 5,000원 (2건) | 35,000원 (1건) |
| `커피 1만5천원` | 10,000원 + 5,000원 (2건) | 15,000원 (1건) |
| `커피 3만 5천원` | 2건 | 35,000원 (1건) |
| `커피 10만5000원` | **100,000원** | 105,000원 |

## 4. 한 글자 조사 제거가 멀쩡한 낱말을 잘랐습니다

V22.8.69 에서 조사 제거를 "낱말 끝에서만"으로 좁힌 것은 개선이었지만 충분하지
않았습니다. 실제 낱말 32개로 재보니 **9개가 잘렸습니다.**

| 입력 | V22.8.69 | V22.8.70 |
|---|---|---|
| 고양이 | `고양` | `고양이` |
| 목걸이 | `목걸` | `목걸이` |
| 어린이 | `어린` | `어린이` |
| 곰팡이 | `곰팡` | `곰팡이` |
| 호랑이 | `호랑` | `호랑이` |
| 을지로 | `을지` | `을지로` |
| 신작로 | `신작` | `신작로` |
| 한강로 | `한강` | `한강로` |

한 글자 조사는 명사의 마지막 글자와 원리적으로 구분되지 않습니다.
**두 글자 조사(`에서`·`으로`·`에게`·`한테`)만** 떼어냅니다.

| 입력 | 저장되는 메모 |
|---|---|
| `마트에서 12000` | `마트` |
| `친구에게 20000` | `친구` |
| `편의점에서 4500` | `편의점` |
| `회사로 5000` | `회사로` (조사 남음) |
| `학교에 3000` | `학교에` (조사 남음) |

메모가 조금 덜 다듬어지는 대신 **내용이 사라지지 않는 쪽**을 택했습니다.
이 클래스의 결함이 이미 두 번 데이터를 손상시켰기 때문입니다.

## 5. 문제가 없던 것

| 항목 | 표본 | 결과 |
|---|---|---|
| 금액 표기 | 21종 | 보정 후 전부 일치 |
| 결제수단 | 8종 | 전부 일치 |
| 날짜(오늘·어제·그제·그저께) | 4종 | 전부 일치 |
| 수입·지출 구분 | 5종 | 전부 일치 |
| 분류 자동추천 | 8종 | 전부 일치 |
| 여러 건 분리 | 4종 | 전부 2건으로 분리 |

`주유 5만원`·`택시 12000` 처럼 분류 이름과 금액만 보내면
`지출로 기록할까요, 예산으로 설정할까요?` 하고 되묻습니다.
`detectKakaoAmbiguity` 의 의도된 흐름이라 바꾸지 않았습니다.
매번 되묻는 것이 번거롭다면 제품 판단이 필요합니다.

## 6. 성능·자원

| 항목 | 예산 | 실측 | 여유 |
|---|---|---|---|
| 기준 픽스처 홈 HTML | 35,840 B | 34,958 B | 882 B |
| 200행 실사용 홈 HTML | 45,056 B | 44,216 B | 840 B |

**HTML 바이트 변화 0.**

| 자원 | 보정 전 | 보정 후 |
|---|---:|---:|
| `mobile-home-*.js` | 30,767 B (`v22869`) | 30,735 B (`v22870`) |
| `mobile-home-shell-*.js` | 33,224 B (`v22869`) | 33,192 B (`v22870`) |
| `accountbook-shell-v22868.css` | 149,831 B | 동일 |

성능 검사의 모바일 홈 런타임 바이트 고정값도 새 해시로 갱신했습니다.

## 7. 자동 검사

- 신규 회귀 **77개** (`validation/validate-amount-parse-v22870.mjs`)
- 저장소 전체 **2,385개** 자동검사 통과
- ESM `default.fetch` 확인, 체크섬 매니페스트 일치

신규 회귀가 고정하는 것:

- 금액 표기 21종이 **한 건으로, 쓴 금액 그대로** 저장된다
- 자릿수 쉼표가 든 금액이 쪼개지지 않고, 항목 구분 쉼표는 계속 나뉜다
- 조사로 끝나는 낱말 15종이 잘리지 않는다
- 두 글자 조사 정리는 계속 동작한다
- 결제수단 8종·구분 3종·분류 4종이 정확히 저장된다
- 여러 건 입력이 계속 두 건으로 나뉜다

## 8. 운영 변경

- 신규 SQL 없음
- 환경변수·Secret 변경 없음
- Kakao Developers·OpenBuilder 변경 없음

## 배포 후 확인 부탁드립니다

1. 카카오톡 `커피 12,345원` → **12,345원 한 건**
2. 카카오톡 `커피 3만5천원` → **35,000원 한 건**
3. `마트에서 12000` → 메모 `마트`
4. `고양이 사료 3만원` → 메모에 `고양이` 가 남아 있는지
5. 오래 열어 둔 웹 탭 새로고침 후 `v22870` 런타임 적용

> ⚠️ **이 배포는 과거에 틀리게 저장된 금액을 되돌리지 않습니다.** 쉼표를 넣어
> 입력한 기록이 실제보다 작은 금액으로 남아 있거나, `3만5천원` 형태가 두 건으로
> 나뉘어 있을 수 있습니다. 최근 내역에서 금액을 한 번 확인해 주세요.
