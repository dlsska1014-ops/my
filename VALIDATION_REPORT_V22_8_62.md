# V22.8.62 검증 보고서

검증일: 2026-07-31

## 점검 범위

사용자 탭 24개(홈, 거래 내역, 캘린더, 정기지출, 가져오기, 영수증 기록, 자산·계좌,
저축·목표, 통계, 종합 리포트, 예산, 월 마감, 연간 리포트, 스마트 도구, 예산 알림,
부부 정산, 참여자·초대, 단톡방 연결, 가계부 전환·추가, 분류·키워드, 백업·복구,
내 계정·보안, 전체 메뉴, 처음 사용 가이드)를 390px·768px·1440px에서
저장소 QA 서버와 실제 Chromium으로 렌더해 총 72개 조합을 점검했습니다.

## 결과

- 자동 검사: 1,603개 통과
- Worker 문법 및 ESM `default.fetch`: 통과
- 72개 조합 전부 HTTP 200, 페이지 오류 없음
- 가로 스크롤 발생: 0건
- 박스 이탈: 0건 (닫힌 `details` 내부 기하 정보는 오탐으로 확인)
- 신규 SQL·환경변수·카카오 설정 변경: 없음

## 보정한 결함

1. 상단바 도킹 버튼이 전역 `button{width:100%}`에 늘어나 화면 밖으로 나감
   - 재현: 월 마감 390px에서 `전체 메뉴` 버튼 폭 165px, 우측 끝 457px(뷰포트 390px)
   - 수정 후: 폭 82px, 우측 끝 374px
2. 최근 내역 필터 폼의 `1fr` 칸이 폼 밖으로 7px 이탈
   - 재현: 폼 316px, 그리드 칸 323px
   - 수정 후: 이탈 0건
3. `#add`·`#feed`·`#calendar` 앵커가 고정 상단바 아래로 들어감
   - 재현: `scroll-padding-top: auto`, 대상 `top:0`(상단바 52px에 가림)
   - 수정 후: `scroll-padding-top: 64px`, 대상 `top:64`

## 오탐으로 확인한 항목

- 닫힌 `<details>` 내부 요소(챌린지 설정 입력, 정기지출 분류 입력)는 Chromium에서
  이전 레이아웃 기하 정보를 반환합니다. `details`를 열고 다시 측정하면 이탈·겹침이
  없습니다.
- 고정 하단 탭이 본문 위를 덮는 것은 오버레이 설계상 정상입니다.
- 영수증 `앨범에서 선택`·`카메라로 촬영`의 파일 입력은 각자의 라벨 안에 있어
  라벨 클릭으로 정상 동작합니다.

## 남은 확인 사항

- `/favicon.ico`, `/apple-touch-icon.png`, `/manifest.json`이 404입니다. 모든 화면에서
  브라우저가 자동 요청하므로 콘솔 404가 남고, 홈 화면에 추가할 때 아이콘이 없습니다.
  브랜드 아이콘·PWA 정책 결정이 필요해 이번 단계에서는 수정하지 않았습니다.
- iOS 사파리·카카오 인앱 브라우저의 빠른 입력 시트 스크롤 잠금(V22.8.61)은
  실기기 확인이 필요합니다.
- 390px·768px·1440px 실기기 레이아웃과 라이트·다크·포인트 컬러
