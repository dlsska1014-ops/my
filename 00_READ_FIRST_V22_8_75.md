# V22.8.75 통계·종합 리포트 역할 분리 안내

버전: `V22.8.75-ANALYSIS-ROLE-SPLIT`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음 · 환경변수 변경 없음
- 홈 기본 스타일시트 주소가 `/assets/mobile-home-v22875.css` 로 바뀝니다

## 이번에 고친 것

### 1. 두 화면이 같은 카드를 두 번 그리던 문제

`소비 분석`(통계)과 `종합 리포트`는 제목이 완전히 같은 카드를 둘 다 그리고
있었습니다. 어느 쪽이 최신인지 알 수 없고, 같은 달을 두 번 읽게 됩니다.

**두 화면과 기존 링크·URL은 그대로 두고 역할만 나눴습니다.**

| 화면 | 역할 | 한눈에 보기 |
|---|---|---|
| 소비 분석 | 빠르게 보는 요약 | **여기서만** |
| 종합 리포트 | 깊게 보는 분석 | 제거 |

`종합 리포트`에서 `한눈에 보기`를 뺀 이유는, 그 코크핏의 네 블록이
**같은 페이지 안에서** 이미 더 자세히 반복되고 있었기 때문입니다.

| 코크핏 블록 | 같은 페이지의 더 깊은 카드 |
|---|---|
| 예산 사용 게이지 | 예산 사용 게이지 |
| 분류별 예산 속도 | 분류별 예산 사용률 (표) |
| KPI 4종 | 총 지출·총 수입·하루 평균·최다 분류·무지출일·월말 예상 지출 |
| 인사이트 한 줄 | 핵심 인사이트 |

없어진 분석은 없습니다. 오가는 길도 서로 남겨 두었습니다.

- 소비 분석 → `깊게 보기(종합 리포트)`
- 종합 리포트 → `← 한눈에 보기(소비 분석)` (코크핏 위치로 바로 이동)

### 2. 소비 분석 안에서도 예산이 두 번 나오던 것

`이번 달 예산` 카드는 바로 위 코크핏이 이미 보여주는 예산 사용률과 분류별
사용률을 다시 그리고 있었습니다. 코크핏에 **없는** 부분만 남겨
`예산 소비 페이스`로 이름을 바꿨습니다.

- 남김: 전체 예산 게이지, 남은 예산, **날짜별 누적 소비 곡선**(페이스 차트)
- 제거: 분류별 상위 6개 사용률 (코크핏의 `분류별 예산 속도`와 같은 값)
- 총액 예산이 없으면 그릴 곡선도 없으므로 카드 자체를 숨깁니다

### 3. 보조 버튼이 전부 파란 주요 버튼으로 칠해지던 문제

역할을 나누고 나니 `종합 리포트` 히어로의 버튼 세 개가 모두 같은 파란색이라
어디로 돌아가야 하는지 구분되지 않았습니다.

원인은 CSS 우선순위였습니다.

```
.abV2281 .btn:not(.light):not(.soft):not(.danger)      → 클래스 5개
body.abV22812Shell.abV2281 :is(a.btn.secondary, ...)   → 클래스 4개 + 요소 2개
```

`:not()` 사슬이 명시도를 부풀려 **주요 버튼 규칙이 보조 버튼 규칙을 이겼습니다.**
바로 옆 `button[type=submit]` 규칙에는 이미 `:not(.secondary)` 가 있었는데
`.btn` 쪽에만 빠져 있었습니다. 같은 예외를 붙여 맞췄습니다.

`.btn secondary` 를 쓰는 화면 6곳(종합 리포트·백업·모임·설정·계정·안전모드)을
라이트·다크로 다시 재어 확인했습니다.

| 테마 | 주요 버튼 | 보조 버튼 |
|---|---:|---:|
| 라이트 | 6.16:1 | 16.56:1 |
| 다크 | 6.16:1 | 7.85:1 |

## 크기 변화

| 화면 | 이전 | 이후 | 차이 |
|---|---:|---:|---:|
| 종합 리포트 HTML | 123,728 B | 122,267 B | **-1,461 B** |
| 소비 분석 HTML | 86,927 B | 87,064 B | +137 B (역할 안내 문구) |
| 소비 분석 스크립트 | 52,064 B | 51,385 B | **-679 B** |

홈 HTML 실사용 예산은 그대로입니다 — 44,962 B / 47,104 B (여유 2,142 B).

## 적용 순서

1. 현재 운영 Worker 전체 소스를 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 교체하고 배포합니다.
3. 저장소 루트에서 아래를 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_75.ps1
```

4. `소비 분석`에 `한눈에 보기`가 있고, `종합 리포트`에는 없는지 봅니다.
5. `종합 리포트` 맨 위 버튼 세 개 중 **파란 것이 하나**인지 봅니다.
6. 오래 열어 둔 웹 탭은 새로고침해야 `mobile-home-v22875` 스타일이 적용됩니다.

기존 SQL 은 다시 실행하지 않습니다.
