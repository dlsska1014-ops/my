# V22.8.65 검증 보고서

검증일: 2026-08-02

## 점검 범위

라이트·다크 × 포인트 컬러 4종 = **8개 테마 조합**에서 사용자 탭 24개를
390px 실제 Chromium으로 렌더해 **192개 조합**의 글자 대비를 측정했습니다.
보이는 텍스트 노드마다 전경색과 실제로 칠해진 배경색을 합성해
WCAG 2.1 기준(일반 4.5:1, 큰 글씨 3:1)으로 판정했습니다.

## 결과

- 자동 검사: 1,755개 통과
- Worker 문법 및 ESM `default.fetch`: 통과
- 8개 테마 조합 전부 **저대비 텍스트 0건**
- 셸 토큰 조합(본문·보조·강조 × 표면·보조표면) 대비를 값으로 고정: 통과
- V22.8.61~64의 토큰 별칭·칩 아이콘·앵커 오프셋·매니페스트 유지: 통과
- 신규 SQL·환경변수·카카오 설정 변경: 없음

## 보정 전후 실측

| 화면 | 요소 | 보정 전 | 기준 |
|---|---|---:|---:|
| 연간 리포트(다크) | 연말정산 참고 금액 | 1.10:1 | 3.0 |
| 연간 리포트(다크) | 연말정산 참고 설명 | 1.10:1 | 4.5 |
| 연간 리포트(라이트) | 연도 이동 화살표 | 1.00:1 | 4.5 |
| 자산·계좌(라이트) | 현재 순자산 라벨 | 1.22:1 | 4.5 |
| 자산·계좌(라이트) | 히어로 칩 | 1.00:1 | 4.5 |
| 저축·목표(라이트) | 진행 금액 | 1.13:1 | 4.5 |
| 종합 리포트(라이트) | 챌린지 설정 라벨 | 1.90:1 | 4.5 |
| 종합 리포트(라이트) | 일자별 빈 날 표시 | 1.90:1 | 4.5 |
| 종합 리포트(라이트) | 증감 배지(감소) | 3.04:1 | 4.5 |

보정 후 8개 조합 전부 기준 이상입니다.

## 원인

한 가지 패턴이 반복됐습니다. 각 화면 CSS가 어두운 히어로·상자를 전제로
`color:#fff`·`#dbeafe`·`#ccfbf1` 같은 밝은 글자색을 `!important`로 고정해 두었는데,
V5 셸이 배경만 `var(--card)`로 바꾸면서 글자색만 남아 배경에 묻혔습니다.
반대로 다크에서는 셸이 다시 칠하지 않는 상자(`.deductBox`)가 밝은 배경을 유지해
다크 모드 밝은 글자와 충돌했습니다.

## 오탐으로 확인한 항목

- **비활성 버튼**(영수증 `선택한 사진 읽기`, `붙여넣은 문자 분석`)은 `disabled`
  상태의 흐림 처리이며 WCAG 1.4.3 대비 요구 대상이 아닙니다.
- **예산 게이지의 `20%`·`예산 사용`**은 도넛 링이 아니라 링 안쪽 표면 위에 있습니다.
  측정기가 conic-gradient 정지점을 배경 후보로 잡아 생긴 오탐이며 스크린샷으로 확인했습니다.
- 측정기에서 두 경우를 제외한 뒤 8개 조합 모두 0건이 되었습니다.

## 함께 고친 검증 결함

`validate-adsense-v2-v22817.mjs`의 "current date is exposed to assistive technology"는
달력을 `2026-07`로 고정해 받아 오늘 표시를 확인했습니다. 날짜가 2026-08로 넘어가면서
원본 `main`에서도 실패했습니다. 해당 항목만 현재 월 달력을 따로 받아 확인하도록 바꿨습니다.

## 자동화 제한

- 실제 기기의 화면 밝기·컬러 프로파일은 반영하지 못합니다.
- 이미지·아이콘 대비, 포커스 표시 대비는 이번 측정 범위 밖입니다.

## 운영에서 남은 수동 확인

- `전체 메뉴 → 화면 설정`에서 라이트·다크와 포인트 컬러 4종 전환
- 연간 리포트 연말정산 참고, 자산·계좌 히어로, 챌린지 설정 폼 가독성
- 카카오톡 인앱 브라우저의 다크 모드 동작
