# V22.8.79 배포 검증표

## 자동 검증 (완료)

- [x] `node --check src/index.js`
- [x] `node .codex/scripts/verify-repository.mjs` — 체크섬 95개 · 자동검사 3,154개
- [x] 이번 릴리스 신규 회귀 검사 280개
- [x] 되돌린 결함 36종이 모두 잡히는 것 확인 (단계별 돌연변이 시험)

## SQL (2026-08-07 적용 완료)

- [x] `03` 적용 → `upsert_ready=true` · `leftover_settings_budget_keys=0` · `duplicate_groups=0`
- [x] `04`·`05` 적용 → 18개 표 전부 `anon_or_authenticated_grants=0` · `truncate_exposed=false`
- [x] 적용 전 `42P10` 실패 → 적용 후 성공을 로컬 PostgreSQL 16.13 에서 재현
- [x] `anon` 으로 `select` 0행 / `truncate` 성공을 재현해 RLS 가 TRUNCATE 를 못 덮는 것 확인
- [x] service_role 의 select·insert·update·delete 가 그대로 동작하는 것 확인

## 월 고정 · 브랜드 아이콘 (P0)

- [x] `/app` 파라미터 없음 → 이번 달, 배지 없음
- [x] `/app?month=2026-07` → "지난달 보는 중" + 이동 링크
- [x] `view=calendar&type=income` 을 걸어도 이동 링크가 필터를 물고 감
- [x] 이동 링크를 따라가면 이번 달이 되고 배지가 사라짐
- [x] 사이드바 로고가 SVG 로 렌더되고 이모지로 되돌아가지 않음
- [x] `.abNavLogo` 세 선언이 모두 가운데 정렬 + 명시적 아이콘 색을 가짐
- [ ] **실기기**: 라이트/다크 양쪽에서 좌측상단·모바일상단 로고가 또렷한지 육안 확인

## 테마 · 사이드 위계 (P2)

- [x] 저장값 `system` → 라이트로 정정 / `dark` · `light` 는 보존 / 없으면 라이트
- [x] `/menu` 화면모드 버튼이 라이트·다크 2개만
- [x] OS 다크 전환이 화면을 흔들지 않음
- [x] 위계 규칙이 전부 `:not([data-ab-resolved-theme="dark"])` 로 막혀 다크 무영향
- [x] 위계 블록에 대비를 낮추는 선언(`--faint`)이 없음
- [ ] **실기기**: 라이트 사이드바에서 대메뉴/하위 위계가 육안 구분되는지

## 거래내역 탭 (P2)

- [x] `/app?tab=transactions` → 전용 뷰, 홈 위젯 숨김
- [x] 필터·검색·분류 select 렌더
- [x] 필터를 걸어도 탭에 남음 (`tab` hidden)
- [x] 하단탭 "거래" 가 활성 표시되고 홈 탭이 풀림
- [x] 페이지 나누기 경계 (0건·50건·51건·마지막·범위 밖·0·음수·문자열)
- [x] 죽은 `#feed` 앵커 8곳 제거

## 정기 IA · 예산 동선 (P1)

- [x] 홈에 고정지출 입력폼 없음, 정기 진입 카드 렌더
- [x] 이번 달 → 실제 집계 / 과거 달 → 건수를 단정하지 않는 문구
- [x] `/admin/recurring/*` 라우트·시그니처 보존
- [x] 리포트 예산 표는 읽기전용 + "예산 설정 →" 링크

## 정기 수입·지출 수정 (P1)

- [x] 카드의 수정 폼이 실제 값으로 프리필
- [x] 수정 저장 → 금액 반영, 옛 값 사라짐
- [x] 구성원(member) 수정 거부 + 미반영
- [x] 없는 id → 실패하고 기존 항목 보존
- [x] 빈 금액 → 거부하고 이전 값 유지
- [x] 수입 항목 `+` 표기 · 매월 반복 배지 · 순액
- [x] 예전 형식(type 없음) 항목이 지출로 뜨고 `-` 부호
- [x] 저장 키 형식 `reserve_plans:<가계부ID>` 무변경

## 게이지 · 소비 흐름 (P3)

- [x] 게이지 분모가 이번 달 수입 (예산 아님)
- [x] 수입 0원 → "수입을 입력하면 사용률이 표시돼요"
- [x] '오늘 써도 되는 돈' 표기 제거
- [x] '살까 말까' 없음 (지시서 금지)
- [x] 기본은 패널 없이 기간 탭만 · 잘못된 값도 기본으로
- [x] 홈 기본 34,681B (기준선 35 KiB 내) · 질의 9회 유지
- [x] 월별 6개월 질의는 `trend=monthly` 일 때만

## 배포 후 (운영자)

- [ ] Cloudflare Worker 에 `src/index.js` 반영 후 배포
- [ ] `/health` 버전 = `V22.8.79-UIUX-INCOME-BUDGET-THEME`
- [ ] `10_VERIFY_AFTER_DEPLOY_V22_8_79.ps1` 통과
- [ ] 웹 탭 새로고침 (자산 주소 6종 변경)
- [ ] 옛 자산 경로 5종이 404
- [ ] 카카오 저장·파서 무회귀 (`/kakao-recent`)
- [ ] 다크 모드에서 신규 요소에 흰 박스 없음
