# V22.8.77 배포 검증표

## 11단계 — 끊김·중단 복구

- [x] 저장 후 뒤로 가기: 거래·예산·챌린지 쓰기가 모두 303 임을 확인 (재제출 없음)
- [x] 세션 만료 저장이 예전 관리자 화면으로 던져지던 것 재현 후 수정
- [x] 로그인 화면으로 보내고 왜 안 됐는지 안내가 함께 가는 것 확인
- [x] 로그인 화면의 폼이 돌아갈 주소를 지니는 것 확인
- [x] 로그인 후 쓰던 화면(`/app?…&quick=1`)으로 복귀하는 것 확인
- [x] 비밀번호를 틀려 다시 묻는 화면에서도 돌아갈 주소가 남는 것 확인
- [x] 브라우저에서 초안 복원 확인 (62000 · "정기점검 비용" · 교통/차량, JS 오류 0건)
- [x] 외부 주소·`//`·`/admin`·줄바꿈 섞기 4종이 모두 차단되는 것 확인
- [x] 적어 둔 내용이 주소에 실리지 않는 것 확인
- [x] 회귀 검사 26개, 되돌린 결함 4종이 모두 잡히는 것 확인
- [x] 기존 검사 수정 없이 전체 2,855개 통과

## 운영 배포 후 수동 확인 (V22.8.77)

- [ ] `/health` 가 `V22.8.77-INTERRUPTED-SAVE-RECOVERY` 반환
- [ ] 브라우저 두 개 중 한쪽에서 로그아웃 후, 다른 쪽에서 기록 저장 시도
- [ ] 로그인 화면으로 가는지 (관리자 화면이 아니라)
- [ ] 왜 안 됐는지 안내가 보이는지
- [ ] 로그인하면 쓰던 화면으로 돌아오는지
- [ ] 적었던 금액·메모가 그대로 있는지

---

# V22.8.76 배포 검증표

## 되묻기 정리

- [x] 22개 표현을 실제로 넣어 되묻기 기준이 21개 단어 목록임을 확인
- [x] `카페 4500`은 묻고 `커피 4500`은 안 묻던 비일관 실측
- [x] 분류+금액 7종이 전부 지출로 기록되고 분류가 유지되는 것 확인
- [x] 금액 없는 모호한 말 4종(`예산`·`가계부`·`초대`·`이름변경`)은 계속 되묻는 것 확인

## 예산 명령 결함 2건

- [x] `식비 예산 30만원` 이 30만원짜리 지출로 기록되던 것 재현 후 수정
- [x] 꼬리가 금액 하나뿐일 때만 받도록 제한 — `회사 예산 회의 5000` 은 지출로 남음
- [x] `남은 예산`·`식비 예산 얼마야` 조회 경로가 그대로인 것 확인
- [x] `전체 예산설정 300만원` 이 항상 실패하던 것을 계측으로 확인 (`__total` → `total`)
- [x] 전체 예산 표기 4종이 모두 저장되는 것 확인

## /skill 인증 관측

- [x] 헤더 없음·틀린 값 403, 사용자에게는 평범한 재시도 안내가 가는 것 확인
- [x] 거부 응답에 비밀값이 새지 않는 것 확인
- [x] `/health` 에 통과·거부 집계와 `header_missing_suspected` 노출
- [x] 통과 0 · 거부만 쌓인 상태를 실제로 만들어 신호가 켜지는 것 확인
- [x] `x-kakao-skill-secret`·`x-api-key`·`Bearer`·앞뒤 공백 4종 통과 확인
- [x] observe 모드에서 막지 않되 실패는 계속 세는 것 확인
- [x] 모드 오타(`observ`)가 열리지 않고 차단 쪽으로 떨어지는 것 확인
- [x] 비밀값이 없으면 예전 그대로 열려 있는 것 확인 (배포만으로 채널이 막히지 않음)

## 판단 후 유지 (2026-08-06)

- [x] 사이드바 달력 34개 — 데스크톱 탭 정지점 54개 중 34개지만 본문 바로가기로 Tab 한 번이면 본문. 유지
- [x] 구성원 예산·정기지출 권한 — 화면·서버 양쪽에서 막고 있음을 역할별 요청으로 확인. 유지

## 11단계 점검 착수 — 카카오 재전송 중복

- [x] 카카오 재전송을 순차·동시 2회·동시 3회로 재어 동시 요청만 중복이 남는 것 확인
- [x] 웹 저장 버튼 중복 제출은 순차·동시 모두 이미 막혀 있는 것 확인
- [x] "처리 중" 표시로 창을 닫고, 성공·오류 양쪽에서 표시를 내리는 것 확인
- [x] V22.8.69 회귀 4종 확인 (선택 전 발화 / 인식 실패 / 오류 후 재시도 / 정상 후 재입력)
- [x] 예산 일괄 저장은 RPC 한 번이라 부분 실패가 없다는 것을 쓰기 경로 추적으로 확인
- [x] 저장 실패 시 오류 문구는 실제 폼 경로(`return_to` 포함)에서 정상적으로 보이는 것 확인 — 앞선 "안 보임" 보고는 probe 오류였음
- [x] 저장 실패 시 적어 둔 금액·메모가 사라지던 것 수정, 브라우저에서 저장→실패→복원→삭제 전 과정 확인
- [x] 적어 둔 내용이 주소에 실리지 않는 것 확인 (메모가 방문 기록에 남지 않도록)

## 회귀 검사

- [x] `validate-kakao-intent-skill-auth-v22876.mjs` 93개 통과
- [x] 되돌린 결함 9종(`__total` 이중 정규화 / 예산 암묵 표기 / observe 집계 / 모드 오타 / 처리 중 표시 제거 / 표시 미해제 / 초안 저장 제거 / 실패해도 초안 버림 / 내용을 주소에 실음)이 모두 잡히는 것 확인
- [x] 기존 검사 수정 없이 전체 2,829개 통과

## 운영 배포 후 수동 확인 (V22.8.76)

- [ ] `/health` 가 `V22.8.76-KAKAO-INTENT-SKILL-AUTH` 반환
- [ ] `/health` 의 `skill_caller_auth` 에서 `accepted` 가 오르고 `denied` 가 0
- [ ] 카카오톡 `주유 5만원` → 되묻지 않고 바로 지출 저장
- [ ] 카카오톡 `식비 예산 30만원` → 예산 설정 (지출로 남지 않음)
- [ ] 카카오톡 `전체 예산설정 300만원` → 전체 월 예산 설정
- [ ] 카카오톡 `남은 예산` 조회가 그대로
- [ ] 카카오톡이 느릴 때 같은 기록이 두 번 남지 않는지
- [ ] 저장이 실패했을 때 적었던 금액·메모가 그대로 남아 있는지
- [ ] 과거 `예산` 이라 말했다가 지출로 남은 기록을 소비 분석에서 확인하고 삭제

---

# V22.8.75 배포 검증표

## 중복 카드 실측

- [x] 120건 픽스처로 세 화면의 `<h2>` 전수 비교 — 완전 동일 제목 2개, 이름만 다른 쌍 4개 확인
- [x] 코크핏의 네 블록이 종합 리포트 같은 페이지 안에서 반복되는 것을 항목별로 대조
- [x] 소비 분석에서는 코크핏이 유일한 달 요약이고 나머지는 전부 필터 반응인 것 확인
- [x] 축약 챌린지가 좁은 화면에서 `display:none!important` 로 숨겨지는 것을 확인 → 챌린지는 양쪽 유지로 결정

## 역할 분리 결과

- [x] 종합 리포트 HTML 123,728 B → 122,267 B (-1,461 B)
- [x] `/my/analysis/app.js` 52,064 B → 51,385 B (-679 B)
- [x] 소비 분석 HTML +137 B (역할 안내 문구), 첫 방문 합산 -542 B
- [x] 두 화면 제목 교집합 2개 → **1개**(챌린지), 월 마감과의 교집합 0개
- [x] 왕복 링크 확인 — `깊게 보기(종합 리포트)` / `← 한눈에 보기(소비 분석)` 앵커가 실제로 존재
- [x] 홈 HTML 실사용 예산 변화 없음 (44,962 B / 47,104 B)

## 보조 버튼 명시도

- [x] 브라우저에서 실제로 이긴 규칙을 조회해 원인 확인 (`:not()` 사슬이 클래스 5개로 부풀림)
- [x] 옆 `button[type=submit]` 규칙에만 있던 `:not(.secondary)` 예외를 `.btn` 에도 적용
- [x] 보조 버튼 6개 화면 라이트 16.56:1 / 다크 7.85:1, 주요 버튼 6.16:1 — 미달 0건
- [x] 가로 넘침 0px, JS 오류 0건
- [x] 측정 도구부터 검증 — 최신 Chromium 은 일반 규칙도 빈 `cssRules` 를 가져 "0건" 거짓 결과가 나옴

## 회귀 검사

- [x] `validate-analysis-role-split-v22875.mjs` 44개 통과
- [x] 코크핏 복귀 / 분류별 사용률 복귀 / `:not(.secondary)` 제거 3종을 되돌려 전부 잡히는 것 확인
- [x] 계약 변경분 반영 — `validate-report-dashboard-v22856`, `validate-ux-principles` 해시 2건, `validate-performance-v22811` 바이트 고정
- [x] 홈 기본 스타일시트 주소 v22875 로 갱신

## 운영 배포 후 수동 확인 (V22.8.75)

- [ ] `/health`가 `V22.8.75-ANALYSIS-ROLE-SPLIT` 반환
- [ ] `소비 분석`에 `한눈에 보기`가 있고 `종합 리포트`에는 없음
- [ ] `종합 리포트` 맨 위 버튼 세 개 중 파란 것이 하나
- [ ] `← 한눈에 보기(소비 분석)` 을 누르면 코크핏 위치로 이동
- [ ] 모바일에서 `소비 분석`의 챌린지가 그대로 보임
- [ ] 총액 예산을 설정한 가계부에서 `예산 소비 페이스` 카드가 보임
- [ ] 오래 열린 탭 새로고침 후 `mobile-home-v22875` 스타일 적용

---

# V22.8.74 배포 검증표

## 예산 재검토

- [x] 검사가 과거 달을 고정해 더 작은 경우를 재던 것을 확인 (그 달 안 45,084 B > 예산 45,056 B)
- [x] 릴리스별 대조로 V22.8.73 이 선을 넘은 지점임을 확인
- [x] 선택되지 않은 option·checkbox 잉여 공백 제거로 38 B 회수
- [x] 검사가 "이번 달"을 재도록 변경, 4개 날짜에서 통과 확인
- [x] 예산 44 KiB → 46 KiB (실측 44,962 B, 여유 2,142 B)
- [x] 카드에 200 B 를 넣으면 검사가 실패하는 것 확인

## 다크 대비

- [x] 빈 상태 상자 1.82:1 → 어두운 표면으로 보정
- [x] 본문 바로가기 배지 2.87:1 → 흰 글자 복구
- [x] 라이트·다크 × 포인트 4종 8조합 전부 0건
- [x] 두 규칙이 실제 서빙되는 셸 자원에 실려 나가는 것 확인
- [x] 셸 스타일시트 주소 v22874 로 갱신

## 실기기 확인 항목 (브라우저로 검증 가능한 범위)

- [x] manifest·아이콘 192/512/512-maskable·apple-touch-icon 전부 200
- [x] 카카오 인앱 UA 로 빠른 입력 시트 열림·날짜 전달·배경 스크롤 잠금·Esc 복구·JS 오류 0
- [x] iOS `apple-mobile-web-app-capable` 추가 여부 제품 판단 → **넣지 않음** (Safari 뒤로 가기 유지, 2026-08-06)
- [ ] 실제 기기에서 손가락 터치 반응·iOS Safari 고유 동작 확인 (**기기 필요**)

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.74-BUDGET-DARK-CONTRAST` 반환
- [ ] 다크 모드에서 홈 빈 상태 상자 글자가 읽힘
- [ ] 다크 모드에서 Tab 한 번 → 본문 바로가기 배지 흰 글자 선명
- [ ] 포인트 컬러 4종 바꿔 가며 위 두 가지 확인
- [ ] 오래 열린 탭 새로고침 후 `v22874` 셸 적용

---

# V22.8.73 배포 검증표

## 병합 무결성

- [x] 받은 파일이 V22.8.70 에서 갈라진 것임을 커밋 대조로 확인
- [x] V22.8.71 표지 3종·V22.8.72 표지 2종이 병합본에 모두 살아 있음
- [x] 받은 작업의 새 식별자 9종이 병합본에 모두 들어옴
- [x] 충돌 3건(버전 문자열)만 발생, 새 번호 v22873 부여
- [x] 기존 회귀 전부 재통과 (수정·삭제·복구 58 / 본문 바로가기 178 / 금액 파싱 77 / 메모·중복 57 / 수정 플로우 130 / 핵심 쓰기 110)

## SQL

- [x] `accountbook_purge_household_v227` 는 기존 RPC — 새로 만들지 않음
- [x] 기존 정의와 직접 대조, 실제 차이 3건만 확인
- [x] `search_path = ''` 안전성 확인 (표 참조 전부 `public.` 정규화, 나머지는 `pg_catalog`)
- [x] 삭제 키 3종 형식이 앱 코드와 일치 (`favorites` 는 접두 일치가 맞음)
- [x] `drop`·`truncate` 없음, 실행 권한 `service_role` 한정
- [x] Supabase SQL Editor 에서 실행 (2026-08-06 운영자 확인, 저장소에서는 직접 확인 불가)

## 시크릿

- [x] 비밀값 없음 → `/skill` 200 (기존과 동일, 배포로 채널이 막히지 않음)
- [x] 비밀값 있음 → 헤더 없음/틀린 값 403
- [x] `x-api-key`·`x-kakao-skill-secret`·`Bearer` 3종 200
- [x] 상수 시간 비교, 차단 응답·`/health` 에 비밀값 미노출
- [x] `KAKAO_SKILL_AUTH_REQUIRED=1` + 비밀값 없음 → 구성 미비로 포착
- [ ] OpenBuilder 헤더 먼저 설정 → 확인 → Worker 비밀값 순서로 적용 (**미적용**)

## 나머지 검토

- [x] `/ready` 필수 10 · 선택 1 분리, 실측 11개 보고
- [x] 가계부 생성 잠금 DB 임대 전환, 메모리 잠금 잔존 없음
- [x] 저장 식별자 암호학적 난수, 시간 기반 식별자 잔존 없음
- [x] 계약이 바뀐 기존 검사 2건 갱신 및 확인 항목 확대
- [x] `mobile-home` 바이트 고정값을 캘린더 도트 2줄 확인 후 의도적으로 갱신
- [x] 홈 HTML 35,065B(예산 35,840B) · 실사용 44,654B(예산 45,056B)
- [x] 신규 회귀 49개 포함 총 2,673개 자동검사·ESM 진입점·체크섬 통과

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.73-CALENDAR-CHALLENGE-SECURITY` 반환
- [ ] `/health`의 `skill_caller_auth_configured` 값 확인
- [ ] `/ready`가 표 11개·RPC 17개와 함께 `ready=true`
- [ ] 캘린더 기록 도트 표시, 선택된 날짜에서도 보임
- [ ] 날짜 상세에서 권한별 수정·삭제·기록 추가
- [ ] 챌린지 3종(무지출·하루 한도·분류별 한도) 동작
- [ ] 카카오톡 `수정 01번 금액 3만5천원`, 연속 삭제 후 두 건 복구 (V22.8.71 유지)
- [ ] Tab 한 번 → `본문 바로가기` (V22.8.72 유지)
- [ ] 오래 열린 탭 새로고침 후 `v22873` 자원 적용

---

# V22.8.72 배포 검증표

## 키보드 본문 바로가기

- [x] 24개 화면 × 390/1440px 전부에서 첫 Tab 이 `본문 바로가기`
- [x] 포커스 시 화면 안으로 들어옴(top 12px), 최소 높이 44px
- [x] Enter 로 `main` 에 착지하고 다음 Tab 이 본문 안에서 이어짐
- [x] 본문 첫 요소까지 Tab: 홈 55회 → 1회, 전 화면 1회
- [x] `display:none`·`visibility:hidden` 미사용(포커스 유지)
- [x] `main` 에 `tabindex="-1"`, 기존 `id` 는 그대로 사용
- [x] 셸 미사용 화면에는 삽입되지 않음
- [x] 포커스 표시 결함 0건 (계측 검증: 규칙 제거 시 103/103 검출)
- [x] 클릭 전용 요소·`aria-hidden` 안 조작 요소 0건
- [x] 홈 HTML 35,058B(예산 35,840B) · 실사용 44,647B(예산 45,056B)
- [x] 셸 스타일시트 주소 v22872 로 갱신
- [x] 신규 회귀 178개 포함 총 2,621개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.72-SKIP-TO-CONTENT` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 아무 화면에서 Tab 한 번 → `본문 바로가기` 노출
- [ ] Enter 후 본문 이동, 다음 Tab 이 본문 안에서 이어짐
- [ ] 마우스 사용 시에는 링크가 보이지 않음
- [ ] 오래 열린 탭 새로고침 후 `v22872` 셸 적용

---

# V22.8.71 배포 검증표

## 수정·삭제·복구 명령

- [x] `수정 NN번 날짜 어제`가 오늘의 NN번을 바꾸고 다른 날 기록은 건드리지 않음
- [x] `수정 NN번 날짜 2026-08-01` 정상 변경
- [x] 접두 날짜 대상 지정(`어제 수정 01번 금액 5000`) 유지
- [x] 연속 삭제 두 건이 안내받은 번호로 모두 복구됨
- [x] 복구 확인된 건만 버퍼에서 제거, 나머지는 계속 복구 가능
- [x] 옛 한 칸짜리 버퍼도 계속 읽힘
- [x] `수정 NN번 금액` 이 6만원·3만5천원·1만5천·12,345·100,000원·7500 모두 수용
- [x] 메뉴 경로(`수정 01번` → `1` → `3만5천원`)도 동일 수용
- [x] 금액 아닌 값(`사과`)은 계속 거절, 저장값 무변화
- [x] 없는 번호(`수정 99번`·`삭제 99번`) 정확히 안내
- [x] 수정 세션에서 확인 거절 시 `취소` 탈출 안내 표시
- [x] 금액·내용·분류·결제수단 수정이 저장값에 반영
- [x] 사용자 화면 HTML 바이트 변화 0 (기준 34,958B·실사용 44,547B, main 과 동일)
- [x] 자원 주소 변경 없음 (모바일 홈 런타임 v22870 유지)
- [x] 신규 회귀 58개 포함 총 2,443개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.71-EDIT-RESTORE-FIX` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 카카오톡 `수정 01번 금액 3만5천원` → 받아들여짐
- [ ] 두 건 연속 삭제 후 안내받은 번호로 둘 다 복구
- [ ] `수정 01번 날짜 어제` → 오늘 01번의 날짜가 바뀜
- [ ] 수정 메뉴 중 새 지출 입력 시 `취소` 안내 노출
- [ ] 과거에 날짜가 안 바뀐 기록이 있는지 최근 내역 점검

---

# V22.8.70 배포 검증표

## 금액 파싱

- [x] 금액 표기 21종이 한 건으로, 쓴 금액 그대로 저장
- [x] `12,345원`·`1,200원`·`60,000`·`100,000원`·`1,500,000원` 정상 저장
- [x] `3만5천원`·`1만5천원`·`3만 5천원`이 각각 한 건으로 저장
- [x] `10만5000원`이 105,000원으로 저장
- [x] 항목 구분 쉼표(`빵 2600, 우산 4700`)는 계속 두 건으로 분리
- [x] 조사로 끝나는 낱말 15종이 잘리지 않음
- [x] 두 글자 조사 정리(`마트에서`→`마트`) 유지
- [x] 결제수단 8종·구분 3종·분류 4종 정확
- [x] 여러 건 입력 4종이 각각 두 건으로 분리
- [x] 사용자 화면 HTML 바이트 변화 0 (기준 34,958B·실사용 44,216B)
- [x] 모바일 홈 런타임 주소 v22870 로 갱신, 성능 검사 바이트 고정값 갱신
- [x] 신규 회귀 77개 포함 총 2,385개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.70-AMOUNT-PARSE-FIX` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 카카오톡 `커피 12,345원` → 12,345원 한 건
- [ ] 카카오톡 `커피 3만5천원` → 35,000원 한 건
- [ ] `마트에서 12000` → 메모 `마트`
- [ ] `고양이 사료 3만원` → 메모에 `고양이` 유지
- [ ] 오래 열린 탭 새로고침 후 `v22870` 런타임 적용
- [ ] 과거에 틀리게 저장된 금액이 있는지 최근 내역 점검

---

# V22.8.69 배포 검증표

## 카카오 입력 메모·중복 방지

- [x] 가계부 선택 안내 뒤 같은 문구 재입력이 저장됨 (보정 전 저장 0건)
- [x] 인식 실패 뒤 같은 문구 재입력이 막히지 않음
- [x] 연속 중복 입력은 여전히 한 건만 저장
- [x] 메모 표본 27개가 입력 그대로 저장 (보정 전 21/29 손상)
- [x] `마트에서`·`회사로`·`결제` 조사·서술어 정리는 유지
- [x] `로또`·`또또`·`사랑`·`가랑비` 가 쪼개지지 않음
- [x] 여러 건 입력 3종이 각각 2건으로 저장
- [x] 웹 홈 빠른 입력 `채우기` 도 동일하게 수정
- [x] 사용자 화면 HTML 바이트 변화 0 (기준 34,958B·실사용 44,216B)
- [x] 모바일 홈 런타임 주소 v22869 로 갱신, 성능 검사 바이트 고정값 갱신
- [x] 신규 회귀 57개 포함 총 2,308개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.69-KAKAO-MEMO-REPEAT-FIX` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 카카오톡 `자동차점검 6만원` → 저장됨
- [ ] 카카오톡 `타이어교체 15만원` → 메모가 `타이어교체` 그대로
- [ ] 같은 지출 연속 2회 → 두 번째 차단
- [ ] 웹 홈 빠른 입력에 `타이어교체 15만원` → `채우기` 후 내용 칸 그대로
- [ ] 오래 열린 탭 새로고침 후 `v22869` 런타임 적용
- [ ] 과거에 글자가 빠진 메모가 남아 있는지 최근 내역 점검

---

# V22.8.68 배포 검증표

## 모바일 조작 영역·접근성

- [x] 사용자 화면 24개 × 390/1440px 실제 렌더로 탭 영역 측정
- [x] 작은 조작 영역 58건 → 1건 (남은 1건은 문장 안 인라인 링크, WCAG 2.5.8 예외)
- [x] 접기 헤더가 목록 표시(::marker)를 유지
- [x] 표 안 링크가 행 높이를 늘리지 않음
- [x] 이미 44px 이상인 요소는 그대로 유지 (min-height 만 사용)
- [x] 이름 없는 컨트롤·버튼 0건, alt 누락 0건
- [x] h1 누락·헤딩 건너뜀·중복 id·끊긴 for/aria 참조·양수 tabindex 0건
- [x] html lang·main 랜드마크 24개 화면 모두 존재
- [x] 72개 조합 레이아웃 재점검: 넘침 0, 이탈 증가 0, 오류 증가 0
- [x] 사용자 화면 HTML 바이트 변화 0 (기준 34,958B·실사용 44,216B)
- [x] 셸 주소 v22868 로 갱신, 이전 v22865 는 404
- [x] 신규 회귀 278개 포함 총 2,251개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.68-TAP-TARGET-ACCESSIBILITY` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 종합 리포트의 접기 헤더가 첫 탭에 열림
- [ ] 자산·계좌 `상세 정보 수정`, 예산 `계산 기준 보기`가 첫 탭에 열림
- [ ] 홈 `최근 기록 확인 →`, `월간 리포트 →`가 첫 탭에 이동
- [ ] 가계부 전환·추가의 `열기`·`옵션`·`멤버` 버튼이 첫 탭에 반응
- [ ] 오래 열린 탭 새로고침 후 `v22868` 셸 적용

---

# V22.8.67 배포 검증표

## 역할별 화면·빈 상태

- [x] 조회 전용·승인 대기·이용 제한이 쓰기 경로 24개에서 데이터 변경 0건
- [x] 조회 전용 화면 13개에 누를 수 있는 저장 수단 없음(소유자 화면에서는 잡힘)
- [x] 구성원·조회 전용의 분류·키워드 화면에 저장 버튼·추가줄·삭제(×) 없음
- [x] 권한 거절 후 착지 화면이 그 역할에게 실제로 200으로 열림
- [x] 소유자 키워드 저장이 온 화면으로 복귀하고 성공 문구 표시
- [x] `return_to` 없는 기존 경로는 설정 화면 복귀 유지
- [x] 기록 0건 가계부 화면 12개에 `NaN`·`undefined`·`Infinity` 없음
- [x] 기록 0건 홈에 지난달 비교 문구 없음, 기록 있는 달은 비교 문구 유지
- [x] 권한 안내 문구에 영어 역할명 없음
- [x] 홈 HTML 바이트 변화 0(기준 34,958B·실사용 44,216B)
- [x] 고정 자원 3종 바이트 동일(경로 변경 없음)
- [x] 신규 회귀 203개 포함 총 1,973개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.67-ROLE-SURFACE-EMPTY-STATE` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 구성원 또는 조회 전용 계정으로 `분류·키워드` → 저장 버튼 없음 + `보기 전용` 안내
- [ ] 소유자 계정으로 키워드 저장 → 분류·키워드 화면 복귀 + 성공 문구
- [ ] 기록이 없는 새 가계부 홈 → `아직 지출 기록이 없어요` 문구
- [ ] 거래 저장·수정·삭제가 기존과 동일한지

---

# V22.8.66 배포 검증표

## 거래 삭제 동작 복구

- [x] 삭제 버튼이 수정 폼 안에 위치
- [x] 소스 전체에 폼 밖 `formaction` 제출 버튼 없음
- [x] 실제 삭제 요청 후 거래 수 감소와 목록 제거 확인
- [x] 브라우저 계측에서 `POST /admin/delete` 발생 확인(보정 전에는 요청 자체가 없었음)
- [x] 주요 기능 흐름 12개 전부 통과, HTTP 4xx/5xx 0건, 페이지 오류 0건
- [x] HTML 바이트 증가 없음
- [x] 총 1,770개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.66-DELETE-BUTTON-FORM-FIX` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 최근 내역에서 기록을 펼쳐 `삭제` → 확인 → 목록에서 사라짐
- [ ] 새로고침 후에도 사라져 있는지
- [ ] 삭제 후 홈 합계·캘린더 표시가 함께 갱신되는지
- [ ] 카카오톡 챗봇 삭제 명령은 기존과 동일한지

---

# V22.8.65 배포 검증표

## 테마 글자 대비

- [x] 라이트·다크 × 포인트 컬러 4종 = 8개 조합 × 24개 탭 실제 렌더 측정
- [x] 8개 조합 전부 저대비 텍스트 0건(WCAG 일반 4.5:1, 큰 글씨 3:1)
- [x] 연간 리포트 연말정산 참고·연도 이동 복구
- [x] 자산·계좌 히어로, 저축·목표 진행 금액 복구
- [x] 챌린지 설정 라벨, 일자별 셀, 증감 배지 복구
- [x] 자체 배경을 가진 칩·배지는 덮어쓰지 않음(스마트 도구 배지 회귀 확인)
- [x] 셸 토큰 조합 대비를 값으로 고정
- [x] V22.8.61~64 결과물 유지
- [x] 셸 immutable 주소와 ETag `v22865`
- [x] 총 1,755개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.65-THEME-TEXT-CONTRAST` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] `전체 메뉴 → 화면 설정`에서 라이트·다크 전환
- [ ] 포인트 컬러 블루·그린·바이올렛·앰버 각각 전환
- [ ] 연간 리포트의 연말정산 참고 상자와 연도 이동 버튼 가독성
- [ ] 자산·계좌 히어로 문구와 칩 가독성
- [ ] 종합 리포트 챌린지 설정 폼 라벨 가독성
- [ ] 카카오톡 인앱 브라우저의 다크 모드 동작

---

# V22.8.64 배포 검증표

## 웹 매니페스트

- [x] `/manifest.json` MIME·ETag·HEAD와 필수 항목 선언
- [x] 안드로이드 설치 요건(192·512 아이콘, maskable) 충족
- [x] `/icon-192.png`, `/icon-512.png` PNG 시그니처·크기·팔레트
- [x] 사용자 화면 5종이 매니페스트를 정확히 한 번만 참조
- [x] 아이콘 링크 태그는 추가하지 않음
- [x] 홈 HTML 35KB·44KB 예산 유지(여유 352B·310B)
- [x] `CompressionStream` 사용과 저장 블록 대체 경로 유지
- [x] Chromium 3개 화면에서 매니페스트·아이콘 로드 확인, 4xx·콘솔 오류 0건
- [x] 총 1,705개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.64-WEB-MANIFEST` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 안드로이드 크롬에서 `설치`/`홈 화면에 추가` 안내가 뜨는지
- [ ] 설치 후 아이콘·앱 이름(`가계부`)·시작 화면(`/app`) 확인
- [ ] iPhone 사파리 `공유 → 홈 화면에 추가` 아이콘 확인
- [ ] 설치 상태에서 카카오 로그인 왕복이 앱 안에서 끝나는지
- [ ] 카카오톡 인앱 브라우저가 기존과 동일한지

---

# V22.8.63 배포 검증표

## 앱 아이콘 자원

- [x] `/favicon.ico` ICO 32×32 32bpp, `image/x-icon`, ETag, HEAD 응답
- [x] `/apple-touch-icon.png` PNG 180×180 인덱스 팔레트, `image/png`, ETag
- [x] `/apple-touch-icon-precomposed.png` 동일 바이트 제공
- [x] 아이콘 경로가 쓰기 요청에 응답하지 않음
- [x] 홈 HTML 마크업 증가 0바이트, 35KB·44KB 예산 유지
- [x] Chromium에서 두 아이콘 디코딩 확인(32×32 / 180×180)
- [x] `/app` 재점검에서 4xx·콘솔 오류 0건
- [x] 총 1,641개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.63-APP-ICON-ASSETS` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 브라우저 탭과 즐겨찾기에 브랜드 아이콘 표시
- [ ] 개발자도구 콘솔에 favicon 404 없음
- [ ] iPhone 사파리 `공유 → 홈 화면에 추가` 시 아이콘 표시
- [ ] 카카오 인앱 브라우저에서 화면 이상 없음

---

# V22.8.62 배포 검증표

## 전체 탭 점검 보정

- [x] 사용자 탭 24개 × 390/768/1440px 실제 브라우저 점검(72개 조합) 전부 HTTP 200
- [x] 가로 넘침 0건, 박스 이탈 0건
- [x] 상단바 도킹 버튼이 전역 `button{width:100%}`에 늘어나지 않음
- [x] 최근 내역 필터 폼 이탈 제거
- [x] `#add`·`#feed`·`#calendar` 앵커가 고정 상단바 아래로 가리지 않음
- [x] V22.8.61의 토큰 별칭·칩 아이콘·시트 스크롤 잠금 유지
- [x] 셸·내비게이션 immutable 주소와 ETag `v22862`
- [x] 총 1,603개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.62-MOBILE-SURFACE-AUDIT-FIX` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 월 마감·부부 정산·내 계정·보안에서 `전체 메뉴` 버튼이 화면 밖으로 나가지 않음
- [ ] 홈 최근 내역 필터가 카드 밖으로 삐져나오지 않음
- [ ] 하단 `기록` 탭·홈 `캘린더` 링크 이동 시 제목이 상단바에 가리지 않음
- [ ] 카카오 인앱 브라우저에서 빠른 입력 시트 스크롤과 배경 고정(V22.8.61)
- [ ] 오래 열린 탭 새로고침 후 `v22862` 셸·내비게이션 자산 적용

---

# V22.8.61 배포 검증표

## 모바일 화면 보정

- [x] 정의되지 않은 CSS 토큰 0개 회귀 검사 고정
- [x] 하단 `입력` 탭 아이콘의 배경·대비 복구
- [x] 상단 공통 작업 버튼을 상단바에 도킹하고 `전체 메뉴`와 겹침 제거
- [x] 전체 메뉴 서랍의 중복 위젯 접기와 48px 터치 영역
- [x] 빠른 입력 시트 스크롤 잠금·복원과 중첩 스크롤 제거
- [x] 빠른 입력 칩·결제수단 칩 아이콘과 CSS 우선순위 일치
- [x] 챌린지 `시작일`·`목표일` 등 날짜 입력의 그리드 이탈 방지
- [x] 홈 HTML 35KB·44KB 예산과 `mobile-home-v22810.css` 바이트 고정 유지
- [x] 셸·내비게이션·V5 immutable 주소와 ETag `v22861`
- [x] 총 1,501개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.61-MOBILE-SURFACE-REPAIR` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 390px에서 `작업`과 `전체 메뉴` 버튼이 겹치지 않고 문구가 잘리지 않음
- [ ] `전체 메뉴`를 열면 메뉴 목록이 먼저 보이고 항목이 손가락으로 눌러짐
- [ ] 하단 `입력` 탭에 원형 배경과 `+` 아이콘이 보임
- [ ] 빠른 입력 칩·결제수단 칩에 아이콘이 붙고 줄바꿈이 깨지지 않음
- [ ] 카카오 인앱 브라우저에서 빠른 입력 시트만 스크롤되고 배경이 고정됨
- [ ] 시트를 닫으면 원래 스크롤 위치로 돌아옴
- [ ] 리포트 챌린지 설정의 날짜 입력이 카드 밖으로 나가지 않음
- [ ] 오래 열린 탭 새로고침 후 `v22861` 셸·내비게이션·V5 자산 적용

---

# V22.8.60 배포 검증표

## 기능·UI 신뢰성

- [x] 챌린지 인라인 저장의 전체 페이지 이동 차단
- [x] 저장 직후 본문·사이드바의 서버 계산 결과 동시 갱신
- [x] 8일 목표의 퍼센트형 즉시 전환과 저장값 8 유지
- [x] 세션 만료·권한 부족·유효성·저장 실패의 상태별 응답
- [x] JavaScript 미지원 시 기존 303 제출 호환
- [x] 연간·목표·예산 알림 별칭의 현재 메뉴 활성
- [x] 거래 수정·챌린지 저장·키워드 삭제 조작 영역 보강
- [x] 공개 홈·`ads.txt` HEAD 응답
- [x] 카카오 `/skill` 256KiB 요청 본문 상한과 데이터 무변경
- [x] 셸·내비게이션·V5 immutable 주소와 ETag `v22860`
- [x] 총 1,432개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.60-FUNCTIONAL-UI-RELIABILITY` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 챌린지 `7일 → 8일` 저장 시 새 페이지가 열리지 않고 주소·스크롤 유지
- [ ] 저장 직후 본문과 좌측 카드가 퍼센트형으로 전환되고 새로고침 뒤 8일 유지
- [ ] 잘못된 목표 일수 입력 시 화면 안에 오류가 표시되고 기존 설정 유지
- [ ] 연간 리포트·목표·예산 알림 화면에서 현재 메뉴가 하나만 활성
- [ ] 390px·768px·1440px에서 가로 넘침과 겹침이 없음
- [ ] 신규 계정 생성 후 가계부 생성 화면 이동, 로그아웃 후 같은 계정 재로그인
- [ ] 오래 열린 탭 새로고침 후 `v22860` 셸·내비게이션·V5 자산 적용

---

# V22.8.59 배포 검증표

## 계정·런타임 신뢰성

- [x] 신규 계정 원자적 생성·강한 식별정보·세션 버전 회귀 검사
- [x] 중복 로그인 이름·비밀번호 불일치·RPC 누락 오류 분류
- [x] 계정 생성 후 세션·초대 참여 실패의 부분 성공 안내
- [x] 사용자 세션 버전 조회 장애 실패 폐쇄
- [x] 우측 최근 기록 DB 81건 제한과 80건 초과 안내
- [x] 필터별 현재 목록 합계 일치
- [x] 12개월 9,000건 초과 경고와 중복 연간 조회 제거
- [x] 셸·V5 immutable 주소와 ETag `v22859`
- [x] GitHub Actions 검증 워크플로
- [x] 총 1,384개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.59-ACCOUNT-RUNTIME-RELIABILITY` 반환
- [ ] `/ready`가 17개 RPC·3개 테이블과 함께 `ready=true` 반환
- [ ] 신규 계정 생성 후 가계부 생성 화면으로 이동
- [ ] 로그아웃 후 방금 만든 로그인 이름·비밀번호로 재로그인
- [ ] 같은 로그인 이름 재가입 시 중복 안내가 표시되고 기존 계정 로그인 가능
- [ ] 1320px 이상 홈 우측 기록과 오늘·이번 주·전체 합계가 현재 목록과 일치
- [ ] 오래 열린 탭 새로고침 후 `v22859` 셸·V5 자산 적용

---

# V22.8.58 배포 검증표

## 챌린지·최근 기록 UI/UX

- [x] 7일 이하 요일·날짜별 성공·지출·오늘·예정 셀
- [x] 8일 이상 달성률 퍼센트 자동 전환과 날짜 그리드 생략
- [x] 진행 중인 오늘을 완료 무지출일로 선계산하지 않음
- [x] 홈 본문·펼친 사이드바의 동일 챌린지 상태 계산
- [x] 사이드바 챌린지 카드 간격·제목·진척 위계
- [x] 수입·식비·카페·교통·의료·쇼핑·주거·구독·교육·여가·기타 SVG 아이콘
- [x] 날짜별 건수·지출 합계와 실제 분류·결제수단·사용자 정보
- [x] 우측 기록 링크 접근성 이름·키보드 포커스
- [x] 1440×1000·390×844 로컬 렌더 및 가로 넘침 없음
- [x] 홈 초기 HTML 35KiB 이하와 `/app` 데이터 요청 9회 이하
- [x] 셸·V5 immutable 주소와 ETag `v22858`
- [x] 총 1,349개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.58-CHALLENGE-ACTIVITY-UX` 반환
- [ ] 새 7일 챌린지가 날짜 7칸과 `오늘 진행 중`으로 시작
- [ ] 지출이 있는 종료일은 `−`, 무지출 종료일은 `✓`로 구분
- [ ] 8일 이상 기간 저장 시 홈·사이드바가 퍼센트형으로 전환
- [ ] 1320px 이상 우측 기록에 분류별 SVG·날짜별 건수·합계 표시
- [ ] 사용자 정의 분류가 기타 영수증 아이콘으로 안전하게 표시
- [ ] 390px·768px에서 우측 기록이 숨고 챌린지 가로 넘침 없음
- [ ] 라이트·다크와 네 포인트 컬러에서 아이콘·분류 배지·상태 대비 정상

---

# V22.8.57 배포 검증표

## 홈 우측 기록·챌린지 보정

- [x] 1320px 이상 우측 최근 기록 패널과 오늘·이번 주·전체 필터 복원
- [x] 타 가계부 ID 조회 차단과 읽기 전용 계약
- [x] 홈 상단·본문 1280px 공통 폭 정렬
- [x] 장부 SVG 브랜드 아이콘과 사이드바 경계 접기 버튼
- [x] 홈 본문·펼친 사이드바 챌린지 표시
- [x] 시작일·목표일·현재 일차·완료 무지출 일수 분리
- [x] 저장 당일 7일 목표가 7/7로 표시되지 않는 회귀 검사
- [x] 셸·V5 immutable 주소와 ETag `v22857` 상향
- [x] 총 1,277개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.57-HOME-RAIL-CHALLENGE-FIX` 반환
- [ ] PC 1320px 이상에서 우측 사용 내역·필터·전체 보기·거래 추가 정상
- [ ] 다른 가계부로 전환하면 우측 기록과 합계도 함께 전환
- [ ] 홈 상단과 카드 본문 좌우 폭 일치
- [ ] 좌측 장부 아이콘과 접기/펼치기 버튼 위치·방향 정상
- [ ] 새 7일 챌린지가 `1/7일차`, `무지출 0/7일`로 시작
- [ ] 챌린지 시작일·목표일 저장 후 새로고침해도 유지
- [ ] 390px·768px에서 우측 패널이 숨고 가로 넘침 없음
- [ ] 라이트·다크와 네 포인트 컬러에서 챌린지·우측 패널 대비 정상

---

# V22.8.56 배포 검증표

## 리포트 대시보드 UX

- [x] 통계·종합 리포트·월 마감의 공통 이전/다음/직접 선택/이번 달 이동
- [x] 모든 월 이동 링크에서 `household_id`와 필요한 `view` 유지
- [x] 예산 속도·수입·지출·남은 예산·월말 예상 중앙 대시보드
- [x] 가계부별 월간 무지출 챌린지와 소유자·관리자 전용 설정
- [x] 공통 셸 사이드바 겹침 회귀 수정
- [x] 390px 반응형 규칙·44px 조작 영역·긴 금액 줄바꿈
- [x] 라이트·다크 모드와 파랑·에메랄드·보라·앰버 포인트 토큰 상속
- [x] 총 1,209개 자동검사·ESM 진입점·체크섬 통과
- [x] 신규 SQL·환경변수·카카오·OpenBuilder 변경 없음

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.56-REPORT-DASHBOARD-UX` 반환
- [ ] `/my/analysis`에서 이전 달·다음 달·직접 월 선택·이번 달 복귀
- [ ] `/my/analysis?view=report`에서 같은 가계부와 기준 월 유지
- [ ] `/reports`에서 월 이동 후 보고서 내용과 인쇄 화면 정상
- [ ] 소유자·관리자의 챌린지 저장, 일반 참여자의 읽기 전용 표시
- [ ] 라이트·다크 모드와 설정된 포인트 컬러에서 글자·입력·게이지 대비
- [ ] PC 1440px 및 모바일 390px에서 겹침·가로 넘침 없음

---

# V22.8.50 배포 검증표

## UI/UX 4단계

- [x] PC 하단 빠른 실행 독과 네 가지 실제 동작
- [x] 빠른 입력 주요 동작 강조와 사이드바 접힘 위치 보정
- [x] 성공·예산 경고·오류 통합 피드백 카드
- [x] 성공 저장 뒤 선택 날짜 일별 상세 자동 복귀
- [x] 오류 저장 뒤 같은 날짜 빠른 입력 자동 복귀
- [x] URL 일회성 결과 상태 정리
- [x] 기존 입력폼·POST 저장·권한·중복 방지 계약 유지
- [x] PC 1440×1000·모바일 390×844 렌더링과 가로 넘침 없음
- [x] 총 1,120개 자동검사·ESM 진입점·체크섬 통과
- [x] V22.8.46 참여자 역할 SQL 운영 적용 완료(사용자 확인)

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.50-UIUX-STAGE4-QUICK-DOCK-SAVE-FEEDBACK` 반환
- [ ] PC 독의 거래 검색·빠른 입력·알림·화면 설정 동작
- [ ] 테스트 저장 성공 후 성공 카드와 해당 날짜 상세 자동 열기
- [ ] 금액 누락 오류 후 오류 카드와 입력 팝업 자동 열기
- [ ] 새로고침 시 이전 성공·오류 카드가 반복되지 않음
- [ ] iOS Safari·Android Chrome에서 가상 키보드·safe-area 확인

---

# V22.8.49 배포 검증표

## UI/UX 3단계

- [x] PC 빠른 입력 620px 중앙 모달
- [x] 모바일 빠른 입력 하단 바텀시트
- [x] 모바일 메뉴 `홈 · 기록 · 입력 · 예산 · 전체`
- [x] 날짜 상세의 `이 날 기록 추가`와 선택 날짜 반영
- [x] 기존 `section#add` 입력폼 단일 DOM 재사용
- [x] 기존 거래 저장·권한·중복 방지 처리기 유지
- [x] 별도 거래 쓰기 API 없음
- [x] Esc·Tab 순환·포커스 복귀·배경 스크롤 잠금
- [x] PC 1440×1000·모바일 390×844 렌더링과 가로 넘침 없음
- [x] 총 1,031개 자동검사·ESM 진입점·체크섬 통과

## 운영 배포 후 수동 확인

- [ ] `/health`가 `V22.8.49-UIUX-STAGE3-QUICK-INPUT-MODAL` 반환
- [ ] PC 빠른 입력 버튼이 중앙 모달을 열고 닫음
- [ ] 모바일 중앙 입력 버튼이 바텀시트를 열고 닫음
- [ ] 빈 날짜의 `이 날 기록 추가`가 해당 날짜를 유지
- [ ] 지출·수입 테스트 저장 후 기존 목록과 날짜 상세 반영
- [ ] iOS Safari·Android Chrome 가상 키보드 상태에서 입력·스크롤·저장 가능

---

# V22.8.46 배포 검증표

## 저장소 판정

- [x] Worker `src/index.js` 전체 교체 필요
- [x] `01_APPLY_MEMBER_ROLE_SCHEMA_V22_8_46.sql` 적용 필요
- [x] SQL은 기존 참여자 행·RLS·GRANT·RPC·인덱스를 변경하지 않음
- [x] 환경변수·Secret 변경 없음
- [x] Kakao Developers 변경 없음
- [x] OpenBuilder 변경 없음
- [x] 참여자 권한 저장 실패가 Worker Error 1101로 전파되지 않음
- [x] 승인대기→관리자 정상 저장과 실패 시 기존 권한 보존 자동 검증
- [x] 텍스트 CHECK·enum 판별과 fail-closed SQL 정적 검증 20개
- [x] 분석·영수증·로그인·가계부 권한 보호 계약 유지
- [x] v1~v4 HTML은 설계 참고만 하고 번들·가상 데이터·localStorage 거래 구조는 미반영

## UI V5 공통 셸·전역 작업 자동 검증

- [x] 홈과 나머지 로그인 화면이 동일한 `abLayoutNav` 사용
- [x] 데스크톱 238px·접힘 72px 및 상태 저장
- [x] 사이드바·모바일 5탭의 공통 SVG 아이콘
- [x] 시스템·라이트·다크와 네 컬러톤 유지
- [x] 모바일 전체 메뉴가 같은 사이드바 DOM 사용
- [x] 홈 HTML 기준 픽스처 35KB 이하·실사용 200행 44KB 이하와 `/app` 데이터 요청 9회 이하
- [x] 홈·기본 소비 분석·정산의 공통 페이지 헤더·컨트롤 바
- [x] KPI·필터·카드 공통 규격과 긴 금액 전체 표시
- [x] 기본 소비 분석의 구형 내부 기능 메뉴 제거
- [x] 홈 중복 하단 메뉴 제거와 모바일 5탭 단일 유지
- [x] 정산 계산·완료 저장 폼 경로와 필드 보존
- [x] 390px·768px·900px·1024px·1440px 기준 반응형 계약과 가로 넘침 방지
- [x] 분석 종합 리포트·설정·참여자·백업·단톡방·시작가이드 구형 내부 메뉴 제거
- [x] 리포트·스마트 도구·자산·분류·가계부 전환·계정 보안 공통 헤더와 카드 표면
- [x] 390px·768px에서 모바일 상단 바·드로어·5탭, 900px·1024px·1440px에서 238px 사이드바
- [x] 모든 전환 화면에서 가계부·월 쿼리와 기존 form action 보존
- [x] `/my/households`가 참여자 대신 가계부 전환·추가를 현재 메뉴로 표시
- [x] 통계·분석, 백업·가져오기, 거래·캘린더의 쿼리·해시별 현재 메뉴 분리
- [x] 899px 이하 전체 메뉴 드로어 표시·Tab 순환·Escape 닫기·초점 복원
- [x] 900px 이상 데스크톱 사이드바 전환과 기존 238px·접힘 72px 유지
- [x] `/app` 홈의 서버 `app`·클라이언트 `home` 키 매핑과 현재 메뉴 1개 유지
- [x] 거래 검색이 기존 `q`·`tab=transactions`·`#feed` 계약과 현재 가계부·월 유지
- [x] 빠른 입력이 기존 `/app#add`, 알림이 실제 `/budget-alerts` 경로 사용
- [x] 화면 설정이 기존 시스템·라이트·다크와 네 컬러톤 저장 계약 사용
- [x] 모바일 단일 작업 버튼과 대화상자, Escape·초점 복원·배경 스크롤 잠금
- [x] 전역 작업을 외부 자산에서 생성해 홈 HTML 기준 픽스처 35KB 기준 유지
- [x] 전 기간 검색·알림·즐겨찾기의 가계부 범위 엄격 검증
- [x] 조회 전용 참여자의 목표 읽기 전용과 서버 변경 차단
- [x] 목표 동시 저장 잠금, 0원 목표·납입 차단과 이탈 저장
- [x] 검색·알림 오버레이 Tab 순환·Escape·초점 복원

## 카카오 수정·웹 피드백 자동 검증

- [x] 날짜별 기록 번호 조회·수정·삭제
- [x] 삭제 복구와 `household_id` 보존
- [x] 복구 버퍼 저장 실패 시 원본 거래 삭제 중단
- [x] 복구 INSERT 실패 중 버퍼 유지와 다음 재시도 성공
- [x] 사용자·대화방별 세션 분리
- [x] 5분 만료·실패 횟수·최대 턴·반복 응답 가드
- [x] 웹 제출 위치 인라인 결과와 모호한 폼의 안전한 기존 위치 유지
- [x] JSON 오류 `error`·`message`·`reason` 계약

## AdSense 심사 자동 검증

- [x] 홈과 정식 공개 콘텐츠 14개 경로의 정확한 소유권 메타 1회
- [x] 공개 별칭 12개 경로의 정확한 소유권 메타 1회
- [x] `/ads.txt` 판매자 한 줄·MIME·공개 캐시·BOM 없음
- [x] 승인 전 광고 런타임·슬롯·광고 호스트·광고 쿠키 코드 없음
- [x] 공개 심사 페이지가 쿠키를 만들지 않음
- [x] 개인 가계부·거래·분석·영수증·설정·백업 화면 광고 제외
- [x] 실제 관리자 인증 화면과 운영 화면 광고 제외

## UI·UX 4단계 화면·테마 자동 검증

- [x] 실데이터 월 지출 강조 카드
- [x] 기록 없는 달력 셀 투명 처리
- [x] 오늘·주말·기록일·선택일 구분과 현재 날짜 접근성
- [x] 선택 셀 금액과 선택 날짜 안내의 라이트·다크 대비
- [x] 분석 필터 표면 fallback과 활성 필터·적용 버튼 대비
- [x] 분석 KPI 증가·감소 의미색과 인사이트 상태색
- [x] 모바일 메뉴 다크 표면과 900px V5 반응형 경계
- [x] 모바일 주요 터치 대상 44px와 360px 달력 폭 보정
- [x] 시스템·라이트·다크와 네 컬러톤 인프라 유지
- [x] 기록·리포트·함께·관리 메뉴 구조와 모바일 5탭
- [x] 238px 사이드바와 접힘·모바일 전체 메뉴 접근성 상태
- [x] 카드·필터·표·입력의 통합 토큰과 다크 의미색
- [x] 라이트 페이지·카드·raised 표면의 공통 보조 텍스트 4.5:1 이상
- [x] 라이트·다크와 네 컬러톤의 accent-soft 조합 4.5:1 이상
- [x] 모바일 전체 메뉴 버튼 44×44px 이상
- [x] 고대비 선호·강제 색상·동작 줄이기 환경 대응

## 전체 자동 검증

- [x] 영수증 56개
- [x] 카카오 그룹 22개
- [x] 가계부·운영 보안 75개
- [x] 참여자 역할 스키마 20개
- [x] UX·분석 보호 56개
- [x] 사용자 화면·홈 버튼·테마·성능 144개
- [x] AdSense 심사·V2·UI·UX·대비 261개
- [x] 카카오 수정·삭제·복구 V4 130개
- [x] V5 권한·범위·저장 안정화 41개
- [x] 핵심 쓰기·권한 스모크 97개
- [x] 총 902개 자동 검사와 ESM `default.fetch`
- [x] V22.8.46 체크섬 목록 검증
- [x] 파라미터 RPC의 빈 JSON PGRST202 오판 방지 회귀검사
- [x] 잘못된 명시 가계부 ID의 정기지출·자산·카드혜택 fallback 차단
- [x] 거래 생성·동시 중복·수정·삭제 실제 fixture DB 상태 확인
- [x] 예산 삭제 성공과 DB·settings 양쪽 실패의 false-success·부분 삭제 차단
- [x] viewer 예산·정산·영수증·자산·참여자 쓰기 무변경 확인
- [x] v2271 자산 RPC 환경의 동일 이름 동시 생성 1건 보장
- [x] 자산 본체 저장 후 스냅샷 실패의 partial-success 안내
- [x] 개별 예산 fallback 정리와 전체 예산 교체 후 삭제 분류 비재등장
- [x] 정산 이력 읽기·쓰기 실패 시 기존 JSON 보존과 실패 후 재시도
- [x] 정산·목표 실제 동시 요청의 가계부 단위 직렬화와 busy 응답
- [x] 목표 조회·저장 실패의 503·reason·화면 피드백과 기존 목록 보존
- [x] 정산·목표 손상 JSON의 fail-closed와 원본 보존
- [x] 변경된 목표 런타임의 `v22843` immutable 캐시 경로와 ETag
- [x] 목표 가계부 공유 키와 즐겨찾기 사용자별 키 범위 분리

## 운영 배포 전

- [ ] 현재 Worker 코드 백업
- [ ] Supabase 전체 백업 또는 복구 가능한 시점 백업 확인
- [ ] 현재 환경변수 이름 목록 백업
- [ ] 외부 HTTP cron 호출이 있으면 GET/query secret 사용 여부 확인
- [ ] 배포 후 코드 고정 게시자 `ca-pub-8422696710972974`가 실제 응답에 적용되는지 확인
- [ ] 저장소 전체 검증 결과와 배포할 `src/index.js` 해시 기록
- [ ] 배포 승인 확인

## 운영 적용

- [x] 운영 `/ready`의 현재 `missing_rpcs` 기록: 없음, 필수 RPC 17개 확인
- [ ] RPC 누락 시 `schema_v22_6_8_operations_integrity.sql` 적용; 중복 검사 실패 시 즉시 중단
- [ ] V22.6.8 성공 뒤 `schema_v22_7_0_auth_atomicity.sql` 적용
- [ ] 자산 원자성 RPC 두 버전이 모두 없으면 `schema_v22_7_1_asset_dashboard.sql` 적용
- [x] 검증된 V22.8.44 `src/index.js` 전체 교체
- [ ] 환경변수·Secret을 변경하지 않음
- [ ] Kakao Developers를 변경하지 않음
- [ ] OpenBuilder를 변경하지 않음
- [ ] 외부 HTTP cron이 있으면 POST + `x-cron-secret` 또는 Bearer로 전환

## 운영·실기기 확인

- [x] `/health` HTTP 200·`alive: true`·V22.8.44 버전 확인
- [x] `/ready` HTTP 200·`ready: true`·빈 `failed_tables`·빈 `missing_rpcs` 확인
- [ ] 홈과 `/privacy` 소유권 메타 정확히 1회
- [x] `/ads.txt` 정확한 판매자 한 줄·MIME
- [ ] `/app`, `/my/analysis`, `/receipts`, 관리자 화면에 광고 코드 없음
- [x] V22.8.44 CSS의 200·MIME·immutable·ETag
- [x] V22.8.43 목표 런타임의 200·MIME·immutable 확인
- [ ] 홈에서 사이드바 `홈`이 현재 메뉴로 유지되는지 확인
- [ ] 거래 검색·빠른 입력·예산 알림이 현재 가계부와 기준 월을 유지하는지 확인
- [ ] 모바일 작업 대화상자의 열기·Escape·바깥 닫기·초점 복원 확인
- [ ] 화면 설정에서 시스템·라이트·다크와 네 컬러톤 전환 확인
- [ ] 전 기간 검색 결과의 현재 가계부 유지와 즐겨찾기 추가·해제 확인
- [ ] 알림 센터 링크와 조회 전용 계정의 목표 읽기 전용 확인
- [ ] PC Chrome 홈 달력·분석·라이트·다크·네 컬러톤
- [ ] iPhone Safari 홈·입력·달력·하단 메뉴
- [ ] Android Chrome 홈·입력·분석·하단 메뉴
- [ ] 768~899px 모바일 드로어, 900~1023px 데스크톱 사이드바 전환
- [ ] 기록 저장, 가계부 전환, 정산, 예산, 계정 보안
- [ ] 영수증 OCR·취소·저장
- [ ] 카카오 1:1·그룹 응답
- [ ] 카카오 기록 조회·수정·삭제·복구와 반복 응답 없음
- [ ] 웹 폼 성공·실패 결과가 제출 위치 근처에 표시

## 실패 시

- [ ] 코드 문제는 배포 직전 백업한 V22.8.43 `src/index.js`로 전체 코드 롤백
- [ ] 복구한 기존 DB 객체는 임의 삭제하지 않고 SQL transaction 결과·Supabase 백업을 기준으로 대응
- [ ] `/ready`가 503이면 일반 화면 정상처럼 보여도 배포 완료로 판정하지 않음
- [ ] 캐시 문제면 V22.8.44 CSS·V5 번들과 V22.8.36 내비게이션 URL, 응답 헤더와 오래 열린 탭을 확인

운영 자동 확인: 2026-07-28 PASS, 필수 RPC 17개. 미완료 항목은 백업 여부, 외부 설정, 로그인 사용자 동작과 실제 기기가 필요한 수동 작업이며 자동 통과로 간주하지 않습니다.
