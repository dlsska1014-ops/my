# V22.8.78 검증 보고서 — 작성 내용 보존과 그룹 동시 입력

버전: `V22.8.78-DRAFT-PERSISTENCE`
측정 환경: Node.js 22.22.2 · QA 픽스처 · Chromium 1194 (iPhone 13 뷰포트)

---

## 1. 계측을 먼저 고쳤습니다

새로고침 경고가 있는지 재는 첫 스크립트가 **"경고 있음"** 을 냈습니다.
그런데 소스에서 `beforeunload` 를 세어 보니 **0건**이었습니다.

원인은 판정식이었습니다.

```js
return e.defaultPrevented || !!e.returnValue;   // ← returnValue 는 기본이 true
```

`Event.returnValue` 는 레거시 속성이라 기본값이 `true` 입니다. 그것을 OR 로
묶으면 리스너가 없어도 언제나 참입니다. `defaultPrevented` 만 보도록 고치고,
**계측 자체를 먼저 점검**했습니다.

| 계측 점검 | 결과 |
|---|---|
| 리스너 없는 상태 | `false` (정상) |
| 일부러 리스너를 달았을 때 | `true` (정상) |

그 뒤 다시 재니 **경고가 없다**는 것이 확인됐습니다.

## 2. 결함 — 적다가 새로고침하면 말없이 사라집니다

V22.8.76 에서 저장 실패 시 적은 내용을 살렸지만, 그 초안은 **제출할 때만**
남겼습니다. 새로고침·화면 이탈은 경고 없이 일어나므로 그대로 사라졌습니다.

| 상황 | 이전 | 이후 |
|---|---|---|
| 적다가 새로고침 | 전부 사라짐 | 그대로 남음 |
| 적다가 다른 화면 갔다 돌아옴 | 전부 사라짐 | 그대로 남음 |
| 저장 실패로 돌아옴 | 남음 (V22.8.76) | 그대로 |
| 저장 성공 | 버림 | 그대로 |

### 브라우저 경고창을 넣지 않은 이유

`beforeunload` 로 "이 사이트에서 나가시겠습니까?" 를 띄울 수 있지만 성가시고,
모바일 사파리에서는 잘 뜨지도 않습니다. **조용히 남겨 두었다가 돌아오면
채워 주는 쪽**을 골랐습니다. 이미 V22.8.76·77 에서 같은 방식으로 실패·만료를
다뤘으므로 흐름도 일관됩니다.

### 안전장치

| 장치 | 이유 |
|---|---|
| 금액이나 메모가 있을 때만 초안 인정 | 날짜·유형은 기본값이라 "적던 중" 신호가 못 됨 |
| 30분 시한 | 어제 쓰다 만 것이 오늘 끼어들지 않게 |
| 이미 적힌 칸은 덮지 않음 | 방금 고친 값이 날아가지 않게 |
| 성공하면 즉시 폐기 | 저장된 내용이 다시 떠오르지 않게 |
| 0.4초 디바운스 | 글자마다 저장하지 않게 |
| 브라우저 안에만 보관 | 메모가 주소창·방문 기록에 남지 않게 |

폐기 규칙도 바로잡았습니다. V22.8.76 은 `실패가 아니면 버림` 이었는데, 그러면
적다가 새로고침한 경우까지 버려집니다. `성공했을 때만 버림` 으로 바꿨습니다.

### 브라우저에서 확인한 것

| 경우 | 결과 |
|---|---|
| 적다가 새로고침 | 금액 `48,000` · 메모 "한참 적은 메모" 복원, 시트 열림 |
| 다른 화면 갔다 돌아옴 | 금액 `21,500` · 메모 "이탈시험" 복원 |
| 저장 실패로 돌아옴 | 금액 `33,000` · 메모 "실패시험" 복원 |
| 저장 성공 | 초안 없음 |
| 분류만 고르고 떠남 | 초안 없음 |
| 1시간 지난 초안 | 되살아나지 않음 |
| JS 오류 | 0건 |

> 새로고침 경우는 처음에 실패로 나왔는데, 제 시험이 새로고침 뒤 **한 번 더**
> 이동해 초안이 이미 소비된 상태를 재고 있었습니다. 새로고침만으로 다시 재어
> 확인했습니다.

## 3. 결함이 아니었던 것 — 카카오 그룹방 동시 입력

중복 방지 열쇠에 사람·대화방·내용이 모두 들어갑니다.

```js
const key = `${userKey}|${hash(conversationScope)}|${hash(text)}`;
```

대화방만으로 잠갔다면 한 사람이 적는 동안 다른 사람 기록이 "방금 같은 내용"
으로 막혔을 것입니다. 실제로 재어 확인했습니다.

| 상황 | 결과 |
|---|---|
| 두 사람이 같은 말을 동시에 | **2건** 저장 (둘 다 안내 받음) |
| 같은 사람이 같은 말을 동시에 (재전송) | **1건** |
| 두 사람이 다른 말을 동시에 | **2건** |

> 첫 측정에서는 두 번째 사람이 "가계부를 찾지 못했어요" 를 받아 1건만 남았습니다.
> 사람마다 가계부 개수가 달라 제가 보낸 번호가 그 사람 목록에 없었던 것입니다.
> 안내에서 번호를 읽어 고르도록 고친 뒤 정확한 그림이 나왔습니다. 검사에도
> 같은 방식으로 넣었습니다.

## 4. 11단계 점검 완료

| 항목 | 결과 |
|---|---|
| 네트워크 끊김 | V22.8.76 — 데이터 안전(0건), 초안 보존 |
| 중복 제출 | V22.8.76 — 카카오 재전송 수정 (웹은 이미 안전) |
| 뒤로 가기 | V22.8.77 — 결함 아님 (모든 쓰기가 303) |
| 세션 만료 | V22.8.77 — 로그인 복귀 흐름 수정 |
| 새로고침 | **V22.8.78 — 이번 수정** |
| 카카오 재시도 | V22.8.76 |
| 부분 실패 | V22.8.76 — 결함 아님 (단일 RPC) |
| 그룹방 동시 입력 | **결함 아님 (이번 확인)** |

7개 항목 중 **결함 4건을 고치고 3건은 이미 안전함을 확인**했습니다.

## 5. 회귀 검사

`validation/validate-draft-persistence-v22878.mjs` (**19개 확인**)

### 되돌려서 잡히는지 확인

| 되돌린 결함 | 검사 결과 |
|---|---|
| 적는 동안 저장 제거 | FAIL: 글자마다 저장하지 않고 잠시 멈췄을 때 남긴다 |
| 성공이 아니어도 초안을 버림 | FAIL: 저장에 성공했을 때만 초안을 버린다 |
| 시한 없이 오래된 초안도 되살림 | FAIL: 시한이 지난 초안은 버린다 |
| 중복 방지가 사람을 구분하지 않음 | FAIL: 중복 방지가 사람·대화방·내용을 모두 구분한다 |

네 경우 모두 검사가 잡았고, 원복하면 다시 통과합니다.

### 계약 변경에 맞춘 기존 검사 수정

`validate-kakao-intent-skill-auth-v22876.mjs` 가 예전 폐기 규칙
(`실패가 아니면 버림`)을 고정하고 있어 새 규칙(`성공했을 때만 버림`)으로
갱신했습니다.

## 6. 전체 결과

- 저장소 자동검사 **2,874개 전부 통과**
- `node --check src/index.js` 통과, ESM 진입점 통과
- 신규 SQL 없음 · 환경변수 변경 없음 · 자산 주소 변경 없음

## 7. 배포 후 사람이 확인할 것

1. 빠른 기록에 금액과 내용을 적고 **새로고침** → 적었던 값이 그대로인지
2. 적다가 다른 화면에 갔다 돌아와도 남아 있는지
3. 저장에 성공한 뒤 다시 열면 **비어 있는지** (지난 내용이 떠오르지 않아야 함)
4. 카카오 그룹방에서 두 사람이 동시에 기록해도 둘 다 남는지

운영 도메인은 이 환경에서 접근할 수 없어 직접 확인하지 못했습니다.
