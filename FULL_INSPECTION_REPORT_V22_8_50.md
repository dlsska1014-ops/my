# V22.8.50 전체 기능 점검 보고서

점검 기준: `VERSION.txt` = `V22.8.50-UIUX-STAGE4-QUICK-DOCK-SAVE-FEEDBACK`
(요청의 "v22.8.5"는 저장소 현재 버전인 V22.8.50 기준으로 해석했습니다.)

점검 대상: `src/index.js` (28,223줄 / 라우트 319개), `validation/`, 배포 정적 자산
점검 방식: 저장소 검증 하네스 전체 실행 + `validation/qa-fixture.mjs` 기반 인메모리
백엔드로 실제 `app.fetch()`를 구동한 독립 프로브 (라우트 스모크, 권한 격리,
입력 경계값, 렌더링 자산 검사)

---

## 0. 수정 상태 (V22.8.51에서 반영)

이 보고서가 기록한 결함 6건은 모두 `V22.8.51-INPUT-AMOUNT-RUNTIME-REGEX-FIX`에서
수정했습니다. 수정 내역과 검증 결과는 `VALIDATION_REPORT_V22_8_51.md`,
배포 주의사항은 `00_READ_FIRST_V22_8_51.md`를 확인하세요.

| 항목 | 상태 |
| --- | --- |
| P1-1 1,000원 미만 금액 저장 불가 | 수정됨 (폼 전용 숫자 파서 분리) |
| P1-2 한 줄 입력 파서 무력화 | 수정됨 (이스케이프 복구 + 회귀 검사 9개) |
| P2-1 `validMonth` 월 범위 미검증 | 수정됨 (`01~12`만 허용) |
| P2-2 거래 금액 상한 부재 | 수정됨 (20억 원 상한) |
| P3-1 예산 저장 0원 덮어쓰기 | 수정됨 (무효 입력 거부, 명시적 0은 유지) |
| P3-2 고정지출 `user_id` 계약 불일치 | 수정됨 (지출자 선택 필드 추가) |

아래 본문은 수정 전 V22.8.50 기준의 점검 기록입니다.

---

## 1. 요약

| 구분 | 결과 |
| --- | --- |
| 저장소 자동 검증 | 1,120개 + 체크섬 51개 **전부 통과** |
| GET 라우트 스모크 (318개) | 처리되지 않은 예외 **0건** |
| 권한·데이터 격리 (45개 시나리오) | 실패 **0건** |
| 신규 발견 결함 | **6건** (P1 2건, P2 2건, P3 2건) |

기존 자동 검증은 모두 통과하지만, 검증이 다루지 않는 영역에서 **사용자에게 직접
보이는 P1 결함 2건**을 확인했습니다. 둘 다 웹 화면의 핵심 입력 경로에 있습니다.

---

## 2. P1 — 사용자 영향이 큰 결함

### P1-1. 웹 거래 입력·수정에서 1,000원 미만 금액을 저장할 수 없음

**증상**

`/app`의 빠른 입력 폼과 거래 목록의 인라인 수정 폼에서 금액에 1~3자리 숫자를
넣으면 저장이 거부되고 `금액을 입력하세요.` 오류로 되돌아옵니다. 금액을 분명히
입력했는데도 "입력하세요"라고 안내하므로 사용자는 원인을 알 수 없습니다.

재현 (인메모리 백엔드, owner 세션):

| 입력 금액 | `/admin/transactions` 결과 | `/admin/update` 결과 |
| --- | --- | --- |
| `500` | 거부 | 거부 (기존 값 유지) |
| `999` | 거부 | 거부 (기존 값 유지) |
| `50` | 거부 | 거부 |
| `1000` | 1000 저장 | 1500 저장 |
| `500원` | 500 저장 | (`type=number`라 입력 불가) |

**원인**

`/app`의 두 폼은 모두 `/admin/transactions`·`/admin/update`로 POST하며, 이 경로는
`readTransactionAmount()` → `parseAmountValue()` → `extractAmount()`를 거칩니다.
`extractAmount()`는 카카오 자연어 문장에서 금액을 뽑아내기 위한 함수이고,
숫자 후보 정규식이 **4자리 이상 또는 콤마 구분 형식만** 인정합니다.

`src/index.js:27225` (`collectStandardNumberCandidates`)

```js
const re = /([0-9]+(?:,[0-9]{3})+|[0-9]{4,})(?:\s*원)?/g;
```

자연어 문장에서 "점심 3인분" 같은 표현을 금액으로 오인하지 않으려는 의도이지만,
**전용 금액 입력 필드에까지 같은 규칙이 적용**되어 정상 입력이 거부됩니다.

우회 수단도 막혀 있습니다. `500원`처럼 단위를 붙이면 통과하지만, 배포 자산
`/assets/mobile-home-shell-v22811.js`의 입력 핸들러가 매 입력마다 숫자 외 문자를
제거하므로 사용자는 `원`을 남길 수 없습니다.

```js
amount.addEventListener('input', function(){ var raw = this.value.replace(/[^0-9]/g,''); ... });
```

**참고: 올바른 구현이 이미 저장소에 있음**

`/my/transactions`(`handleMyAddTransaction`)는 전용 파서를 쓰고 정상 동작합니다.

`src/index.js:16796`

```js
function parseMyAmount(form) {
  const amount = Math.round(Number(String(form.get("amount") || "0").replace(/,/g, "")));
  return Number.isFinite(amount) ? amount : 0;
}
```

`500` → 500 정상 저장. 다만 현재 이 엔드포인트로 POST하는 화면이 없어,
동작하는 파서는 쓰이지 않고 결함이 있는 파서만 사용자에게 노출됩니다.

**영향** — 커피·간식·버스요금 등 1,000원 미만 지출을 웹에서 기록할 수 없고,
기존 거래를 1,000원 미만으로 수정할 수도 없습니다.

**권고** — 폼 금액 필드는 자연어 추출기가 아니라 `parseMyAmount`와 같은 숫자 파서를
사용합니다. `extractAmount()`는 카카오 발화 해석 전용으로 유지합니다.

---

### P1-2. `/app` 스마트 한 줄 입력 파서가 배포 자산에서 전부 무력화됨

**증상**

`/app`의 "한 줄 입력 … 채우기" 기능(`챗봇처럼 한 줄로 쓰면 금액·내용·결제수단이
자동으로 채워집니다`)이 어떤 문장에도 금액을 채우지 못합니다.

실제 배포 자산에서 함수를 추출해 실행한 결과:

| 입력 | 배포된 코드 | 소스가 의도한 값 |
| --- | --- | --- |
| `커피 5천` | **0** | 5000 |
| `월급 250만원` | **0** | 2500000 |
| `3만 5천` | **0** | 35000 |
| `점심 12000` | **0** | 12000 |
| `택시 1만2천` | **0** | 12000 |

**원인**

이 클라이언트 런타임은 `renderMobileV81Html()`의 **템플릿 리터럴 안에 문자열로**
작성되어 있습니다. 템플릿 리터럴은 문자열 리터럴과 동일하게 이스케이프를
처리하므로, 정규식에 쓴 `\d`·`\s`·`\B`·`\[`의 백슬래시가 평가 시점에 사라집니다.

소스(`src/index.js:20411`)에는 이렇게 적혀 있지만

```js
var m = text.match(/(\d+(?:[.,]\d+)?)\s*만\s*(\d+(?:[.,]\d+)?)\s*천/);
```

실제 배포되는 `/assets/mobile-home-shell-v22811.js`에는 이렇게 나갑니다.

```js
var m = text.match(/(d+(?:[.,]d+)?)s*만s*(d+(?:[.,]d+)?)s*천/);
```

즉 `\d`(숫자)가 **문자 `d`** 로, `\s`(공백)가 **문자 `s`** 로 바뀌어 어떤 실제
입력과도 일치하지 않습니다. 문법 오류가 아니어서 조용히 잘못 동작합니다.

같은 원인으로 함께 깨진 것들:

- `abNorm()` — `replace(/s+/g,' ')`가 되어 **영문 `s`를 공백으로 치환**합니다.
  `"starbucks coffee"` → `"tarbuck  coffee"`. 메모의 영문이 손상됩니다.
- `abNorm()`의 문자 클래스 — `\[`·`\]`가 소실되어 `[~!@#$%^&*_=+\`|\{}[]:;"'<>?]`가
  되며 의도와 다른 범위를 치환합니다.
- 천단위 콤마 표시 — `/\B(?=(\d{3})+(?!\d))/g`가 `/B(?=(d{3})+(?!d))/g`가 되어
  **금액 입력 시 콤마가 전혀 표시되지 않습니다.**
- `parseQuickDate()`, `detectQuickType()`, `stripQuickMemo()`의 날짜·유형·메모 정리
  정규식 전부 동일하게 손상.

영향 범위는 이 인라인 런타임에서 파생된 두 자산에 한정됩니다.

| 자산 | 이스케이프 소실 | 정상 이스케이프 |
| --- | --- | --- |
| `/assets/mobile-home-v22810.js` | 24 | 7 |
| `/assets/mobile-home-shell-v22811.js` | 24 | 7 |
| `/assets/accountbook-v5-v22850.js` | 0 | 정상 |
| `/receipts` 인라인 스크립트 | 0 | 정상 |

정상인 쪽은 클라이언트 코드를 **실제 함수로 작성한 뒤 `.toString()`으로 직렬화**
(`mobileHomeNavStateClientMain`, `accountbookStage4NavClientMain` 등)하므로
이스케이프가 보존됩니다. 손상된 쪽만 문자열 템플릿 방식입니다.

**권고** — 해당 인라인 런타임도 `.toString()` 직렬화 방식으로 옮깁니다. 즉시 조치가
필요하면 템플릿 리터럴 안에서 `\\d`, `\\s`, `\\B`처럼 백슬래시를 이중으로 씁니다.
회귀 방지를 위해 "배포 자산에 `\d`가 최소 N개 존재" 같은 검사를 검증에 추가하는
것을 권합니다. 현재 1,120개 검증은 자산의 **문자열 포함 여부**만 보고 정규식이
살아 있는지는 확인하지 않습니다.

---

## 3. P2 — 오류 응답·데이터 정합성

### P2-1. `validMonth()`가 월 범위를 검사하지 않아 500 발생

`src/index.js:27496`

```js
function validMonth(value) {
  return /^20\d{2}-\d{2}$/.test(String(value || "")) ? String(value) : null;
}
```

`2026-13`, `2026-00`, `2026-99`가 통과합니다. 이 값은 그대로 PostgREST 질의의 날짜
리터럴이 되어 `transaction_date=gte.2026-13-01`처럼 전송됩니다. Postgres는 이를
`22008 date/time field value out of range`로 거절합니다.

Postgres의 날짜 검증을 재현한 백엔드로 확인한 결과:

| 경로 | `month=2026-07` | `month=2026-13` | `month=2026-00` |
| --- | --- | --- | --- |
| `/app` | 200 | **500** | **500** |
| `/budgets` | 200 | **500** | **500** |
| `/reports` | 200 | **500** | **500** |

월 파라미터를 쓰는 라우트가 100개 이상이므로 동일하게 영향받습니다. 추가로 같은
요청 안에서도 경로에 따라 해석이 갈립니다 — 거래 목록은 `2026-13-01`을 그대로
보내는 반면 `fetchMonthAmountRows`는 `nextMonthStart()`의 `new Date(y, m, 1)`
정규화 때문에 `2026-12-01 ~ 2027-01-01`을 조회합니다.

**권고** — `validMonth()`에서 월을 `01`~`12`로 제한합니다.

```js
return /^20\d{2}-(0[1-9]|1[0-2])$/.test(String(value || "")) ? String(value) : null;
```

### P2-2. 거래 금액에 상한이 없어 정밀도 손실과 표시 붕괴 발생

`validateRecordFormFields()`는 `amount > 0`만 확인하고 상한이 없습니다.

| 입력 | 저장 값 |
| --- | --- |
| `9999999999999999` | `10000000000000000` (조용히 값이 바뀜, 2^53 초과) |
| `999999999999999999999` | `1e+21` |

`1e+21`이 한 건 저장되면 월 지출 합계가
`1,000,000,000,000,001,400,000원`으로 표시되어 화면이 무너집니다. JSON 본문에
`1e+21`이 그대로 실려 나가므로 컬럼 타입에 따라 DB 오류도 발생할 수 있습니다.

저장소 안에 이미 상한을 두는 선례가 있습니다 — `src/index.js:16546`은
`Math.min(2_000_000_000, ...)`을 적용합니다. 거래 생성·수정 경로에는 없습니다.

**권고** — 거래 금액에도 동일한 상한과 `Number.isSafeInteger` 검사를 적용합니다.

---

## 4. P3 — 일관성·유지보수

### P3-1. `/admin/budget/save`가 빈 금액을 0으로 저장하고 성공으로 안내

```js
const amount = Math.max(0, Math.round(parseAmountValue(form.get("amount") || "0")));
```

금액이 비었거나 해석 불가여도 `amount = 0`으로 upsert한 뒤 `msg=budget_saved`를
반환합니다. 기존 예산 800,000원이 0원으로 바뀌면서 화면에는 "저장됨"으로 보입니다.
삭제 전용 경로(`/admin/budget/delete`)가 따로 있으므로 0 저장은 삭제 의도로도
볼 수 없습니다.

다만 **현재 이 라우트로 POST하는 사용자 화면은 없습니다.** 실제 UI는
`/my/budget-bulk/save`(월 단위 일괄 교체)를 쓰며, 여기서는 빈 값이 행 제외로
처리되어 폼 의미상 타당합니다. 따라서 영향은 레거시 관리자 콘솔과 직접 POST에
한정되며, 우선순위는 낮지만 일관성 문제로 남습니다.

**권고** — `handleMyRecurringSave`와 동일하게 `if (!amount) return ... err` 가드를
추가합니다.

### P3-2. `/admin/recurring/save`는 `user_id`가 필수인데 이를 보내는 화면이 없음

`src/index.js:21019`

```js
if (!spenderId || !memberExists(members, spenderId))
  return redirectResponse(addQueryToUrl(returnTo, { err: "고정항목의 지출자를 가계부 참여자 중에서 선택하세요." }));
```

`renderMobileV81Html`의 고정지출 폼에는 `user_id` 필드가 없어 이 경로로 들어오는
모든 요청이 실패합니다. 다만 V5 후처리에서 해당 섹션 자체가 제거되므로
`/app`에는 렌더링되지 않습니다.

```js
source = source.replace(/<section id="fixed" class="panel">[\s\S]*?<\/section>/, "");
```

실제 고정지출 UI는 `/my/settings` → `/my/recurring/save`이며 **정상 동작을
확인했습니다**(저장 1건 성공). 따라서 사용자 영향은 없고, 도달 불가 코드와
불일치한 계약이 남아 있는 상태입니다.

### P3-3. HTML 문자열 후처리 파이프라인의 구조적 위험

V5 화면은 레거시 HTML을 렌더링한 뒤 문자열 치환으로 재구성합니다(`src/index.js:4100`
부근). 섹션 삭제에 `[\s\S]*?</section>` 비탐욕 정규식을 쓰므로 해당 섹션에
`<section>`이 중첩되는 순간 조용히 잘못된 범위가 잘려 나갑니다. 현재는 중첩이
없어 동작하지만, P1-2 역시 같은 "HTML을 문자열로 다루는" 구조에서 나왔습니다.

---

## 5. 정상 동작을 확인한 영역

**권한·데이터 격리 — 45개 시나리오 전부 통과**

- 비참여자가 `household_id`를 지정해 타 가계부 조회 시도 (18개 화면) → 전부 차단
- `pending`·`blocked` 역할의 조회 시도 → 전부 차단
- `viewer`·`pending`·`blocked`·비참여자의 거래 생성 → 전부 차단, `member`는 정상 저장
- 비관리자의 참여자 권한 변경·가계부 삭제·고정지출 저장·예산 저장 → 전부 차단
- 타 가계부 거래의 수정·삭제 → 전부 차단
- `/u/api/*` JSON 5종의 교차 가계부 조회 → 데이터 유출 없음

**입력 안전성**

- XSS — 메모·분류·결제수단·가계부 이름에 `<img onerror>`, `<script>` 주입 후
  `/app`·`/analysis`·`/my`·`/calendar` 재조회 → 전부 이스케이프
- PostgREST 파라미터 주입 — `household_id`에 `*`, `eq.`, `&select=*`,
  `'; DROP TABLE--` 주입 → 범위 이탈 없음
- 날짜 검증 — `2026-07-32`, `2026-02-30`, `2026-13-01`, `1900-01-01`, `2999-12-31`
  전부 거부, 빈 값은 KST 오늘로 대체

**쓰기 안정성**

- 동시 이중 제출 2건 → 1건만 저장 (operation lease 정상)
- 순차 반복 제출 → 중복 차단

**인증·세션**

- 관리자 세션 HMAC-SHA256 + 만료 + `session_version` 무효화
- 비밀번호 PBKDF2-SHA256 210,000회, 비교는 상수 시간
- CSRF: Origin/Referer/Fetch Metadata 다층 검사, `/skill`·`/cron/*`만 예외
- 쿠키 `HttpOnly; Secure; SameSite=Lax`, 개인정보 HTML `no-store`
- CSP·HSTS·`X-Frame-Options: DENY` 등 보안 헤더 적용

**라우팅**

- GET 라우트 318개 스모크 → 처리되지 않은 예외 0건
- 503 2건은 픽스처 환경 한계(관리자 시크릿·카카오 로그인 미설정)로 정상
- 404 85건은 기능 플래그로 의도적으로 숨긴 라우트 및 POST 전용 라우트

---

## 6. 이번 점검으로 확인하지 못한 항목

- **고정지출 월 반영** — `accountbook_apply_recurring_v227` RPC가 픽스처에
  구현되어 있지 않아 `/admin/recurring/apply` 경로를 실행하지 못했습니다.
  결함이 아니라 검증 공백입니다.
- **카카오 스킬 심층 시나리오** — 그룹 연결·수정 세션 등은 저장소 검증
  152개(그룹 22 + 수정·삭제·복구 130)에 의존했고 별도 프로브는 하지 않았습니다.
- **실기기·운영 도메인** — iOS Safari·Android Chrome의 가상 키보드, safe-area,
  보조 기술 사용성은 `KNOWN-ISSUES.md` 및 `RELEASE-CHECKLIST.md`의 수동 항목입니다.
- **운영 DB 상태** — `/ready`의 실제 RPC 존재 여부는 운영 환경에서 확인해야 합니다.

---

## 7. 권고 조치 순서

1. **P1-1** 거래 생성·수정의 금액 파서를 숫자 전용 파서로 교체
2. **P1-2** 손상된 인라인 런타임을 `.toString()` 직렬화로 이전 + 자산 정규식 회귀 검사 추가
3. **P2-1** `validMonth()` 월 범위 제한
4. **P2-2** 거래 금액 상한 및 안전 정수 검사
5. **P3-1** 예산 저장 금액 가드
6. **P3-2** 도달 불가한 고정지출 경로 정리

1·2번은 사용자가 매일 쓰는 입력 경로의 결함이므로 우선 처리를 권합니다.
모두 SQL·환경변수·Kakao Developers·OpenBuilder 변경 없이 `src/index.js`
수정만으로 해결 가능합니다.
