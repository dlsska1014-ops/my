# V22.8.66 검증 보고서

검증일: 2026-08-02

## 점검 범위

순차 점검 2단계로 **주요 기능 흐름을 브라우저에서 실제로 클릭**해 확인했습니다.
지금까지는 화면이 렌더되는지만 봤기 때문에, 프론트 JS가 중간에서 막는 종류의
결함은 잡히지 않았습니다.

브라우저 출처를 운영 도메인으로 맞춰 프록시했습니다. QA 서버가 요청 호스트를
운영 도메인으로 바꿔 쓰는 탓에, 그대로 두면 `fetch` 요청이 CSRF 검사에 걸려
403이 납니다. 이는 하네스 아티팩트이며 제품 결함이 아닙니다.

## 확인한 흐름 (12/12 통과)

| 흐름 | 결과 |
|---|---|
| 한 줄 입력 `점심 12000 국민카드` 파싱 | 12,000 / 점심 / 국민카드 / 식비 |
| 한글 금액 `커피 5천 현금` 파싱 | 5,000 / 현금 / 카페·간식 |
| 빠른 입력 저장 → 최근 내역 반영 | 통과 |
| 저장 직후 성공 피드백 | 통과 |
| 최근 내역에서 수정 → 내용·금액 반영 | 통과 |
| **삭제 → 목록에서 제거** | **보정 후 통과** |
| 빠른 입력 시트에서 저장 | 통과 |
| 예산 저장 → 값 유지 | 통과 |
| 정기지출 추가 → 목록 반영 | 통과 |
| 캘린더 날짜 클릭 → 일별 상세 | 통과 |
| 챌린지 목표 인라인 저장 (주소 유지) | 통과 |
| 화면 설정 다크 모드 즉시 적용 | 통과 |

HTTP 4xx/5xx 0건, 페이지 오류 0건.

## 찾아 고친 결함

**웹 화면에서 거래 삭제가 전혀 동작하지 않았습니다.**

최근 내역 카드 마크업이 아래와 같았습니다.

```html
<form action="/admin/update">…<button type="submit">수정 저장</button></form>
<button type="submit" formaction="/admin/delete" onclick="return confirm('삭제할까요?')">삭제</button>
```

`formaction`은 폼에 소속된 제출 버튼에서만 동작합니다. 이 버튼은 `</form>` 뒤에
있어 소유 폼이 없었고(`button.form === null`), 클릭해도 요청이 나가지 않았습니다.
확인창은 떴기 때문에 사용자는 삭제된 줄 알지만 기록은 남아 있었습니다.

브라우저 계측으로 확인한 증상:
- 보정 전: `dialog: 삭제할까요?` → **POST 없음** → 새로고침해도 기록 남음
- 보정 후: `dialog` → `POST /admin/delete` → 200 → 목록·새로고침 모두에서 사라짐

버튼을 `</form>` 앞으로 옮겨 소유 폼에 연결했습니다. HTML 바이트 증가는 없습니다.

## 회귀 방지

- 카드 마크업에서 삭제 버튼이 소유 폼 안에 있는지 위치로 검사
- 소스 전체에서 `</form>` 뒤에 `formaction` 제출 버튼이 남지 않는지 검사
- 실제 삭제 요청 후 픽스처 거래 수가 하나 줄고 목록에서도 사라지는지 검사

## 자동화 제한

- 카카오톡 챗봇의 삭제 명령은 별도 경로이며 기존 130개 검사로 확인합니다.
- 실기기 터치 입력과 인앱 브라우저 동작은 배포 후 확인이 필요합니다.

## 운영에서 남은 수동 확인

- 최근 내역에서 기록을 펼쳐 `삭제` → 확인 → 새로고침 후에도 사라졌는지
- 삭제 후 홈 합계와 캘린더 표시가 함께 갱신되는지
