# V22.8.3 Claude 검토 통합·추가 안정화 보고서

검토일: 2026-07-16 (Asia/Seoul)

## 결론

Claude 안정화본을 그대로 덮어쓰지 않고 현재 V22.8.2 보안·UI 구조와 항목별로 비교했습니다. 카카오 오류 단계 분리, 복구 가능한 제출 상태, 보조 로그인 조회 보호 등 유효한 개선은 반영했고, 명시적 활성화와 고정 Redirect URI를 약화하는 변경은 제외했습니다.

최종 Worker 버전은 `V22.8.3-REVIEWED-STABILITY`이며 새 데이터베이스 마이그레이션은 없습니다.

## 입력본 검증

- Claude 소스 버전: `V22.8.2-AUTH-UIUX-STABILIZATION`
- Claude 소스 SHA-256: `cdde4fa71d933d2ca2ba2f0741ae87836e4469136b35180afb42ee52929741be`
- ZIP 무결성 검사: 통과
- Claude 제공 안정화 smoke: 54/54 통과
- Claude 제공 전체 검증: 6,052개 통과

## 선별 반영

- 카카오 OAuth 오류를 설정, state, token, profile, 계정 저장 단계로 분리
- 토큰·프로필 단계 실패 시 사용자용 오류 코드와 재시도·백업 로그인 안내 제공
- 성공·취소·실패 경로에서 OAuth 임시 쿠키 정리
- 브라우저 뒤로가기 시 제출 잠금 복원
- 보조 로그인 식별자 조회를 `try/catch`로 보호
- 계정 통합·가계부 삭제·정기지출 삭제 등 파괴적 작업의 위험 표시와 설명 강화
- 복구 가이드에 운영 기준 callback 권장값 표시

## 유지한 기존 보안 정책

다음은 현재 구현이 더 안전해 Claude 안으로 교체하지 않았습니다.

- `KAKAO_LOGIN_ENABLED=1`이 없으면 REST API 키가 있어도 로그인 자동 활성화 금지
- `PUBLIC_BASE_URL`과 `KAKAO_REDIRECT_URI` 명시값 필수
- 요청 호스트에서 OAuth callback 자동 추정 금지
- 공개 기준 주소와 Redirect URI origin 불일치 시 fail-closed 503
- OAuth state 상수 시간 비교
- 여러 `Set-Cookie`를 보존하는 응답 헬퍼
- 첫 사용 3단계, 영수증·리포트 메뉴, 사용자 백업 `/my/backup` 유지

## 추가로 발견해 보강한 문제

1. 여러 제출 버튼이 있는 폼에서 첫 버튼을 잠그던 문제를 `event.submitter` 기준으로 수정했습니다.
2. 가계부 생성·참여 페이지의 중복 인라인 잠금을 제거하고 공통 잠금이 단독으로 상태를 관리하게 했습니다.
3. `pageshow`에서 모든 POST 폼의 `disabled`, `aria-busy`, 구형 `data-submitting` 상태를 복원합니다.
4. 잘못된 percent-encoding 쿠키는 빈 세션으로 처리해 로그인 화면 500을 차단합니다.
5. 숫자 환경변수가 비어 있거나 잘못돼도 `NaN`이 되지 않도록 기본값·상한·하한을 적용합니다.
6. rate-limit, 유효 사용자 캐시, 반복 발화 캐시에 최대 엔트리와 만료 정리를 적용해 장기 실행 메모리 증가를 제한합니다.
7. 저장·삭제 뒤 복귀 주소는 `/`로 시작하는 같은 사이트 경로만 허용해 외부 이동과 헤더 개행을 차단합니다.
8. 정기지출 준비 항목 삭제가 이미 기록된 거래를 지우지 않는다는 점을 확인창에 명시했습니다.

## 카카오 오류 정보 보호

- 카카오 token/profile 응답 본문과 access token은 로그에 기록하지 않습니다.
- 운영 로그에는 실패 단계, HTTP 상태, 임의 추적 코드만 남깁니다.
- 사용자 화면에는 비밀값이나 공급자 원문 오류 대신 한국어 복구 안내를 표시합니다.

## 남은 외부 작업

소스는 카카오 Developers 관리자센터의 실제 등록값을 읽을 수 없습니다. 운영자는 같은 카카오 앱에서 Web domain과 `https://ttokttok-accountbook.com/auth/kakao/callback`을 정확히 등록하고 실제 카카오 계정으로 성공 callback을 확인해야 합니다.

설정 확인 전에는 `KAKAO_LOGIN_ENABLED=0`을 유지하면 KOE006 화면으로 사용자를 보내지 않습니다.

