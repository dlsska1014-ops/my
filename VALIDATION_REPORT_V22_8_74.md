# V22.8.74 검증 보고서

검증일: 2026-08-06

## 점검 범위

세 가지를 요청받았습니다.

1. 200행 실사용 홈 HTML 예산 재검토
2. `/skill` 호출자 인증 켜기 점검
3. 실기기 확인 항목 전체 점검

## 1. 예산 재검토 — 검사가 더 작은 경우를 재고 있었습니다

예산 여유가 402 B 라고 보고했는데, **그 수치 자체가 잘못된 것을 재고 있었습니다.**

이 검사는 `month=2026-07` 을 고정으로 봅니다. 그 달이 지나면 "오늘" 표시가
붙지 않아 페이지가 작아집니다. 즉 검사가 재던 것은 사용자가 흔히 보는 화면이
아니라 **우연히 더 작은 화면**이었습니다.

같은 소스를 날짜만 바꿔 재면:

| 측정 시점 | 바이트 | 예산 45,056 B 대비 |
|---|---:|---|
| 2026-07-15 (그 달 안) | 45,083 | **초과 27 B** |
| 2026-07-20 (그 달 안) | 45,084 | **초과 28 B** |
| 2026-08-06 (그 달 밖) | 44,654 | 여유 402 B |

**이미 예산을 넘고 있었고, CI 가 8월에 돌아서 통과했을 뿐입니다.**

### 언제 넘었는지

같은 날짜(2026-07-20)로 릴리스별로 재봤습니다.

| 버전 | 바이트 | 판정 |
|---|---:|---|
| V22.8.68 ~ V22.8.71 | 44,977 | 아래 79 B |
| V22.8.73 | 45,084 | **초과 28 B** |

V22.8.73(본문 바로가기·캘린더 도트)이 선을 넘은 릴리스입니다.

### 조치

**(1) 낭비부터 회수했습니다.** `<option value="x" ${cond ? "selected" : ""}>` 는
선택되지 않으면 `value="x" >` 가 되어 option 하나마다 1바이트가 샙니다.
공백을 조건 안으로 옮겨 **38 B** 를 회수했습니다(패턴 85곳).

**(2) 검사가 최악 사례를 재게 했습니다.** 픽스처를 "이번 달"로 만들어,
검사가 언제 돌든 사용자가 가장 자주 보는(그리고 가장 큰) 화면을 재도록 했습니다.
네 개 날짜(7월·8월·12월·다음 해 3월)에서 모두 통과하는 것을 확인했습니다.

**(3) 예산을 근거와 함께 다시 정했습니다.** 44 KiB → **46 KiB**.

| 항목 | 값 |
|---|---:|
| 이번 달 화면 실측 | 44,962 B |
| 새 예산 | 47,104 B |
| 여유 | 2,142 B |

여유를 피드 카드 한 장(약 1,700 B)보다 크게 둬서 카드 마크업이 커지는 회귀는
계속 잡히게 했습니다. 실제로 카드에 200 B 를 일부러 넣자 검사가 즉시 실패했습니다.

> 예산을 올린 것은 맞습니다. 다만 **올리기 전 상태가 이미 초과**였고, 낭비를
> 먼저 회수한 뒤, 최악 사례를 재도록 고쳐서 정한 값입니다.

## 2. `/skill` 호출자 인증

**이 저장소에서는 Cloudflare 환경변수를 설정할 수 없고, 운영 도메인도
이 실행 환경의 네트워크 정책에 막혀 있어(프록시 403) 직접 켜거나 확인하지 못했습니다.**
대신 운영자가 밟을 각 단계가 코드에서 실제로 그렇게 동작하는지 확인했습니다.

| 단계 | 확인한 동작 |
|---|---|
| 비밀값 없음 (지금) | `/skill` **200** · `/health.skill_caller_auth_configured: false` |
| `AUTH_REQUIRED=1` 인데 비밀값 누락 | `/health.configured: false`, `missing_count: 1` — 반쪽 설정을 잡아냄 |
| 비밀값까지 설정 | 헤더 없음 **403** · 틀린 값 **403** |
| `x-api-key` (OpenBuilder 기본) | **200** |
| `x-kakao-skill-secret` | **200** |
| `Authorization: Bearer …` | **200** |
| 앞뒤 공백이 섞인 헤더 | **200** (양쪽 모두 trim) |
| 비밀값 노출 | `/health` 응답에 없음 |

비교는 상수 시간(`constantTimeTextEqual`)입니다.

> ⚠️ **순서를 지켜 주세요.** OpenBuilder 스킬 헤더를 **먼저** 넣고 실제 발화로
> 확인한 뒤에 Worker `KAKAO_SKILL_SECRET` 을 넣으십시오. 반대로 하면 그 사이
> 카카오 채널 전체가 403 으로 막힙니다. `KAKAO_SKILL_AUTH_REQUIRED=1` 은
> 둘 다 확인한 뒤 마지막에 켭니다.

## 3. 실기기 확인 항목

브라우저로 실제 검증이 가능한 것은 전부 돌렸습니다.

### 라이트·다크 × 포인트 4종 (8조합) — **실제 결함 2건**

iPhone 13 크기로 8조합을 렌더해 글자 대비를 다시 쟀습니다.

| 결함 | 실측 | 원인 |
|---|---:|---|
| 빈 상태 상자(`.homeEmpty`) 다크 | **1.82:1** | 밝은 배경 `#f8fafc` 이 `!important` 로 박혀 있어 다크에서도 그대로 남고, 글자만 흐린 색으로 바뀜 |
| 본문 바로가기 배지 다크 | **2.87:1** | 다크의 전역 링크색 규칙(`(0,2,3)`)이 배지의 흰 글자 규칙`(0,2,1)`을 덮어씀 |

두 번째는 **V22.8.72 에서 제가 넣은 요소**입니다. 다크에서 확인하지 않았습니다.

보정 후 **8조합 전부 0건**입니다.

### 홈 화면 추가 (manifest·아이콘)

| 항목 | 결과 |
|---|---|
| `link[rel=manifest]` | `/manifest.json` |
| `name` / `display` / `start_url` | 똑똑한 가계부 / `standalone` / `/app` |
| 아이콘 192·512·512(maskable) | 전부 200 |
| `apple-touch-icon.png` | 200 |
| `theme-color` | `#1d4ed8` |
| `apple-mobile-web-app-capable` | **없음** ← 아래 참고 |

### 카카오 인앱 브라우저 빠른 입력 시트

카카오톡 UA 로 열어 확인했습니다.

| 항목 | 결과 |
|---|---|
| 시트 열림 · `role="dialog"` | 정상 |
| 선택 날짜가 입력칸에 전달 | `2026-08-06` |
| 배경 스크롤 잠금 | `body.overflow: hidden`, 400px 스크롤 시도 → 움직이지 않음 |
| Esc 로 닫기 | 정상, 스크롤 복구됨 |
| JS 오류 | 0건 |

### 계측이 또 틀렸습니다 (2건)

라이트 모드에서 챌린지 카드 4건이 대비 미달로 잡혔지만, **화면을 직접 찍어 보니**
카드가 의도된 진한 남색 배경이고 글자가 선명했습니다.
제 대비 계산기가 `background-image: linear-gradient` 를 읽지 못하고
`background-color: transparent` 로 보고 흰 조상까지 거슬러 올라간 것이었습니다.

## 4. 성능·자원

| 항목 | 예산 | 실측 | 여유 |
|---|---|---|---|
| 기준 픽스처 홈 HTML | 35,840 B | 35,027 B | 813 B |
| 이번 달 실사용 홈 HTML | 47,104 B | 44,962 B | 2,142 B |

셸 스타일시트 주소 `v22873` → `v22874` (다크 규칙 2건 추가).

## 5. 자동 검사

- 신규 회귀 **18개** (`validation/validate-budget-dark-contrast-v22874.mjs`)
- 저장소 전체 **2,691개** 자동검사 통과

신규 회귀가 고정하는 것:

- 예산 검사가 **이번 달**을 재고, 과거 달을 고정하지 않는다
- 선택되지 않은 `option`·`checkbox` 에 잉여 공백이 남지 않는다
- 다크에서 빈 상태 상자가 어두운 표면을 쓰고, 본문 바로가기가 흰 글자를 지킨다
- 두 규칙이 **실제로 서빙되는 셸 자원에 실려 나간다** (소스에만 있으면 소용없다)
- 이번 달 화면이 예산 안에 있고, 여유가 피드 카드 한 장보다 크다

## 6. 운영 변경

- 신규 SQL 없음
- 환경변수 변경 없음
- 셸 스타일시트 주소 `v22874` (웹 탭 새로고침 필요)

## 배포 후 확인 부탁드립니다

1. 다크 모드로 바꾸고 홈의 빈 상태 상자 글자가 읽히는지
2. 다크 모드에서 Tab 한 번 → `본문 바로가기` 배지의 흰 글자가 선명한지
3. 포인트 컬러 4종을 바꿔 가며 위 두 가지 확인
4. 오래 열어 둔 웹 탭 새로고침 후 `v22874` 셸 적용

## 아직 남은 것 — 판단이 필요합니다

**iPhone 홈 화면 추가가 완전한 앱처럼 열리지 않습니다.**
`manifest.json` 에 `display: standalone` 이 있지만, iOS Safari 는 이 값을 따르지 않고
`<meta name="apple-mobile-web-app-capable" content="yes">` 를 봅니다. 지금은 없어서
홈 화면 아이콘을 눌러도 주소창이 있는 Safari 화면으로 열립니다.

넣으면 앱처럼 전체 화면으로 열리지만, **Safari 의 뒤로 가기 버튼이 사라집니다.**
앱 자체 내비게이션만으로 모든 화면에서 되돌아갈 수 있어야 하므로,
넣을지는 제품 판단이 필요해 이번에는 넣지 않았습니다.

**실제 손가락·실제 기기가 필요한 항목은 그대로 남습니다.** 탭 영역 크기는
회귀 검사(278개)로 고정돼 있지만, 실제 터치 반응·iOS Safari 고유 동작·
카카오톡 인앱 브라우저의 실제 렌더는 기기에서만 확인할 수 있습니다.
