# V22.8.45 참여자 권한 저장 오류 적용 안내

## 적용 파일

- Cloudflare Worker의 `src/index.js`를 ZIP의 파일 전체로 교체합니다.
- 부분 복사·부분 병합은 하지 않습니다.

## SQL과 외부 설정

- 신규 SQL: 없음
- 기존 SQL: 재실행하지 않음
- 환경변수·Secret: 변경 없음
- Kakao Developers·OpenBuilder: 변경 없음

이번 오류는 권한 저장용 DB 요청 실패가 Worker 밖으로 전파되어 Error 1101이 된 코드 경로에서 확인됐습니다. 운영 DB의 실제 제약 오류 내용은 확인하지 못했으므로 근거 없는 스키마 변경을 포함하지 않았습니다.

## 개선 내용

- `승인대기 → 관리자` 권한 저장 성공을 자동 회귀 검사로 보호
- DB 조회·저장 실패를 Error 1101 대신 참여자 화면의 인라인 오류로 반환
- 실패 시 기존 참여자 권한을 유지하고 성공 메시지를 표시하지 않음
- 수정 대상이 없거나 실제 반영 행이 0개면 성공으로 오판하지 않음
- 허용하지 않은 역할 값을 `member`로 조용히 바꾸지 않고 거부
- 유일한 소유자 보호 규칙을 유지하면서 권한 판단용 조회 실패 시 쓰기를 중단
- 가계부 보안 검사 75개, 전체 자동 검사 882개 통과

## 적용 순서

1. 현재 운영 V22.8.44 Worker 소스 전체를 백업합니다.
2. ZIP의 `src/index.js` 전체를 Cloudflare Worker에 붙여넣습니다.
3. 저장·배포합니다.
4. PowerShell에서 `12_VERIFY_AFTER_DEPLOY_V22_8_45.ps1`을 실행합니다.
5. `13_MEMBER_ROLE_SMOKE_GUIDE_V22_8_45.md`에 따라 테스트 참여자의 권한을 확인합니다.

## 완료 기준

- `/health`: HTTP 200, `V22.8.45-MEMBER-ROLE-SAFE-UPDATE`
- `/ready`: HTTP 200, `ready:true`, 빈 `failed_tables`, 빈 `missing_rpcs`
- 승인대기 참여자를 관리자로 저장했을 때 Error 1101이 발생하지 않음
- 성공하면 관리자 상태가 저장되고, 실패하면 기존 승인대기 상태와 화면 내 오류가 유지됨

문제가 있으면 배포 전에 백업한 V22.8.44 Worker 소스로 전체 롤백합니다. DB SQL은 실행하거나 되돌리지 않습니다.
