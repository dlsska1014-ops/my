# 알려진 제한과 운영 확인 사항

## V22.8.76 카카오 의도·스킬 인증

- **`KAKAO_SKILL_SECRET` 을 넣는 순간부터 헤더 없는 `/skill` 호출은 403 입니다.** OpenBuilder 헤더를 아직 안 넣었다면 그때부터 모든 카카오톡 대화가 실패합니다. 실패한 사용자에게는 평범한 "다시 보내주세요"가 가므로 겉으로는 고장으로 보이지 않습니다. `/health` 의 `skill_caller_auth.header_missing_suspected` 로 확인하고, `KAKAO_SKILL_AUTH_MODE=observe` 한 줄로 즉시 복구할 수 있습니다.
- `skill_caller_auth` 집계는 **isolate 단위 메모리**입니다. Cloudflare 가 isolate 를 새로 만들면 0부터 다시 셉니다. "누적 통계"가 아니라 "지금 통과하고 있는가"를 보는 신호로 쓰세요. 헤더가 빠져 있으면 어느 isolate 에서 보든 통과 0 · 거부만 쌓입니다.
- **과거에 `식비 예산 30만원` 으로 잘못 기록된 지출은 이 배포가 고쳐 주지 않습니다.** 앞으로의 입력만 예산으로 갑니다. 소비 분석의 `큰 금액 TOP` 에서 확인하고 직접 삭제해야 합니다.
- **`예산 식비 30만원` 처럼 분류가 `예산` 뒤에 오는 순서는 여전히 지출로 기록됩니다.** `식비 예산 30만원` 순서만 받습니다. 두 순서를 모두 받으면 `예산 30만원` 같은 말과 구분이 어려워져 이번에는 한쪽만 열었습니다.
- **사이드바 달력 날짜 34개는 Tab 순서에 남깁니다.** 데스크톱 전체 탭 정지점 54개 중 34개가 달력이지만, `본문 바로가기` 로 Tab 한 번이면 본문에 갑니다. 빼면 키보드로 달력 날짜를 아예 누를 수 없게 되어 손실이 더 큽니다. (2026-08-06 판단)
- **구성원(member)에게 예산·정기지출 저장 권한을 열지 않습니다.** 역할별 실제 요청으로 확인한 결과 화면과 서버 양쪽에서 막고 있어 보안 문제는 없습니다. 권한이 필요하면 `관리자` 로 승격하면 됩니다. (2026-08-06 판단)
- 역할별 권한을 잴 때는 역할마다 **고유한 값**을 써야 합니다. 같은 이름을 쓰면 앞 역할이 만든 데이터를 뒤 역할 차례에서 다시 세어 거짓 결함이 나옵니다.
- 실제 손가락 터치·iOS Safari 고유 동작·카카오톡 인앱 브라우저의 실제 렌더는 기기에서만 확인할 수 있습니다.
- 신규 SQL 은 없습니다. 환경변수는 `KAKAO_SKILL_AUTH_MODE` 1개가 선택적으로 늘었습니다.

## V22.8.75 통계·종합 리포트 역할 분리

- **이름만 다르고 내용이 겹치는 4쌍은 남겨 두었습니다.** 지출 흐름↔일별 소비 그래프, 분류별 구성↔이번 달 지출 구성, 요일 패턴↔요일별 소비 추이, 이번 달 예산↔예산 사용 게이지. 통계 쪽은 필터(기간·분류·결제수단·구성원·금액·검색어)에 반응해 제목까지 바뀌고, 리포트 쪽은 달 전체를 고정해 보여 줍니다. 입력이 다르므로 한쪽을 지우면 필터 경험이 깨집니다. 정리하려면 필터 결과를 리포트 쪽 카드에 연결하는 설계가 먼저 필요합니다.
- **`무지출 데이` 챌린지는 의도적으로 양쪽에 남겼습니다.** 데스크톱 사이드바의 축약 챌린지(`.abNavChallenge`)는 좁은 화면에서 `display:none!important` 로 숨겨집니다. 소비 분석에서 본문 챌린지를 빼면 모바일에서 챌린지를 볼 수 없게 됩니다. 설정 폼은 종합 리포트(`#reportChallenge`)에만 있어 관리 지점은 이미 하나입니다.
- **총액 예산이 없는 가계부에서는 `예산 소비 페이스` 카드가 보이지 않습니다.** 분류별 예산만 설정한 경우 그릴 누적 곡선이 없기 때문이며, 분류별 사용률은 바로 위 `한눈에 보기`가 보여 줍니다.
- 중복 CSS 정리는 **하지 않기로 했습니다.** 104개 선택자가 겹쳐 28,328 B 를 차지하지만, `immutable` 로 1년 캐시되는 자산이라 실제 절감은 gzip 기준 1,063 B(brotli 745 B)에 그칩니다. 24개 화면 × 8개 테마 조합을 다시 확인해야 하는 위험에 비해 이득이 작습니다.
- 최신 Chromium 은 중첩 문법 때문에 **일반 CSS 규칙도 빈 `cssRules` 를 가집니다.** 그것을 그룹 규칙 신호로 쓰는 도구는 모든 규칙을 재귀로 흘려보내 "일치 0건"을 냅니다. 규칙을 훑을 때는 `selectorText` 로 판별해야 합니다.
- 실제 손가락 터치·iOS Safari 고유 동작·카카오톡 인앱 브라우저의 실제 렌더는 기기에서만 확인할 수 있습니다.
- 신규 SQL·환경변수 변경은 없습니다.

## V22.8.74 예산 재검토·다크 대비

- 날짜 상세 팝업의 상태 문구(`#abDayDetailStatus`)는 팝업을 **처음 열 때** 만들어집니다. 첫 열기의 "불러오는 중 → N건" 전환은 스크린리더가 놓칠 수 있습니다(두 번째부터는 같은 노드를 재사용). 미리 만들어 두면 모든 화면에 바이트가 붙어, 팝업 이름(날짜)이 포커스 시점에 읽히는 것으로 충분하다고 보고 두었습니다.
- **iPhone 홈 화면 추가는 Safari 화면으로 여는 것이 확정된 선택입니다.** iOS Safari 는 `manifest.json` 의 `display: standalone` 을 따르지 않고 `<meta name="apple-mobile-web-app-capable" content="yes">` 를 봅니다. 이 메타를 넣으면 앱처럼 전체 화면으로 열리지만 **Safari 뒤로 가기 버튼이 사라집니다.** 2026-08-06 운영자 판단으로 **넣지 않기로 했습니다.** 홈 화면 아이콘은 주소창과 뒤로 가기가 있는 Safari 화면으로 열립니다. 이 선택을 되돌리려면 앱 자체 내비게이션만으로 모든 화면에서 되돌아갈 수 있는지부터 확인해야 합니다.
- 실사용 홈 HTML 예산을 44 KiB → 46 KiB 로 올렸습니다. 올리기 전 상태가 이미 초과였고(45,084 B), 낭비 38 B 를 회수하고 최악 사례를 재도록 고친 뒤 정한 값입니다. 현재 여유 2,142 B 는 피드 카드 한 장(약 1.7 KB)보다 큽니다.
- 예산 검사는 이제 **이번 달**을 잽니다. 과거 달을 고정하면 "오늘" 표시가 빠져 더 작게 측정되어 검사가 거짓 통과합니다. 비슷한 검사를 만들 때 같은 함정을 주의해야 합니다.
- 대비 계산기를 만들 때 `background-image: linear-gradient` 를 읽지 못하면 `background-color: transparent` 로 보고 엉뚱한 조상 배경과 비교하게 됩니다. 챌린지 카드 4건이 이 이유로 거짓 결함으로 잡혔습니다.
- 실제 손가락 터치·iOS Safari 고유 동작·카카오톡 인앱 브라우저의 실제 렌더는 기기에서만 확인할 수 있습니다.
- 신규 SQL·환경변수 변경은 없습니다.

## V22.8.73 캘린더·챌린지·보안 병합

- `02_APPLY_HOUSEHOLD_PURGE_V22_8_71.sql` 은 **2026-08-06 운영자가 Supabase 에서 실행했다고 알려 왔습니다.** 이 저장소에서는 운영 DB 에 접근하지 않으므로 직접 확인하지는 못했습니다. 실행 결과의 `purge_rpc_count` 가 1인지 확인해 두면 좋습니다.
- **적용 전에 삭제한 가계부에는 `report_challenge:`·`goals:v5:`·`favorites:v5:` 설정이 남아 있습니다.** 이 SQL 은 앞으로의 삭제만 완전하게 만들 뿐, 이미 남은 찌꺼기를 청소하지 않습니다. 같은 이름으로 가계부를 다시 만들어도 옛 설정이 붙지는 않습니다(키에 가계부 ID 가 들어가고 ID 는 새로 생기기 때문).
- **`/skill` 인증을 켤 때는 순서가 중요합니다.** OpenBuilder 스킬 헤더를 먼저 넣고 확인한 뒤 Worker `KAKAO_SKILL_SECRET` 을 넣어야 합니다. 반대로 하면 그 사이 카카오 채널 전체가 403 으로 막힙니다. `KAKAO_SKILL_AUTH_REQUIRED=1` 은 둘 다 확인한 뒤 마지막에 켭니다.
- 새 환경변수 3개는 전부 선택입니다. 넣지 않으면 현재 동작이 그대로 유지됩니다.
- 받은 파일(`V22.8.71-CALENDAR-CHALLENGE`)을 **그대로 배포하면 안 됩니다.** V22.8.70 에서 갈라진 것이라 V22.8.71(날짜 수정 오지정·복구 소실·금액 표기 거절)과 V22.8.72(본문 바로가기) 수정이 되살아납니다. 반드시 이 병합본을 쓰십시오.
- 챌린지 기존 설정은 무지출 방식으로 호환되지만, 하루 한도·분류별 한도는 새로 설정해야 동작합니다.
- 날짜 상세의 기록 추가 버튼은 기본으로 감춰져 있고 `can_write` 권한이 있을 때만 보입니다. 조회 전용 사용자에게는 보이지 않는 것이 정상입니다.
- **200행 실사용 홈 HTML 여유가 402 B 로 줄었습니다.** 측정일에 따라 흔들리는 값이라 다음 변경에서 예산을 넘길 수 있습니다. 예산 재검토가 시급합니다.
- immutable 자원 주소가 `v22873` 으로 바뀌었습니다. 오래 열어 둔 탭은 새로고침해야 적용됩니다.

## V22.8.72 키보드 본문 바로가기

- 데스크톱 홈·거래 내역·캘린더는 사이드바 달력의 **날짜 링크 31개**가 여전히 탭 순서에 있습니다. 본문 바로가기로 건너뛸 수는 있지만, 달력 자체를 탭 순서에서 줄이는 것은 별도 판단이 필요합니다.
- 본문 바로가기는 셸(`abV22812Shell`)을 쓰는 화면에만 들어갑니다. 운영·관리 화면과 카카오 스킬 안내 화면에는 없습니다.
- `main` 이 없는 화면에는 넣지 않습니다. 현재 사용자 화면 24개는 모두 `main` 이 있습니다.
- **200행 실사용 홈 HTML 여유가 409 B 까지 줄었습니다.** 이 수치는 픽스처가 날짜 기준으로 만들어져 측정일에 따라 흔들리므로, 다음 변경에서 카드 마크업이 조금만 커져도 45,056 B 예산을 넘길 수 있습니다. 예산 재검토가 필요합니다.
- 셸 스타일시트 주소가 `v22872` 로 바뀌었습니다. 오래 열어 둔 탭은 새로고침해야 적용됩니다.
- 포커스 표시 점검에서 "0건"은 네 번의 계측 교정 뒤에 나온 값입니다. 비슷한 점검을 다시 만들 때는 프로그램 포커스(`el.focus()`)가 아니라 **실제 Tab 키**로 이동하고, 검사가 결함을 잡는지 먼저 확인해야 합니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.71 수정·삭제·복구 명령

- **이 배포는 과거에 잘못 처리된 기록을 되돌리지 않습니다.** `수정 NN번 날짜 …`로 "변경했어요"를 받았지만 실제로는 바뀌지 않은 기록이 있을 수 있고, 연속 삭제 후 복구하지 못한 기록은 이미 지워져 되살릴 수 없습니다. 최근 내역에서 날짜가 이상한 항목을 확인해야 합니다.
- 되돌리기 버퍼는 최근 삭제 **10건**까지만 보관합니다. 그보다 많이 지우면 오래된 것부터 복구할 수 없습니다. TTL 은 24시간 그대로입니다.
- 같은 번호로 여러 건을 지운 경우 `복구 NN번`은 **가장 최근에 지운 것부터** 되돌립니다. 지운 순서의 역순이며, 특정 건을 골라 복구할 수는 없습니다.
- 수정 세션이 열려 있는 동안에는 새 지출이 저장되지 않습니다. 확인 질문에 `아니오`로 답하면 `취소`로 빠져나가라고 안내하지만, 사용자가 세션 상태를 모른 채 여러 번 입력할 여지는 남아 있습니다.
- 금액 수정은 입력과 같은 추출기를 쓰지만, 금액처럼 생긴 문구만 통과시킵니다. 한글 수사(`삼만오천원`)는 통과하고 `대충 3만` 같은 혼합 문구는 거절합니다.
- 200행 실사용 홈 HTML 여유가 509 B 까지 줄었습니다. 이 수치는 픽스처가 날짜 기준으로 만들어져 측정일에 따라 흔들리므로, 다음 변경에서 카드 마크업이 커지면 예산을 넘길 수 있습니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다. 자원 주소가 그대로라 웹 탭 새로고침도 필요 없습니다.

## V22.8.70 금액 파싱

- **이 배포는 과거에 틀리게 저장된 금액을 되돌리지 않습니다.** 쉼표를 넣어 입력한 기록이 실제보다 작은 금액으로 남아 있거나, `3만5천원` 형태가 두 건으로 나뉘어 있을 수 있습니다. 최근 내역에서 금액을 한 번 확인해야 합니다.
- 한 글자 조사(이·가·로·에·은·는·을·를)는 더 이상 떼어내지 않습니다. `회사로 5000`은 메모가 `회사로`로 남습니다. 명사의 마지막 글자와 원리적으로 구분되지 않기 때문이며, 더 정확히 하려면 형태소 분석기 도입을 별도로 판단해야 합니다.
- `주유 5만원`·`택시 12000`처럼 분류 이름과 금액만 보내면 `지출로 기록할까요, 예산으로 설정할까요?`를 되묻습니다. 의도된 흐름이지만, 자주 쓰는 분류에서 매번 되묻는 것이 번거로울 수 있어 제품 판단 대상입니다.
- 카카오 스킬은 한 사용자당 분당 60건 제한이 있습니다. 대량 입력 검사를 만들 때 이 상한에 걸리면 파싱 결함처럼 보이므로, 검사에서 상한을 조정하거나 사용자 키를 나눠야 합니다.
- 파싱 검사는 응답 문구가 아니라 **저장된 행**을 읽어야 합니다. "저장했어요"만 확인하면 금액이 틀려도 통과합니다. 메모에 숫자를 넣으면 금액 파싱을 오염시켜 검사 자체가 거짓 결과를 냅니다.
- 모바일 홈 런타임 주소가 `v22870`으로 바뀌었습니다. 오래 열어 둔 탭은 새로고침해야 적용됩니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.69 카카오 입력 메모·중복 방지

- **이 배포는 과거에 잘못 저장된 메모를 되돌리지 않습니다.** 글자가 빠진 기록(`타 어교체`, `발`, `방` 등)이 남아 있을 수 있으니 최근 내역을 한 번 확인하고 필요한 항목만 수정해야 합니다.
- 한국어 조사 제거는 형태소 분석이 아니라 규칙 기반입니다. 낱말 끝에서만 떼어내도록 좁혔지만, 조사로 끝나는 고유명사(예: `우리은행로점` 같은 변형)는 여전히 잘릴 수 있습니다. 정확도가 더 필요하면 분석기 도입을 별도로 판단해야 합니다.
- 접속사 분리에 lookbehind 정규식을 씁니다. Cloudflare Workers(V8)에서는 문제가 없지만, 다른 런타임으로 옮길 때는 지원 여부를 먼저 확인해야 합니다.
- 중복 방지는 응답 문구로 "저장이 일어났는지"를 판정합니다. 저장 성공 문구를 바꿀 때는 `AB_KAKAO_PERSISTED_RESPONSE` 를 함께 고쳐야 합니다. 그러지 않으면 중복 방지가 조용히 꺼집니다.
- 이번 결함 3건은 모두 화면 렌더 검사로는 잡히지 않았습니다. 입력 문구를 넣고 **저장된 값을 되읽는** 검사가 필요했습니다.
- 모바일 홈 런타임 주소가 `v22869` 로 바뀌었습니다. 오래 열어 둔 탭은 새로고침해야 적용됩니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.68 모바일 조작 영역·접근성

- 측정 기준을 먼저 검증해야 합니다. 체크박스는 `<label>` 안에 있으면 라벨 전체가 탭 영역이라, 입력 상자만 재면 오탐이 대량으로 나옵니다.
- 통계 화면 문장 안의 `종합 리포트` 인라인 링크(62×14)는 키우지 않았습니다. 문장 흐름 안의 인라인 링크는 WCAG 2.5.8 예외이며, 키우면 줄 간격이 벌어집니다.
- 접기 헤더의 세로 가운데 정렬은 `align-content` 에 기대고 있어, 지원하지 않는 브라우저에서는 글자가 44px 상자의 위쪽에 붙습니다. 탭 영역 자체는 두 경우 모두 44px입니다.
- 표 안 링크는 여백을 음수 마진으로 바깥에 넓혔습니다. 행 높이가 24px보다 작아지면 위아래 행의 탭 영역이 겹칠 수 있으므로, 표 행 높이를 줄이는 변경을 할 때 함께 확인해야 합니다.
- 대비·조작 영역 외의 접근성(스크린리더 읽기 순서, 라이브 영역 알림, 키보드 포커스 표시)은 아직 보지 않았습니다.
- 셸 스타일시트 주소가 `v22868`로 바뀌었습니다. 오래 열어 둔 탭은 새로고침해야 적용됩니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.67 역할별 화면·빈 상태

- 이 결함은 "쓰기가 막히는지"만 보면 잡히지 않습니다. 차단 자체는 정상이었고, 문제는 **막힌 뒤 돌아갈 화면**이 그 역할에게 403 이라는 점이었습니다. 앞으로 권한 거절 리다이렉트를 추가할 때는 착지 화면이 그 역할에게 실제로 200 으로 열리는지 함께 확인해야 합니다.
- 키워드 저장 권한은 여전히 소유자·관리자 전용입니다(`canManageMyHousehold`). 구성원에게도 열어 줄지는 제품 결정 사항이며 이번 릴리스에서 정책은 바꾸지 않았습니다.
- 같은 이유로 구성원은 `예산`·`정기지출`·`자산·계좌`·`월 마감`·`부부 정산`·`통계 챌린지` 화면에서도 저장 수단을 볼 수 없습니다. 이 화면들은 이미 각자 사유를 안내하고 있으며, 권한 범위를 넓히려면 별도 결정이 필요합니다.
- 빈 상태 점검은 저장소 QA 픽스처의 "기록 0건 가계부" 기준입니다. 참여자가 여럿이거나 예산만 있고 기록이 없는 조합은 아직 따로 보지 않았습니다.
- 권한 조합 점검에 쓴 QA 하네스는 역할별 쿠키를 헤더(`x-qa-as`)로 고르는 임시 서버입니다. 저장소에 포함하지 않았고, 자동 회귀는 `app.fetch` 직접 호출로 같은 조합을 재현합니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.66 거래 삭제 동작 복구

- 이 결함은 화면 렌더링 검사로는 잡히지 않았습니다. 버튼이 보이고 확인창까지 떴기 때문입니다. 앞으로 버튼을 폼 밖에 두거나 `formaction`을 쓸 때는 소유 폼 연결을 함께 확인해야 합니다.
- 배포 전 삭제된 줄 알았지만 남아 있는 기록이 사용자 데이터에 있을 수 있습니다. 배포 후 최근 내역을 한 번 훑어보길 권합니다.
- 브라우저 기능 흐름 점검은 저장소 QA 픽스처 기준입니다. 실제 데이터량·권한 조합에서는 다시 확인이 필요합니다.
- QA 서버는 요청 호스트를 운영 도메인으로 바꿔 해석합니다. 브라우저에서 `fetch`로 쓰기를 검사하려면 출처를 운영 도메인으로 맞춰 프록시해야 하며, 그러지 않으면 CSRF 검사에 걸려 403이 납니다(하네스 아티팩트).
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.65 테마 글자 대비

- 측정은 텍스트 대비만 다룹니다. 아이콘·구분선·포커스 표시의 대비는 아직 확인하지 않았습니다.
- 예산 게이지의 `20%`는 도넛 링 안쪽 표면 위에 있습니다. 자동 측정기가 conic-gradient 정지점을 배경으로 잡으면 오탐이 나므로 링 그라데이션은 배경 후보에서 제외합니다.
- 비활성(`disabled`) 컨트롤은 WCAG 1.4.3 대비 요구 대상이 아니어서 측정에서 제외합니다. 흐림 정도가 과하다고 판단되면 별도 결정이 필요합니다.
- 연간 리포트의 `안내` 상자(`.notice`)는 다크에서도 밝은 주황 배경을 유지합니다. 자체 색으로 대비는 확보되지만 다크 화면에서 밝게 튑니다. 미관 조정은 별도 판단이 필요합니다.
- 화면별 CSS가 어두운 히어로를 전제로 밝은 글자색을 `!important`로 고정하는 패턴이 남아 있습니다. 새 화면을 추가할 때 같은 실수가 반복되면 다시 묻힐 수 있어 테마 토큰 사용을 우선해야 합니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.64 웹 매니페스트

- `display`는 `standalone`입니다. 홈 화면에서 열면 주소창과 뒤로가기 버튼이 없습니다. 브라우저 UI를 남기려면 `AB_WEB_MANIFEST`의 `display`를 `browser`로 바꾸면 됩니다.
- 설치 상태(`standalone`)에서 카카오 로그인 리다이렉트가 앱 안에서 끝나는지는 실기기 확인이 필요합니다. 외부 브라우저로 튀면 `display`를 `browser`로 두는 편이 안전합니다.
- 매니페스트 링크는 `</head>`가 있는 모든 사용자 화면에 붙습니다. 홈 HTML 여유는 기준 352바이트·실사용 310바이트로 줄었으므로 이후 마크업 추가 전에 예산을 다시 측정해야 합니다.
- 아이콘 압축은 런타임 `CompressionStream`에 의존합니다. 제공하지 않는 런타임에서는 저장 블록으로 대체되어 apple-touch-icon이 약 32KB가 됩니다.
- 아이콘 색상은 브랜드 블루 `#3182f6` 고정이라 포인트 컬러 설정을 따라가지 않습니다. `theme_color`도 같은 값 고정입니다.
- 카카오톡 인앱 브라우저는 매니페스트를 사용하지 않으므로 동작이 달라지지 않습니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.63 앱 아이콘 자원

- `manifest.json`은 제공하지 않습니다. PWA 설치를 지원하려면 홈 HTML에 `<link rel="manifest">` 한 줄이 필요한데 현재 예산 여유가 400바이트 미만이라 별도 결정과 예산 재측정이 필요합니다.
- 아이콘은 요청 시 생성해 모듈 변수에 캐시합니다. Worker 인스턴스가 새로 뜨면 첫 요청에서 한 번 더 계산합니다.
- PNG는 압축기를 넣지 않으려고 저장(비압축) deflate를 씁니다. 180×180 인덱스 컬러 기준 약 32KB이며 1주일 캐시와 ETag로 반복 전송을 막습니다.
- 사파리는 SVG favicon을 지원하지 않아 ICO로 제공합니다. 실제 탭 아이콘 표시는 운영 도메인에서 확인해야 합니다.
- 아이콘 색상은 브랜드 블루 `#3182f6` 고정입니다. 포인트 컬러(그린·바이올렛·앰버)를 바꿔도 아이콘은 따라가지 않습니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.62 전체 탭 점검 보정

- `/favicon.ico`, `/apple-touch-icon.png`, `/manifest.json`이 404입니다. 브라우저가 자동으로 요청하므로 모든 화면에서 콘솔 404가 남고, 홈 화면에 추가할 때 아이콘이 없습니다. 브랜드 아이콘·PWA 정책 결정이 필요해 이번 단계에서는 수정하지 않았습니다.
- 닫힌 `<details>` 내부 요소는 Chromium이 이전 레이아웃 기하 정보를 반환합니다. 자동 점검에서 이탈·겹침으로 잡히면 `details`를 열고 다시 측정해 판단해야 합니다.
- `scroll-padding-top`은 899px 이하 64px, 900px 이상 20px입니다. 상단바 높이를 바꾸면 이 값도 함께 확인해야 합니다.
- 점검은 저장소 QA 픽스처 기준입니다. 실사용 데이터량이 크게 다르면 목록 길이에 따른 레이아웃은 다시 확인해야 합니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.61 모바일 화면 보정

- 빠른 입력 시트는 열릴 때 `position:fixed`로 배경 스크롤을 잠그고 닫을 때 원래 위치로 되돌립니다. 잠금은 899px 이하에서만 적용하며 데스크톱은 기존 `overflow:hidden`을 유지합니다.
- 시트가 열린 동안 브라우저 뒤로가기 등으로 화면이 벗어나면 스크롤 위치 복원이 생략될 수 있습니다. 다음 화면 진입 시 정상 동작합니다.
- 모바일 공통 `작업` 버튼은 JavaScript로 상단바에 이동합니다. 스크립트가 실행되지 않으면 기존 고정 레이어로 남으며, 이때 오프셋을 112px로 넓혀 겹침을 줄였습니다.
- 전체 메뉴 서랍은 899px 이하에서 달력·예산 위젯을 숨깁니다. 같은 정보는 홈 화면과 데스크톱 사이드바에 그대로 있습니다.
- 빠른 입력 칩 아이콘은 홈 HTML 예산(기준 35KB·실사용 44KB) 때문에 마크업이 아니라 CSS로 그립니다. 사용자 정의 분류 이름이 내장 키워드와 겹치지 않으면 기본 아이콘이 표시됩니다.
- 칩 아이콘은 CSS 생성 콘텐츠라 일부 보조기술이 이모지 이름을 함께 읽을 수 있습니다. 칩의 텍스트 라벨은 그대로 유지됩니다.
- `input[type=date]`의 고유 최소 너비는 브라우저마다 다릅니다. 이번 수정은 그리드 칸이 항상 줄어들 수 있게 해 브라우저와 무관하게 이탈을 막습니다.
- 앱 내 브라우저 정책 때문에 iOS 사파리·카카오 인앱의 실제 스크롤 잠금은 자동화하지 못했습니다. 배포 후 수동 확인이 필요합니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.60 기능·UI 신뢰성

- 챌린지 설정은 지원 브라우저에서 화면 안에서 저장되며 주소와 스크롤 위치를 유지합니다. JavaScript가 꺼졌거나 실행에 실패하면 기존 303 이동 방식으로 안전하게 제출됩니다.
- 저장 후 진행률 재조회만 실패한 경우 설정 자체는 보존되며 다음 화면 이동 때 최신 값이 표시됩니다.
- 7일 이하는 날짜 칸, 8일 이상은 퍼센트형입니다. `7일 → 8일` 변경 직후에도 이 규칙을 적용합니다.
- 앱 내 브라우저 정책이 로컬 주소를 차단해 이번 단계의 390px·1440px 실제 창 검증은 자동화하지 못했습니다. 서버 렌더링·CSS 계약과 운영 배포 후 수동 확인으로 보완합니다.
- `/skill`은 256KiB 초과 요청을 안전 응답으로 종료하지만 `botUserKey` 자체의 공유 시크릿 문제와 인스턴스 메모리 기반 호출 제한은 남아 있습니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.59 계정·런타임 신뢰성

- 신규 계정 생성은 기존 `accountbook_create_local_user_v227` RPC를 사용합니다. `/ready`가 성공하더라도 실제 생성은 배포 후 테스트용이 아닌 사용자 흐름에서 한 번 확인해야 합니다.
- 계정 생성 오류는 운영 패널에 원인 코드만 기록하며 비밀번호·해시·세션 값은 기록하지 않습니다.
- 우측 기록은 최근 80건 표시용입니다. 월 전체 합계가 아니라 현재 선택한 목록의 합계이며, 전체 데이터는 거래 내역 화면에서 확인합니다.
- 종합 리포트의 최근 12개월 기록이 9,000건을 넘으면 일부 분석 경고를 표시합니다. 정확한 대규모 집계는 별도 집계 RPC 설계가 필요합니다.
- 세션 보안 테이블 조회 장애 시 기존 로그인도 다시 로그인 화면으로 이동합니다. 이는 데이터 노출보다 재로그인을 우선한 의도된 실패 폐쇄 동작입니다.
- `/skill`의 `botUserKey` 신뢰 문제와 인스턴스 메모리 기반 제한은 이번 단계에 포함하지 않았습니다.

## V22.8.58 챌린지·최근 기록 UI/UX

- 7일 이하 기간은 날짜별 칸을 표시하고, 8일 이상은 날짜 칸이 과밀해지지 않도록 퍼센트형으로 자동 전환합니다.
- 오늘은 아직 지출이 추가될 수 있어 완료 무지출일로 계산하지 않고 `오늘 진행 중`으로 표시합니다.
- 날짜별 상태는 거래 기록 기준입니다. 기록하지 않은 실제 지출을 자동으로 알 수는 없습니다.
- 우측 최근 기록 아이콘은 저장된 `category` 문자열로 결정됩니다. 사용자 정의 분류가 기존 유형과 맞지 않으면 영수증 모양의 기타 아이콘을 사용합니다.
- `필요·충동` 같은 소비 판단 태그는 저장 데이터가 없어 임의 표시하지 않습니다.
- 우측 최근 기록은 기존과 같이 1320px 이상에서만 보이며 더 작은 화면은 거래 탭을 사용합니다.
- 새 셸·V5 자산 적용 뒤 오래 열린 탭은 한 번 새로고침해야 합니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.57 홈 우측 기록·챌린지 보정

- 통계 화면의 중앙 월간 대시보드는 선택한 기준 월 전체 요약이고, 아래 차트는 최근 7일·30일·직접 선택 등 필터에 따라 달라질 수 있습니다.
- 챌린지 목표는 가계부 전체에 공유되며 소유자·관리자만 변경할 수 있습니다. 별도 SQL 없이 기존 `accountbook_settings`의 가계부 범위 키를 사용합니다.
- 우측 최근 기록 패널은 콘텐츠 폭을 확보할 수 있는 1320px 이상에서만 표시합니다. 더 작은 화면은 기존 거래 탭과 하단 메뉴를 사용합니다.
- 패널은 선택 월의 최근 80건을 받아 최대 40건을 렌더링합니다. 대량 월의 전체 기록은 `전체 보기`에서 확인합니다.
- 챌린지의 오늘은 아직 지출이 추가될 수 있으므로 다음 날부터 완료 무지출일로 계산합니다. 화면의 `1/7일차`와 `무지출 0/7일`은 서로 다른 의미입니다.
- 챌린지 기간은 최대 90일, 무지출 목표는 1~20일이며 목표 일수는 선택 기간을 넘을 수 없습니다.
- 기존 설정에 시작일·목표일이 없으면 확인 시점의 오늘부터 목표 일수만큼 기본 기간을 만듭니다. 소유자·관리자가 한 번 저장하면 명시 날짜가 공유됩니다.
- 셸·V5 자산 주소가 바뀌므로 배포 후 오래 열어 둔 탭은 새로고침해야 우측 패널과 새 접기 버튼이 보입니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.56 홈 HTML 예산 개정

- **홈 HTML 예산은 이제 측정 조건별 두 값입니다.** 기준 픽스처(당월 6행) 35KB 이하, 실사용 부하(당월 200행, 피드 카드 10장) 44KB 이하입니다. 35KB는 카드가 6장뿐인 픽스처에서 정한 값이라 실사용 부하에는 적용되지 않습니다.
- 실사용 예산 44KB의 여유는 약 1.4KB(실측 43,542바이트)뿐입니다. 카드 마크업을 늘리는 변경은 `validate-performance-v22811.mjs`에서 먼저 실패합니다. 예산을 올리려면 실측 근거가 필요합니다.
- **카드당 약 1.2KiB인 수정 폼은 그대로 즉시 렌더됩니다.** 지연 렌더로 바꾸면 약 12KiB를 더 줄일 수 있지만 즉시 펼침이 페이지 재요청으로 바뀌므로, 이번에는 예산 개정을 선택하고 UX를 보존했습니다. 향후 크기 압박이 생기면 이 방향을 다시 검토합니다.
- `src/index.js`와 정적 자산은 변경되지 않았습니다. 운영 배포 파일은 V22.8.55와 동일합니다.
- `/app` DB 호출 9회 예산은 여전히 초과 상태(대량 월 18회)이며 별도 과제입니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.55 피드 카드 마크업 축소

- 실사용 200행 기준 홈 HTML을 45.3KiB → 42.5KiB로 줄였습니다. 남은 크기의 주 원인은 거래 카드마다 즉시 렌더되는 수정 폼(카드당 약 1.2KiB × 10장)입니다. V22.8.56에서 이 크기를 실사용 예산으로 문서화했습니다.
- 검증에 실사용 200행 측정을 추가했습니다. 기존 6행 픽스처만 보던 사각지대가 이 측정으로 닫혔습니다.
- 거래 카드의 삭제는 이제 수정 폼의 `formaction`으로 전송됩니다. 삭제 요청 본문에 수정 필드가 함께 실리지만 `/admin/delete`는 `id`·`household_id`·`month`·`return_to`만 사용하므로 계약은 그대로입니다. 지출자 `required` 때문에 막히지 않도록 `formnovalidate`를 붙였습니다.
- 빠른 검색이 `data-search` 속성 대신 카드 본문 텍스트를 읽습니다. 화면에 보이지 않는 값(예: 원문 `raw_text`)으로는 더 이상 검색되지 않습니다.
- `/assets/mobile-home-v22855.js`와 `/assets/mobile-home-shell-v22855.js`는 immutable입니다. 배포 후 오래 열려 있던 탭은 한 번 새로고침해야 검색이 정상 동작합니다.
- `/app` DB 호출 9회 예산은 V22.8.54의 18회에서 그대로입니다. 집계 질의·RPC 설계가 필요합니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.54 조회 페이지 크기

- `fetchPostgrestRows` 기본 페이지 크기는 1000입니다. **이 값을 운영 PostgREST의 `max-rows`보다 크게 올리면 짧게 돌아온 페이지를 데이터 끝으로 오인해 조회 결과가 조용히 잘립니다.** 1000을 넘기려면 먼저 운영 `max-rows` 설정을 확인하고, 확인 전에는 올리지 않습니다.
- **성능 예산 2건이 미해결로 남아 있습니다.** 별도 과제로 다룹니다.
  - 개인 홈 HTML: 실사용 데이터에서 약 45KiB로 `AGENTS.md`의 35KB 예산을 초과합니다. 피드 카드 10장이 21.5KiB(카드당 약 2.1KiB)입니다. 현재 검증은 6행 픽스처(34.9KiB)만 측정해 이 사실을 잡지 못합니다.
  - `/app` DB 호출: 이번 조정으로 8,000건 월이 25회 → 18회가 됐지만 9회 예산은 여전히 초과합니다. 홈이 합계·차트를 만들려고 당월 전체 행을 가져오는 구조라, 집계 질의 또는 RPC 도입이 정공법이며 실행 계획을 포함한 별도 설계가 필요합니다.
- 응답 시간은 인메모리 검증에서 개선이 보이지 않습니다. 이번 조정의 이득은 **DB 왕복 횟수**이며 실제 지연 감소는 운영 네트워크에서 나타납니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.53 예산 사용률 표기

- 홈의 예산 사용률이 100%를 넘어 표시될 수 있습니다. 막대는 100%에서 멈추고 숫자만 실제 사용률을 보여줍니다.
- 분류별 예산이 하나라도 있으면 월 예산은 분류 예산의 합계를 씁니다. 예전 `__total` 값은 분류 예산이 전혀 없을 때만 호환용으로 사용하며 현재 사용자 화면에는 입력 수단이 없습니다.
- `/skill`의 근본 위험은 V22.8.52와 동일하게 남아 있습니다. `botUserKey`는 만료·회수 수단이 없는 자격증명이므로 공유 시크릿 헤더 검증을 별도로 진행해야 합니다.
- `accountbook_apply_recurring_v227` RPC가 픽스처에 없어 `/admin/recurring/apply`는 자동 검증 범위 밖입니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.52 스킬 IP 가드·CSV 안전·이름 검증

- **`/skill`의 근본 위험은 남아 있습니다.** IP 상한은 대량 남용만 막습니다. `botUserKey`는 여전히 만료·회수 수단이 없는 사실상의 자격증명이므로, 값 하나가 유출되면 해당 사용자의 가계부를 읽고 쓸 수 있습니다. 운영 로그·스크린샷·문의 캡처에 이 값이 남지 않도록 관리하고, Kakao OpenBuilder에서 커스텀 헤더 설정이 가능한지 확인해 공유 시크릿 검증을 추가해야 합니다.
- `SKILL_IP_GUARD_LIMIT` 기본값은 분당 3,000입니다. 대규모 단톡방에서 정상 요청이 제한되면 이 값을 올리고, 남용이 계속되면 낮춥니다. Worker 인스턴스 메모리 기반이라 인스턴스별로 계산됩니다.
- CSV 내보내기는 `=`·`+`·`-`·`@`로 시작하는 셀 앞에 작은따옴표를 붙입니다. 내보낸 파일을 다시 가져올 때 이 접두어가 메모 앞에 남을 수 있습니다. 순수 숫자 셀은 변경하지 않습니다.
- 웹 가계부 이름에서 `가족`·`생활비`·`모임`·`여행` 등을 허용했습니다. 카카오 대화에서 같은 낱말로 가계부를 이름으로 전환하려 하면 종류 선택 흐름과 혼동될 수 있으므로 번호 선택을 안내합니다.
- 신규 SQL·Supabase 스키마·Secret·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.51 입력 금액·런타임 정규식 수정

- 거래 금액 상한을 20억 원으로 두었습니다. 그보다 큰 금액이 필요한 운영 사례가 생기면 상한과 표시 형식을 함께 검토해야 합니다.
- 금액 칸은 이제 숫자·콤마·소수점만 받습니다. `오백원` 같은 한글 금액은 카카오 대화에서만 인식하며 웹 폼에서는 숫자로 입력해야 합니다.
- `/assets/mobile-home-v22851.js`와 `/assets/mobile-home-shell-v22851.js`는 immutable입니다. 배포 후 오래 열려 있던 탭은 한 번 새로고침해야 복구된 한 줄 입력이 적용됩니다.
- 이 런타임은 여전히 템플릿 리터럴 안 문자열로 작성돼 있어 백슬래시를 이중으로 써야 합니다. 새 정규식을 추가할 때 같은 소실이 재발할 수 있으므로 `validate:performance`의 배포 자산 실행 검사를 함께 확인하세요.
- `?month=2026-13` 같은 범위 밖 월은 오류 대신 현재 월로 대체합니다. 잘못된 링크를 눌러도 화면은 열리지만 사용자가 의도한 월이 아닐 수 있습니다.
- 예산 저장은 사용자가 명시한 `0`은 그대로 저장하고 빈 값·문자만 거부합니다.
- `validation/validate-performance-v22810.mjs`는 하네스에 연결되지 않은 오래된 파일이며 홈 HTML 바이트 예산 단언(35,000 vs 35 KiB) 때문에 V22.8.50 시점부터 실패합니다. 이번 릴리스에서는 자산 주소만 맞췄습니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.50 빠른 실행 독·저장 피드백

- 자동 렌더링은 PC 1440×1000과 모바일 390×844에서 독·피드백·바텀시트 위치와 가로 넘침, 브라우저 오류 0건을 확인했지만 실제 iOS Safari·Android Chrome의 가상 키보드와 safe-area를 대신하지 않습니다.
- 저장은 기존 POST·페이지 복귀 계약을 유지합니다. 네트워크 왕복 없이 모달 안에서 비동기 저장하고 월 합계·목록을 부분 갱신하는 기능은 이번 단계에 포함하지 않았습니다.
- 오류 카드가 열린 모바일 바텀시트의 하단 입력 일부를 잠시 덮을 수 있으나 닫기 버튼을 제공하며 오류는 자동으로 사라지지 않습니다. 실제 기기에서 높이와 키보드 조합을 확인해야 합니다.
- V22.8.46 참여자 역할 SQL은 운영 적용 완료로 확인됐으며 이번 단계 추가 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.49 빠른 입력 모달·바텀시트

- 자동 렌더링은 1440×1000 PC와 390×844 모바일에서 가로 넘침·선택 날짜·하단 밀착·브라우저 오류를 확인했지만 실제 iOS Safari와 Android Chrome의 가상 키보드 동작을 대신하지 않습니다.
- 거래 저장은 기존 POST 계약을 그대로 사용하므로 저장 성공 뒤 기존 결과 화면으로 전환됩니다. 팝업 내부 비동기 저장과 일별 목록 즉시 갱신은 이번 단계에 포함하지 않았습니다.
- JavaScript가 실행되지 않으면 기존 `/app#add` 입력 섹션을 사용하는 점진적 대체 흐름을 유지합니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.46 참여자 역할 스키마 정합성

- V22.8.45에서 Error 1101은 제거됐지만 운영 DB 역할 규칙이 `admin`을 거부하는 것이 확인됐습니다.
- V22.8.46은 실제 역할 컬럼 타입을 판별해 `admin`을 추가합니다. SQL 결과 `readiness_ok=true` 전에는 Worker 배포를 완료로 간주하지 않습니다.
- SQL은 최대 5초 동안 역할 테이블 잠금을 기다립니다. 잠금 시간 초과 시 데이터 변경 없이 중단되므로 이용이 적은 시간에 한 번만 재시도합니다.
- 알 수 없는 기존 역할이나 예상하지 못한 제약이 발견되면 자동 수정하지 않습니다. 오류 결과를 검토한 뒤 별도 SQL을 결정합니다.
- 적용 후 실제 테스트 참여자의 승인대기→관리자 저장과 새로고침 후 상태 유지가 필요합니다.

## V22.8.44 테마 대비·접근성 안정화

- 운영 로그인 홈의 라이트 표면에서 보조 텍스트가 4.26:1로 계측되어 공통 muted 토큰을 보정했습니다. 새 CSS는 `/assets/accountbook-shell-v22844.css`이며 이전 immutable 주소를 재사용하지 않습니다.
- 자동 검증은 라이트·다크와 네 컬러톤의 고정 토큰 조합, 390px·768px·1024px·1440px 레이아웃을 확인하지만 iPhone Safari·Android Chrome 실기기를 대신하지 않습니다.
- 고대비 선호, Windows 강제 색상, 동작 줄이기 설정을 보강했지만 실제 보조 기술별 사용성은 운영 기기에서 별도 확인합니다.
- 화면 설정은 계속 기기별 브라우저 저장소를 사용하므로 다른 기기로 동기화되지 않습니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.43 정산·목표 저장 정합성

- 정산 이력과 목표는 기존 `accountbook_settings` JSON을 계속 사용하며, 가계부 단위 operation lease와 strict read로 읽기 실패 시 덮어쓰기를 차단합니다.
- 설정 읽기·쓰기 실패 시 기존 JSON은 유지되고 정산 화면 또는 목표 API가 재시도를 안내합니다. 오류 상태에서 같은 버튼을 반복하지 말고 잠시 후 현재 목록을 다시 확인합니다.
- 목표는 가계부 공유 키, 즐겨찾기는 가계부·사용자 키를 사용합니다. 대규모 목표 이력·감사 로그가 필요하면 별도 스키마 승인이 필요합니다.
- 목표 오류 피드백 런타임은 `/assets/accountbook-goals-v22843.js`로 캐시 버전을 올렸습니다. 배포 후 오래 열린 목표 탭은 한 번 새로고침합니다.
- 완전한 DB 함수 단위 원자성은 새 SQL로 확장하지 않았습니다. 운영 `/ready`가 정상인 기존 잠금 RPC를 재사용합니다.
- 동시 저장과 장애 주입은 운영 데이터를 바꾸므로 격리 fixture에서 수행했습니다.

## V22.8.42 자산·예산 정합성

- 자산 변경은 가계부 단위 operation lease와 기존 자산 RPC를 함께 사용합니다. `/ready`에서 잠금 RPC와 자산 대체 RPC 중 하나가 정상이어야 합니다.
- 자산 본체 변경 뒤 월 순자산 스냅샷만 실패하면 본체 변경은 유지하고 전용 안내를 표시합니다. 다음 자산 변경 때 해당 월 스냅샷을 다시 기록합니다.
- 개별 예산 table 저장은 같은 분류의 settings fallback을 정리합니다. 정리가 실패해도 table 저장은 유지하고 운영 이벤트와 전용 안내로 구분합니다.
- 완전한 다중 저장소 원자성을 새 SQL로 확장하지 않았습니다. 운영 `/ready`가 정상인 현재 구조에서 기존 RPC와 손실 없는 재시도 계약을 사용합니다.
- 실기기 자산 동시 제출과 장애 주입은 운영 데이터를 바꾸므로 자동 fixture에서만 수행했습니다.

## V22.8.41 운영 쓰기 안전화

- 일반 웹 거래 중복 방지는 기존 DB operation lease로 교차 Worker 요청을 직렬화합니다. `/ready`에서 `accountbook_claim_operation`·`accountbook_release_operation`이 정상이어야 합니다.
- 예산 삭제는 테이블과 설정 fallback 정리가 모두 성공해야 완료로 안내합니다. 일시적 DB 오류에서는 기존 예산을 유지하고 재시도를 안내합니다.
- 거래의 원자적 수정·DB 감사가 성공한 뒤 화면 표시용 보조 이력만 실패하면 거래 수정은 성공으로 안내하고 운영 이벤트에 경고를 남깁니다.
- 조회 전용 사용자의 개인 즐겨찾기는 금융 데이터가 아닌 사용자별 UI 설정으로 현재 허용합니다. 다른 사용자의 즐겨찾기 키를 공유하거나 덮어쓰지 않는 현 계약을 유지합니다.
- 자산 동일 이름 교차 Worker 직렬화와 예산 table/settings 정리는 V22.8.42에서 기존 RPC 범위로 보강했습니다. 근거 없이 신규 SQL을 적용하지 않습니다.

## V22.8.39 운영 보강

- 현재 운영 점검에서 `/health`는 응답했지만 `/ready`는 필수 V22.7 RPC 누락으로 503이 확인됐습니다. V22.8.39 배포만으로 DB 함수가 생기지 않으므로 백업 후 복구 SQL 적용이 필요합니다.
- `/health`는 Worker 프로세스 생존만 뜻합니다. 실제 트래픽 준비 완료 판정은 반드시 `/ready`의 HTTP 200과 빈 `failed_tables`, 빈 `missing_rpcs`로 확인합니다.
- 외부 시스템이 `/cron/...?...key=...`를 GET으로 호출했다면 V22.8.39부터 실패합니다. URL 로그에 비밀값이 남지 않도록 POST와 비밀 헤더 방식으로 전환해야 합니다.
- 카카오 QA payload 경로와 raw text 테스트는 운영에서 닫혀 있습니다. 필요할 때만 QA 플래그와 관리자 인증을 함께 사용하고 검증 후 플래그를 끕니다.
- 과거 SQL 복구 파일은 transaction으로 감싸져 있지만, 적용 전에 Supabase 백업과 V22.6.8의 중복 사전 조건 확인이 필요합니다. 자산 저장은 V22.8.0 또는 V22.7.1 원자성 RPC 중 하나가 있어야 `/ready`를 통과합니다.

## V22.8.38 V5 기능 통합 안정화

- 전 기간 검색은 신규 SQL 없이 기존 거래 조회를 재사용하므로 한 가계부에서 최대 100,000건을 스캔하고 화면에는 최대 50건을 표시합니다. 운영 데이터가 이 범위에 가까워지면 서버 검색 RPC·인덱스를 실행 계획과 함께 별도 설계해야 합니다.
- 목표와 즐겨찾기는 신규 테이블 대신 기존 `accountbook_settings`를 사용합니다. 목표 쓰기는 기존 작업 잠금 RPC로 보호하지만 대규모 목표 이력·감사 로그가 필요하면 별도 스키마 승인이 필요합니다.
- 거래 목록 행 자체의 즐겨찾기 마크업은 보호된 분석·거래 렌더러 회귀를 피하기 위해 활성화하지 않았습니다. 통합 검색의 ★ 즐겨찾기는 정상 제공됩니다.
- 운영 배포 뒤 PC·모바일에서 검색·알림 열기, Escape·Tab·초점 복원, 조회 전용 목표 화면과 10억 원 이상 금액 표시를 확인합니다.
- `/assets/accountbook-shell-v22838.css`, `/assets/accountbook-nav-v22836.js`, `/assets/accountbook-v5-v22838.js`는 immutable입니다. 배포 후 오래 열린 탭은 한 번 새로고침합니다.
- 신규 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.25 UI V5 전역 작업 컨트롤

- 전역 거래 검색은 새 검색 API가 아니라 기존 `/app` 거래 목록의 `q` 필터를 사용합니다. 현재 가계부와 기준 월 범위 안에서 검색합니다.
- 알림은 실제 제공 중인 `/budget-alerts`만 연결했습니다. 읽음 상태나 푸시 알림처럼 서버 계약이 없는 기능은 만들지 않았습니다.
- 운영 배포 뒤 모바일 작업 대화상자의 Escape·바깥 영역 닫기·초점 복원과 키보드 `/` 검색을 실제 브라우저에서 확인합니다.
- `/assets/accountbook-shell-v22825.css`와 `/assets/accountbook-nav-v22825.js`는 immutable입니다. 배포 후 오래 열린 탭은 한 번 새로고침합니다.
- 이번 단계는 기존 URL·API·저장 구조를 재사용하며 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.24 UI V5 공통 셸 정확도 보정

- V5 시안의 저축·목표, 연간 리포트, 전역 검색·알림·빠른 추가는 현재 서버 제품 경로가 없어 이번 단계에서 가짜 링크나 localStorage 기능으로 만들지 않았습니다. 별도 데이터·API 판단 뒤 순차 구현합니다.
- 운영 배포 뒤 899px 이하에서 전체 메뉴 열기·Tab 순환·Escape 닫기·초점 복원과 900px 이상 사이드바 전환을 실제 Chrome·Safari에서 확인합니다.
- `/my/households`, `/my/members`, `/my/groups`, 백업·가져오기, 통계·분석에서 현재 메뉴가 각각 하나만 선택되는지 확인합니다.
- `/assets/accountbook-shell-v22824.css`와 `/assets/accountbook-nav-v22824.js`는 immutable입니다. 배포 후 오래 열린 탭은 한 번 새로고침합니다.
- 이번 단계는 셸 정확도 보정이며 SQL·환경변수·Kakao Developers·OpenBuilder 변경은 없습니다.

## V22.8.23 UI V5 로그인 화면 3단계

- 화면별 저장/API 구조는 유지하고 렌더링 결과의 셸·표면만 통합했습니다. 운영 배포 뒤 예산 저장, 참여자 권한 변경, 백업 미리보기, 정산 완료를 실제 계정에서 한 번씩 확인합니다.
- 영수증 등록과 모바일 홈은 촬영·빠른 입력에 맞춘 전용 본문 구조를 유지하지만 공통 메뉴·테마 토큰은 동일합니다.
- 390px·768px·1024px·1440px 로컬 브라우저 검증은 통과했지만 iPhone Safari와 Android Chrome 실제 기기 확인을 대신하지 않습니다.
- `/assets/accountbook-shell-v22823.css`와 `/assets/accountbook-nav-v22821.js`는 immutable입니다. 배포 후 오래 열린 탭은 한 번 새로고침합니다.

## V22.8.22 UI V5 핵심 화면 2단계

- 이번 단계는 홈, 기본 소비 분석(`/my/analysis`), 정산 본문을 우선 전환합니다. 예산·리포트·함께·관리의 나머지 로그인 화면은 다음 단계에서 같은 규격으로 순차 전환합니다.
- 소비 분석의 종합 리포트 세부 보기와 기존 저장·CSV·정산 계산 계약은 회귀 위험을 줄이기 위해 기능 구조를 유지합니다.
- 자동 검증과 로컬 브라우저 확인은 390px·1024px·1440px 기준이며 iPhone Safari와 Android Chrome 실제 기기 확인을 대신하지 않습니다.
- `/assets/accountbook-shell-v22822.css`와 `/assets/accountbook-nav-v22821.js`는 immutable입니다. 배포 후 오래 열린 탭은 한 번 새로고침합니다.

## V22.8.21 UI V5 공통 셸 1단계

- 이번 단계는 공통 셸·토큰·아이콘·사이드바만 전환합니다. 홈·분석·정산 본문 카드 재구성은 후속 단계입니다.
- 모바일 전체 메뉴는 데스크톱 사이드바와 같은 DOM을 사용하며 JavaScript가 실행된 뒤 SVG 아이콘과 열기·닫기 상태가 동기화됩니다.
- 자동 검증은 1024px 반응형 경계와 접근성 상태를 확인하지만 PC Chrome, iPhone Safari, Android Chrome의 실제 보이는 결과를 대신하지 않습니다.
- `/assets/accountbook-shell-v22821.css`와 `/assets/accountbook-nav-v22821.js`는 immutable입니다. 배포 후 오래 열린 탭은 한 번 새로고침합니다.

## V22.8.20 카카오 수정·웹 피드백 통합

- 카카오 수정 세션은 5분, 삭제 복구 정보는 24시간 유지되며 만료 뒤에는 새 명령으로 다시 시작해야 합니다.
- 실제 카카오 1:1·그룹 대화의 재전송·네트워크 지연은 자동 픽스처를 대신하지 않으므로 운영 배포 후 별도로 확인합니다.
- 웹 결과 안내는 같은 경로의 POST 폼이 하나로 식별될 때 제출 위치로 이동하며, 모호하면 기존 서버 위치에 남겨 잘못된 폼에 표시하지 않습니다.
- V22.8.19 정적 CSS 경로를 그대로 사용하므로 오래 열린 탭은 배포 후 한 번 새로고침합니다.

## V22.8.19 색상·대비 핫픽스

- 자동 브라우저 점검은 공개·로그인 주요 화면의 라이트·다크와 네 컬러톤을 확인했지만 PC Chrome, iPhone Safari, Android Chrome 실기기를 대신하지 않습니다.
- `/assets/accountbook-shell-v22819.css`는 immutable입니다. 배포 후 오래 열린 탭은 한 번 새로고침합니다.
- 장식용 가운데점과 화면 밖 접근성 라벨은 보이는 본문 텍스트 대비 결함과 분리해 판정했습니다.

## V22.8.18 UI·UX 4단계

- 첨부 v4는 독립 데모이므로 거래·예산·참여자를 localStorage에 저장합니다. 운영본은 이 저장 방식을 적용하지 않고 기존 서버 DB와 권한 검사를 유지합니다.
- 홈은 빠른 입력과 최근 기록을 한 화면에서 제공하므로 다른 사용자 화면의 그룹형 사이드바 대신 간결한 홈 전용 사이드바를 사용합니다. 폭·색·상태 규칙은 4단계 체계와 동일합니다.
- `/assets/accountbook-shell-v22818.css`는 immutable입니다. 중간 개발본을 같은 주소로 연 로컬 브라우저에서는 캐시 삭제 또는 새 시크릿 창으로 확인합니다.
- 자동 브라우저 점검은 1440px 데스크톱과 390px 모바일의 라이트·다크를 확인했지만 iPhone Safari와 Android Chrome 실기기를 대신하지 않습니다.

## V22.8.17 AdSense 심사와 V2 화면

- 소유권 메타 대상은 홈과 정책·주요 공개 콘텐츠 26개 URL(정식 14개, 별칭 12개)이며 개인·관리자 화면은 의도적으로 제외합니다.
- 운영 V22.8.16에서 기존 게시자 환경값 `354646…`이 확인됐습니다. V22.8.17은 환경변수를 바꾸지 않고 심사 게시자 `842269…`를 코드에서 고정해 이 값을 무시합니다. 배포 후 실제 메타와 `/ads.txt`가 바뀌었는지 확인해야 합니다.
- 승인 전 광고 실행 스크립트와 광고 쿠키 코드는 포함하지 않았습니다. 승인 이후 광고 활성화는 별도 버전·별도 검증으로 진행합니다.
- `/assets/accountbook-shell-v22817.css`는 immutable이므로 이미 중간 개발본을 같은 URL로 열었던 로컬 브라우저에서는 새 호스트 또는 캐시 삭제 후 확인합니다. 운영에는 이 경로가 처음 배포되는 것을 전제로 합니다.
- 자동 브라우저 점검은 데스크톱과 375px 모바일에서 홈 달력·분석·다크·앰버를 확인했지만 iPhone Safari와 Android Chrome 실기기를 대신하지 않습니다.

## V22.8.16 사용자 화면 전체 대비

- 운영 배포 후 저장된 라이트·다크 상태에서 메뉴, 자산, 적립계획, 영수증, 가계부 관리, 참여자, 키워드, 백업, 시작 안내를 확인해야 합니다.
- 오래 열어둔 탭은 V22.8.15 CSS를 유지할 수 있으므로 배포 후 한 번 새로고침합니다.
- `/assets/accountbook-shell-v22816.css`의 200 응답, CSS MIME, immutable 캐시와 새 ETag를 확인해야 합니다.
- 자동 브라우저 점검은 데스크톱 Chrome 환경을 기준으로 하며 iPhone Safari와 Android Chrome 실기기를 대신하지 않습니다.

## V22.8.15 다크 인증 화면 대비

- 운영 배포 후 저장된 다크모드 상태에서 로그인과 `내 계정·보안`을 각각 확인해야 합니다.
- 오래 열어둔 탭은 V22.8.14 CSS를 유지할 수 있으므로 배포 후 한 번 새로고침합니다.
- `/assets/accountbook-shell-v22815.css`의 200 응답, CSS MIME, immutable 캐시와 새 ETag를 확인해야 합니다.
- 자동 브라우저 계측은 데스크톱과 375px 모바일을 확인했지만 PC Chrome, iPhone Safari, Android Chrome 실기기를 대신하지 않습니다.

## V22.8.14 홈 전체 조회 버튼

- 버튼은 월 기록이 10건을 넘고 기본 최근 10건 보기일 때만 나타납니다. 운영 배포 후 실제 11건 이상인 가계부에서 확인해야 합니다.
- 오래 열어둔 탭은 V22.8.13 CSS를 유지할 수 있으므로 배포 후 한 번 새로고침합니다.
- `/assets/accountbook-shell-v22814.css`의 200 응답, CSS MIME, immutable 캐시와 새 ETag를 확인해야 합니다.

## V22.8.13 다크모드 전체 보정

- 로컬 브라우저 계측은 홈·메뉴·분석·예산·정산·설정의 라이트·다크와 내 설정의 네 컬러톤을 확인했지만 PC Chrome, iPhone Safari, Android Chrome 실기기를 대신하지 않습니다.
- 운영 배포 후 `/assets/accountbook-shell-v22813.css`의 200 응답, CSS MIME, immutable 캐시와 새 ETag를 확인해야 합니다.
- 오래 열어둔 탭은 V22.8.12 HTML·CSS를 계속 표시할 수 있으므로 배포 후 한 번 새로고침해야 합니다.
- 화면 설정은 계속 브라우저 `localStorage`에 기기별로 저장되며 다른 기기로 동기화되지 않습니다.

## V22.8.12 접근성 테마

- 화면 모드와 컬러톤은 개인정보를 저장하지 않는 브라우저 `localStorage`에 기기별로 저장됩니다. 다른 PC나 휴대전화에는 자동 동기화되지 않습니다.
- `시스템` 모드는 운영체제의 라이트·다크 설정 변경을 즉시 반영합니다. 브라우저가 색상 선호 미디어 쿼리를 지원하지 않으면 라이트 모드로 동작합니다.
- 자동 검증은 테마 자원, 저장 동작, 고정 대비 토큰과 주요 HTML 연결을 확인하지만 PC Chrome, iPhone Safari, Android Chrome의 실제 화면을 대신하지 않습니다.
- 운영 배포 후 `/assets/accountbook-shell-v22812.css`와 `/assets/accountbook-theme-v22812.js`의 200 응답, MIME, immutable 캐시와 네 가지 컬러톤을 확인해야 합니다.

## V22.8.11 홈 UX 셸 수동 확인 범위

- 자동 검증은 마크업, 정적 자원, 접근성 보호, 성능 예산을 확인하지만 PC Chrome, iPhone Safari, Android Chrome의 실제 보이는 결과를 대신하지 않습니다.
- 운영 배포 후 `/assets/accountbook-shell-v22811.css`와 `/assets/mobile-home-shell-v22811.js`의 200 응답, Content-Type, 캐시 재사용을 확인해야 합니다.
- 새 셸은 화면 내부 데이터·폼을 재작성하지 않습니다. 일부 오래된 화면의 고유 레이아웃은 공통 토큰만 적용되고 구조는 그대로 유지됩니다.

## V22.8.10 홈 성능 측정 범위

- 자동 측정값은 고정 QA 데이터에서 DB 요청 수와 생성 HTML 크기를 비교한 결과입니다. 실제 체감 시간은 Supabase 리전, 운영 데이터 행 수, Cloudflare 위치, 기기와 네트워크에 따라 달라집니다.
- 공통 CSS·JavaScript는 첫 방문에 한 번 내려받아야 합니다. 이후에는 버전형 immutable 캐시를 재사용하므로 반복 진입과 새로고침에서 이득이 커집니다.
- 개인 데이터 HTML은 보안을 위해 계속 `no-store`입니다. 브라우저 캐시로 가계부 내용을 재사용하지 않습니다.
- 이번 버전에는 추측성 DB 인덱스를 추가하지 않았습니다. 운영 배포 뒤에도 느리다면 Supabase 실행 계획과 느린 쿼리 로그를 확인해야 합니다.
- 브라우저·프록시가 `/assets/mobile-home-v22810.css` 또는 `.js`를 차단하면 화면 스타일이나 입력 보조가 정상 동작하지 않을 수 있습니다. 배포 후 두 경로의 200 응답과 Content-Type을 확인합니다.

## 카카오 그룹 챗봇 beta 규격

- 제공된 그룹 챗봇 스킬 서버 개발 가이드 v1.11.1은 그룹 응답에서 QuickReplies, CommerceCard, Carousel을 지원하지 않는 것으로 정리되어 있습니다. V22.8.7부터 그룹 요청에서 이를 제거하고 V22.8.11도 같은 정책을 유지하지만, 1:1 챗봇의 일반 응답 규격과는 차이가 있으므로 카카오가 beta 규격을 갱신하면 출시 전에 다시 확인해야 합니다.
- 그룹 챗봇 사용에는 카카오의 제휴·권한 부여와 운영 채널 심사가 필요할 수 있으며 코드 배포만으로 활성화되지 않습니다.
- 챗봇은 그룹방 전체 대화를 읽지 않습니다. 챗봇에게 직접 전달된 요청만 처리합니다.
- Event API 기반 선톡과 자동 푸시는 이번 버전에 포함하지 않았습니다.
- `plusfriendUserKey`는 신규 식별자로 사용하지 않지만 기존 저장 사용자 연결을 보호하기 위해 레거시 폴백이 남아 있습니다. 실제 기존 데이터 마이그레이션 계획 없이 제거하면 안 됩니다.

## 카카오톡 사진 영수증

카카오톡 대화방에 올린 사진을 챗봇이 직접 OCR 하는 기능은 현재 제공하지 않습니다. 사진을 기기에 저장한 뒤 웹의 `영수증 등록`에서 `앨범에서 선택`을 사용하거나, 전자영수증 문자를 복사해 붙여넣어야 합니다.

## OCR 네트워크와 기기 차이

- 첫 OCR 실행은 고정 버전 Tesseract와 한국어 데이터를 내려받기 때문에 이후 실행보다 느릴 수 있습니다.
- 네트워크 차단 또는 오프라인 환경에서는 사진 인식이 시작되지 않으며 직접 입력으로 대체할 수 있습니다.
- HEIC 디코딩 지원은 브라우저와 OS에 따라 다릅니다. 실패하면 사진 화면을 캡처해 JPG·PNG로 다시 선택합니다.
- 90초 안에 완료되지 않으면 자동 중단되며 사진을 더 가까이 찍거나 해상도를 낮춰 다시 시도합니다.

## 앨범·카메라 권한

권한 창과 앨범 선택 화면은 운영체제와 브라우저가 제공합니다. 서비스는 사용자가 해당 버튼을 눌렀을 때만 접근을 요청하며, 거부된 권한은 기기 설정에서 다시 허용해야 할 수 있습니다.

## 카카오 로그인 KOE006

KOE006은 배포 코드만으로 해결되지 않을 수 있습니다. 카카오 개발자 콘솔의 앱, REST API 키, 정확한 Redirect URI, 활성화 상태가 실제 인가 요청과 일치하는지 확인해야 합니다. Client Secret 값은 소스나 ZIP에 포함하지 않습니다.

## 카카오 영구 삭제 재인증

- V22.8.9는 기존 카카오 로그인 설정과 같은 Redirect URI를 사용하고 `prompt=login`으로 로그인 화면을 다시 요청합니다.
- 카카오 계정 세션·브라우저 정책·인앱 브라우저 상태에 따라 화면 전환 방식이 달라질 수 있으므로 실제 PC·iPhone·Android에서 확인해야 합니다.
- 다른 카카오 계정으로 인증하면 현재 계정과 ID가 달라 삭제가 차단됩니다.
- 취소·만료·네트워크 실패 시 가계부는 변경되지 않으며 다시 본인 확인해야 합니다.
- 카카오 로그인이 운영에서 꺼져 있거나 설정이 불완전하면 `내 계정·보안`에서 계정 로그인 비밀번호를 설정해 삭제를 확인할 수 있습니다.

## 삭제와 수동 연결해제 안내 구분

V22.8.9는 새 개인정보·감사 데이터를 저장하지 않기 위해 삭제된 연결과 사용자가 직접 해제한 연결을 서버 응답에서 하나의 안전 문구로 안내합니다. 두 경우 모두 기록은 저장하지 않습니다. 운영 문의에서 정확한 사유 구분이 실제로 필요하다고 확인되기 전에는 장기 이력 저장을 추가하지 않습니다.

## 배포 주소

Sites 배포 주소와 `ttokttok-accountbook.com` Cloudflare 운영 도메인은 별도 배포 대상입니다. Sites에서 검증된 코드가 운영 도메인에 자동 반영된다고 가정하지 않습니다.
