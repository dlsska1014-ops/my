# V22.5.4 최종 적용 체크리스트

## 배포 전

- [ ] 현재 Worker 코드와 환경변수 백업
- [ ] Supabase 운영 데이터 백업
- [ ] V22.5.3 이하 ZIP이 아니라 V22.5.4 ZIP인지 확인
- [ ] OpenBuilder 시나리오·블록·Skill URL은 변경하지 않음
- [ ] Supabase 신규 SQL 적용은 필요하지 않음

## Worker 배포

- [ ] 패키지의 `src/index.js` 전체 반영
- [ ] `/health`가 `V22.5.4-CONVERSATION-STATE-INTEGRITY-HARDENING` 반환
- [ ] `/kakao-command-menu.json` 대표 명령어 10개 확인

## 최소 운영 확인 8개

1. `새 가계부 만들기 → 5번 → 테스트 가계부 → 응`
2. 생성 중 `도움말`, `남은 예산`이 이름으로 저장되지 않는지 확인
3. `예산 설정 → 카테고리별 예산 → 식비 → 100만원 → 응`
4. 미연결 단톡방에서 거래가 저장되지 않는지 확인
5. owner/admin으로 단톡방 연결 후 거래 결과에 가계부 이름 확인
6. 다른 가계부로 재연결할 때 현재·변경 가계부와 확인 단계 확인
7. 거래 후 `지출자 변경`, 이어서 금액 수정 확인
8. `삭제`만으로 삭제되지 않고 `삭제 확인`을 요구하는지 확인

## 대표 명령어 수동 설정

`KAKAO_REPRESENTATIVE_COMMANDS_V22_5_4.md`의 10개 문구를 그대로 등록합니다. 설명은 세 항목에만 입력합니다.

## 이상 발생 시

- `/nlu-ops`에서 의도·폴백·확인 질문 확인
- `/identity-audit`에서 웹/챗봇 계정 분리 확인
- `/group-household-links`에서 방별 연결 확인
- 같은 저장 문장을 반복하지 말고 `오늘 기록 보기`로 먼저 확인
- V22.5.3으로 되돌리기 전에 운영 DB 백업과 신규 방별 연결 설정을 보존
