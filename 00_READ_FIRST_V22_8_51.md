# V22.8.51 입력 금액·런타임 정규식 수정 — 먼저 읽기

버전: `V22.8.51-INPUT-AMOUNT-RUNTIME-REGEX-FIX`
기준: V22.8.50 UI/UX 4단계 누적본

## 이번 릴리스 성격

기능 추가가 아니라 V22.8.50 전체 점검(`FULL_INSPECTION_REPORT_V22_8_50.md`)에서
확인된 결함을 고치는 안정화 릴리스입니다.

## 반드시 알아야 할 변경

- **거래 금액 입력이 다시 정상 동작합니다.** 웹 폼의 금액 칸이 카카오 발화용
  자연어 추출기를 거치지 않고 전용 숫자 파서를 사용합니다. 이전에는 1,000원
  미만 금액이 `금액을 입력하세요.` 오류로 거부됐습니다.
- **거래 금액 상한 20억 원이 생겼습니다.** 초과 입력은 저장 전에 차단합니다.
- **`/app` 스마트 한 줄 입력 파서가 복구됐습니다.** 배포 자산의 정규식
  이스케이프 소실을 고쳤습니다.
- **변경된 immutable 자산 주소가 바뀌었습니다.**
  - `/assets/mobile-home-v22810.js` → `/assets/mobile-home-v22851.js`
  - `/assets/mobile-home-shell-v22811.js` → `/assets/mobile-home-shell-v22851.js`
  - 배포 후 오래 열려 있던 탭은 한 번 새로고침해야 합니다.
  - CSS(`/assets/mobile-home-v22810.css`)는 변경이 없어 주소를 유지합니다.
- **월 파라미터 검증이 강화됐습니다.** `?month=2026-13` 같은 범위 밖 값은
  현재 월로 대체합니다. 이전에는 500으로 끝났습니다.
- **예산 저장이 빈 값·문자 입력을 거부합니다.** 사용자가 의도한 `0`은 그대로
  저장하고, 해석 불가한 입력만 막아 기존 예산을 지킵니다.

## 배포 전 확인

1. `node .codex/scripts/verify-repository.mjs` — 체크섬 55개, 자동 검사 1129개
2. `RELEASE-CHECKLIST.md`의 운영 수동 확인 항목
3. 배포 후 `/app`에서 500원 저장, 한 줄 입력 `커피 5천` 채우기, 금액 콤마 표시 확인

## 변경하지 않은 것

신규 SQL, Supabase 스키마, 환경변수, Secret, Kakao Developers, OpenBuilder 설정은
변경이 없습니다. V22.8.46 참여자 역할 SQL은 이미 적용된 상태를 전제로 합니다.
