# V22.8.53 성능·접근성·카카오 흐름 점검 보고서 (4라운드)

점검 기준: `V22.8.53-BUDGET-RATE-DISPLAY-FIX`

선행 점검
- 1라운드 `FULL_INSPECTION_REPORT_V22_8_50.md` — 기능 6건 (V22.8.51 수정)
- 2라운드 `SECURITY_AND_FEATURE_AUDIT_V22_8_51.md` — 보안 2건·기능 1건 (V22.8.52 수정)
- 3라운드 `DEEP_AUDIT_REPORT_V22_8_52.md` — 표시 1건 (V22.8.53 수정)

이번 라운드는 **대량 데이터에서의 성능 예산 준수, 접근성, 카카오 수정·삭제 세션
흐름**을 점검했습니다.

---

## 1. 요약

| 영역 | 검사 | 결과 |
| --- | ---: | --- |
| 대량 데이터 성능·예산 | 25 | **예산 위반 2건** |
| 접근성 | 36 | 이상 없음 |
| 카카오 수정·삭제·복구 세션 | 8 | 이상 없음 |
| **합계** | **69** | **위반 2건** |

이번 라운드의 발견은 **코드 결함이 아니라 `AGENTS.md`가 필수 보호 기준으로 정한
성능 예산과 실제 동작의 불일치**입니다. 데이터가 늘어나면 예산을 지킬 수 없는
구조이며, 현재 검증은 6행 픽스처만 측정해 이 사실을 잡지 못합니다.

---

## 2. P1 (Medium) — 개인 홈 HTML이 실사용 데이터에서 예산 35KB를 항상 초과

`AGENTS.md` 필수 보호 기준

> 기준 성능 예산은 `/my` 데이터 요청 4회 이하, `/app` 9회 이하, 개인 홈 HTML 35KB 이하이다.

### 측정 결과

| 월 거래 수 | `/app` HTML | 예산(35KiB=35,840) |
| ---: | ---: | --- |
| 6건 (현재 픽스처) | **34.9KiB** | 통과 (여유 900바이트) |
| 50건 | **45.1KiB** | 초과 |
| 100건 | 45.4KiB | 초과 |
| 400건 | 45.3KiB | 초과 |
| 3,200건 | 45.3KiB | 초과 |
| 8,000건 | 45.6KiB | 초과 |

**50건부터 초과하며 이후 ~45KiB에서 평탄해집니다.** 무한 증가는 아니지만,
현실적인 사용량이면 상시 29% 초과 상태입니다.

### HTML 구성 (3,200건 기준)

| 구획 | 크기 |
| --- | ---: |
| 피드 섹션 (카드 10장) | **21.5KiB** |
| 사이드 내비 | 5.7KiB |
| `datalist` 분류·결제 | 2.5KiB |
| `<head>` | 0.7KiB |

피드 카드 10장이 21.5KiB, **카드 1장당 약 2.1KiB**입니다. 6행일 때는 카드가 6장뿐이라
전체가 34.9KiB로 예산 안에 들어갑니다.

### 검증이 이 사실을 잡지 못하는 이유

`validation/validate-performance-v22811.mjs`

```js
ok(homeBytes < 35 * 1024, `personal home HTML stays below 35 KiB (${homeBytes} bytes)`);
```

이 검사는 **6행 픽스처만** 측정합니다. 34.9KiB로 900바이트 여유를 두고 통과하므로,
실사용 데이터에서 45KiB가 되는 것을 알 수 없습니다. 1·2라운드에서 발견한 "배포 자산
정규식 손실"과 같은 종류의 사각지대입니다.

### 권고

셋 중 하나를 선택해야 합니다.

1. **피드 카드 마크업 축소** — 카드당 2.1KiB는 큽니다. 인라인 `style` 속성과 반복
   래퍼를 CSS 클래스로 옮기면 상당 부분 줄어듭니다. 예산을 지키는 정공법입니다.
2. **기본 피드 건수 축소** — 10장 → 5장이면 약 10KiB 줄어듭니다. 다만 사용자가 보는
   정보가 줄어듭니다.
3. **예산을 현실에 맞게 개정** — 35KB가 더 이상 유효하지 않다면 `AGENTS.md`의 숫자를
   근거와 함께 올립니다.

어느 쪽을 택하든 **검증을 현실적인 데이터량(예: 200행)으로 측정하도록 바꿔야**
합니다. 그러지 않으면 같은 사각지대가 남습니다.

---

## 3. P2 (Medium) — `/app` DB 호출이 데이터량에 비례해 늘어 예산 9회를 초과

### 측정 결과

| 월 거래 수 | `/app` DB 호출 | 응답 시간 | 예산(9회) |
| ---: | ---: | ---: | --- |
| 6건 | 10회 | 3ms | 초과 |
| 50~400건 | 9회 | 8~20ms | 통과 |
| 800건 | 10회 | 60ms | 초과 |
| 1,600건 | 12회 | 180ms | 초과 |
| 3,200건 | 15회 | 450ms | 초과 |
| 8,000건 | **25회** | **1,003ms** | 초과 |

`/my`는 3회로 4회 예산을 지킵니다.

### 원인 — 중복 질의는 없고, 페이지네이션 구조 자체

1,200행에서 관측한 `transactions` 질의 전체:

```
1. select=id,household_id,user_id,type,amount,...  2026-07-01~2026-08-01  limit=500 offset=0
2. select=type,amount                             2026-06-01~2026-07-01  limit=500 offset=0
3. select=id,household_id,user_id,type,amount,...  2026-07-01~2026-08-01  limit=500 offset=500
4. select=id,household_id,user_id,type,amount,...  2026-07-01~2026-08-01  limit=500 offset=1000
```

- 1·3·4는 **당월 전체 행**을 500행씩 페이징 (`fetchPostgrestRows`, `maxRows: 10000`)
- 2는 전월 합계 비교용 (`fetchMonthAmountRows`)

**중복 질의는 없습니다.** 질의 수는 `ceil(당월 행수 / 500) + 1 + 고정 8회` 구조이므로,
당월 거래가 500건을 넘으면 9회 예산을 지킬 수 없습니다.

근본 원인은 **홈이 합계·차트·카드 10장을 만들기 위해 당월 전체 행을 가져오는
것**입니다. 8,000건 월이면 8,000행을 16번에 걸쳐 받아 그중 10장만 렌더합니다.

### 권고

- **단기**: 페이지 크기를 500 → 2,000으로 올리면 8,000행이 16회 → 4회로 줄어듭니다.
  코드 한 줄이며 SQL 변경이 없습니다. 다만 응답 페이로드가 커집니다.
- **정공법**: 월 합계·분류별 합계를 집계 질의(또는 RPC)로 받고, 목록은 필요한
  10~50행만 가져옵니다. `AGENTS.md`가 근거 없는 SQL 추가를 금지하므로, 실행 계획을
  포함한 별도 설계·승인이 필요합니다.
- `KNOWN-ISSUES.md`의 V22.8.38 항목이 이미 "한 가계부에서 최대 100,000건을 스캔"하는
  구조를 인지하고 있습니다. 이번 측정은 그 한계가 홈 화면에서 실제로 나타나는 지점을
  수치로 확인한 것입니다.

---

## 4. 이상 없음을 확인한 영역

### 접근성 (36개 전부 통과)

주요 화면 10개(`/app`·`/budgets`·`/my/analysis`·`/my/settings`·`/my/households`·
`/receipts`·`/payment-methods`·`/settlement-summary`·`/my/backup`·`/keyword-guide`)

| 항목 | 결과 |
| --- | --- |
| `<html lang="ko">`·`<title>`·`<h1>`·`<main>`·viewport | 10/10 |
| 접근 이름 없는 `<button>` | 0개 |
| label·`aria-label` 없는 `<input>` | 0개 |
| 접근 이름 없는 `<select>` | 0개 |
| `alt` 없는 `<img>` | 0개 |
| 오류·성공 안내의 `aria-live`/`role=alert` | 적용됨 |

이전 릴리스들의 테마 대비·포커스 작업이 반영되어 정적 접근성은 견고합니다.
참고로 장식용 `<i>` 중 `aria-hidden`이 없는 것이 화면당 1개 있으나, 내용이 텍스트
아이콘이라 실질 영향은 낮습니다.

### 카카오 수정·삭제·복구 세션 (8개 전부 통과)

| 흐름 | 결과 |
| --- | --- |
| 거래 입력 → `오늘 기록 보기` | `01번 - 점심 / 12,000원 / 국민카드 / 식비` 형식으로 번호 목록 표시 |
| `수정 01번 금액 15000` | 정확히 반영 |
| `삭제 01번` | 처리 및 안내 |
| `복구` | 응답 정상 |
| `취소` | 세션 종료, 오류 없음 |
| 없는 번호(`99번`) | "찾지 못" 안내 |
| **viewer 권한의 카카오 입력** | **차단** |

번호 표기가 `01번`(2자리)이라 첫 프로브가 오탐을 냈으나, 실제 흐름은 정상입니다.

---

## 5. 확인하지 못한 항목

- **운영 환경 실제 지연** — 측정은 인메모리 백엔드 기준이라 네트워크 왕복이 없습니다.
  DB 호출 25회는 운영 Supabase에서 훨씬 큰 지연으로 나타납니다. 이번 수치는
  **호출 횟수**가 근거이며 시간은 하한선으로만 봐야 합니다.
- **실기기 접근성** — 스크린리더(VoiceOver·TalkBack) 실사용, 가상 키보드, 포커스
  이동은 정적 검사로 대체할 수 없습니다.
- **`accountbook_apply_recurring_v227`** — 4라운드 연속 픽스처 미구현으로 검증 범위 밖.
- **`/skill` 근본 위험** — `botUserKey`가 만료·회수 없는 자격증명이라는 점은 그대로입니다.

---

## 6. 네 라운드 종합

| 라운드 | 범위 | 발견 | 조치 |
| --- | --- | ---: | --- |
| 1 | 기능·라우트·권한·입력 경계 | 6 | V22.8.51 수정 완료 |
| 2 | 비인증 보안·인젝션·E2E | 3 | V22.8.52 수정(S1은 부분) |
| 3 | 금액 정확성·동시성·실패·수명주기 | 1 | V22.8.53 수정 완료 |
| 4 | 성능 예산·접근성·카카오 세션 | 2 | **미조치 — 판단 필요** |

기능·보안·데이터 무결성 계층은 네 라운드를 거쳐 정리됐습니다. 남은 것은 성격이
다른 두 가지입니다.

1. **성능 예산 불일치** (이번 라운드) — 코드 결함이 아니라 예산과 구조의 괴리입니다.
   피드 마크업 축소·페이지 크기 조정·예산 개정 중 어느 방향인지 결정이 필요합니다.
2. **`/skill` 호출자 인증** (2라운드) — Kakao OpenBuilder 콘솔에서 커스텀 헤더 지원
   여부 확인이 선행되어야 합니다.

두 건 모두 **판단이 먼저 필요한 사안**이라 이번 라운드에서는 코드를 바꾸지 않았습니다.
