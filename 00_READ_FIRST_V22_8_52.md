# V22.8.52 스킬 IP 가드·CSV 안전·이름 검증 — 먼저 읽기

버전: `V22.8.52-SKILL-IP-GUARD-CSV-SAFE-NAME-FIX`
기준: V22.8.51 입력 금액·런타임 정규식 수정

## 이번 릴리스 성격

`SECURITY_AND_FEATURE_AUDIT_V22_8_51.md`에서 확인된 보안 2건과 기능 1건을 고치는
보안 릴리스입니다. 기능 추가는 없습니다.

## 반드시 알아야 할 변경

- **카카오 스킬에 IP 단위 상한이 생겼습니다.** `/skill`은 호출자를 인증하지 않고
  요청 본문의 `botUserKey`를 신원으로 쓰므로, 그 값을 바꿔가며 보내면 사용자별
  제한이 그대로 우회됐습니다. 기본 분당 3,000회의 IP 상한을 추가했습니다.
  - 정상 그룹 챗봇 트래픽보다 훨씬 높은 값이라 운영 영향은 없습니다.
  - 필요하면 `SKILL_IP_GUARD_LIMIT` 환경변수로 조정합니다(60~100000).
  - **이는 완화책이며 근본 해결이 아닙니다.** 아래 "남은 위험"을 확인하세요.
- **CSV 내보내기가 수식 실행을 막습니다.** `=`·`+`·`-`·`@`·탭·CR로 시작하는 셀
  앞에 작은따옴표를 붙입니다. 순수 숫자는 그대로 둡니다.
- **웹 가계부 이름 검증이 완화됐습니다.** `가족 생활비`, `생활비`, `모임`, `여행`
  같은 자연스러운 이름을 이제 만들 수 있습니다. 카카오 대화의 종류 선택 흐름은
  그대로입니다.

## 남은 위험 — 반드시 확인

IP 상한은 대량 남용만 막습니다. **`botUserKey`가 여전히 만료·회수 수단이 없는
사실상의 자격증명**이므로, 값 하나가 유출되면 그 사용자의 가계부를 읽고 쓸 수
있습니다.

근본 해결은 Kakao OpenBuilder 스킬 설정에 **공유 시크릿 헤더**를 추가하고 Worker가
검증하는 것입니다. 콘솔에서 커스텀 헤더 설정이 가능한지 확인한 뒤 다음 단계로
진행하세요. 운영 로그·스크린샷·문의 캡처에 `botUserKey`가 남지 않도록 관리해야
합니다.

## 배포 전 확인

1. `node .codex/scripts/verify-repository.mjs` — 체크섬 57개, 자동 검사 1141개
2. `RELEASE-CHECKLIST.md`의 운영 수동 확인 항목
3. 배포 후 카카오 챗봇 정상 응답, CSV 다운로드 열기, `가족 생활비` 이름 생성 확인

## 변경하지 않은 것

신규 SQL, Supabase 스키마, Secret, Kakao Developers, OpenBuilder 설정 변경은
없습니다. `SKILL_IP_GUARD_LIMIT`은 선택 환경변수이며 미설정 시 기본값 3,000을
사용합니다. 정적 자산 내용 변경이 없어 자산 주소도 그대로입니다.
