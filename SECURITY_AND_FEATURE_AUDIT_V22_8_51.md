# V22.8.51 보안·기능 심층 점검 보고서

점검 기준: `VERSION.txt` = `V22.8.51-INPUT-AMOUNT-RUNTIME-REGEX-FIX` (main `bfac141`)
선행 점검: `FULL_INSPECTION_REPORT_V22_8_50.md` (기능 결함 6건, V22.8.51에서 수정 완료)

이번 점검은 선행 점검이 다루지 않은 **비인증 관점의 보안**과 **기능 단위 E2E**에
집중했습니다.

---

## 0. 수정 상태 (V22.8.52에서 반영)

| 항목 | 상태 |
| --- | --- |
| S1 `/skill` 호출자 인증 없음 | **부분 조치** — IP 단위 상한(`SKILL_IP_GUARD_LIMIT`, 기본 3,000/분) 추가. 공유 시크릿 헤더는 OpenBuilder 콘솔 확인 후 별도 진행 |
| S2 CSV 수식 인젝션 | 수정됨 — `csvSafeText()`로 전 내보내기 경로 이스케이프 |
| F1 웹 가계부 이름 거부 | 수정됨 — `sanitizeWebHouseholdNameInput()` 분리 |

**S1은 아직 열려 있습니다.** IP 상한은 대량 남용을 막을 뿐,
`botUserKey`가 만료·회수 없는 자격증명이라는 점은 그대로입니다. 값이 유출되면
해당 사용자의 가계부를 읽고 쓸 수 있습니다.

수정 내역은 `VALIDATION_REPORT_V22_8_52.md`, 배포 주의사항은
`00_READ_FIRST_V22_8_52.md`를 확인하세요.

아래 본문은 수정 전 V22.8.51 기준의 점검 기록입니다.

---

## 1. 요약

| 구분 | 결과 |
| --- | --- |
| 무인증 GET 프로브 (258개) | 운영 데이터·비밀값 노출 **0건** |
| 반사형 XSS·오픈 리다이렉트·CRLF | **0건** |
| 권한 경계 (관리자/백업/운영 23개 라우트) | 우회 **0건** |
| 기능 E2E (11개 영역 44개 항목) | 기능 결함 **1건** |
| **신규 보안 결함** | **2건** (High 1, Medium 1) |
| **신규 기능 결함** | **1건** (Medium) |

---

## 2. S1 (High) — 카카오 스킬 엔드포인트에 호출자 인증이 없음

### 확인된 사실

`POST /skill`은 호출자를 검증하지 않습니다. 공유 시크릿, 서명 검증, IP 허용목록이
코드 전체에 존재하지 않습니다(`SKILL_SECRET`·서명 헤더 검색 결과 없음). 요청 본문의
`botUserKey`를 그대로 사용자 신원으로 신뢰합니다.

동시에 IP 단위 트래픽 가드가 이 경로에서 **명시적으로 비활성**입니다.

`src/index.js:499`

```js
if (p === "/skill") return false; // 그룹 챗봇 대량 유입은 IP 단위 가드 대신 botUserKey 단위 스킬 가드로 처리
```

남은 방어는 `checkSkillRateLimit(userKey)` 하나인데, **그 키를 공격자가 요청 본문에
직접 지정**하므로 값을 바꾸며 보내면 그대로 무력화됩니다.

### 재현 결과 (인메모리 백엔드, 인증 없음)

| 시나리오 | 결과 |
| --- | --- |
| 임의 `botUserKey`로 `/skill` 호출 | 200 정상 응답, `users` 행 1건 신규 생성 |
| 같은 키로 70회 연속 호출 | 10회 제한됨 (키 단위 제한 동작) |
| **키를 매번 바꿔 70회 호출** | **0회 제한** — 제한 완전 우회 |
| 잘못된 초대코드 8회 (키 회전) | **0회 차단**, 매번 정상 오류 응답 |
| 알려진 사용자 키로 `이번 달 요약` | 해당 사용자의 **가계부 목록 노출** |
| 알려진 사용자 키로 `점심 12000 국민카드` | 해당 가계부에 **거래 저장 성공** |

마지막 두 줄이 핵심입니다. `botUserKey`는 추가 검증 없는 **베어러 자격증명**으로
동작하며, 값 하나만 알면 그 사용자의 가계부를 읽고 쓸 수 있습니다.

### 영향

1. **자원 소모 공격** — 인증·IP 제한 없이 운영 Supabase의 `users` 테이블에 무제한
   행을 생성할 수 있습니다. 현재 가장 실현 가능성이 높은 피해입니다.
2. **계정 탈취** — `botUserKey`가 로그·스크린샷·문의 캡처·운영 화면 등으로 한 번
   유출되면 그 사용자의 가계부 전체가 읽기·쓰기로 열립니다. 만료도 회수 수단도
   없습니다.
3. **초대코드 대입** — 무제한 시도가 가능합니다. 다만 코드 공간이
   32<sup>8</sup> ≈ 1.1×10<sup>12</sup>(`makeInviteCode`)이라 특정 가계부를 노린
   대입은 현실적이지 않습니다. 실질 위험은 1·2번입니다.

`/deployment-check`는 이 상태를 공개적으로 알려줍니다.

```json
"skill_ip_guard": "disabled_for_group_chatbot; use botUserKey guard instead"
```

### 권고

우선순위 순입니다.

1. **공유 시크릿 헤더 검증** — OpenBuilder 스킬 설정에 커스텀 헤더를 추가하고
   Worker에서 `constantTimeTextEqual`로 검증합니다. 기존 `verifyCronExecutionAuth`와
   동일한 패턴을 재사용할 수 있습니다. 미설정 환경에서도 기존처럼 동작하도록
   환경변수 존재 시에만 강제하면 단계적 적용이 가능합니다.
2. **IP 단위 하한 가드 복원** — 그룹 챗봇 대량 유입을 고려하더라도 카카오 서버
   IP에서 오는 정상 트래픽보다 훨씬 높은 한도(예: 분당 3,000)를 두면 대량 신규
   `botUserKey` 생성은 막으면서 정상 운영에는 영향이 없습니다.
3. **신규 사용자 생성 속도 제한** — 기존 `users` 행이 없는 `botUserKey`에 대해서만
   별도의 IP 단위 한도를 적용합니다.

SQL 변경 없이 Worker와 OpenBuilder 설정만으로 1·2번을 적용할 수 있습니다.

---

## 3. S2 (Medium) — CSV 내보내기 수식 인젝션

`/my/backup.csv`와 `/admin/csv`가 셀 값을 그대로 내보내므로, `=`·`+`·`-`·`@`로
시작하는 메모가 Excel·Google Sheets에서 **수식으로 실행**됩니다.

재현 — 메모에 `@SUM(1+1)`, `=cmd|'/c calc'!A1`을 넣고 내보낸 결과:

```
/my/backup.csv : 2026-07-12,지출,1000,기타,@SUM(1+1),,Bin,web_user,transactions-5
/admin/csv     : "2026-07-12","expense","1000","기타","@SUM(1+1)",""
```

`/admin/csv`는 따옴표로 감싸지만, 따옴표는 CSV 구분자 이스케이프일 뿐 수식 실행을
막지 못합니다.

가계부는 여러 사람이 함께 쓰므로, 참여자 한 명이 넣은 메모가 다른 참여자의 PC에서
실행될 수 있습니다.

**권고** — 셀 값이 `= + - @ 탭 캐리지리턴`으로 시작하면 앞에 작은따옴표(`'`)를 붙이거나
값을 `"\t"`로 시작하게 만듭니다. 내보내기 헬퍼 한 곳에서 처리하면 됩니다.

---

## 4. F1 (Medium) — 웹 가계부 생성에서 자연스러운 이름이 거부됨

`/my/create`의 `sanitizeHouseholdNameInput()`이 **카카오 대화 흐름용 선택지 키워드**를
그대로 이름 검증에 사용합니다.

`src/index.js:sanitizeHouseholdNameInput`

```js
if (isExplicitKakaoTopLevelCommandV2254(t) || isKakaoReservedCreateFlowReply(t)
    || parseKakaoCreateKind(t) || isUnsafeKakaoNameInputV2254(t)) return "";
```

`parseKakaoCreateKind()`는 카카오 채팅에서 `1. 가족 생활비 / 2. 부부·커플 /
3. 모임 회비 / 4. 여행 경비` 중 하나를 고르게 하려고 만든 함수입니다. 채팅에서는
"가족"이 이름인지 선택지인지 모호하므로 타당하지만, **웹 폼에는 그 모호함이
없습니다.**

확인된 거부 목록 (24개 전부 거부):

```
가족, 생활비, 가족 생활비, 우리집, 우리 집, 부부, 커플, 부부 커플, 둘이,
모임, 회비, 모임 회비, 동호회, 친목, 여행, 여행비, 여행 경비, 휴가,
개인, 기타, 일반, 직접, 그냥, 직접 입력
```

반면 `우리집 생활비`, `가족 여행`, `부부 생활비`, `제주 여행`은 정상 생성됩니다.
즉 **가장 짧고 자연스러운 이름일수록 거부**됩니다.

오류 문구도 원인을 설명하지 못합니다.

> 가계부 이름은 2~40자의 일반 이름으로 입력해 주세요. 명령어·전화번호·초대코드·금액만 있는 이름은 사용할 수 없습니다.

"가족 생활비"는 명령어도 전화번호도 초대코드도 금액도 아니므로, 사용자는 무엇을
고쳐야 하는지 알 수 없습니다.

**권고** — `parseKakaoCreateKind` 검사는 카카오 스킬 경로에만 적용하고 웹 폼 검증에서는
제외합니다. 웹에서는 길이·안전성 검사(`isUnsafeKakaoNameInputV2254`)만으로 충분합니다.

---

## 5. 이상 없음을 확인한 영역

### 비인증 접근 (258개 GET 라우트)

운영 데이터(가계부명·거래·금액·초대코드·닉네임)와 비밀값(서비스롤 키·세션 시크릿·
관리자 토큰·cron 시크릿·Supabase URL) 노출 **0건**. 5xx는 카카오 로그인 미설정에
따른 `/auth/kakao/callback` 503 하나뿐입니다.

### 인젝션·리다이렉트

| 항목 | 결과 |
| --- | --- |
| 반사형 XSS (13개 파라미터 × 전 라우트) | 0건 |
| `/u/api/tx/search` 반사 | JSON + `nosniff` — 실행 불가 |
| 오픈 리다이렉트 (`//evil`, `/\evil`, `///evil`, `javascript:`, `\\evil`) | 전부 차단, 동일 출처 경로로 대체 |
| CRLF 헤더 인젝션 (`Set-Cookie`·`Location`) | 주입 불가 |

### 인증·권한

| 항목 | 결과 |
| --- | --- |
| 관리자 전용 23개 라우트 | 무인증·일반사용자 전부 303/401 차단 |
| `/admin-view` 무인증 200 | 로그인 폼만 렌더, 데이터 없음 (정상) |
| 관리자 로그인 무차별 대입 | 8회 후 429 |
| 위조 세션 쿠키 4종 | 전부 거부, 데이터 노출 없음 |
| `/cron/*` | 무인증 401, URL `?key=` 401, 잘못된 헤더 401, 올바른 헤더만 200, GET 404 |
| `/my/import` 미리보기 토큰 위조 | HMAC 검증으로 거부 |
| `/u/api/favorites`·`/u/api/goals` 타 가계부 쓰기 | 차단 |
| `/backup/*` 6개 경로 | 무인증·일반사용자 303 |
| `/admin/settings/password` | 무인증·일반사용자 401 |
| `/admin/identity/merge` | 일반사용자 차단 |
| viewer 권한의 초대코드 조회 | 노출 없음 (`/my/members` 403) |

### 기능 E2E (44개 항목 중 43개 통과)

| 영역 | 결과 |
| --- | --- |
| 거래 생성·수정·삭제·목록·검색 | 6/6 |
| 예산 저장·표시·삭제·알림 | 4/4 |
| 고정지출 저장·표시·삭제 | 3/3 |
| 분석·달력·리포트·연간·스마트도구 | 5/5 (`/analysis`·`/calendar`·`/categories`는 303 후 정상 렌더) |
| 정산 화면·완료 이력 | 2/2 |
| 목표·즐겨찾기 JSON API | 3/3 |
| 가계부 생성·참여·승인·잘못된 코드 거부 | 3/4 (F1) |
| 백업 CSV·화면·관리자 JSON | 3/3 |
| 영수증·분류·결제수단·정기지출·키워드 | 5/5 |
| 카카오 스킬 6개 명령 | 6/6 |
| `/health`·cron 정상 경로 | 2/2 |

---

## 6. 확인하지 못한 항목

- **운영 환경 실제 동작** — 모든 점검은 인메모리 백엔드 기준입니다. 운영 Supabase의
  RLS·GRANT가 Worker 서비스롤 키 뒤에서 어떻게 동작하는지는 별도 확인이 필요합니다.
- **카카오 서버의 실제 요청 형태** — S1 권고 1번(공유 시크릿)은 OpenBuilder에서
  커스텀 헤더 설정이 가능한지 콘솔에서 확인해야 합니다.
- **`accountbook_apply_recurring_v227`** — 픽스처 미구현으로 `/admin/recurring/apply`
  경로는 여전히 자동 검증 범위 밖입니다.
- **실기기·브라우저** — 접근성, 가상 키보드, 실제 CSV 열람 동작.

---

## 7. 권고 조치 순서

| 순위 | 항목 | 근거 |
| --- | --- | --- |
| 1 | **S1** `/skill` 공유 시크릿 + IP 하한 가드 | 인증 없는 공개 쓰기 경로 |
| 2 | **S2** CSV 내보내기 수식 이스케이프 | 공유 가계부 참여자 간 공격 가능 |
| 3 | **F1** 웹 가계부 이름 검증에서 채팅 키워드 제외 | 가장 자연스러운 이름이 거부됨 |

세 건 모두 신규 SQL·스키마 변경 없이 `src/index.js` 수정으로 해결할 수 있습니다.
S1의 공유 시크릿만 Kakao OpenBuilder 콘솔 설정이 함께 필요합니다.
