# V22.8.71 검증 보고서

검증일: 2026-08-03

## 점검 범위

순차 점검 **8단계 — 수정·삭제·복구 명령의 저장값 되읽기**입니다.

V22.8.69·V22.8.70 에서 입력 경로를 저장값 기준으로 훑었으니, 같은 방식을
**기록을 고치고 지우고 되살리는** 명령으로 넓혔습니다.
명령을 카카오 스킬로 보내고 **DB 에 저장된 행을 직접 읽어** 대조했습니다.

응답 문구를 믿지 않은 것이 핵심이었습니다. 아래 1번 결함은 "✅ 변경했어요"를
정확히 돌려주면서 아무것도 바꾸지 않았습니다.

## 1. 날짜 수정이 다른 날의 기록을 건드렸습니다

`parseKakaoEditCommandV4` 가 **문장 전체**에서 날짜 힌트를 뽑고 있었습니다.
`수정 01번 날짜 어제` 의 `어제` 는 바꿀 값인데, 대상 기록의 날짜로도 읽혀
어제의 01번을 잡았습니다.

| 입력 | 보정 전 | 보정 후 |
|---|---|---|
| `수정 01번 날짜 어제` | 어제 01번을 대상으로 잡고 "✅ 변경했어요" (오늘 01번 무변화) | 오늘 01번의 날짜가 어제로 바뀜 |
| `수정 01번 날짜 2026-08-01` | `2026-08-01 01번 기록을 찾지 못했어요` | 정상 변경 |

실패 문구가 스스로 제시하는 복사용 예시에 `👉 수정 01번 날짜 어제` 가 있습니다.
안내대로 따라 한 사람이 성공 응답을 받고도 아무것도 바꾸지 못했습니다.

날짜 힌트는 이제 `splitKakaoEditDatePrefixV4` 로 **명령 앞 접두부**에서만 읽습니다.
`어제 수정 01번 금액 5000` 같은 대상 지정은 그대로 동작합니다.

## 2. 연속 삭제 후 한 건만 복구됐습니다 (기록 소실)

되돌리기 버퍼가 사용자·대화당 **한 칸**이었습니다.
삭제마다 `되돌리려면: 복구 NN번` 이라고 안내하므로, 두 번 지운 사람은
두 건 모두 되돌릴 수 있다고 믿습니다. 실제로는 두 번째 삭제가 첫 번째를 덮어썼습니다.

| 순서 | 응답 | 보정 전 결과 | 보정 후 결과 |
|---|---|---|---|
| `삭제 01번` | `되돌리려면: 복구 01번` | — | — |
| `삭제 01번` | `되돌리려면: 복구 01번` | 첫 버퍼 덮어씀 | 두 건 모두 보관 |
| `복구 01번` | — | 둘째 건 복구 | 둘째 건 복구 |
| `복구 01번` | — | **복구할 삭제 기록이 없어요** | 첫째 건 복구 |

최근 삭제 **10건**까지 쌓고, 번호가 겹치면 **가장 최근에 지운 것부터** 되돌립니다.
복구가 확인된 건만 버퍼에서 빼므로 나머지는 계속 되돌릴 수 있습니다.
배포 전에 남아 있던 옛 한 칸짜리 버퍼도 그대로 읽습니다(`kakaoEditUndoItemsV4`).

## 3. 입력에서 통하는 금액 표기가 수정에서 막혔습니다

수정 경로만 `/^\d+$/` 로 검사하고 있었습니다.

| 입력 | 보정 전 | 보정 후 |
|---|---|---|
| `수정 01번 금액 6만원` | `숫자만 보내주세요` | 60,000원 |
| `수정 01번 금액 3만5천원` | `숫자만 보내주세요` | 35,000원 |
| `수정 01번 금액 1만5천` | `숫자만 보내주세요` | 15,000원 |
| `수정 01번 금액 12,345` | 12,345원 | 12,345원 |
| 메뉴 → `1` → `3만5천원` | `금액은 숫자로 입력해 주세요` | 35,000원 |

수정도 입력과 같은 `extractAmount` 를 씁니다. 다만 금액처럼 생긴 문구만
통과시켜(`KAKAO_EDIT_AMOUNT_SHAPE`) `사과` 같은 값은 계속 거절합니다.

## 4. 수정 세션에서 빠져나갈 방법을 알려 주지 않았습니다

수정 메뉴를 열어 둔 채 새 지출(`라면 3300`)을 보내면 확인 질문이 나오고,
`아니오` 라고 답해도 탈출 안내가 없어 같은 질문만 반복됐습니다.
저장은 되지 않지만 사용자는 갇힙니다.
`새로 기록하려면 '취소'를 먼저 보내주세요` 를 함께 안내합니다.

## 5. 문제가 없던 것

| 항목 | 결과 |
|---|---|
| 금액·내용·분류·결제수단 수정 | 저장값 일치 |
| 접두 날짜 대상 지정(`어제 수정 01번 …`) | 정확 |
| 메뉴 방식(`수정 01번` → 번호 → 값) | 정확 |
| 없는 번호(`수정 99번`·`삭제 99번`) | 정확히 안내 |
| 삭제 후 목록 번호 재배열 | 정확 |
| 복구 버퍼가 빈 상태 | 정직하게 안내 |

## 6. 성능·자원

| 항목 | 예산 | 실측 | 여유 |
|---|---|---|---|
| 기준 픽스처 홈 HTML | 35,840 B | 34,958 B | 882 B |
| 200행 실사용 홈 HTML | 45,056 B | 44,547 B | 509 B |

**HTML 바이트 변화 0** — `origin/main` 과 이 버전을 같은 조건에서 재어 동일했습니다.
자원 주소도 그대로입니다(모바일 홈 런타임 `v22870`, 셸 CSS `v22868`).

> 200행 실사용 수치는 픽스처가 날짜를 기준으로 만들어져 **측정일에 따라 흔들립니다.**
> V22.8.70 보고서의 44,216 B 는 그날 값이며, 이 버전이 늘린 것이 아닙니다.
> 다만 여유가 509 B 까지 줄었으므로 다음 변경에서는 카드 마크업 증가를 주의해야 합니다.

## 7. 자동 검사

- 신규 회귀 **58개** (`validation/validate-edit-restore-v22871.mjs`)
- 저장소 전체 **2,443개** 자동검사 통과
- ESM `default.fetch` 확인, 체크섬 매니페스트 일치

신규 회귀는 **검사 자체가 결함을 잡는지** 먼저 확인했습니다.
세 결함을 하나씩 되돌린 소스로 돌려 각각 다른 지점에서 실패하는 것을 봤습니다.
(첫 시도에서 검사가 세 번 잘못됐습니다 — 앞선 날짜 수정으로 목록 번호가 밀린 것,
복구된 행이 배열 끝에 다시 붙는 것을 위치로 대조한 것, 어제에 기록이 둘일 때
`어제 01번`이 모호해진 것. 모두 검사의 결함이라 대조 방식을 고쳤습니다.)

- 날짜 고정 해제 → `오늘 01번의 날짜가 실제로 어제로 바뀐다` 실패
- 버퍼 한 칸으로 되돌림 → `둘째 복구도 성공한다` 실패
- 금액 표기 좁힘 → `수정 NN번 금액 3만5천원 가 받아들여진다` 실패

신규 회귀가 고정하는 것:

- 수정 결과가 **응답 문구가 아니라 저장된 행**에 반영된다
- `수정 NN번 날짜 …` 이 오늘의 NN번을 바꾸고 다른 날 기록은 건드리지 않는다
- 접두 날짜(`어제 수정 …`)는 계속 대상 지정으로 동작한다
- 안내한 복구 번호대로 연속 삭제 두 건이 모두 되돌아온다
- 금액 표기 6종이 수정에서도 통하고, 금액 아닌 값은 계속 거절된다
- 수정 세션에서 빠져나가는 방법이 안내된다
- 배포 전에 저장된 옛 한 칸짜리 버퍼도 계속 복구된다

## 8. 운영 변경

- 신규 SQL 없음
- 환경변수·Secret 변경 없음
- Kakao Developers·OpenBuilder 변경 없음
- 자원 주소 변경 없음 (웹 탭 새로고침 불필요)

## 배포 후 확인 부탁드립니다

1. 카카오톡 `수정 01번 금액 3만5천원` → 받아들여지는지
2. 두 건을 연달아 삭제하고 안내받은 번호로 **둘 다** 복구되는지
3. `수정 01번 날짜 어제` → **오늘 01번**의 날짜가 바뀌는지
4. 수정 메뉴 중 새 지출을 보냈을 때 `취소` 안내가 보이는지

> ⚠️ **이 배포는 과거에 잘못 처리된 기록을 되돌리지 않습니다.**
> `수정 NN번 날짜 …` 로 "변경했어요"를 받았지만 실제로는 바뀌지 않은 기록이 있을 수 있고,
> 연속 삭제 후 복구하지 못한 기록은 이미 지워졌습니다.
> 최근 내역에서 날짜가 이상한 항목을 확인해 주세요.
