# V22.8.67 검증 보고서

검증일: 2026-08-02

## 점검 범위

순차 점검 **3단계(권한별 화면)** 와 **4단계(빈 상태)** 입니다.

역할 6종(소유자 / 관리자 / 구성원 / 조회 전용 / 승인 대기 / 이용 제한)과
기록이 0건인 새 가계부를 픽스처에 추가해, 화면 렌더뿐 아니라
**실제 쓰기 요청을 보낸 뒤 데이터가 바뀌었는지까지** 확인했습니다.

## 1. 권한 차단 (결함 없음)

조회 전용·승인 대기·이용 제한 계정으로 쓰기 경로 24개에 직접 POST 를 보냈습니다.

| 역할 | 시도한 쓰기 경로 | 데이터 변경 |
|---|---|---|
| 조회 전용 | 24 | **0건** |
| 승인 대기 | 24 | **0건** |
| 이용 제한 | 24 | **0건** |

거래 저장·수정·삭제, 예산 저장·삭제, 정기지출·정기지출 준비·자산 생성/삭제,
참여자 권한 변경·방출, 가계부 이름 변경·삭제·초대코드 재발급을 포함합니다.
모두 리다이렉트로 끝났고 서버 오류(5xx)는 없었습니다.

## 2. 화면에 남은 쓰기 유도 요소 (결함 1건 → 보정)

조회 전용 계정으로 화면 13개를 열어 **누를 수 있는 저장 수단**이 남았는지 봤습니다.
(`disabled` 가 아닌 제출 버튼, 폼 밖 `formaction` 버튼)

| 화면 | 보정 전 | 보정 후 |
|---|---|---|
| 홈 · 거래 내역 · 캘린더 | 없음 | 없음 |
| 정기지출 · 자산·계좌 · 예산 · 월 마감 · 부부 정산 | 없음 | 없음 |
| 통계 · 종합 리포트 | 없음 | 없음 |
| 영수증 | 없음 (`disabled` + 안내) | 없음 |
| 가져오기 | 없음 (`disabled` + 안내) | 없음 |
| **분류·키워드** | **`/my/category-keywords/bulk-save` 저장 버튼 활성** | **없음** |

같은 검사를 소유자 화면에 돌리면 반드시 저장 수단이 잡히는지도 확인했습니다
(검사 자체가 죽지 않았는지 확인).

### 분류·키워드 막다른 길

`구성원(member)` 은 조회 전용이 아닌 정상 기록 권한자인데도 같은 문제를 겪었습니다.

브라우저에서 확인한 순서:

| | 보정 전 | 보정 후 |
|---|---|---|
| 화면 | 저장 버튼·추가줄·× 모두 활성 | 저장 버튼 없음, `보기 전용` 안내 |
| 저장 클릭 | 303 → `/my/settings` | (버튼 없음) |
| 착지 | **403 관리자 권한이 필요한 화면** | — |
| 강제 POST | 저장 안 됨, 화면 밖으로 이탈 | 저장 안 됨, 분류·키워드 화면 복귀 + 사유 표시 |
| 소유자 저장 | `/my/settings` 로 이동 | 분류·키워드 화면 복귀 + 성공 문구 |

`return_to` 를 보내지 않는 기존 경로(설정 화면의 키워드 편집기)는
그대로 설정 화면으로 돌아갑니다.

## 3. 승인 대기·이용 제한 안내

| 역할 | 기록 화면 | 안내 |
|---|---|---|
| 승인 대기 | 403 | `참여 승인 대기 중` + 다음 행동 3단계 |
| 이용 제한 | 403 | `이 가계부의 이용이 제한됨` + 다음 행동 3단계 |

`정기지출`·`자산·계좌`·`저축·목표`·`연간 리포트` 는 303 을 한 번 거쳐
같은 안내 화면에 도착합니다. 최종 착지 화면은 동일합니다.

## 4. 빈 상태 (기록 0건 새 가계부)

화면 12개를 서버 렌더와 브라우저 양쪽에서 확인했습니다.

| 검사 항목 | 결과 |
|---|---|
| HTTP 상태 | 12/12 200 |
| `NaN` 노출 | 0건 |
| `undefined` 노출 | 0건 |
| `Infinity` 노출 | 0건 |
| 페이지 JS 오류 | 0건 |
| HTTP 4xx/5xx | 0건 |
| 빈 상태 안내 문구 | 각 카드에 `기록이 없어요` 계열 문구 표시 |

홈은 `첫 사용 3단계` 온보딩이 그대로 나오고, 통계·리포트·예산·월 마감은
카드마다 빈 상태 문구를 보여 줍니다.

발견한 문구 문제 1건: 이번 달·지난달 지출이 모두 0원인데
`지난달과 같은 금액을 썼어요` 라고 안내했습니다. 이 경우에만
`아직 지출 기록이 없어요. 한 줄로 첫 기록을 남겨보세요.` 로 바꿨습니다.
기록이 있는 달의 지난달 비교 문구는 회귀 검사로 고정했습니다.

## 5. 성능·자원

| 항목 | 예산 | 실측 | 여유 |
|---|---|---|---|
| 기준 픽스처 홈 HTML | 35,840 B | 34,958 B | 882 B |
| 200행 실사용 홈 HTML | 45,056 B | 44,216 B | 840 B |

보정 전과 **바이트 차이 0** 입니다. 홈 문구 변경은 기록이 전혀 없을 때만 적용되고,
키워드 편집기는 홈 화면에 없습니다.

고정 자원 3종도 바이트가 같아 경로를 바꾸지 않았습니다.

| 자원 | 크기 | SHA-256(앞 16자) |
|---|---|---|
| `/assets/accountbook-shell-v22865.css` | 147,979 B | `4d68eea7f001fe35` |
| `/assets/accountbook-nav-v22862.js` | 19,742 B | `6d3bc79ecabc36f3` |
| `/assets/accountbook-v5-v22861.js` | 62,354 B | `3eb61ad54cf1f83a` |

## 6. 자동 검사

- 신규 회귀 **203개** (`validation/validate-role-surface-v22867.mjs`)
- 저장소 전체 **1,973개** 자동검사 통과
- 저장소 전체 검증 통과 (`node .codex/scripts/verify-repository.mjs`)
- ESM `default.fetch` 확인, 체크섬 매니페스트 일치

신규 회귀가 고정하는 것:

- 키워드 편집기가 권한 플래그를 받고, 권한이 없으면 저장·추가·삭제 수단이 사라진다
- 키워드 저장 결과가 **온 화면으로** 되돌아온다 (성공·실패·권한 거절 모두)
- 되돌아온 화면이 그 역할에게 **실제로 200 으로 열린다**
- 조회 전용·승인 대기·이용 제한이 쓰기 경로 12개에서 데이터를 바꾸지 못한다
- 조회 전용 화면 13개에 누를 수 있는 저장 수단이 없다 (소유자 화면에서는 잡힌다)
- 기록 0건 가계부 화면 12개에 `NaN`·`undefined`·`Infinity` 가 없다
- 기록이 없으면 지난달 비교 문구를 쓰지 않고, 기록이 있으면 그대로 쓴다
- 권한 안내 문구에 영어 역할명(`owner/admin`)이 남지 않는다

## 7. 운영 변경

- 신규 SQL 없음
- 환경변수·Secret 변경 없음
- Kakao Developers·OpenBuilder 변경 없음

## 배포 후 확인 부탁드립니다

1. 구성원 또는 조회 전용 계정으로 `분류·키워드` 화면 → 저장 버튼이 없고 `보기 전용` 안내가 보이는지
2. 소유자 계정으로 같은 화면에서 키워드 저장 → **분류·키워드 화면으로 돌아오며** 성공 문구가 뜨는지
3. 기록이 하나도 없는 새 가계부 홈 → `아직 지출 기록이 없어요` 문구가 보이는지
