# V22.8.72 키보드로 본문에 바로 가기 안내

버전: `V22.8.72-SKIP-TO-CONTENT`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음
- 환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음
- 셸 스타일시트 주소가 `/assets/accountbook-shell-v22872.css` 로 바뀝니다 (immutable 자원)

## 배경 (중요)

순차 점검 **9단계 — 키보드 포커스 표시와 읽기 순서**입니다.
사용자 화면 24개를 390px·1440px 실제 Chromium으로 열고 **Tab 키를 눌러가며** 쟀습니다.

### 결함: 본문에 닿으려면 매번 처음부터 다시 눌러야 했습니다

키보드만 쓰는 사람은 화면을 열 때마다 상단바와 사이드바를 전부 지나야 본문에 닿습니다.
건너뛸 방법이 **24개 화면 전부에 없었습니다.**

| 화면 | 본문 첫 요소까지 Tab | 보정 후 |
|---|---|---|
| 홈·거래 내역·캘린더 (1440px) | **55회** | 1회 |
| 통계·종합 리포트 (1440px) | **52회** | 1회 |
| 나머지 (1440px) | 11~16회 | 1회 |
| 전체 (390px) | 9~13회 | 1회 |

데스크톱 홈이 55회인 것은 사이드바에 달력이 있어 **날짜 링크 31개**를 지나야 하기 때문입니다.
화면을 옮길 때마다 그 55번을 처음부터 다시 눌러야 했습니다.

이제 **첫 번째 Tab**에서 `본문 바로가기` 가 나타나고, Enter를 누르면 본문으로 이동합니다.
그다음 Tab은 본문 안에서 이어집니다.

평소에는 화면 밖에 있다가 포커스를 받으면 나타납니다.
`display:none` 이나 `visibility:hidden` 으로 감추면 포커스 자체가 가지 않아
링크가 없는 것과 같아지므로, 위치만 옮기는 방식을 썼습니다.

## 문제가 없어서 그대로 둔 것

**포커스 표시는 결함 0건입니다.** 24개 화면 × 2개 폭에서 Tab으로 닿는 모든 요소를
하나씩 확인했고, 전부 눈에 보이는 표시가 있었습니다.

이 결과를 믿기 전에 검사부터 확인했습니다. 앱의 포커스 규칙을 브라우저에서 지우고
같은 검사를 돌리자 **103개 중 103개**를 결함으로 잡았습니다.

클릭은 되는데 키보드로 닿지 않는 요소, `aria-hidden` 안에 있는 조작 요소도 0건이었습니다.

## 이미 저장된 기록에 대해

이 배포는 데이터를 바꾸지 않습니다. 화면 동작만 달라집니다.

## 적용 순서

1. 현재 운영 Worker 전체 소스를 별도 파일로 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 운영 소스와 교체합니다.
3. 저장 후 배포합니다.
4. 저장소 루트에서 아래 명령을 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_72.ps1
```

5. 아무 화면이나 열고 **Tab을 한 번** 눌러 `본문 바로가기` 가 왼쪽 위에 나타나는지 확인합니다.
6. Enter를 누르면 본문으로 이동하고, 그다음 Tab이 본문 안에서 이어지는지 확인합니다.
7. 오래 열어 둔 웹 탭은 새로고침해야 `v22872` 셸 스타일이 적용됩니다.

기존 V22.6.8·V22.7.0·V22.7.1·V22.8.46 SQL은 `/ready`가 정상이라면 다시 실행하지 않습니다.
