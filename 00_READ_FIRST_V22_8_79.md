# V22.8.79 배포 안내

버전: `V22.8.79-UIUX-INCOME-BUDGET-THEME`

## 적용 파일

- `src/index.js` 전체 교체
- **신규 SQL 있음** — `03`·`04`·`05`. **2026-08-07 이미 적용 완료**입니다.
  다시 실행할 필요 없습니다(재실행해도 안전하지만 아무 일도 일어나지 않습니다).
- 환경변수 변경 없음
- **자산 주소 변경 있음** → 배포 후 웹 탭을 한 번 새로고침하세요

## 이번에 바뀐 것

### 예산이 두 곳에 나뉘어 저장되던 문제 (SQL, 적용 완료)

개별 예산 저장이 유니크 인덱스가 없어 `42P10` 으로 거절당하면, 코드가 조용히
설정 JSON 폴백으로 넘어가고 있었습니다. **화면에는 "저장됨" 이 떠서 겉으로는
아무 문제가 없어 보였습니다.** 같은 가계부의 예산이 어느 경로로 저장했느냐에
따라 다른 곳에 있었습니다. 자세한 내용은
`00_READ_FIRST_V22_8_79_BUDGET_STORAGE_SQL.md` 를 보세요.

같은 조사에서 `public` 스키마 18개 표 중 11개가 `anon`·`authenticated` 에
기본 권한을 그대로 갖고 있는 것을 발견했습니다. RLS 가 켜져 있어 읽기는
막혀 있었지만 **RLS 는 `TRUNCATE` 를 덮지 않습니다.** `users`·`transactions`
포함해 전부 회수했습니다.

### 화면

| 무엇 | 이전 | 이후 |
|---|---|---|
| 달 | URL 에 박힌 달이 계속 따라다님 | "이번 달로 이동" 링크가 나타남 |
| 좌측상단 로고 | 배경과 붙어 흐릿하게 보일 수 있었음 | 어느 스타일이 이겨도 같은 대비 |
| 화면 모드 | 시스템 / 라이트 / 다크 | **라이트 / 다크 2택** |
| 라이트 사이드바 | 대메뉴와 하위가 밋밋 | 구분선·자간·들여쓰기로 층 구분 |
| 거래내역 | 홈 안 섹션에 묻힘 | **독립 탭** (필터·검색·분류 select) |
| 고정지출 | 홈에 입력폼 (실제로는 안 보였음) | 정기 관리 진입 카드 |
| 정기 항목 | 삭제만 가능 | **수정 가능** + 수입/지출 + 매월 반복 |
| 홈 우측 | "오늘 써도 되는 돈" | **수입 대비 사용률 게이지** |
| 홈 그래프 | 최근 6건 목록 | 소비 흐름 일/주/월 전환 |

## 배포 순서

1. Cloudflare Worker 에 `src/index.js` 를 붙여 넣고 저장·배포
2. `/health` 에서 버전이 `V22.8.79-UIUX-INCOME-BUDGET-THEME` 인지 확인
3. `10_VERIFY_AFTER_DEPLOY_V22_8_79.ps1` 실행
4. 웹 탭 새로고침 (자산 주소가 바뀌었습니다)

## 알아 둘 것

- **예전에 화면 모드를 '시스템' 으로 저장해 둔 브라우저는 라이트로 정정됩니다.**
  마이그레이션 코드는 따로 없고 폴백이 알아서 처리합니다.
- **홈의 소비 흐름은 기본이 기간 탭만입니다.** 고른 사람만 그래프를 받습니다.
  일별은 56KB 로 무거워서, 홈의 바이트 예산(35 KiB 기준선)과 질의 수(9회)를
  둘 다 지키려면 어떤 패널도 미리 그릴 수 없었습니다.
- **정기 항목의 `type`·`is_recurring` 은 없으면 지출·주기에서 파생됩니다.**
  예전 데이터는 손대지 않아도 예전과 똑같이 동작합니다.
- **`/admin/recurring/*` 라우트는 남아 있지만 UI 진입점이 없습니다.** 홈에서
  고정지출 섹션을 걷어냈기 때문입니다. 정기 항목 관리는 `/reserve-plans`
  한 곳으로 모였습니다. 두 계열을 합칠지는 별도 판단이 필요합니다.
- **`budgets`(맨 이름) 표는 코드 호출이 0건인 잔존 표입니다.** 권한만 정리하고
  지우지 않았습니다. 내용을 확인한 뒤 판단하세요.
- 실제 손가락 터치·iOS Safari 고유 동작·카카오톡 인앱 브라우저의 실제 렌더는
  기기에서만 확인할 수 있습니다.

## 되돌리기

`src/index.js` 를 이전 버전으로 되돌리면 화면은 즉시 돌아갑니다. SQL 은 별개이며
되돌리는 방법은 각 SQL 파일 맨 아래에 있습니다. 다만 예산 저장소 정리는 되돌릴
이유가 거의 없습니다 — 되돌리면 이중화가 다시 시작됩니다.

## 검증

- `node .codex/scripts/verify-repository.mjs` — 체크섬 91개, 자동검사 3,154개
- 이번 릴리스 신규 회귀 검사 280개
- 단계마다 되돌린 결함이 실제로 잡히는 것을 확인 (총 36종)
