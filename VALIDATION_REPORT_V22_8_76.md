# V22.8.76 검증 보고서 — 카카오 의도 정리·스킬 인증 관측

버전: `V22.8.76-KAKAO-INTENT-SKILL-AUTH`
측정 환경: Node.js 22.22.2 · QA 픽스처 · Chromium 1194

---

## 1. 되묻기 — 문제는 "되묻는다"가 아니라 일관성이 없다는 것

`주유 5만원` 처럼 분류 이름과 금액만 오면 지출인지 예산인지 되물었습니다.
판단 기준은 코드에 박힌 21개 단어 목록입니다. 22개 표현을 실제로 넣어 봤습니다.

| 입력 | 이전 | 이유 |
|---|---|---|
| `카페 4500` | 되묻기 | 카페가 목록에 있음 |
| `커피 4500` | 지출 기록 | 커피는 목록에 없음 |
| `주유 5만원` | 되묻기 | |
| `주유소 5만원` | 지출 기록 | 한 글자 차이 |
| `외식 2만원` | 되묻기 | |
| `점심 12000` | 지출 기록 | |
| `주유 5만원 썼어` | 지출 기록 | "썼어"가 붙으면 안 물음 |

사용자 눈에는 규칙이 없습니다. **전부 지출로 통일**했습니다.

금액이 없어 진짜로 무엇을 할지 알 수 없는 말(`예산`, `가계부`, `초대`, `이름변경`)은
그대로 되묻습니다. 이 네 가지는 회귀 검사로 고정했습니다.

측정 후:

| 입력 | 결과 | 분류 |
|---|---|---|
| `주유 5만원` | 지출 50,000원 | 교통/차량 |
| `카페 4500` | 지출 4,500원 | 카페/간식 |
| `커피 4500` | 지출 4,500원 | 카페/간식 |
| `외식 2만원` | 지출 20,000원 | 식비 |
| `택시 12000` | 지출 12,000원 | 교통/차량 |

---

## 2. 되묻기를 없애다 발견한 결함 2건

### 2.1 "예산"이라고 말했는데 지출이 남았습니다

되묻기를 없애면 예산 설정 경로가 말로만 남으므로 그 길을 먼저 확인했습니다.

| 입력 | 이전 | 이후 |
|---|---|---|
| `식비 예산설정 30만원` | 예산 설정 ✓ | 그대로 |
| `식비 예산 30만원` | **지출 300,000원 기록** | 식비 예산 설정 |
| `예산 식비 30만원` | **지출 300,000원 기록** | (지출 유지 — 아래 참고) |

`예산` 뒤에 `설정`·`저장`·`등록` 이 있어야만 예산 명령으로 봤습니다. 이제
**꼬리가 금액 하나뿐일 때**도 예산으로 받습니다.

꼬리 조건을 둔 이유는 가로채기를 막기 위해서입니다.

| 입력 | 결과 |
|---|---|
| `회사 예산 회의 5000` | 지출로 남음 (금액 앞에 다른 말) |
| `남은 예산` | 예산 현황 조회 |
| `식비 예산 얼마야` | 설정/확인 되묻기 |

### 2.2 앱이 도움말에 안내하는 명령이 한 번도 동작하지 않았습니다

`전체 예산설정 300만원` 은 소개 화면과 도움말이 직접 드는 예시인데,
**"등록된 카테고리를 찾지 못했어요"** 만 나왔습니다.

원인을 계측으로 찾았습니다.

```
[DBG] 입력분류= "__total"  정규화= "total"  해석= ""
```

`normalizeBudgetCategoryInput` 이 두 번 돕니다. 명령 파서가 `전체` → `__total` 로
바꾸고, 분류 해석이 그 `__total` 을 **또** 정규화하면서 `normalizeText` 가 밑줄을
지워 `total` 이 됩니다. 등록된 분류에 `total` 은 없으니 항상 실패했습니다.

함수를 멱등으로 만들어(`__total` 이 들어오면 그대로 돌려줌) 고쳤습니다.

| 입력 | 이후 |
|---|---|
| `전체 예산설정 300만원` | 전체 월 예산 3,000,000원 ✓ |
| `전체 예산 300만원` | 전체 월 예산 3,000,000원 ✓ |
| `월 전체 예산설정 300만원` | 전체 월 예산 3,000,000원 ✓ |
| `총액 예산설정 300만원` | 전체 월 예산 3,000,000원 ✓ |

---

## 3. `/skill` 인증 — 켜면 조용히 전부 실패하는 구조였습니다

비밀값을 넣은 상태로 실제 요청을 보내 확인했습니다.

| 상황 | HTTP | 사용자가 보는 것 |
|---|---:|---|
| 헤더 없음 | 403 | "잠시 처리 흐름을 정리하고 있어요 / 아래처럼 다시 보내주세요" |
| 틀린 값 | 403 | 같음 |
| 맞는 값 | 200 | 정상 |

**실패가 평범한 재시도 안내로 보입니다.** 사용자는 계속 다시 보내고, 운영자는
`/health` 의 `skill_caller_auth_configured: true` 만 보고 정상이라고 생각합니다.
그 값은 "비밀값이 설정됨"만 뜻하고 통과 여부는 알려주지 않습니다.

### 넣은 것

**(1) 통과·거부 집계를 상태 응답에 노출**

```json
"skill_caller_auth": {
  "mode": "enforce", "configured": true,
  "accepted": 4, "denied": 2,
  "last_accepted_at": "...", "last_denied_at": "...",
  "header_missing_suspected": false
}
```

통과가 0인데 거부만 쌓이면 `header_missing_suspected` 가 `true` 가 됩니다.
헤더 설정이 빠졌다는 뜻이고, 검사가 이 상태를 직접 재현해 확인합니다.

**(2) `KAKAO_SKILL_AUTH_MODE=observe`** — 막지 않고 세기만 하는 모드.

| 설정 | 헤더 없음 | 헤더 있음 | 거부 집계 |
|---|---:|---:|---|
| 비밀값 없음 | 200 | 200 | 안 셈 (mode=off) |
| 비밀값만 (기본) | **403** | 200 | 셈 (mode=enforce) |
| observe 추가 | 200 | 200 | **계속 셈** |
| 오타 `observ` | **403** | 200 | 셈 |

기본값은 계속 차단입니다. 이미 켜서 카카오톡이 멈췄다면 `observe` 한 줄로 즉시
복구되고, 헤더를 맞춘 뒤 지우면 다시 차단됩니다. 오타는 **차단 쪽으로** 떨어져
실수로 인증이 풀리지 않습니다.

거부 응답에 비밀값이 새지 않는 것도 확인했습니다.

---

## 4. 판단하고 그대로 두기로 한 2건

### 4.1 사이드바 달력 34개는 Tab 순서에 남깁니다

데스크톱 1440px 에서 실제 Tab 키로 걸어 셌습니다.

| 화면 | 전체 탭 정지점 | 그중 사이드바 달력 |
|---|---:|---:|
| 홈 | 54 | **34 (63%)** |
| 소비 분석 | 57 | **34 (60%)** |
| 예산 (달력 없음) | 19 | 0 |

사이드바 전체 포커스 대상 61개 중 34개가 미니 달력입니다. 다만 V22.8.72 의
`본문 바로가기` 로 **Tab 한 번이면 본문**에 갑니다. 빼면 키보드로 달력 날짜를
아예 누를 수 없게 되어, 사이드바 훑기 편의보다 손실이 큽니다.

### 4.2 구성원 예산·정기지출 저장 권한은 열지 않습니다

역할별로 실제 요청을 보내 확인했습니다.

| 역할 | 예산 저장 폼 | 정기지출 등록 폼 | 폼 없이 강제 전송 |
|---|---|---|---|
| owner | 보임 | 보임 | 저장됨 (정상) |
| admin | 보임 | 보임 | 저장됨 (정상) |
| member | 없음 | 없음 | **저장 안 됨** |

화면에서 숨기는 것뿐 아니라 서버에서도 막고 있어 보안 문제가 없습니다.
권한이 필요하면 그 사람을 `관리자` 로 올리면 됩니다.

**측정 도구를 먼저 고쳤습니다.** 첫 스크립트는 역할마다 같은 이름을 써서, 앞선
`owner` 가 만든 데이터를 뒤의 `member` 차례에서 다시 세어 "member 도 저장됨"이라는
거짓 결함을 냈습니다. 역할별 고유 이름을 붙여 다시 재고 나서야 정확한 그림이
나왔습니다.

---

## 4.5 11단계 점검 착수 — 카카오 재전송 중복

`NEXT_UPDATE_PLAN.md` 11단계(끊김·중단 상황에서의 복구)를 시작하면서 첫 항목을
재다 결함이 나와 이번 판에 함께 고쳤습니다.

카카오는 응답이 늦으면 같은 발화를 **다시 보냅니다**. 중복 방지는 있었지만
"저장이 끝난 뒤"에 걸리는 구조(V22.8.69 의 의도된 설계)라, 처리 중인 동안 문이
열려 있었습니다.

| 상황 | 이전 | 이후 |
|---|---:|---:|
| 순차 2회 | 1건 | 1건 |
| **동시 2회** | **2건** | 1건 |
| **동시 3회** | **3건** | 1건 |
| 웹 저장 버튼 순차 2회 | 1건 | 1건 |
| 웹 저장 버튼 동시 2회 | 1건 | 1건 |

웹은 이미 막혀 있었습니다. 카카오 경로만 열려 있었습니다.

"처리 중" 표시를 먼저 걸어 창을 닫되, 처리가 끝나면 **성공·실패 양쪽에서 반드시
내립니다.** 표시가 남으면 저장에 실패한 요청까지 잠겨, V22.8.69 에서 고쳤던
"다시 보내도 아무것도 저장되지 않는" 결함이 되살아납니다. 네 가지로 확인했습니다.

| 확인한 경우 | 결과 |
|---|---|
| 가계부 선택 전 발화 → 선택 후 같은 말 | 1건 저장 ✓ |
| 인식하지 못한 말을 두 번 | 같은 안내 그대로 ✓ |
| 저장 중 오류 뒤 재시도 | 1건 저장 ✓ |
| 정상 저장 뒤 8초 안에 재입력 | 막힘 (의도한 동작) ✓ |

### 같은 점검에서 결함이 아니었던 것

- **웹 저장 버튼 중복 제출** — 순차·동시 모두 이미 막혀 있습니다.
- **예산 여러 건 저장 중 실패** — `accountbook_replace_budget_plan_v227` RPC
  한 번으로 처리되어 전부 되거나 전부 안 됩니다. 처음에는 "일부만 저장되고
  성공으로 표시됨"으로 보였는데, **제 계측이 엉뚱한 표를 재고 있었습니다.**
  실제 쓰기 경로를 추적해 단일 RPC임을 확인하고 철회했습니다.

### 확인했지만 이번에 고치지 않은 것

저장 도중 네트워크가 끊기면 데이터는 망가지지 않고(0건 저장) 서버는 정확한 문구까지
만들어 넘깁니다. 그런데 돌아가는 주소가 예전 관리자 화면이라 화면에 뜨지 않습니다.

| 돌아가는 화면 | 오류 문구 |
|---|---|
| 지금 돌아가는 곳 (`/?legacy=1`) | **안 보임** |
| 일반 홈 (`/app`) | 보임 |

돌아갈 화면을 바꾸는 일이라 별도 판단이 필요해 `KNOWN-ISSUES.md` 에 남겼습니다.

---

## 5. 회귀 검사

`validation/validate-kakao-intent-skill-auth-v22876.mjs` (**81개 확인**)

인증 집계는 모듈 수준에 쌓이므로 "거부만 있고 통과가 없는" 상태를 먼저 재도록
검사 순서를 고정했습니다.

### 되돌려서 잡히는지 확인

| 되돌린 결함 | 검사 결과 |
|---|---|
| `__total` 이중 정규화 복귀 | FAIL: "전체 예산설정 300만원"가 전체 예산으로 저장된다 |
| 예산 암묵 표기 제거 | FAIL: "식비 예산 30만원"가 지출로 기록되지 않는다 |
| observe 모드에서 거부 세기 중단 | FAIL: observe 모드에서도 실패는 계속 세어진다 |
| 모드 오타를 통과로 처리 | FAIL: 모드 값을 잘못 적으면 열리지 않고 막힌다 |
| 처리 중 표시 제거 (재전송 중복 복귀) | FAIL: 동시 2회 재전송도 한 건만 남긴다 |
| 처리 중 표시를 안 내림 (실패 요청 잠김) | FAIL: 저장에 실패한 요청은 잠기지 않고 다시 보내면 저장된다 |

여섯 경우 모두 검사가 잡았고, 원복하면 다시 통과합니다.

---

## 6. 전체 결과

- 저장소 자동검사 **2,817개 전부 통과** (기존 검사 수정 없음)
- `node --check src/index.js` 통과, ESM 진입점 통과
- 신규 SQL 없음 · 자산 주소 변경 없음
- 환경변수 1개 추가(선택): `KAKAO_SKILL_AUTH_MODE`

## 7. 배포 후 사람이 확인할 것

1. `/health` 의 `skill_caller_auth` — `accepted` 가 오르고 `denied` 가 0인지
2. 카카오톡에서 `주유 5만원` → 바로 지출로 저장되는지
3. `식비 예산 30만원` → 예산으로 설정되는지 (지출로 남지 않는지)
4. `전체 예산설정 300만원` → 전체 월 예산이 설정되는지
5. `남은 예산` 조회가 그대로인지

운영 도메인(`https://ttokttok-accountbook.com`)은 이 환경에서 접근할 수 없어
직접 확인하지 못했습니다.

**과거에 `식비 예산 30만원` 으로 잘못 기록된 지출은 이 배포가 고치지 않습니다.**
소비 분석에서 큰 금액을 확인하고 직접 삭제해야 합니다.
