# V22.7 검증 보고서

검증일: 2026-07-15 KST

## 자동 검증 결과

| 구분 | 결과 | 범위 |
|---|---:|---|
| JavaScript 문법 | 통과 | `node --check src/index.js` |
| V22.7 통합 smoke | 169/169 | 인증, 경로 복귀, 지출자, 감사, 가져오기, 백업, 예산, 정기거래, 삭제, 무료·숨김 정책 |
| SQL 정적 안전성 | 155/155 | 13개 함수, 트랜잭션, RLS, 권한 회수, Worker RPC 일치, 삭제 범위 |
| UI 정적 검증 | 566/566 | 23개 인증 화면, 320·360·390px 목표, 레이블, ID, 내비게이션, 푸터, 개인정보 |
| 자연어 회귀 | 4,738/4,738 | 22개 의도, 합성 평가 데이터 |

## 통합 smoke에서 확인한 핵심

- `/health`, `/ready` 정상·실패 닫힘 동작
- 외부 출처 쓰기 요청 403
- 관리자 보안 설정의 정확한 `return_to` 보존
- 잘못된 로그인으로 계정 미생성
- 개인 비밀번호 저장 후 가계부 관리 앵커 복귀와 세션 갱신
- 기록 목록 지출자 표시, 지출자 수정, 거래 감사 생성
- 제목 유사어 표와 자연어 행의 동시 미리보기
- 가져오기 지출자 별칭 연결, 선택 행만 저장, 재전송 중복 차단
- 백업 CSV 지출자 표시명 보존과 UUID 비노출
- 수입 항목·분류별 예산 원자적 교체
- 정기거래 원자적 반영
- 가계부 삭제의 이름·비밀번호 이중 확인과 단일 purge RPC
- 내부 완료 코드·사용자 UUID·미완성 기능·불필요 문구 비노출

## SQL 정적 검증

- 마이그레이션 전체가 `BEGIN`/`COMMIT`으로 묶임
- 파괴적 `DROP TABLE`·`TRUNCATE` 없음
- 모든 SECURITY DEFINER 함수의 `search_path` 고정
- 13개 함수의 매개변수·IF·LOOP 블록 균형
- public/anon/authenticated 함수 실행권한 회수, service_role 전용 실행권한
- 5개 보안·감사 테이블 RLS와 클라이언트 권한 회수
- Worker가 호출하는 12개 V22.7 RPC가 SQL에 모두 존재
- 가져오기 가계부·지출자 범위, 정기거래 지출자, 거래 변경 전·후 감사 검증
- 가계부 삭제 시 14개 설정 키 범위 정리 검증

## UI 정적 검증 화면

`/app`, 가계부 목록, 참여자, 메뉴, 스마트 도구, 영수증, 리포트, 정산 4개 모드, 분석, 예산, 설정, 백업, 결제수단, 정기계획, 개인 비밀번호, 프로필, 키워드, 시작가이드, 가계부 템플릿, 예산알림의 총 23개 화면을 서버 HTML·DOM·CSS 기준으로 확인했습니다.

확인 항목은 viewport, 중복 ID, 폼 레이블, 미완성 기능 링크, 인증 화면 사업자 푸터, 제거 요청 문구, 사용자 UUID, 원시 상태 코드, 통합 내비게이션, 모바일 overflow·터치 CSS, 보안 복귀 경로입니다.

## 검증 한계

- 이 환경에서 Chromium 실행 파일을 확보하지 못해 V22.7의 실제 픽셀 스크린샷은 생성하지 못했습니다.
- Supabase 운영 DB에 접속하거나 마이그레이션을 실제 실행하지 않았습니다. SQL은 정적 안전성까지 확인했습니다.
- 실제 카카오 OAuth, OpenBuilder, 단톡방 식별키는 운영 자격증명과 실제 단톡방에서 확인해야 합니다.
- 자연어 4,738건은 합성 평가 데이터이므로 실제 사용자 오탈자·은행별 신규 내보내기 형식은 운영 실패 사유를 수집해 지속 확장해야 합니다.

## 배포 판정

코드·정적 SQL·fixture 통합 회귀 기준으로는 배포 후보입니다. 운영 배포 승인은 `FINAL_APPLY_CHECKLIST_V22_7_0.md`의 DB 적용과 실기기·실계정 항목을 완료한 뒤 결정합니다.
