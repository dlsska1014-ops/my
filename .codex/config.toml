# Shared safety defaults for trusted clones of this repository.
# Model and reasoning effort remain user-controlled.

approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
writable_roots = []
network_access = false

[agents]
max_threads = 3
max_depth = 1
