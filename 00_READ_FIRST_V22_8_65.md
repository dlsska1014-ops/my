# V22.8.65 테마 글자 대비 보정 적용 안내

버전: `V22.8.65-THEME-TEXT-CONTRAST`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음
- 환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## 배경

라이트·다크 × 포인트 컬러 4종(블루·그린·바이올렛·앰버) = 8개 조합에서
사용자 탭 24개를 실제 브라우저로 렌더해 글자 대비를 WCAG 기준(일반 4.5:1,
큰 글씨 3:1)으로 측정했습니다.

여러 화면이 "어두운 히어로·상자"를 전제로 밝은 글자색을 고정해 두었는데,
V5 셸이 배경만 표면색으로 바꾸면서 글자가 배경에 묻혔습니다.

## 보정한 지점

| 화면 | 증상 | 대비 |
|---|---|---|
| 연간 리포트 · 연말정산 참고 | 다크에서 흰 상자에 흰 글자 | 1.1:1 → 통과 |
| 연간 리포트 · 연도 이동 | 라이트에서 흰 배경에 흰 화살표 | 1.0:1 → 통과 |
| 자산·계좌 · 히어로 | 라이트에서 흰 배경에 연파랑 문구 | 1.2:1 → 통과 |
| 저축·목표 · 히어로 | 라이트에서 흰 배경에 민트 문구 | 1.1:1 → 통과 |
| 챌린지 설정 폼 | 라이트에서 어두운 카드에 어두운 라벨 | 1.9:1 → 통과 |
| 일자별 셀 | 빈 날 표시가 과하게 흐림 | 1.9:1 → 통과 |
| 증감 배지 | 밝은 카드에서 기준 미달 | 3.0:1 → 통과 |

## 적용 순서

1. 현재 운영 Worker 전체 소스를 별도 파일로 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 운영 소스와 교체합니다.
3. 저장 후 배포합니다.
4. 저장소 루트에서 아래 명령을 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_65.ps1
```

5. 실기기에서 `전체 메뉴 → 화면 설정`으로 라이트·다크와 포인트 컬러 4종을 바꿔가며
   연간 리포트, 자산·계좌, 종합 리포트의 챌린지 설정을 읽어봅니다.

기존 V22.6.8·V22.7.0·V22.7.1·V22.8.46 SQL은 `/ready`가 정상이라면 다시 실행하지 않습니다.
