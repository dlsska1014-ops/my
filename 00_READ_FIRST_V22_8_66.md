# V22.8.66 거래 삭제 동작 복구 적용 안내

버전: `V22.8.66-DELETE-BUTTON-FORM-FIX`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음
- 환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음

## 배경 (중요)

**웹 화면에서 거래 삭제가 동작하지 않았습니다.**

최근 내역 카드의 `삭제` 버튼이 수정 폼의 `</form>` **바깥**에 있었습니다.
`formaction`은 폼에 소속된 제출 버튼에서만 동작하는데, 이 버튼은 소유 폼이 없어
(`button.form === null`) 눌러도 요청이 전혀 나가지 않았습니다.
`삭제할까요?` 확인창은 떴기 때문에 사용자는 삭제된 줄 알았지만
기록은 그대로 남아 있었습니다.

버튼을 `</form>` 앞으로 옮겨 소유 폼에 연결했습니다. HTML 바이트 증가는 없습니다.

## 영향 범위

- 웹 화면(`/app` 최근 내역)의 거래 삭제
- 카카오톡 챗봇 삭제 명령은 별도 경로라 영향이 없었습니다
- 거래 저장·수정은 정상이었습니다

## 적용 순서

1. 현재 운영 Worker 전체 소스를 별도 파일로 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 운영 소스와 교체합니다.
3. 저장 후 배포합니다.
4. 저장소 루트에서 아래 명령을 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_66.ps1
```

5. 실기기에서 최근 내역의 기록을 펼쳐 `삭제`를 누르고 확인창을 수락한 뒤,
   새로고침해도 기록이 사라져 있는지 확인합니다.

기존 V22.6.8·V22.7.0·V22.7.1·V22.8.46 SQL은 `/ready`가 정상이라면 다시 실행하지 않습니다.
