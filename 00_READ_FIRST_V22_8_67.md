# V22.8.67 역할별 화면·빈 상태 보정 적용 안내

버전: `V22.8.67-ROLE-SURFACE-EMPTY-STATE`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음
- 환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음
- `/assets/...` 고정 자원 3종 바이트 동일 → 자원 경로 변경 없음

## 배경

순차 점검 3단계(권한별 화면)·4단계(빈 상태)에서 확인한 내용입니다.

### 1. 구성원·조회 전용이 분류·키워드 화면에서 막다른 길에 갇혔습니다

`분류·키워드` 화면의 **변경사항 저장** 버튼이 모든 역할에게 그대로 열려 있었습니다.
키워드 저장은 원래 소유자·관리자만 가능한데, 화면에서는 그 사실을 알려 주지 않았습니다.

더 나쁜 점은 되돌아가는 곳이었습니다. 저장이 막히면 `/my/settings`로 보냈는데
그 화면은 소유자·관리자 전용이라 **403 "관리자 권한이 필요한 화면"** 이 떴습니다.
즉 구성원이 저장을 누르면 아무것도 저장되지 않은 채 보던 화면 밖으로 튕겨 나갔습니다.

보정 내용:

- 권한이 없으면 저장 버튼·키워드 추가줄·삭제(×)를 처음부터 감추고 `보기 전용` 안내를 띄웁니다.
- 저장 결과(성공·실패 모두)를 **온 화면으로** 되돌립니다. 분류·키워드 화면에서 시작했으면
  분류·키워드 화면으로 돌아오고, 결과 문구를 그 화면에 표시합니다.
- 권한 사유를 `조회 전용` 대신 `분류 키워드 저장은 가계부 소유자·관리자만 할 수 있습니다`로
  정확히 안내합니다. 구성원도 조회 전용이 아니기 때문입니다.

### 2. 권한 안내 문구가 화면마다 달랐습니다

일부 화면이 `가계부 owner/admin만`처럼 영어 역할명을 그대로 보여 줬습니다.
전부 `가계부 소유자·관리자만`으로 통일했습니다.

### 3. 기록이 하나도 없는 새 가계부 홈 문구

이번 달·지난달 지출이 모두 0원인데 `지난달과 같은 금액을 썼어요`라고 안내해
없는 지난달 기록이 있는 것처럼 읽혔습니다. 이 경우에만
`아직 지출 기록이 없어요. 한 줄로 첫 기록을 남겨보세요.`로 바꿨습니다.
기록이 있는 달의 지난달 비교 문구는 그대로입니다.

## 함께 확인했지만 문제가 없었던 것

- **권한 차단은 정상입니다.** 조회 전용·승인 대기·이용 제한 계정으로 거래 저장·수정·삭제,
  예산·정기지출·자산·참여자 권한·가계부 삭제 등 쓰기 경로 24개에 직접 요청을 보냈고
  **데이터가 바뀐 경우는 한 건도 없었습니다.**
- 승인 대기·이용 제한은 기록 화면에서 사유와 다음 행동을 안내하는 화면으로 막힙니다.
- 기록 0건 새 가계부의 모든 화면에서 `NaN`·`undefined`·`Infinity`·HTTP 4xx·JS 오류가
  한 건도 없었습니다.

## 영향 범위

- `분류·키워드`(`/keyword-guide`) 화면과 `설정` 화면의 키워드 편집기
- 홈 지출 요약 문구 (기록이 전혀 없을 때만)
- 권한 안내 문구 4곳

거래 저장·수정·삭제, 예산, 정기지출, 카카오톡 챗봇 경로는 바뀌지 않았습니다.

## 적용 순서

1. 현재 운영 Worker 전체 소스를 별도 파일로 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 운영 소스와 교체합니다.
3. 저장 후 배포합니다.
4. 저장소 루트에서 아래 명령을 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_67.ps1
```

5. 실기기에서 구성원 또는 조회 전용 계정으로 `분류·키워드` 화면을 열어
   저장 버튼이 없고 `보기 전용` 안내가 보이는지 확인합니다.
6. 소유자 계정으로 같은 화면에서 키워드를 바꾸고 저장한 뒤,
   **분류·키워드 화면으로 되돌아오며** 저장 성공 문구가 보이는지 확인합니다.

기존 V22.6.8·V22.7.0·V22.7.1·V22.8.46 SQL은 `/ready`가 정상이라면 다시 실행하지 않습니다.
