# V22.8.77 검증 보고서 — 11단계 끊김·중단 상황에서의 복구

버전: `V22.8.77-INTERRUPTED-SAVE-RECOVERY`
측정 환경: Node.js 22.22.2 · QA 픽스처 · Chromium 1194 (iPhone 13 뷰포트)

---

## 1. 11단계에서 잰 것

`NEXT_UPDATE_PLAN.md` 11단계는 "정상적으로 끝나는 길"이 아니라 **중간에 끊겼을 때**를
봅니다. 이번 판에서 남은 항목을 실패를 주입해 재현했습니다.

| 항목 | 결과 |
|---|---|
| 저장 후 뒤로 가기 → 재제출 | **결함 아님** — 모든 쓰기가 303 |
| 세션이 풀린 채로 저장 | **결함** — 낯선 화면 · 설명 없음 · 입력 소실 |

## 2. 결함 — 로그인이 풀리면 낯선 화면으로 던져집니다

세션이 만료된 상태로 기록을 저장하면 이렇게 됐습니다.

```
HTTP 303 → /?legacy=1
```

`/?legacy=1` 은 **예전 관리자 화면**입니다. 일반 사용자는 그 화면을 본 적이 없고,
왜 저장이 안 됐는지 알 방법도 없습니다. 적어 둔 금액·메모도 사라집니다.

데이터는 안전합니다(0건 저장). 문제는 **사용자가 영문도 모른 채 적은 내용까지
잃는 것**이었습니다.

### 고친 흐름

| 겪는 일 | 이전 | 이후 |
|---|---|---|
| 돌아가는 화면 | 예전 관리자 화면 | 로그인 화면 |
| 왜 안 됐는지 | 설명 없음 | "로그인이 풀려서 저장하지 못했습니다" |
| 적어 둔 내용 | 사라짐 | 그대로 남음 |
| 로그인한 뒤 | 가계부 목록 | **쓰던 화면으로 복귀** |

왕복 전 과정을 실제로 재어 확인했습니다.

| 단계 | 결과 |
|---|---|
| 세션 만료 상태로 저장 | `303 → /my?return_to=/app?…&err=…&quick=1` |
| 저장된 기록 | 0건 |
| 로그인 화면의 폼 | `return_to` 를 지님 (`/app?…`) |
| 로그인 후 | `/app?…&quick=1` 로 복귀 |
| 비밀번호 오입력 후 재시도 화면 | `return_to` 유지 |

브라우저에서 초안 복원까지 확인했습니다.

| 단계 | 결과 |
|---|---|
| 입력 후 제출 | 초안 저장 (`62000 · "정기점검 비용" · 교통/차량`) |
| 복귀 후 | 시트 열림 · 안내 보임 · 세 값 모두 복원 · 초안 삭제 |
| JS 오류 | 0건 |

### 돌아갈 주소는 앱 안 화면만 허용합니다

로그인 뒤 이동할 주소를 폼으로 받으므로, 남이 정해 준 곳으로 갈 수 없어야 합니다.
`safeUserReturnPath` 의 허용 목록을 실제 요청으로 확인했습니다.

| 시도한 값 | 결과 |
|---|---|
| `https://evil.example/steal` | `/my` 로 차단 |
| `//evil.example/steal` | `/my` 로 차단 |
| `/admin/secret` | `/my` 로 차단 |
| 줄바꿈 섞기 (`/app\r\nSet-Cookie:…`) | `/my` 로 차단 |

### 적은 내용은 주소에 싣지 않습니다

메모가 주소창과 방문 기록에 남지 않도록, 초안은 브라우저 안(`sessionStorage`)에만
둡니다. 검사가 `amount=`·`memo=` 가 주소에 실리지 않는 것까지 확인합니다.

## 3. 결함이 아니었던 것

**저장 후 뒤로 가기.** 쓰기 응답이 본문이면 뒤로 가기가 "양식을 다시
제출하시겠습니까"를 띄우고, 확인을 누르면 같은 기록이 한 번 더 남습니다.
실제로 재보니 모든 쓰기가 303 리다이렉트로 끝나 그 일이 없습니다.

| 경로 | 응답 |
|---|---|
| 거래 저장 | 303 |
| 예산 저장 | 303 |
| 챌린지 저장 | 303 |

## 4. 회귀 검사

`validation/validate-interrupted-save-v22877.mjs` (**26개 확인**)

### 되돌려서 잡히는지 확인

| 되돌린 결함 | 검사 결과 |
|---|---|
| 관리자 화면으로 던지기 복귀 | FAIL: 예전 관리자 화면이 아니라 로그인으로 보낸다 |
| 로그인이 복귀 주소를 무시 | FAIL: 로그인하면 쓰던 화면으로 돌아온다 |
| 복귀 주소 검증 제거 (외부로 열림) | FAIL: 외부 주소로는 돌려보내지 않는다 |
| 적은 내용을 복귀 주소에 실음 | FAIL: 적어 둔 내용은 주소에 실리지 않는다 |

네 경우 모두 검사가 잡았고, 원복하면 다시 통과합니다.

## 5. 전체 결과

- 저장소 자동검사 **2,855개 전부 통과** (기존 검사 수정 없음)
- `node --check src/index.js` 통과, ESM 진입점 통과
- 신규 SQL 없음 · 환경변수 변경 없음 · 자산 주소 변경 없음

## 6. 배포 후 사람이 확인할 것

브라우저 두 개를 열고 한쪽에서 로그아웃한 뒤, 다른 쪽에서 기록을 저장해 봅니다.

1. 로그인 화면으로 이동하는지 (관리자 화면이 아니라)
2. 왜 안 됐는지 안내가 보이는지
3. 로그인하면 쓰던 화면으로 돌아오는지
4. 적었던 금액·메모가 그대로 있는지

운영 도메인은 이 환경에서 접근할 수 없어 직접 확인하지 못했습니다.

## 7. 11단계 남은 항목

- 새로고침 중 작성 내용 경고
- 카카오 그룹방에서 두 사람이 동시에 입력할 때
