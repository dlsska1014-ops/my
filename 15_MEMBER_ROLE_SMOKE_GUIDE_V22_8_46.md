# V22.8.46 참여자 관리자 승격 수동 확인

삭제해도 되는 테스트 참여자를 사용하며 소유자 본인의 역할은 변경하지 않습니다.

1. SQL 실행 결과가 `admin_allowed=true`, `invalid_role_count=0`, `readiness_ok=true`인지 확인합니다.
2. `참여자·초대 관리`에서 현재 `승인대기`인 테스트 참여자를 찾습니다.
3. 역할을 `관리자`로 선택하고 `권한 저장`을 한 번만 누릅니다.
4. 성공 안내와 함께 역할이 `관리자`로 표시되는지 확인합니다.
5. 새로고침한 뒤에도 `관리자`가 유지되는지 확인합니다.
6. 새 관리자 계정으로 해당 가계부의 참여자 화면이 열리는지 확인합니다.
7. 다른 가계부의 `household_id`를 주소에 넣어도 접근할 수 없는지 확인합니다.
8. 새 관리자가 유일한 소유자를 내보내거나 역할을 낮출 수 없는지 확인합니다.

완료 기준은 Error 1101과 빨간 DB 역할 규칙 오류가 모두 나타나지 않고 관리자 상태가 새로고침 후 유지되는 것입니다.

오류가 다시 표시되면 같은 버튼이나 기존 SQL을 반복 실행하지 말고 SQL 결과와 참여자 화면을 함께 전달합니다.
