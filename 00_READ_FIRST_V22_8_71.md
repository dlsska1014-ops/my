# V22.8.71 수정·삭제·복구 명령 결함 수정 안내

버전: `V22.8.71-EDIT-RESTORE-FIX`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음
- 환경변수·Secret·Kakao Developers·OpenBuilder 변경 없음
- 자원 주소 변경 없음 (모바일 홈 런타임은 `v22870` 그대로)

## 배경 (중요)

V22.8.69·V22.8.70 에서 세운 방식(명령을 보내고 **저장된 값을 되읽는다**)을
수정·삭제·복구 명령으로 넓혔습니다. **결함 3건**을 찾았고, 그중 2건은
사용자가 안내받은 대로 했는데도 **기록이 사라지거나 엉뚱한 기록이 바뀌는** 문제였습니다.

### 1. 날짜를 바꾸려 하면 다른 날의 기록을 건드렸습니다

`수정 01번 날짜 어제` 에서 `어제` 는 **바꿀 값**입니다. 그런데 이 값을
**대상 기록의 날짜**로도 읽어, 오늘의 01번이 아니라 **어제의 01번**을 잡았습니다.

| 입력 | 보정 전 | 보정 후 |
|---|---|---|
| `수정 01번 날짜 어제` | 어제의 01번을 건드리고 "✅ 변경했어요" | 오늘의 01번 날짜가 어제로 바뀜 |
| `수정 01번 날짜 2026-08-01` | "찾지 못했어요" | 정상 변경 |

오늘 01번은 그대로인데 **성공했다고 답했습니다.** 사용자는 바뀐 줄 알고 넘어갑니다.
게다가 앱이 실패할 때 스스로 보여 주는 예시 문구에 `👉 수정 01번 날짜 어제` 가 있었습니다.

날짜 힌트는 이제 명령 **앞에 붙은 접두부**에서만 읽습니다.
`어제 수정 01번 금액 5000` 처럼 앞에 날짜를 붙여 대상을 고르는 방식은 그대로입니다.

### 2. 두 건을 지우면 한 건만 되돌아왔습니다 (기록 소실)

삭제할 때마다 `되돌리려면: 복구 01번` 이라고 안내하는데,
되돌리기 버퍼는 **한 칸뿐**이었습니다. 두 번째 삭제가 첫 번째를 덮어써
**첫 기록은 안내와 달리 영영 복구되지 않았습니다.**

| 순서 | 보정 전 | 보정 후 |
|---|---|---|
| `삭제 01번` → "복구 01번" 안내 | | |
| `삭제 01번` → "복구 01번" 안내 | | |
| `복구 01번` | 두 번째 것만 복구 | 두 번째 것 복구 |
| `복구 01번` | **"복구할 삭제 기록이 없어요"** | 첫 번째 것도 복구 |

최근 삭제 **10건**까지 쌓아 두고, 번호가 겹치면 **가장 최근에 지운 것부터** 되돌립니다.
배포 전에 남아 있던 옛 버퍼(한 칸짜리)도 그대로 읽습니다.

### 3. 처음 기록할 때 쓴 금액 표기를 수정에는 쓸 수 없었습니다

`점심 6만원` 은 받아 주면서 `수정 01번 금액 6만원` 은
`숫자만 보내주세요` 라며 되돌렸습니다.

| 입력 | 보정 전 | 보정 후 |
|---|---|---|
| `수정 01번 금액 6만원` | 거절 | 60,000원 |
| `수정 01번 금액 3만5천원` | 거절 | 35,000원 |
| `수정 01번 금액 1만5천` | 거절 | 15,000원 |
| `수정 01번 금액 12,345` | 12,345원 | 12,345원 |

수정도 입력과 **같은 추출기**를 씁니다. 금액처럼 생기지 않은 값(`사과`)은 계속 거절합니다.

### 덧붙여: 수정 중에 새 지출을 보내면 갇혔습니다

수정 메뉴를 열어 둔 채 `라면 3300` 을 보내면 "혹시 내용을 바꾸는 건가요?" 라고 묻고,
`아니오` 라고 답해도 빠져나가는 방법을 알려 주지 않아 계속 같은 질문만 돌았습니다.
이제 `새로 기록하려면 '취소'를 먼저 보내주세요` 를 함께 안내합니다.

## 함께 확인했지만 문제가 없던 것

금액·내용·분류·결제수단 수정, 없는 번호 안내, 접두 날짜 대상 지정,
메뉴 방식(번호 고르기), 삭제 후 번호 재배열은 모두 정확했습니다.

## 이미 저장된 기록에 대해

**이 배포는 과거에 잘못 처리된 기록을 되돌리지 않습니다.**
`수정 NN번 날짜 …` 을 써서 "변경했어요"를 받았는데 실제로는 안 바뀐 기록이 있을 수 있고,
연속으로 삭제한 뒤 복구하지 못한 기록은 이미 지워졌습니다.
최근 내역에서 날짜가 이상한 항목이 있는지 확인해 주세요.

## 적용 순서

1. 현재 운영 Worker 전체 소스를 별도 파일로 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 운영 소스와 교체합니다.
3. 저장 후 배포합니다.
4. 저장소 루트에서 아래 명령을 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_71.ps1
```

5. 카카오톡에서 `수정 01번 금액 3만5천원` 이 받아들여지는지 확인합니다.
6. 두 건을 연달아 지운 뒤 안내받은 번호로 **둘 다** 복구되는지 확인합니다.
7. `수정 01번 날짜 어제` 를 보내고 **오늘 01번**의 날짜가 바뀌었는지 확인합니다.

자원 주소가 그대로이므로 웹 탭 새로고침은 필요하지 않습니다.

기존 V22.6.8·V22.7.0·V22.7.1·V22.8.46 SQL은 `/ready`가 정상이라면 다시 실행하지 않습니다.
