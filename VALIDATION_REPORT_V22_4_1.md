# V22.4.1 검증 보고서

## 정적 검증

- `node --check src/index.js` 통과
- ES module import 통과
- ZIP 무결성 검사 통과

## 기능 회귀

- V22.4 공개 페이지·AdSense 설정 회귀 통과
- 가계부 생성 무한루프 복구 회귀 통과
- 자연어 거래 저장 회귀 통과
- 닉네임 자연어 회귀 통과

## 계정 안정성 검증

- 사용자 식별값 누락 시 임시 사용자 미생성
- 동일 botUserKey 반복 요청 시 사용자 1명 유지
- 명시적 가계부 생성 시 소유자 참여행 1개 생성
- 화면 조회 시 pending 사용자를 owner로 승격하지 않음
- 관리자 감사 화면에서 복수 소유자 감지
- 관리자 계정 통합 후 거래 user_id 이전
- 보조 참여행 제거 및 주 계정 owner 유지
- 보조 사용자 행은 `merged:` 이력 계정으로 보존

## 운영 주의

- 실제 운영 계정 통합은 데이터 백업 후 관리자 판단으로만 실행
- 어떤 계정을 주 계정으로 남길지 자동 결정하지 않음
- 중복 정리 전 선택 제약 SQL 실행 금지

## 자동 시험 결과

- `smoke_v2241_identity.mjs` 통과
- V22.4 공개 페이지·온보딩 smoke 통과
- NLU 합성 평가 4,738/4,738 통과
- Supabase 요청당 450ms 지연 시험 자연어·그룹 멘션·`/기록` 약 3.9초 통과
