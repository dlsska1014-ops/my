# V22.4 검증 보고서

## 정적 검증
- `node --check src/index.js` 통과
- ES module import 및 Worker fetch 실행 통과
- ZIP 경로·버전 파일 일치 확인

## 공개 페이지 검증
- 13개 공개 HTML 페이지 HTTP 200
- 모든 페이지 공통 공개 내비게이션·내부 링크·모바일 CSS 확인
- 페이지별 텍스트 분량 약 900~1,800자
- 루트 페이지가 `/my`로 자동 이동하지 않고 공개 서비스 소개를 표시
- `/my`, `/app`, `/skill`, 관리자 경로는 기존 동작 유지
- `robots.txt`와 `sitemap.xml` 생성 확인
- `ADSENSE_PUBLISHER_ID` 미설정 시 `/ads.txt` 404
- 실제 형식의 ID 설정 시 `/ads.txt` 200과 Google DIRECT 레코드 확인
- 실제 광고 비활성 상태에서 확인 메타 태그만 포함

## 온보딩 회귀
- `가계부 생성` 시 텍스트 선택 목록 표시
- `어떤 선택이 있는데`, `선택`에서 목록 재표시
- `우리집 가계부`, `그냥 가계부`를 이름으로 인식해 확인 단계 진입
- 이름 확인 후 가계부·소유자·초대코드 생성
- `종류 다시 선택`, `이름 다시 입력`, `취소` 복구 경로 확인
- 기존 가족 종류 선택 → 이름 입력 → 생성 경로 유지

## 전체 회귀
- V22.1·V22.2·V22.3 전체 smoke 통과
- 자연어 평가 4,738/4,738 통과
- Supabase 요청당 450ms 지연 시험에서 자연어·그룹 멘션·`/기록` 약 3.9초 통과
- 거래 저장·예산·요약·정산·닉네임·초대코드 회귀 통과

## 변경하지 않은 영역
- OpenBuilder 시나리오·블록·발화
- `/skill` URL
- Supabase 기존 스키마
- NLU 운영 집계 선택 SQL
- 분석 스튜디오와 거래 저장 핵심 로직
