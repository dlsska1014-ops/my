# V22.8.72 검증 보고서

검증일: 2026-08-03

## 점검 범위

순차 점검 **9단계 — 키보드 포커스 표시와 읽기 순서**입니다.

사용자 화면 **24개 × 390/1440px**을 실제 Chromium으로 열고, 화면마다
**Tab 키를 끝까지 눌러가며** 포커스가 닿는 요소를 하나씩 쟀습니다.

## 1. 검사를 네 번 고친 뒤에야 숫자를 믿었습니다

이 점검은 "결함 0건"이 계속 나왔습니다. 0건은 좋은 소식이 아니라
**계측을 의심할 이유**였고, 실제로 네 번 모두 검사가 틀렸습니다.

| 검사 결함 | 증상 | 교정 |
|---|---|---|
| `el.focus()` 로만 확인 | 프로그램 포커스는 `:focus-visible` 을 켜지 않아 실제와 다름 | 실제 Tab 키로 이동 |
| `blur()` 후 재포커스 | 탭 순서가 문서 처음으로 되돌아가 10번째에서 끊김 | 기준값을 미리 모으고 blur 제거 |
| 나중에 생긴 요소가 모두 `null` 키 | 두 번째 요소에서 중복으로 판정해 조기 종료 | 그 자리에서 고유 키 부여 |
| `<input type=month>` 내부 필드 | 한 요소가 Tab 3번을 먹는데 같은 요소로 보고돼 조기 종료 | 한 바퀴 돌아왔을 때만 종료 |

교정 전 홈은 탭 정지가 **10개**로 보였지만 실제로는 **103개**였습니다.

그리고 **검사가 결함을 실제로 잡는지** 확인했습니다.
브라우저에서 앱의 포커스 규칙을 지우고 같은 검사를 돌리자
**103개 중 103개**를 결함으로 보고했습니다. 그제야 0건을 믿었습니다.

## 2. 결함: 본문에 닿는 길이 없었습니다

키보드만 쓰는 사람은 화면을 열 때마다 상단바·사이드바를 전부 지나야 본문에 닿습니다.
건너뛸 방법이 **24개 화면 전부에 없었습니다** (WCAG 2.4.1 Bypass Blocks, Level A).

| 화면 | 보정 전 | 보정 후 |
|---|---:|---:|
| 홈·거래 내역·캘린더 (1440px) | **55회** | 1회 |
| 통계·종합 리포트 (1440px) | **52회** | 1회 |
| 예산·월 마감·연간·스마트 도구·예산 알림·설정 (1440px) | 16회 | 1회 |
| 자산·계좌·저축·목표 (1440px) | 11회 | 1회 |
| 홈·거래 내역·캘린더 (390px) | 13회 | 1회 |
| 나머지 (390px) | 9회 | 1회 |

데스크톱 홈이 55회인 것은 사이드바 달력의 **날짜 링크 31개** 때문입니다.
화면을 옮길 때마다 그 55번을 처음부터 다시 눌러야 했습니다.

### 보정하며 지킨 제약

| 결정 | 이유 |
|---|---|
| `display:none`·`visibility:hidden` 을 쓰지 않음 | 그렇게 감추면 **포커스가 가지 않아** 링크가 없는 것과 같다 |
| `position:fixed` 로 화면 밖에 두고 포커스 시 이동 | 레이아웃을 밀지 않으면서 포커스는 살아 있다 |
| 셸 삽입 지점 한 곳에서 처리 | 화면 템플릿 88곳을 고치면 빠뜨리는 화면이 생긴다 |
| `main` 에 `id` 가 이미 있으면 그것을 사용 | 남의 `id` 를 갈아 끼우면 그 `id` 를 쓰던 링크가 끊긴다 |
| `main` 에 `tabindex="-1"` 부여 | 없으면 주소만 바뀌고 **포커스는 그대로**여서 다음 Tab이 다시 내비로 간다 |
| `main:focus-visible` 은 왼쪽 짧은 표시만 | 본문 전체를 두르는 굵은 사각형은 신호보다 오류처럼 읽힌다 |
| 최소 높이 44px | V22.8.68 에서 세운 조작 영역 기준을 새 요소에도 적용 |

## 3. 문제가 없던 것

| 항목 | 표본 | 결과 |
|---|---|---|
| 포커스 표시 | 24화면 × 2폭, Tab으로 닿는 전 요소 | **0건** |
| 클릭 전용(키보드로 못 닿는) 요소 | 24화면 | 0건 |
| `aria-hidden` 안의 조작 요소 | 24화면 × 2폭 | 0건 |
| 탭 순서가 화면 순서를 크게 거스르는 곳 | 24화면 | 0건 |

포커스 표시는 전역 `:focus-visible` 규칙이 `!important` 로 덮고 있어
`.form input{outline:none!important}` 같은 개별 제거보다 우선합니다.

## 4. 성능·자원

| 항목 | 예산 | 실측 | 여유 |
|---|---|---|---|
| 기준 픽스처 홈 HTML | 35,840 B | 35,058 B | 782 B |
| 200행 실사용 홈 HTML | 45,056 B | 44,647 B | 409 B |

건너뛰기 링크와 `main` 속성으로 화면당 **약 100 B** 늘었습니다.

| 자원 | 보정 전 | 보정 후 |
|---|---:|---:|
| `accountbook-shell-*.css` | 149,831 B (`v22868`) | 151,228 B (`v22872`) |
| `mobile-home-*.js` | 30,735 B (`v22870`) | 동일 |

> 실사용 여유가 **409 B** 까지 줄었습니다. 이 수치는 픽스처가 날짜 기준으로
> 만들어져 측정일에 따라 흔들리므로, 다음 변경에서 카드 마크업이 조금만 커져도
> 예산을 넘길 수 있습니다. 예산 재검토가 필요합니다.

## 5. 자동 검사

- 신규 회귀 **178개** (`validation/validate-skip-to-content-v22872.mjs`)
- 저장소 전체 **2,621개** 자동검사 통과
- ESM `default.fetch` 확인, 체크섬 매니페스트 일치

신규 회귀도 **결함을 되돌려** 실제로 잡히는지 확인했습니다.

- 링크 삽입을 끔 → `홈: 본문 바로가기 링크가 있다` 실패
- `main` 의 `tabindex` 제거 → `홈: main 이 포커스를 받을 수 있다` 실패

신규 회귀가 고정하는 것:

- 24개 화면 전부에 본문 바로가기가 있고, **문서에서 첫 번째 포커스 대상**이다
- 링크가 가리키는 `main` 이 실제로 있고 포커스를 받을 수 있다
- 포커스를 막는 방식(`display:none`·`visibility:hidden`)으로 감추지 않는다
- 포커스를 받으면 화면 안으로 들어오고, 최소 높이 44px 을 지킨다
- 이미 `id` 가 있는 `main` 은 그 `id` 를 그대로 쓴다
- 셸을 쓰지 않는 화면에는 넣지 않는다

## 6. 운영 변경

- 신규 SQL 없음
- 환경변수·Secret 변경 없음
- Kakao Developers·OpenBuilder 변경 없음
- 셸 스타일시트 주소 `v22868` → `v22872` (immutable)

## 배포 후 확인 부탁드립니다

1. 아무 화면에서 **Tab 한 번** → `본문 바로가기` 가 왼쪽 위에 나타나는지
2. Enter → 본문으로 이동하고, 그다음 Tab이 본문 안에서 이어지는지
3. 마우스로 쓸 때는 이 링크가 보이지 않는지
4. 오래 열어 둔 웹 탭 새로고침 후 `v22872` 셸 적용

이 배포는 데이터를 바꾸지 않습니다.
