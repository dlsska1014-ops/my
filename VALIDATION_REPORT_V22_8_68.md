# V22.8.68 검증 보고서

검증일: 2026-08-02

## 점검 범위

순차 점검 **5단계 — 접근성·조작 영역**입니다.

사용자 화면 24개를 390px(모바일)과 1440px(데스크톱) 실제 Chromium으로 렌더하고,
보이는 조작 요소마다 **실제 탭 영역 크기**와 이름·구조를 검사했습니다.

## 1. 측정 기준을 먼저 고쳤습니다

첫 측정에서 체크박스 61건이 잡혔지만 대부분 오탐이었습니다.
체크박스가 `<label>` 안에 들어 있으면 **라벨 전체가 탭 영역**인데,
입력 상자(13×13px)만 재고 있었습니다.

라벨 크기로 다시 재자 61건 → 58건이 되었고, 남은 것은 전부 실제 결함이었습니다.

## 2. 조작 영역 (결함 58건 → 1건)

| 대상 | 보정 전 | 보정 후 |
|---|---|---|
| 상단바 가계부 이름 링크 (18개 화면) | 185×28 | 44px 이상 |
| 종합 리포트 접기 헤더 6종 | 336×29 | 44px 이상 |
| 자산·계좌 `상세 정보 수정` | 310×20 | 44px 이상 |
| 자산·계좌 `은행·발급사와 메모 입력` | 308×20 | 44px 이상 |
| 자산·계좌 `누가 이 정보를 볼 수 있나요?` | 336×20 | 44px 이상 |
| 통계 `표로 보기` | 324×19 | 44px 이상 |
| 예산 `계산 기준 보기` | 324×25 | 44px 이상 |
| 정기지출 `분류·결제수단·메모 추가 입력` | 336×25 | 44px 이상 |
| 영수증 `사진이 잘 읽히지 않을 때` | 328×25 | 44px 이상 |
| 처음 사용 가이드 `함께 쓰기·예산 등 추가 설정` | 324×25 | 44px 이상 |
| 가계부 전환·추가 `초대코드 보기` | 306×25 | 44px 이상 |
| 홈 `월간 리포트 →` | 75×15 | 44px 이상 |
| 홈 `최근 기록 확인 →` | 92×19 | 44px 이상 |
| 통계·리포트 `거래 캘린더 보기` | 92×16 | 44px 이상 |
| 통계·리포트 `전체 예산 보기` | 77×19 | 44px 이상 |
| 가계부 카드 `열기`·`옵션`·`멤버` | 58×39 | 44px 이상 |
| 가계부 전환·추가 `목록만 보기` | 84×25 | 44px 이상 |
| 자산 등록 `＋ 빠른 등록` | 83×22 | 44px 이상 |
| 자산 등록 `＋ 카카오페이 등록` 등 | 129×35 | 44px 이상 |
| 자산·계좌 표의 자산명 링크 | 51×15 | 여백 확장 |
| 예산 알림 표의 분류 링크 | 26×15 | 여백 확장 |
| 가져오기·백업 중복 제외 체크박스 라벨 | 280×15 | 44px 이상 |
| 종합 리포트 `챌린지 표시` 라벨 | 318×24 | 44px 이상 |
| 부부 정산 완료 확인 라벨 | 316×39 | 44px 이상 |

**남은 1건**: 통계 화면 하단 문장 안의 `종합 리포트` 링크(62×14).
문장 흐름 안에 놓인 인라인 링크는 WCAG 2.5.8이 명시적으로 예외로 두므로
키우지 않고 그대로 두었습니다.

## 3. 보정 방식과 지킨 제약

규칙은 **셸 스타일시트에만** 넣었습니다. 사용자 화면 HTML은 한 바이트도 늘지 않았습니다.

| 결정 | 이유 |
|---|---|
| 접기 헤더는 `display`를 바꾸지 않고 `min-height`만 | `display:flex`로 바꾸면 목록 표시(`::marker`)가 사라져 접힘 여부를 알 수 없다 |
| `align-content:center`는 진행적 향상으로만 | 지원하지 않는 브라우저에서는 위쪽 정렬로 남고, 탭 영역은 그대로 44px |
| 표 안 링크는 여백을 바깥으로 (`margin-block:-12px`) | 행 높이를 늘리지 않으면서 탭 영역만 넓힌다 |
| `min-height`만 사용 (`height` 아님) | 이미 44px 이상인 요소는 그대로 유지된다 |

중간에 한 번 되돌린 결정이 있습니다. 처음에는 `.kwHead`(분류·키워드 접기 헤더)를
"이미 충분히 크다"고 보고 규칙에서 제외했는데, 실측 23px이었습니다.
`min-height`는 큰 요소를 줄이지 않으므로 제외 자체가 불필요했고, 제거했습니다.

`inline` 라벨에 `min-height`가 걸리지 않는다는 점도 재측정으로 확인해
표시 방식을 함께 바꿨습니다.

## 4. 구조 접근성 (결함 없음)

24개 화면 × 2개 폭에서 아래는 **전부 0건**이었습니다.

| 항목 | 결과 |
|---|---|
| 이름 없는 폼 컨트롤 | 0건 |
| 이름 없는 버튼·링크·`summary` | 0건 |
| `alt` 없는 이미지 | 0건 |
| `h1` 누락 / 중복 | 0건 |
| 헤딩 단계 건너뜀 | 0건 |
| 중복 `id` | 0건 |
| 존재하지 않는 `for`·`list`·`aria-*` 참조 | 0건 |
| 양수 `tabindex` | 0건 |
| `html lang` 누락 | 0건 |
| `main` 랜드마크 누락 | 0건 |

## 5. 회귀 없음 확인

같은 72개 조합(24개 화면 × 390/768/1440px) 레이아웃 점검을 보정 전후로 돌렸습니다.

| 항목 | main | V22.8.68 | 차이 |
|---|---:|---:|---:|
| 페이지 오류 | 0 | 0 | 0 |
| HTTP 실패 | 0 | 0 | 0 |
| 가로 넘침 | 0 | 0 | 0 |
| 박스 이탈 | 8 | 8 | 0 |
| 요소 겹침 | 513 | 511 | −2 |
| 정의되지 않은 CSS 토큰 | 0 | 0 | 0 |
| 작은 조작 영역 | 292 | 150 | **−142** |

박스 이탈 8건과 겹침 511건은 보정 전부터 있던 값으로,
겹침은 고정 하단바가 스크롤된 본문 위에 그려지는 정상 동작을 세는 오탐입니다.

## 6. 성능·자원

| 항목 | 예산 | 실측 | 여유 |
|---|---|---|---|
| 기준 픽스처 홈 HTML | 35,840 B | 34,958 B | 882 B |
| 200행 실사용 홈 HTML | 45,056 B | 44,216 B | 840 B |

**HTML 바이트 변화 0.** 규칙이 전부 외부 스타일시트에 있기 때문입니다.

| 자원 | 보정 전 | 보정 후 |
|---|---:|---:|
| 셸 스타일시트 | 147,979 B | 149,831 B (+1,852 B) |
| `accountbook-nav-v22862.js` | 19,742 B | 동일 |
| `accountbook-v5-v22861.js` | 62,354 B | 동일 |

셸 주소를 `/assets/accountbook-shell-v22868.css`로 올렸습니다.
이전 주소 `v22865`는 404로 응답합니다(회귀 검사로 고정).

## 7. 자동 검사

- 신규 회귀 **278개** (`validation/validate-tap-target-a11y-v22868.mjs`)
- 저장소 전체 **2,251개** 자동검사 통과
- ESM `default.fetch` 확인, 체크섬 매니페스트 일치

신규 회귀가 고정하는 것:

- 셸 스타일시트에 조작 영역 규칙이 실려 있고, 접기 헤더의 `display`는 바뀌지 않는다
- 24개 화면 전부에 `h1`·`main`·`lang`·제목이 있다
- 24개 화면에 중복 `id`, 끊긴 `for`·`list` 참조, 양수 `tabindex`, 이름 없는 입력,
  `alt` 없는 이미지가 없다
- 셸을 쓰는 화면은 새 스타일시트를 정확히 한 번만 부르고, 이전 주소는 404다
- 홈 HTML 이 35KiB 예산 안에 있다

## 8. 운영 변경

- 신규 SQL 없음
- 환경변수·Secret 변경 없음
- Kakao Developers·OpenBuilder 변경 없음

## 배포 후 확인 부탁드립니다

1. 종합 리포트의 접기 헤더(`이번 달 지출 구성`, `큰 지출 체크`)를 **한 번에** 눌러지는지
2. 자산·계좌의 `상세 정보 수정`, 예산의 `계산 기준 보기`가 첫 탭에 열리는지
3. 홈의 `최근 기록 확인 →`, `월간 리포트 →` 링크가 첫 탭에 이동하는지
4. 오래 열어 둔 탭을 새로고침해 `v22868` 셸이 적용되는지
