# V22.8.52 심층 점검 보고서 (3라운드)

점검 기준: `V22.8.52-SKILL-IP-GUARD-CSV-SAFE-NAME-FIX`

선행 점검
- 1라운드 `FULL_INSPECTION_REPORT_V22_8_50.md` — 기능 결함 6건 (V22.8.51에서 수정)
- 2라운드 `SECURITY_AND_FEATURE_AUDIT_V22_8_51.md` — 보안 2건·기능 1건 (V22.8.52에서 수정)

이번 라운드는 앞선 두 번이 다루지 않은 **금액 계산 정확성, 동시성, 실패 주입,
데이터 수명주기, 카카오 심층 흐름, 경계값**을 점검했습니다.

---

## 1. 요약

| 영역 | 검사 | 결과 |
| --- | ---: | --- |
| 금액 계산·집계 정확성 | 19 | 결함 **1건** |
| 동시성 (공유 설정 쓰기 손실) | 3 | 이상 없음 |
| 실패 주입 (DB 오류 중 데이터 보존) | 5 | 이상 없음 |
| 데이터 수명주기 | 8 | 이상 없음 |
| 카카오 그룹 응답 계약 | 4 | 이상 없음 |
| 경계값 (장문·이모지·대량) | 6 | 이상 없음 |
| **합계** | **45** | **결함 1건** |

앞선 두 라운드에서 각각 6건·3건이 나온 것과 달리, 이번에는 **1건**만 확인됐습니다.
동시성·실패 처리·수명주기는 설계 의도대로 견고하게 동작합니다.

---

## 2. M1 (Medium) — 홈 화면의 예산 사용률이 100%에서 잘려 초과 지출을 숨김

### 증상

예산을 초과해도 `/app` 홈의 "예산 사용률"이 항상 **100%**로 표시됩니다.

예산 200,000원에 지출 414,680원(실제 **207%**)인 상태에서:

| 화면 | 표시 |
| --- | --- |
| `/app` 홈 | 예산 사용률 **100%** · 이번 달 쓸 수 있는 돈 0원 |
| `/budgets` | **207%** |

같은 지표가 화면마다 다르게 나옵니다.

### 원인

`src/index.js` — 막대 너비와 표기 숫자가 **같은 상한 처리 값**을 씁니다.

```js
const budgetPercent = budgetTotal ? Math.round((budgetUsed / budgetTotal) * 100) : 0;
const displayBudgetPercent = Math.max(0, Math.min(100, budgetPercent || 0));
...
<em>예산 사용률 ${displayBudgetPercent}%</em>
<div class="homeProgress"><i style="width:${displayBudgetPercent}%"></i></div>
```

막대 너비를 100%에서 멈추는 것은 정상이지만, **표기 숫자까지 같이 잘립니다.**

상한 없는 `budget.rate`를 쓰는 `<section id="budget">`이 원래 홈에 있었지만, V5
후처리가 그 섹션을 제거하므로(`source.replace(/<section id="budget" class="panel">[\s\S]*?<\/section>/, "")`)
`/app`에서는 실제 사용률을 볼 수 있는 곳이 없습니다.

### 영향

예산 관리가 이 서비스의 핵심 가치인데, 가장 먼저 보는 홈 지표가 초과 정도를
숨깁니다. 207% 사용자와 100% 사용자가 같은 화면을 봅니다. "쓸 수 있는 돈 0원"과
"전체 예산 200,000원 / 지출 414,680원"에서 유추할 수는 있지만, 헤드라인 숫자 자체가
틀렸습니다.

### 수정 (V22.8.53에 반영)

막대와 표기를 분리했습니다.

```js
const budgetBarPercent = Math.max(0, Math.min(100, budgetPercent || 0));  // 막대 너비
const displayBudgetPercent = Math.max(0, budgetPercent || 0);             // 표기 숫자
```

| 상태 | 홈 표기 | 막대 너비 |
| --- | --- | --- |
| 414,680 / 200,000 | **207%** | 100% |
| 414,680 / 1,000,000 | 41% | 41% |

`/budgets`와 값이 일치합니다. 회귀 검사 2개를 추가했습니다.

---

## 3. 이상 없음을 확인한 영역

### 금액 계산·집계 (19개 중 18개 통과)

| 항목 | 결과 |
| --- | --- |
| 월 수입·지출 합계 | 독립 계산과 정확히 일치 |
| 1원 거래 저장·집계 | 정상 (V22.8.51 소액 수정 확인) |
| 분류 예산 사용률 | 정확 |
| 정산 총액·인원·1인 부담 | 나누어떨어지지 않는 금액에서도 오차 인원수 이내 |
| 분석 화면 집계 | 8월 지출 414,680 정확 (7월 행은 전월 비교용) |
| 인접 월 혼입 | 7·8·9월 상호 격리 |
| 12월→1월 롤오버 | 정확 |
| 수입/지출 부호 분리 | 정확 |

### 동시성 — 쓰기 손실 없음

`accountbook_settings` JSON을 읽고-수정-쓰는 경로(목표·즐겨찾기)에 동시 요청을
보냈습니다.

- 목표 5건 동시 생성 → 성공 응답 수와 저장 건수 일치, 손실 0
- 즐겨찾기 4건 동시 토글 → 성공분 전부 보존
- 경합은 409 `goal_write_in_progress`로 거절되며 조용히 덮어쓰지 않음

operation lease가 의도대로 동작합니다.

### 실패 주입 — 부분 쓰기 없음

Supabase가 쓰기 도중 `53300`을 반환하도록 주입했습니다.

| 시나리오 | 결과 |
| --- | --- |
| 거래 INSERT 실패 | 행 미생성, 오류 안내, 예산 등 다른 저장소 무변경 |
| 설정(목표) 쓰기 실패 | 기존 목표 그대로 보존, 4xx/5xx 오류 응답 |

"실패 시 기존 데이터 유지" 계약이 실제로 지켜집니다.

### 데이터 수명주기

| 항목 | 결과 |
| --- | --- |
| 가계부 삭제 | 거래·참여자 연결까지 함께 정리, 고아 데이터 0 |
| 가계부 삭제 재인증 | `reauth=household-delete` 단계 필수 (보안 통제 정상) |
| 참여자 방출 | 참여 연결만 제거, **가계부 거래는 보존** |
| 방출자 거래 렌더 | 화면 정상 |

### 카카오 그룹 응답 계약 (AGENTS.md 보호 기준)

`botGroupKey`가 있는 요청 응답에 **QuickReplies·CommerceCard·Carousel 모두 없음**을
확인했습니다. 1:1 응답은 정상 동작합니다.

### 경계값

| 입력 | 결과 |
| --- | --- |
| 5,000자 메모 | 저장 시 잘림, 화면 정상 |
| 이모지 + `<b>` 태그 메모 | 저장·렌더 정상, HTML 이스케이프 적용 |
| 이모지 CSV 내보내기 | 정상 |
| 600행 가져오기 미리보기 | 정상 처리 |

---

## 4. 확인하지 못한 항목

- **운영 Supabase의 RLS·GRANT** — 모든 점검은 인메모리 백엔드 기준입니다.
- **`accountbook_apply_recurring_v227`** — 픽스처 미구현으로 `/admin/recurring/apply`는
  3라운드 연속 자동 검증 범위 밖입니다. 픽스처에 이 RPC를 추가하면 닫힙니다.
- **S1의 근본 위험** — `/skill`의 `botUserKey`는 여전히 만료·회수 없는 자격증명입니다.
  V22.8.52의 IP 상한은 완화책이며, 공유 시크릿 헤더는 Kakao OpenBuilder 콘솔 확인
  후 진행해야 합니다.
- **실기기·실제 스프레드시트** — 접근성, 가상 키보드, Excel에서의 CSV 열람.

---

## 5. 세 라운드 종합

| 라운드 | 범위 | 결함 |
| --- | --- | ---: |
| 1 | 기능·라우트·권한·입력 경계 | 6 |
| 2 | 비인증 보안·인젝션·E2E | 3 |
| 3 | 금액 정확성·동시성·실패·수명주기·카카오·경계값 | 1 |

라운드를 거듭할수록 결함이 줄었고, 남은 1건도 표시 계층에 한정됩니다. 데이터
무결성·권한·동시성·실패 처리 같은 기반 계층에서는 이번 라운드에서 결함이 나오지
않았습니다.

**유일하게 열려 있는 위험은 `/skill`의 호출자 인증**이며, 이는 Kakao OpenBuilder
콘솔 확인이 선행되어야 합니다.
