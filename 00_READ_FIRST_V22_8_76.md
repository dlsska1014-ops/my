# V22.8.76 카카오 의도 정리·스킬 인증 관측 안내

버전: `V22.8.76-KAKAO-INTENT-SKILL-AUTH`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음
- **환경변수 1개 추가(선택)**: `KAKAO_SKILL_AUTH_MODE`
- 자산 주소 변경 없음 (웹 탭 새로고침 불필요)

---

## ⚠️ 먼저 확인하세요 — `/skill` 인증이 이미 켜져 있을 수 있습니다

`KAKAO_SKILL_SECRET` 을 Cloudflare 에 넣는 **그 순간부터** 헤더 없는 `/skill` 호출은
전부 403 입니다. OpenBuilder 헤더를 아직 안 넣었다면 그때부터 모든 카카오톡 대화가
실패합니다.

그런데 실패한 사용자에게는 평범한 **"다시 보내주세요"** 가 갑니다. 운영자가 볼 수
있는 신호는 `/health` 의 `skill_caller_auth_configured: true` 뿐이었고, 이것은
"비밀값이 설정됨"만 뜻합니다. **통과하고 있는지는 알 수 없었습니다.**

### 이번에 넣은 것

`/health` 에 실제 통과·거부 집계가 실려 나갑니다.

```json
"skill_caller_auth": {
  "mode": "enforce",
  "configured": true,
  "accepted": 128,
  "denied": 0,
  "last_accepted_at": "2026-08-06T09:20:00.000Z",
  "last_denied_at": "",
  "header_missing_suspected": false
}
```

`accepted` 가 0인데 `denied` 만 쌓이면 `header_missing_suspected` 가 `true` 가 됩니다.
**헤더 설정이 빠졌다는 뜻입니다.**

### 안전하게 켜는 순서

1. Cloudflare 에 `KAKAO_SKILL_AUTH_MODE = observe` 를 먼저 넣습니다.
2. `KAKAO_SKILL_SECRET` 을 넣습니다. → 이 상태에서는 **막지 않고 세기만** 합니다.
3. OpenBuilder 스킬 서버 설정에서 헤더를 추가합니다. 셋 중 아무거나 됩니다.
   - `x-kakao-skill-secret: <비밀값>`
   - `x-api-key: <비밀값>`
   - `Authorization: Bearer <비밀값>`
4. `/health` 에서 `accepted` 가 올라가고 `denied` 가 멈추는지 봅니다.
5. 확인되면 `KAKAO_SKILL_AUTH_MODE` 를 **지웁니다**. 그때부터 차단됩니다.

### 이미 켜서 카카오톡이 멈춘 경우

`KAKAO_SKILL_AUTH_MODE = observe` 한 줄만 넣으면 **즉시 복구**됩니다.
그 뒤 위 3~5단계를 진행하세요.

> 값을 오타로 적으면(`observ` 등) 열리지 않고 **차단 쪽으로** 떨어집니다.
> 실수로 인증이 풀리는 일은 없습니다.

---

## 이번에 고친 것

### 1. 되묻기가 왜 뜨는지 알 수 없던 문제

`주유 5만원` 처럼 **분류 이름 + 금액**만 보내면 지출인지 예산인지 되물었습니다.
판단 기준은 코드에 박힌 21개 단어 목록이라, 사용자 눈에는 규칙이 없어 보였습니다.

| 입력 | 이전 | 이후 |
|---|---|---|
| `카페 4500` | 되묻기 | 지출 기록 |
| `커피 4500` | 지출 기록 | 지출 기록 |
| `주유 5만원` | 되묻기 | 지출 기록 |
| `주유소 5만원` | 지출 기록 | 지출 기록 |
| `외식 2만원` | 되묻기 | 지출 기록 |
| `점심 12000` | 지출 기록 | 지출 기록 |

**전부 지출로 통일했습니다.** 대부분 지출이라는 뜻으로 보내고, 잘못 기록돼도
`수정`·`삭제` 가 이미 있습니다.

금액이 없어 진짜로 모호한 말(`예산`, `가계부`, `초대`, `이름변경`)은 그대로 되묻습니다.

### 2. "예산"이라고 말했는데 지출이 남던 결함

| 입력 | 이전 | 이후 |
|---|---|---|
| `식비 예산 30만원` | **지출 30만원 기록** | 식비 예산 설정 |
| `교통 예산 10만원` | **지출 10만원 기록** | 교통 예산 설정 |

`예산` 뒤에 `설정`·`저장`·`등록` 이 붙어야만 예산 명령으로 봤습니다.
이제 **꼬리가 금액 하나뿐이면** 그 말도 예산으로 받습니다.

`회사 예산 회의 5000` 처럼 금액 앞에 다른 말이 붙으면 지출로 남습니다.
`남은 예산`·`식비 예산 얼마야` 같은 조회도 그대로입니다.

### 3. 앱이 안내하는 명령이 한 번도 동작하지 않았습니다

도움말과 소개 화면이 직접 예시로 드는 문장입니다.

| 입력 | 이전 | 이후 |
|---|---|---|
| `전체 예산설정 300만원` | **"등록된 카테고리를 찾지 못했어요"** | 전체 월 예산 설정 |
| `총액 예산설정 300만원` | 같은 오류 | 전체 월 예산 설정 |

원인은 정규화가 두 번 도는 것이었습니다. 명령 파서가 `전체` → `__total` 로 바꾸고,
분류 해석이 그 `__total` 을 **또** 정규화하면서 밑줄이 지워져 `total` 이 됐습니다.
등록된 분류에 `total` 은 없으니 매번 실패했습니다.

---

## 판단하고 그대로 두기로 한 것

- **사이드바 달력 날짜 34개를 Tab 순서에서 빼지 않습니다.** 데스크톱에서 전체 탭
  정지점 54개 중 34개가 달력이지만, V22.8.72 의 `본문 바로가기` 로 Tab 한 번이면
  본문에 갑니다. 빼면 키보드로 달력 날짜를 아예 누를 수 없게 되어 손실이 더 큽니다.
- **구성원(member)에게 예산·정기지출 저장 권한을 열지 않습니다.** 역할별로 실제
  요청을 보내 확인한 결과 화면뿐 아니라 서버에서도 제대로 막고 있어 보안 문제는
  없습니다. 권한이 필요하면 그 사람을 `관리자` 로 올리면 됩니다.

## 적용 순서

1. 현재 운영 Worker 전체 소스를 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 교체하고 배포합니다.
3. 저장소 루트에서 아래를 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_76.ps1
```

4. `/health` 의 `skill_caller_auth` 를 확인합니다.
5. 카카오톡에서 `주유 5만원` 을 보내 바로 지출로 저장되는지 봅니다.
6. `전체 예산설정 300만원` 을 보내 전체 예산이 설정되는지 봅니다.

기존 SQL 은 다시 실행하지 않습니다.
