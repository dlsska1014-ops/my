# 다음 업데이트 계획

현재 기준은 `V22.8.67-ROLE-SURFACE-EMPTY-STATE`입니다.

순차 점검 1단계(테마 대비)·2단계(기능 흐름)·3단계(권한별 화면)·4단계(빈 상태)를 끝냈습니다.
남은 순서는 아래와 같습니다.

1. 실기기에서 거래 삭제, 라이트·다크 × 포인트 컬러 4종, 홈 화면 추가 확인
2. 실기기에서 구성원·조회 전용 계정의 분류·키워드 화면과 저장 복귀 확인
3. 카카오 인앱 브라우저에서 빠른 입력 시트 스크롤 잠금 확인
4. 구성원에게 예산·정기지출 저장 권한을 열지 여부 결정(현재는 소유자·관리자 전용)
5. `/skill` 호출자 검증을 위한 OpenBuilder 커스텀 헤더 지원 여부 확인 및 선택적 공유 시크릿 설계
6. 9,000건 이상 분석을 위한 가계부·기간 범위 집계 RPC 설계와 별도 승인
7. 통계·종합 리포트·월 마감 사이 중복 카드와 중복 CSS 정리

운영 검증 없이 비동기 저장이나 데이터 계약을 확대하지 않으며, 기존 URL과 서버 저장 흐름을 유지합니다.
