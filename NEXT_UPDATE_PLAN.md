# 다음 업데이트 계획

현재 기준은 `V22.8.84-RECEIPT-PARSE-ACCURACY`입니다.

## 남은 판단

1. **실기기 확인** — 라이트/다크 로고 또렷함, 사이드바 위계, 소비 흐름 탭,
   정기 항목 수정 폼, **컬러톤 4종 × 라이트/다크 히어로**. 손가락 터치와
   iOS Safari 는 기기에서만 확인된다
2. **`budgets`(맨 이름) 잔존 표** — 코드 호출 0건. 운영 DB 에서 내용을 본 뒤
   지울지 결정한다(`select count(*) from public.budgets;`)
3. **홈 일별 그래프 56KB** — 지금은 고른 사람만 받는다. 더 가벼운 일별 렌더가
   필요하면 `renderReadableDailyTrend` 의 홈 전용 축약본을 따로 만들어야 한다
4. **영수증 OCR 엔진 자체** — V22.8.84 는 파싱 계층만 고쳤다. 인식 원문의 품질을
   올리려면 판단이 필요하다. 아래 "영수증 OCR" 절 참고
5. **페이지 인라인 CSS 의 남은 리터럴 색** — V22.8.83 에서 히어로 62곳을 톤에
   연결했다. 카드·배지 등 히어로가 아닌 리터럴은 아직 남아 있다. 실제로 톤과
   어긋나 보이는 것이 나올 때 그 화면만 옮긴다

### 끝난 판단

- ~~`/admin/recurring/*` 계열을 어떻게 할지~~ — V22.8.80 에서 `/reserve-plans` 로 합쳤다
- ~~예산 저장소 폴백 코드 정리~~ — V22.8.80 에서 걷어냈다(SQL 03 적용으로 안전해짐)

## 영수증 OCR

지금은 **브라우저 안에서 Tesseract.js 5.1.1** 로 돌린다. 사진이 기기 밖으로
나가지 않는 것이 이 방식의 가장 큰 장점이다.

- V22.8.84 에서 **파싱 계층**을 고쳤다. 인식 원문이 같아도 결과가 달라진다
  - 사업자등록번호·카드번호·승인번호·전화번호 줄을 먼저 버린다
  - 합계·총액이 OCR 로 깨져도(`합게`·`힙계`·`총맥`) 알아본다
  - 키워드가 아예 안 잡히면 "가장 큰 숫자" 가 아니라 **돈처럼 생긴 것**(쉼표 또는
    "원")만 후보로 삼는다. 예전에는 승인번호 `30412857` 이 금액이 됐다
  - 날짜도 첫 매치가 사업자번호면 포기하지 않고 계속 찾는다
- **인식 원문 자체를 올리려면 다음 중 하나가 필요하다. 아직 판단 전이다**
  1. Tesseract 튜닝 — `tessedit_pageseg_mode=6`(영수증은 한 칸짜리 세로 열),
     이진화·대비 보정, 축소 상한(현재 1400~1800px) 재검토. 저비용이지만
     **실기기 사진 없이는 개선을 증명할 수 없다.** 표본 사진이 먼저 필요하다
  2. 서버측 클라우드 OCR — 네이버 CLOVA OCR 은 영수증 전용 모델이 있어 상호·날짜·
     총액을 구조화해 돌려준다. 정확도 차이가 크지만 **비용·API 키·사진이 외부로
     나가는 문제**가 따라오고 개인정보처리방침도 함께 고쳐야 한다

순차 점검 1~10단계를 끝냈습니다.
(테마 대비 · 기능 흐름 · 권한별 화면 · 빈 상태 · 조작 영역 · 카카오 입력 파싱 ·
저장값 되읽기 · 수정·삭제·복구 왕복 · 키보드 포커스·읽기 순서 ·
스크린리더 읽기·상태 변화 알림)

## 11단계 — 끊김·중단 상황에서의 복구 (완료)

지금까지는 "정상적으로 끝나는 길"만 봤습니다. 11단계는 **중간에 끊겼을 때**를 봅니다.
사용자가 실제로 겪는 실패는 대부분 여기서 나오고, 아직 한 번도 훑지 않은 영역입니다.

| 볼 것 | 확인할 질문 |
|---|---|
| ~~네트워크 끊김~~ | V22.8.76 — 데이터 안전(0건), 오류 문구 정상, 입력 초안 보존 추가 |
| ~~중복 제출~~ | V22.8.76 — 웹은 이미 막혀 있었고 카카오 재전송만 열려 있어 수정 |
| ~~뒤로 가기~~ | V22.8.77 — 모든 쓰기가 303 이라 재제출 없음 (결함 아님) |
| ~~세션 만료~~ | V22.8.77 — 로그인으로 보내고 쓰던 화면·입력까지 복구 |
| ~~새로고침~~ | V22.8.78 — 경고가 아예 없어 말없이 사라지던 것 수정 |
| ~~카카오 재시도~~ | V22.8.76 — 동시 재전송 중복 수정 |
| ~~부분 실패~~ | V22.8.76 — 예산 일괄 저장은 단일 RPC 로 전부 되거나 전부 안 됨 (결함 아님) |

7개 항목 중 **결함 4건을 고치고 3건은 이미 안전함을 확인**했습니다.
그룹방 동시 입력(V22.8.78)도 함께 확인해 결함이 없었습니다.

남은 순서는 아래와 같습니다.

1. 배포 후 카카오톡에서 `커피 12,345원`·`커피 3만5천원` 금액 확인
2. 과거에 틀리게 저장된 금액·잘린 메모 점검 (자동 복구 없음)
3. 실기기에서 거래 삭제, 라이트·다크 × 포인트 컬러 4종, 홈 화면 추가 확인
4. 실기기에서 접기 헤더·카드 헤더 링크가 첫 탭에 반응하는지 확인
5. 카카오 인앱 브라우저에서 빠른 입력 시트 스크롤 잠금 확인
6. ~~`주유 5만원`·`택시 12000` 되묻기를 유지할지 제품 판단~~ — V22.8.76 에서 제거(전부 지출로 통일)
7. 배포 후 `수정 01번 금액 3만5천원`과 연속 삭제 후 두 건 복구 확인
8. 과거에 날짜가 바뀌지 않은 기록 점검 (자동 복구 없음)
9. `/skill` 인증 켜기 — V22.8.76 에서 관측 수단과 observe 모드를 준비. **남은 것은 OpenBuilder 헤더 설정(운영자)**
10. ~~데스크톱 사이드바 달력의 날짜 링크 31개를 탭 순서에서 줄일지 판단~~ — 유지로 결정(2026-08-06)
11. ~~구성원에게 예산·정기지출 저장 권한을 열지 여부 결정~~ — 소유자·관리자 전용 유지로 결정(2026-08-06)
12. 9,000건 이상 분석을 위한 가계부·기간 범위 집계 RPC 설계와 별도 승인
13. ~~통계·종합 리포트·월 마감 사이 중복 카드와 중복 CSS 정리~~ — V22.8.75 에서 처리
    - 중복 카드: 역할을 나눠 `한눈에 보기`는 소비 분석에만 남기고 종합 리포트에서 제거, 소비 분석 안의 예산 중복도 정리
    - 중복 CSS: **하지 않기로 결정** — `immutable` 1년 캐시 자산이라 실제 절감이 gzip 1,063 B 에 그치는데, 24개 화면 × 8개 테마 조합 재확인 위험이 큼
    - 남은 판단: 이름만 다른 4쌍(지출 흐름↔일별 소비 그래프 등)을 합칠지 — 합치려면 필터 결과를 리포트 카드에 연결하는 설계가 먼저 필요

운영 검증 없이 비동기 저장이나 데이터 계약을 확대하지 않으며, 기존 URL과 서버 저장 흐름을 유지합니다.
