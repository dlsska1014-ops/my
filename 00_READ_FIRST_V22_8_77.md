# V22.8.77 끊김·중단 상황에서의 복구 안내

버전: `V22.8.77-INTERRUPTED-SAVE-RECOVERY`

## 적용 파일

- `src/index.js` 전체 교체
- 신규 SQL 없음 · 환경변수 변경 없음 · 자산 주소 변경 없음

## 이번에 고친 것

### 로그인이 풀린 채로 저장하면 낯선 화면으로 던져졌습니다

세션이 만료된 상태에서 기록을 저장하면 이런 일이 벌어졌습니다.

| 겪는 일 | 이전 | 이후 |
|---|---|---|
| 돌아가는 화면 | **예전 관리자 화면**(`/?legacy=1`) | 로그인 화면 |
| 왜 안 됐는지 | **설명 없음** | "로그인이 풀려서 저장하지 못했습니다" |
| 적어 둔 금액·메모 | **사라짐** | 그대로 남음 |
| 로그인한 뒤 | 가계부 목록으로 | **쓰던 화면으로 복귀** |

기록은 저장되지 않고 기존 데이터도 그대로입니다(0건). 문제는 **사용자가 왜
안 됐는지 모른 채 적은 내용까지 잃는 것**이었습니다.

이제 이렇게 흘러갑니다.

1. 저장 → 로그인이 풀려 있음
2. 로그인 화면으로 이동 (원래 화면 주소를 지닌 채)
3. 로그인
4. 쓰던 화면으로 돌아오고 입력 시트가 다시 열리며 **적었던 내용이 그대로**

비밀번호를 틀려 다시 묻는 화면에서도 돌아갈 주소를 잃지 않습니다.

> 돌아갈 주소는 **앱 안의 화면만** 허용합니다. 외부 주소(`https://…`),
> 스킴 없는 외부 주소(`//…`), 관리자 경로(`/admin/…`), 줄바꿈을 섞은 값은
> 모두 무시하고 기본 화면으로 보냅니다.

### 적은 내용은 주소에 싣지 않습니다

메모가 주소창과 방문 기록에 남지 않도록, 입력 초안은 브라우저 안
(`sessionStorage`)에만 둡니다. 검사가 `amount=`·`memo=` 가 주소에 실리지 않는
것까지 확인합니다.

## 함께 확인하고 결함이 아니었던 것

- **저장 후 뒤로 가기** — 모든 쓰기가 리다이렉트(303)로 끝나 "양식을 다시
  제출하시겠습니까"가 뜨지 않습니다. 뒤로 가도 기록이 한 번 더 남지 않습니다.

## 적용 순서

1. 현재 운영 Worker 전체 소스를 백업합니다.
2. 이 패키지의 `src/index.js` 전체를 복사해 교체하고 배포합니다.
3. 저장소 루트에서 아래를 실행합니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\10_VERIFY_AFTER_DEPLOY_V22_8_77.ps1
```

4. 브라우저 두 개를 열어 한쪽에서 로그아웃한 뒤, 다른 쪽에서 기록을 저장해
   봅니다. 로그인 화면으로 가고, 로그인하면 쓰던 화면으로 돌아오며 적었던
   금액·메모가 남아 있어야 합니다.

기존 SQL 은 다시 실행하지 않습니다.
