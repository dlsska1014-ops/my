# V22.8.79 검증 보고서

버전: `V22.8.79-UIUX-INCOME-BUDGET-THEME`
작성일: 2026-08-08

## 요약

작업지시서 V22.8.79(수입·예산 IA / 월 기본값 / 테마 2택 / 거래내역 탭 /
정기 수입·지출 편집 / 아이콘 / 트렌드·게이지)의 P0~P3 전 항목과, 그 과정에서
따로 요청된 예산 저장소 SQL 을 처리했습니다.

- 저장소 전체 자동검사 **3,154개 통과** (체크섬 91개)
- 이번 릴리스 신규 회귀 검사 **280개**
- 되돌린 결함 **36종**이 모두 잡히는 것을 확인

## SQL (2026-08-07 적용 완료)

| 파일 | 내용 | 결과 |
|---|---|---|
| `03_APPLY_BUDGET_STORAGE_V22_8_79.sql` | 예산 표/설정 JSON 이중화 해소 | `upsert_ready=true`, 폴백 키 0건 |
| `04_APPLY_BUDGET_GRANTS_V22_8_79.sql` | `accountbook_budgets` 권한 회수 | anon 권한 0 |
| `05_APPLY_TABLE_GRANTS_V22_8_79.sql` | 나머지 표 11개 권한 회수 | 18개 표 전부 0 / `truncate_exposed=false` |

진단 근거는 추측이 아니라 재현으로 확정했습니다. 로컬 PostgreSQL 16.13 에
운영과 같은 상태를 만들어

- 적용 전 `on_conflict` upsert 가 실제로 `42P10` 으로 실패 → 적용 후 성공
- `anon` 으로 `select` 는 0행(RLS 가 막음)이지만 `truncate` 는 **성공**
  (RLS 가 `TRUNCATE` 를 덮지 않는다는 것)

을 직접 확인했습니다.

## 코드 (6단계)

| 단계 | 항목 | 신규 검사 |
|---|---|---:|
| 1 | 월 고정 버그 · 브랜드 아이콘 CSS 충돌 | 43 |
| 2 | 테마 라이트/다크 2택 · 라이트 사이드바 위계 | 47 |
| 3 | 거래내역 독립 탭 | 54 |
| 4 | 정기 IA · 예산 진입 동선 | 43 |
| 5 | 정기 수입·지출 수정 · 반복 · 수입 구분 | 50 |
| 6 | 수입 대비 사용률 게이지 · 소비 흐름 | 43 |

## 작업지시서 전제와 달랐던 것

지시서는 소스를 기준으로 쓰였고, 실제 렌더와 다른 지점이 세 곳 있었습니다.
`improveUiV5`(`normalizeUserFacingUi`)가 모바일 앱 페이지에서 일부 마크업을
통째로 걷어내기 때문입니다.

| 지시서 전제 | 실제 |
|---|---|
| 하단탭 "기록"이 `#feed` 를 가리킴 | `nav.bottom` 은 통째로 제거되고 통합 내비가 그려짐 |
| 홈 `id="fixed"` 에 고정지출 폼이 박혀 있음 | 이미 제거되어 화면에 없었음 |
| 홈 `id="budget"` 예산 패널 | 마찬가지로 제거되어 있었음 |

그래서 실제로 필요했던 것은 "폼 제거"가 아니라 "진입 카드 신설"이었고,
하단탭도 통합 내비 쪽을 고쳐야 했습니다. 소스 문자열만 봤으면 놓쳤을 부분이라
단계마다 실제 렌더로 확인했습니다.

## 지시서와 다르게 판단한 것

**P3 트렌드 토글을 JS show/hide 가 아니라 서버측 링크 전환으로 만들었습니다.**

지시서는 "서버 프리렌더 + 최소 JS(show/hide)"를 지정했지만, 세 패널을 미리
그리면 홈이 62KB 가 되어 실사용 예산(46 KiB)을 16KB 넘겼습니다. 실측값은
일별 21.5KB · 주별 4.7KB · 월별 1.4KB 입니다.

서버측 전환으로 바꿔도 두 가드가 서로 밀어냈습니다.

- 기본을 주별로 → 39.2KB, 기준선 예산(35 KiB) 초과
- 기본을 월별로 → 6개월 질의가 붙어 홈 질의 9회 → 15회 초과

그래서 **기본은 패널 없이 기간 탭만** 두고, 고른 사람만 비용을 내게 했습니다.
월별 6개월 질의도 `trend=monthly` 일 때만 돕니다.

결과적으로 기본 홈은 **34,681B** 로 트렌드를 넣기 전(35,614B)보다 작고, 질의도
9회 그대로입니다. 중간에 기준선 예산을 36 KiB 로 올렸다가 **되돌렸습니다** —
가드를 완화할 이유가 없어졌기 때문입니다.

**작업지시서 5-⑤(`/admin/recurring/update`)는 넣지 않았습니다.** 지시서에도
"(선택, 대칭성)"으로 표시돼 있고, 4단계에서 홈의 고정지출 섹션을 걷어내면서
그 라우트에 UI 진입점이 하나도 남지 않았습니다. 화면이 없는 라우트에 수정
기능을 더하는 것은 의미가 없다고 봤습니다. 라우트 자체는 지시서 금지사항대로
남겼습니다.

## 기존 검사를 손댄 곳과 이유

가드를 통과시키려고 약화시킨 것이 아니라, 계약이 바뀐 것을 반영했습니다.

| 검사 | 무엇을 | 왜 |
|---|---|---|
| `validate-performance` 테마 계약 | system/light/dark → light/dark | 시스템 선택지 제거. **검사를 오히려 늘렸다** — OS 다크 전환이 화면을 흔들지 않는지, 저장된 `system` 이 라이트로 정정되는지를 새로 본다 |
| `validate-performance` 자산 해시 3건 | 내용 고정 해시 갱신 | CSS·JS 를 의도적으로 바꿨다 |
| `validate-ux-principles` 함수 해시 | `renderMyAnalysisHtml` 핀 갱신 | 예산 표에 설정 링크를 붙였다. 표와 게이지 렌더는 그대로 |
| `validate-transactions-tab` 다크 판정 | 셀렉터 문자열 → 클래스 포함 여부 | 목록이 늘어도 견디도록. 검사는 한 개 늘었다 |
| `validate-recurring-ia` 월 합계 | 정확한 라인 → 뜻 | 5단계에서 수입이 생기며 "지출만" 으로 좁혀졌다 |

## 스스로 잡은 결함

검사가 통과했다고 넘어가지 않고 실제로 돌려 본 덕에 찾은 것들입니다.

1. **정기 항목 수입 저장이 안 됐다.** `addReservePlan` 에만 `type` 을 넣고
   핸들러가 폼 값을 넘기지 않아, 수입으로 저장해도 지출로 들어갔습니다.
   실제 왕복에서 발견했습니다.
2. **"없는 id 수정" 검사가 엉뚱한 이유로 통과했다.** `amount="999"` 를 썼는데
   `extractAmount` 가 맨 세 자리를 금액으로 보지 않아(추가 경로도 같은 동작)
   이름·금액 검사에서 먼저 막히고 있었습니다. 돌연변이 테스트에서 회귀가
   안 잡히길래 파고들어 찾았고, 금액을 파서가 받는 값으로 바꾸고 오류 문구까지
   특정해 이제 잡힙니다.
3. **하단탭 수정이 죽은 코드였다.** `nav.bottom` 은 셸이 통째로 교체합니다.
   스모크가 "active: false" 를 내서 알았습니다.
4. **정기 카드가 데이터가 있는데도 "없음" 이라고 했다.** 정기항목은 이번 달을
   볼 때만 불러오는데(`includeReserve`) 과거 달에서 "없음" 으로 단정했습니다.
   로드 여부를 넘겨 문구를 나눴습니다.

## 자산 주소

내용이 바뀐 자산은 경로를 올려야 `immutable` 1년 캐시를 넘어 반영됩니다.
옛 경로가 404 가 되는 것까지 확인했습니다.

```
mobile-home.css        v22875 → v22879
accountbook-shell.css  v22874 → v22879
accountbook-theme.js   v22812 → v22879
mobile-home.js         v22870 → v22879
mobile-home-shell.js   v22870 → v22879
accountbook-nav.js     v22862 → v22879
```

`mobile-home.css` 의 ETag 가 경로(v22875)와 어긋나 v22873 으로 박혀 있던 것도
함께 맞췄습니다.

## 확인하지 못한 것

- **실기기 렌더** — 라이트/다크 로고 또렷함, 사이드바 위계, 실제 손가락 터치,
  iOS Safari 고유 동작, 카카오톡 인앱 브라우저
- **운영 DB 의 실제 데이터** — SQL 결과는 운영자가 보내 준 사후점검 출력으로
  판정했습니다. 이 저장소는 운영 DB 에 접근하지 않습니다
- **저장소 밖의 DB 소비자** — 다른 앱이나 Realtime 구독이 anon 키로 붙어
  있었다면 권한 회수의 영향을 받습니다
