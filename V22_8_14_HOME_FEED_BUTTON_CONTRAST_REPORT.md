# V22.8.14 홈 전체 조회 버튼 대비 보고서

## 운영 확인에서 발견한 문제

홈 기록이 10건을 넘을 때만 나타나는 `전체 N건 조회` 링크가 다크모드에서 컬러톤 강조색 글자를 사용했습니다. 기존 `.btn:not(...)` 규칙의 높은 우선순위 때문에 V22.8.13의 일반 링크 버튼 규칙이 글자색을 덮지 못했습니다.

## 적용

- 링크에 `homeFeedAllBtn` 전용 표식 추가
- `abMobileAppSurface #feed` 범위로 주 행동 배경과 흰 글자를 우선 적용
- 새 CSS를 `/assets/accountbook-shell-v22814.css`로 분리해 immutable 캐시 충돌 방지
- QA 거래를 일시적으로 11건으로 확장해 실제 버튼 HTML과 전용 범위를 검증

## 검증 결과

- 다크 블루·그린·바이올렛·앰버: 각각 표시 텍스트 대비 미달 0건
- 라이트 블루: 표시 텍스트 대비 미달 0건
- 버튼 전경: `rgb(255, 255, 255)`
- 브라우저 오류 로그: 0건

## 배포 판정

- `src/index.js` 전체 교체 필요
- SQL·환경변수·Kakao Developers·OpenBuilder 변경 없음
- 운영 도메인에서 실제 11건 이상 홈을 다시 확인해야 함
